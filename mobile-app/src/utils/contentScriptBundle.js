// AUTO-GENERATED — Do not edit manually.
// Run: node scripts/bundle-extension.js
// Total content scripts: 9
// Total size: 345621 bytes (before CSS)
// Generated: 2026-09-08T08:44:29.313Z

export const CONTENT_SCRIPT_BUNDLE = "\n(function() {\n  if (window.__flirtEasyBundleLoaded) {\n    console.log('[FlirtEasy] Bundle already active on this page');\n    return;\n  }\n  window.__flirtEasyBundleLoaded = true;\n  console.log('[FlirtEasy] Injected Full Content Script Bundle (On-Device Mode)');\n\n  \n// ── Inject FlirtEasy Status Bar CSS ──\n(function() {\n  var style = document.createElement('style');\n  style.setAttribute('data-flirteasy', 'status-bar');\n  style.textContent = `/*\r\n * FlirtEasy In-Page Status Bar — 6 State Variations\r\n */\r\n\r\n/* ── Mobile Touch Defense for Tinder Dialogs ── */\r\n/* Ensure the mobile navigation drawer doesn't trap aria-modal or block touches when login modal is active */\r\nbody.fe-login-modal-active #rebrand-mobile-menu,\r\nbody:has([aria-labelledby=\"MODAL_LOGIN\"]) #rebrand-mobile-menu,\r\n.mobile-navigation-drawer-overlay, [class*=\"drawer-overlay\"] {\r\n    display: none !important;\r\n    opacity: 0 !important;\r\n    pointer-events: none !important;\r\n    visibility: hidden !important;\r\n    z-index: -9999 !important;\r\n}\r\n\r\n/* Elevate ONLY the login modal / dialog, NOT #rebrand-mobile-menu (which is also role=\"dialog\") */\r\n[aria-labelledby=\"MODAL_LOGIN\"],\r\n.StretchedBox:has([aria-labelledby=\"MODAL_LOGIN\"]),\r\ndiv[aria-modal=\"true\"]:not(#rebrand-mobile-menu),\r\n[role=\"dialog\"]:not(#rebrand-mobile-menu) {\r\n    z-index: 9999999 !important;\r\n    pointer-events: auto !important;\r\n}\r\n\r\n[aria-labelledby=\"MODAL_LOGIN\"] button,\r\n[aria-labelledby=\"MODAL_LOGIN\"] a,\r\n[aria-labelledby=\"MODAL_LOGIN\"] input,\r\n[aria-labelledby=\"MODAL_LOGIN\"] iframe,\r\n[role=\"dialog\"]:not(#rebrand-mobile-menu) button,\r\n[role=\"dialog\"]:not(#rebrand-mobile-menu) a,\r\n[role=\"dialog\"]:not(#rebrand-mobile-menu) input,\r\n[role=\"dialog\"]:not(#rebrand-mobile-menu) iframe {\r\n    pointer-events: auto !important;\r\n    touch-action: manipulation !important;\r\n    cursor: pointer !important;\r\n    -webkit-tap-highlight-color: rgba(0,0,0,0) !important;\r\n}\r\n\r\n#flirteasy-status-bar {\r\n    display: block !important;\r\n    position: fixed !important;\r\n    top: 16px !important;\r\n    left: 50% !important;\r\n    transform: translateX(-50%) translateY(-140%) !important;\r\n    opacity: 0 !important;\r\n    z-index: 9999999 !important;\r\n    font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif !important;\r\n    transition: transform 0.42s cubic-bezier(0.16, 1, 0.3, 1), opacity 0.3s ease !important;\r\n    pointer-events: none !important;\r\n    white-space: nowrap !important;\r\n    line-height: normal !important;\r\n}\r\n\r\n#flirteasy-status-bar * {\r\n    box-sizing: border-box !important;\r\n    font-family: inherit !important;\r\n    line-height: normal !important;\r\n    list-style: none !important;\r\n}\r\n\r\n#flirteasy-status-bar.fe-visible {\r\n    transform: translateX(-50%) translateY(0) !important;\r\n    opacity: 1 !important;\r\n    pointer-events: auto !important;\r\n}\r\n\r\n/* ── Base inner bar ── */\r\n#flirteasy-status-bar .fe-bar-inner {\r\n    display: inline-flex !important;\r\n    align-items: center !important;\r\n    background: #eef1f6 !important;\r\n    border: none !important;\r\n    border-radius: 9999px !important;\r\n    height: 60px !important;\r\n    padding: 0 18px 0 8px !important;\r\n    margin: 0 !important;\r\n    box-shadow: 10px 10px 28px rgba(0,0,0,0.35), -8px -8px 20px rgba(0,0,0,0.18) !important;\r\n    min-width: 760px !important;\r\n    gap: 0 !important;\r\n    position: relative !important;\r\n    overflow: hidden !important;\r\n    backdrop-filter: blur(24px) !important;\r\n    -webkit-backdrop-filter: blur(24px) !important;\r\n    transition: box-shadow 0.3s ease, transform 0.25s ease !important;\r\n    cursor: grab !important;\r\n}\r\n\r\n#flirteasy-status-bar .fe-bar-inner:active {\r\n    cursor: grabbing !important;\r\n    box-shadow: 6px 6px 14px rgba(0,0,0,0.28), -5px -5px 12px rgba(0,0,0,0.14) !important;\r\n}\r\n\r\n#flirteasy-status-bar .fe-bar-inner:hover {\r\n    box-shadow: 14px 14px 32px rgba(0,0,0,0.40), -10px -10px 24px rgba(0,0,0,0.20) !important;\r\n    transform: translateY(-2px) !important;\r\n}\r\n\r\n/* Shimmer sweep on hover */\r\n#flirteasy-status-bar .fe-bar-inner::before {\r\n    content: '' !important;\r\n    position: absolute !important;\r\n    top: 0 !important;\r\n    left: -100% !important;\r\n    width: 35% !important;\r\n    height: 100% !important;\r\n    background: linear-gradient(90deg, transparent, rgba(255,255,255,0.5), transparent) !important;\r\n    transform: skewX(-15deg) !important;\r\n    pointer-events: none !important;\r\n    z-index: 1 !important;\r\n    opacity: 0 !important;\r\n    transition: none !important;\r\n}\r\n\r\n#flirteasy-status-bar .fe-bar-inner:hover::before {\r\n    animation: fe-shimmer-hover 0.65s ease forwards !important;\r\n}\r\n\r\n@keyframes fe-shimmer-hover {\r\n    0%   { left: -100%; opacity: 1; }\r\n    100% { left: 150%; opacity: 1; }\r\n}\r\n\r\n/* ── State: Idle ── */\r\n#flirteasy-status-bar .fe-bar-inner.fe-state-idle {\r\n    box-shadow: 10px 10px 28px rgba(0,0,0,0.35), -8px -8px 20px rgba(0,0,0,0.18) !important;\r\n    opacity: 1 !important;\r\n}\r\n\r\n#flirteasy-status-bar .fe-bar-inner.fe-state-idle:hover {\r\n    opacity: 1 !important;\r\n    box-shadow: 10px 10px 28px rgba(0,0,0,0.32), -8px -8px 20px rgba(0,0,0,0.16) !important;\r\n}\r\n\r\n#flirteasy-status-bar .fe-state-idle .fe-dot-ping  { animation: none !important; opacity: 0 !important; }\r\n#flirteasy-status-bar .fe-state-idle .fe-dot-inner { background: #cbd5e1 !important; }\r\n#flirteasy-status-bar .fe-state-idle .fe-phase-label { color: #94a3b8 !important; }\r\n#flirteasy-status-bar .fe-state-idle .fe-brand-name  { color: #64748b !important; }\r\n\r\n#flirteasy-status-bar .fe-state-idle .fe-activity-pill {\r\n    background: #eef1f6 !important;\r\n    border: none !important;\r\n    box-shadow: inset 3px 3px 7px #c8ccd4, inset -2px -2px 5px rgba(0,0,0,0.08) !important;\r\n}\r\n\r\n#flirteasy-status-bar .fe-state-idle .fe-activity-icon {\r\n    color: #cbd5e1 !important;\r\n    animation: none !important;\r\n}\r\n\r\n#flirteasy-status-bar .fe-state-idle .fe-activity-text { color: #94a3b8 !important; }\r\n#flirteasy-status-bar .fe-state-idle .fe-stat-pill {\r\n    background: #eef1f6 !important;\r\n    box-shadow: 3px 3px 6px #d0d4db, -2px -2px 5px rgba(0,0,0,0.10) !important;\r\n}\r\n#flirteasy-status-bar .fe-state-idle .fe-pill-num  { color: #94a3b8 !important; opacity: 1 !important; }\r\n#flirteasy-status-bar .fe-state-idle .fe-pill-unit { color: #cbd5e1 !important; opacity: 1 !important; }\r\n\r\n/* ── State: Match (full pink gradient) ── */\r\n#flirteasy-status-bar .fe-bar-inner.fe-state-match {\r\n    background: linear-gradient(to right, #e8197d, #f06292) !important;\r\n    box-shadow: 10px 10px 24px rgba(220, 60, 130, 0.45), -8px -8px 20px rgba(255,180,210,0.35) !important;\r\n}\r\n\r\n#flirteasy-status-bar .fe-bar-inner.fe-state-match:hover {\r\n    box-shadow: 14px 14px 30px rgba(220, 60, 130, 0.55), -10px -10px 24px rgba(255,180,210,0.4) !important;\r\n}\r\n\r\n#flirteasy-status-bar .fe-bar-inner.fe-state-match::before {\r\n    background: linear-gradient(90deg, transparent, rgba(255,255,255,0.3), transparent) !important;\r\n    animation: fe-shimmer-continuous 3s linear infinite !important;\r\n    opacity: 1 !important;\r\n    left: -100% !important;\r\n}\r\n\r\n@keyframes fe-shimmer-continuous {\r\n    0%   { left: -100%; }\r\n    100% { left: 200%; }\r\n}\r\n\r\n/* ── State: Lead (golden amber gradient) ── */\r\n#flirteasy-status-bar .fe-bar-inner.fe-state-lead {\r\n    background: linear-gradient(to right, #f59e0b, #fbbf24) !important;\r\n    box-shadow: 10px 10px 24px rgba(245,158,11,0.4), -8px -8px 20px rgba(254,243,199,0.5) !important;\r\n}\r\n\r\n#flirteasy-status-bar .fe-bar-inner.fe-state-lead:hover {\r\n    box-shadow: 14px 14px 30px rgba(245,158,11,0.52), -10px -10px 24px rgba(254,243,199,0.6) !important;\r\n    transform: translateY(-2px) !important;\r\n}\r\n\r\n/* Golden fill bursts from center — grey overlay shrinks via clip-path */\r\n#flirteasy-status-bar .fe-bar-inner.fe-state-lead::after {\r\n    content: '' !important;\r\n    position: absolute !important;\r\n    inset: 0 !important;\r\n    background: #eef1f6 !important;\r\n    border-radius: inherit !important;\r\n    pointer-events: none !important;\r\n    z-index: 0 !important;\r\n    animation: fe-lead-burst 0.65s cubic-bezier(0.16, 1, 0.3, 1) forwards !important;\r\n}\r\n\r\n@keyframes fe-lead-burst {\r\n    0%   { clip-path: circle(150% at 50% 50%); }\r\n    100% { clip-path: circle(0% at 50% 50%); }\r\n}\r\n\r\n/* Exit: grey covers back from center outward */\r\n#flirteasy-status-bar .fe-bar-inner.fe-state-lead.fe-lead-exiting::after {\r\n    animation: fe-lead-burst-out 0.5s cubic-bezier(0.4, 0, 1, 1) forwards !important;\r\n}\r\n\r\n@keyframes fe-lead-burst-out {\r\n    0%   { clip-path: circle(0% at 50% 50%); }\r\n    100% { clip-path: circle(150% at 50% 50%); }\r\n}\r\n\r\n#flirteasy-status-bar .fe-bar-inner.fe-state-lead::before {\r\n    background: linear-gradient(90deg, transparent, rgba(255,255,255,0.35), transparent) !important;\r\n    animation: fe-shimmer-continuous 3s linear infinite 0.6s !important;\r\n    opacity: 1 !important;\r\n    left: -100% !important;\r\n    z-index: 2 !important;\r\n}\r\n\r\n#flirteasy-status-bar .fe-state-lead .fe-identity    { position: relative !important; z-index: 2 !important; }\r\n#flirteasy-status-bar .fe-state-lead .fe-right       { position: relative !important; z-index: 2 !important; }\r\n#flirteasy-status-bar .fe-state-lead .fe-controls    { position: relative !important; z-index: 2 !important; }\r\n#flirteasy-status-bar .fe-state-lead .fe-ctrl-divider { position: relative !important; z-index: 2 !important; }\r\n#flirteasy-status-bar .fe-state-lead .fe-brand-name  { color: #78350f !important; }\r\n#flirteasy-status-bar .fe-state-lead .fe-phase-label { color: #92400e !important; }\r\n#flirteasy-status-bar .fe-state-lead .fe-dot-ping    { background: #b45309 !important; animation-duration: 1s !important; }\r\n#flirteasy-status-bar .fe-state-lead .fe-dot-inner   { background: #b45309 !important; }\r\n#flirteasy-status-bar .fe-state-lead .fe-ctrl-divider { background: linear-gradient(to bottom, transparent, rgba(120,53,15,0.3), transparent) !important; }\r\n#flirteasy-status-bar .fe-state-lead .fe-ctrl        { color: rgba(120,53,15,0.7) !important; }\r\n#flirteasy-status-bar .fe-state-lead .fe-ctrl:hover  { color: #78350f !important; background: rgba(255,255,255,0.2) !important; }\r\n\r\n/* Lead pill */\r\n#flirteasy-status-bar .fe-lead-pill {\r\n    background: rgba(255,255,255,0.3) !important;\r\n    border: 1.5px solid rgba(255,255,255,0.55) !important;\r\n    box-shadow: inset 0 1px 6px rgba(255,255,255,0.2), 0 4px 16px rgba(0,0,0,0.06) !important;\r\n    backdrop-filter: blur(4px) !important;\r\n    gap: 10px !important;\r\n}\r\n\r\n#flirteasy-status-bar .fe-lead-icon {\r\n    display: inline-flex !important;\r\n    align-items: center !important;\r\n    justify-content: center !important;\r\n    color: #78350f !important;\r\n    flex-shrink: 0 !important;\r\n    animation: fe-phone-ring 1.6s ease-in-out infinite !important;\r\n}\r\n\r\n@keyframes fe-phone-ring {\r\n    0%, 100% { transform: rotate(0deg) scale(1); }\r\n    10%       { transform: rotate(-15deg) scale(1.1); }\r\n    20%       { transform: rotate(15deg) scale(1.1); }\r\n    30%       { transform: rotate(-10deg) scale(1.05); }\r\n    40%       { transform: rotate(10deg) scale(1.05); }\r\n    50%       { transform: rotate(0deg) scale(1); }\r\n}\r\n\r\n#flirteasy-status-bar .fe-lead-text {\r\n    font-size: 14px !important;\r\n    font-weight: 600 !important;\r\n    color: #78350f !important;\r\n    letter-spacing: -0.01em !important;\r\n    white-space: nowrap !important;\r\n}\r\n\r\n#flirteasy-status-bar .fe-lead-text strong {\r\n    font-weight: 800 !important;\r\n    color: #451a03 !important;\r\n}\r\n\r\n/* Take Over button (lead state) */\r\n#flirteasy-status-bar .fe-action-btn-lead {\r\n    background: rgba(255,255,255,0.3) !important;\r\n    color: #78350f !important;\r\n    border: 1.5px solid rgba(255,255,255,0.6) !important;\r\n    backdrop-filter: blur(4px) !important;\r\n    box-shadow: 0 2px 8px rgba(0,0,0,0.08) !important;\r\n    padding: 5px 14px 5px 16px !important;\r\n    font-weight: 700 !important;\r\n}\r\n\r\n#flirteasy-status-bar .fe-action-btn-lead:hover {\r\n    background: rgba(255,255,255,0.45) !important;\r\n    transform: translateY(-1px) !important;\r\n    opacity: 1 !important;\r\n}\r\n\r\n/* ── State: Cooldown ── */\r\n#flirteasy-status-bar .fe-bar-inner.fe-state-cooldown {\r\n    box-shadow: 10px 10px 28px rgba(0,0,0,0.32), -8px -8px 20px rgba(0,0,0,0.16) !important;\r\n}\r\n\r\n#flirteasy-status-bar .fe-bar-inner.fe-state-cooldown:hover {\r\n    box-shadow: 14px 14px 32px rgba(0,0,0,0.40), -10px -10px 24px rgba(0,0,0,0.20) !important;\r\n}\r\n\r\n/* ── State: Finished ── */\r\n#flirteasy-status-bar .fe-bar-inner.fe-state-finished {\r\n    box-shadow: 10px 10px 28px rgba(0,0,0,0.32), -8px -8px 20px rgba(0,0,0,0.16) !important;\r\n}\r\n\r\n#flirteasy-status-bar .fe-bar-inner.fe-state-finished:hover {\r\n    box-shadow: 14px 14px 32px rgba(0,0,0,0.40), -10px -10px 24px rgba(0,0,0,0.20) !important;\r\n}\r\n\r\n/* ── State: Analyzing ── */\r\n#flirteasy-status-bar .fe-bar-inner.fe-state-analyzing {\r\n    box-shadow: 10px 10px 28px rgba(0,0,0,0.32), -8px -8px 20px rgba(0,0,0,0.16) !important;\r\n}\r\n\r\n#flirteasy-status-bar .fe-bar-inner.fe-state-analyzing:hover {\r\n    box-shadow: 14px 14px 32px rgba(0,0,0,0.40), -10px -10px 24px rgba(0,0,0,0.20) !important;\r\n}\r\n\r\n/* ── Identity section ── */\r\n#flirteasy-status-bar .fe-identity {\r\n    display: inline-flex !important;\r\n    align-items: center !important;\r\n    gap: 0 !important;\r\n    flex-shrink: 0 !important;\r\n    padding: 0 !important;\r\n    margin: 0 !important;\r\n}\r\n\r\n#flirteasy-status-bar .fe-logo-wrap {\r\n    width: 40px !important;\r\n    height: 40px !important;\r\n    flex-shrink: 0 !important;\r\n    display: flex !important;\r\n    align-items: center !important;\r\n    justify-content: center !important;\r\n    margin: 0 !important;\r\n    padding: 0 !important;\r\n}\r\n\r\n#flirteasy-status-bar .fe-identity-text {\r\n    display: flex !important;\r\n    flex-direction: column !important;\r\n    justify-content: center !important;\r\n    gap: 3px !important;\r\n    margin: 0 0 0 4px !important;\r\n    padding: 0 !important;\r\n}\r\n\r\n#flirteasy-status-bar .fe-brand-name {\r\n    font-size: 13px !important;\r\n    font-weight: 600 !important;\r\n    color: #3d4a5c !important;\r\n    letter-spacing: -0.025em !important;\r\n    line-height: 1.2 !important;\r\n    margin: 0 !important;\r\n    padding: 0 !important;\r\n}\r\n\r\n#flirteasy-status-bar .fe-state-match .fe-brand-name    { color: #ffffff !important; }\r\n#flirteasy-status-bar .fe-state-cooldown .fe-brand-name { color: #64748b !important; }\r\n\r\n/* ── Status row ── */\r\n#flirteasy-status-bar .fe-status-row {\r\n    display: flex !important;\r\n    align-items: center !important;\r\n    gap: 6px !important;\r\n    margin: 0 !important;\r\n    padding: 0 !important;\r\n}\r\n\r\n/* ── Ping dot ── */\r\n#flirteasy-status-bar .fe-dot-wrap {\r\n    position: relative !important;\r\n    width: 8px !important;\r\n    height: 8px !important;\r\n    display: flex !important;\r\n    align-items: center !important;\r\n    justify-content: center !important;\r\n    flex-shrink: 0 !important;\r\n    margin: 0 !important;\r\n    padding: 0 !important;\r\n}\r\n\r\n#flirteasy-status-bar .fe-dot-ping {\r\n    position: absolute !important;\r\n    inset: 0 !important;\r\n    border-radius: 50% !important;\r\n    background: #e8197d !important;\r\n    opacity: 0.75 !important;\r\n    animation: fe-ping 1.5s cubic-bezier(0, 0, 0.2, 1) infinite !important;\r\n    margin: 0 !important;\r\n    padding: 0 !important;\r\n}\r\n\r\n#flirteasy-status-bar .fe-dot-inner {\r\n    position: relative !important;\r\n    width: 6px !important;\r\n    height: 6px !important;\r\n    border-radius: 50% !important;\r\n    background: #e8197d !important;\r\n    flex-shrink: 0 !important;\r\n    margin: 0 !important;\r\n    padding: 0 !important;\r\n}\r\n\r\n@keyframes fe-ping {\r\n    75%, 100% { transform: scale(2); opacity: 0; }\r\n}\r\n\r\n/* Cooldown: solid blue, no ping */\r\n#flirteasy-status-bar .fe-state-cooldown .fe-dot-ping   { animation: none !important; opacity: 0 !important; }\r\n#flirteasy-status-bar .fe-state-cooldown .fe-dot-inner  { background: #3b82f6 !important; }\r\n\r\n/* Match: white ping */\r\n#flirteasy-status-bar .fe-state-match .fe-dot-ping  { background: #ffffff !important; animation-duration: 1s !important; }\r\n#flirteasy-status-bar .fe-state-match .fe-dot-inner { background: #ffffff !important; }\r\n\r\n/* Finished: solid emerald, no ping */\r\n#flirteasy-status-bar .fe-state-finished .fe-dot-ping  { animation: none !important; opacity: 0 !important; }\r\n#flirteasy-status-bar .fe-state-finished .fe-dot-inner { background: #10b981 !important; }\r\n\r\n/* Analyzing: purple, no ping */\r\n#flirteasy-status-bar .fe-state-analyzing .fe-dot-ping  { animation: none !important; opacity: 0 !important; }\r\n#flirteasy-status-bar .fe-state-analyzing .fe-dot-inner { background: #8b5cf6 !important; }\r\n\r\n/* ── Phase label ── */\r\n#flirteasy-status-bar .fe-phase-label {\r\n    font-size: 10px !important;\r\n    font-weight: 700 !important;\r\n    letter-spacing: 0.12em !important;\r\n    text-transform: uppercase !important;\r\n    color: #e8197d !important;\r\n    margin: 0 !important;\r\n    padding: 0 !important;\r\n    line-height: 1 !important;\r\n}\r\n\r\n#flirteasy-status-bar .fe-state-cooldown .fe-phase-label  { color: #3b82f6 !important; }\r\n#flirteasy-status-bar .fe-state-match .fe-phase-label     { color: rgba(255,255,255,0.9) !important; }\r\n#flirteasy-status-bar .fe-state-finished .fe-phase-label  { color: #10b981 !important; }\r\n#flirteasy-status-bar .fe-state-analyzing .fe-phase-label { color: #8b5cf6 !important; }\r\n\r\n/* ── Activity center ── */\r\n#flirteasy-status-bar .fe-activity {\r\n    flex: 1 !important;\r\n    display: flex !important;\r\n    justify-content: center !important;\r\n    align-items: center !important;\r\n    padding: 0 12px !important;\r\n    margin: 0 !important;\r\n    min-width: 0 !important;\r\n    position: relative !important;\r\n    z-index: 2 !important;\r\n}\r\n\r\n/* Activity pill — base */\r\n#flirteasy-status-bar .fe-activity-pill {\r\n    display: inline-flex !important;\r\n    align-items: center !important;\r\n    justify-content: center !important;\r\n    gap: 10px !important;\r\n    padding: 6px 28px !important;\r\n    border-radius: 9999px !important;\r\n    margin: 0 !important;\r\n    width: 100% !important;\r\n    transition: background 0.2s ease !important;\r\n}\r\n\r\n/* ACTIVE: neumorphic inset pink */\r\n#flirteasy-status-bar .fe-state-active .fe-activity-pill {\r\n    background: #eef1f6 !important;\r\n    border: none !important;\r\n    box-shadow: inset 4px 4px 8px #d6d9e0, inset -2px -2px 5px rgba(0,0,0,0.08), 0 0 0 1.5px rgba(236,72,153,0.15) !important;\r\n}\r\n\r\n/* DRAFTING: neumorphic inset pink tint */\r\n#flirteasy-status-bar .fe-state-drafting .fe-activity-pill {\r\n    background: #f5eef4 !important;\r\n    border: none !important;\r\n    box-shadow: inset 4px 4px 8px #d8ccd6, inset -2px -2px 5px rgba(0,0,0,0.08), 0 0 0 1.5px rgba(236,72,153,0.2) !important;\r\n}\r\n\r\n/* COOLDOWN: neumorphic inset blue tint */\r\n#flirteasy-status-bar .fe-state-cooldown .fe-activity-pill:not(.fe-cooldown-pill):not(.fe-transition-pill) {\r\n    background: #edf2f8 !important;\r\n    border: none !important;\r\n    box-shadow: inset 4px 4px 8px #d0d8e4, inset -2px -2px 5px rgba(0,0,0,0.08), 0 0 0 1.5px rgba(59,130,246,0.18) !important;\r\n}\r\n\r\n/* MATCH: translucent white pill */\r\n#flirteasy-status-bar .fe-state-match .fe-activity-pill {\r\n    background: rgba(255, 255, 255, 0.22) !important;\r\n    border: 1px solid rgba(255, 255, 255, 0.45) !important;\r\n    box-shadow: inset 0 1px 6px rgba(255,255,255,0.15), 0 4px 16px rgba(0,0,0,0.08) !important;\r\n}\r\n\r\n/* FINISHED: neumorphic inset emerald tint */\r\n#flirteasy-status-bar .fe-state-finished .fe-activity-pill {\r\n    background: #edf6f1 !important;\r\n    border: none !important;\r\n    box-shadow: inset 4px 4px 8px #ccddd5, inset -2px -2px 5px rgba(0,0,0,0.08), 0 0 0 1.5px rgba(16,185,129,0.2) !important;\r\n}\r\n\r\n/* ANALYZING: neumorphic inset violet tint */\r\n#flirteasy-status-bar .fe-state-analyzing .fe-activity-pill {\r\n    background: #f0eef8 !important;\r\n    border: none !important;\r\n    box-shadow: inset 4px 4px 8px #d4ceea, inset -2px -2px 5px rgba(0,0,0,0.08), 0 0 0 1.5px rgba(139,92,246,0.18) !important;\r\n}\r\n\r\n/* Activity icon color per state */\r\n#flirteasy-status-bar .fe-activity-icon {\r\n    display: flex !important;\r\n    align-items: center !important;\r\n    flex-shrink: 0 !important;\r\n    color: #e8197d !important;\r\n    margin: 0 !important;\r\n    padding: 0 !important;\r\n}\r\n\r\n#flirteasy-status-bar .fe-state-cooldown .fe-activity-icon  { color: #3b82f6 !important; }\r\n#flirteasy-status-bar .fe-state-match .fe-activity-icon     { color: #fde047 !important; }\r\n#flirteasy-status-bar .fe-state-finished .fe-activity-icon  { color: #10b981 !important; }\r\n#flirteasy-status-bar .fe-state-analyzing .fe-activity-icon { color: #8b5cf6 !important; }\r\n\r\n/* Heart bounce on active */\r\n@keyframes fe-heart-bounce {\r\n    0%, 100% { transform: translateY(0) scale(1); }\r\n    40%       { transform: translateY(-2px) scale(1.15); }\r\n    60%       { transform: translateY(0) scale(1); }\r\n    75%       { transform: translateY(-1px) scale(1.08); }\r\n}\r\n\r\n#flirteasy-status-bar .fe-state-active .fe-activity-icon {\r\n    animation: fe-heart-bounce 2s ease-in-out infinite !important;\r\n}\r\n\r\n/* Lightning pulse on match */\r\n@keyframes fe-lightning-pulse {\r\n    0%, 100% { opacity: 1; transform: scale(1); }\r\n    50%       { opacity: 0.7; transform: scale(1.2); }\r\n}\r\n\r\n#flirteasy-status-bar .fe-state-match .fe-activity-icon {\r\n    animation: fe-lightning-pulse 1.5s cubic-bezier(0.4, 0, 0.6, 1) infinite !important;\r\n}\r\n\r\n/* Activity text */\r\n#flirteasy-status-bar .fe-activity-text {\r\n    font-size: 13px !important;\r\n    font-weight: 500 !important;\r\n    color: #475569 !important;\r\n    white-space: nowrap !important;\r\n    margin: 0 !important;\r\n    padding: 0 !important;\r\n}\r\n\r\n#flirteasy-status-bar .fe-activity-text strong {\r\n    font-weight: 600 !important;\r\n    color: #0f172a !important;\r\n    margin: 0 !important;\r\n    padding: 0 !important;\r\n}\r\n\r\n#flirteasy-status-bar .fe-state-active .fe-activity-text            { color: #e8197d !important; }\r\n#flirteasy-status-bar .fe-state-active .fe-activity-text strong     { color: #e8197d !important; font-weight: 700 !important; }\r\n#flirteasy-status-bar .fe-state-drafting .fe-activity-text          { color: #e8197d !important; }\r\n#flirteasy-status-bar .fe-state-drafting .fe-activity-text strong   { color: #e8197d !important; font-weight: 700 !important; }\r\n#flirteasy-status-bar .fe-state-cooldown .fe-activity-text          { color: #1e3a8a !important; }\r\n#flirteasy-status-bar .fe-state-cooldown .fe-activity-text strong   { color: #1d4ed8 !important; }\r\n#flirteasy-status-bar .fe-state-match .fe-activity-text             { color: #ffffff !important; font-weight: 600 !important; }\r\n#flirteasy-status-bar .fe-state-match .fe-activity-text strong      { color: #ffffff !important; font-weight: 700 !important; }\r\n#flirteasy-status-bar .fe-state-finished .fe-activity-text          { color: #065f46 !important; }\r\n#flirteasy-status-bar .fe-state-finished .fe-activity-text strong   { color: #047857 !important; }\r\n#flirteasy-status-bar .fe-state-analyzing .fe-activity-text         { color: #4c1d95 !important; }\r\n#flirteasy-status-bar .fe-state-analyzing .fe-activity-text strong  { color: #5b21b6 !important; }\r\n\r\n/* ── Transition pill (Getting ready to message) ── */\r\n#flirteasy-status-bar .fe-transition-pill {\r\n    background: linear-gradient(135deg, #f0fdf4, #dcfce7) !important;\r\n    border: none !important;\r\n    box-shadow: inset 3px 3px 7px #c6e8d2, inset -2px -2px 5px rgba(0,0,0,0.08), 0 0 0 1.5px rgba(16,185,129,0.2) !important;\r\n    gap: 10px !important;\r\n}\r\n\r\n#flirteasy-status-bar .fe-transition-icon {\r\n    display: inline-flex !important;\r\n    align-items: center !important;\r\n    justify-content: center !important;\r\n    color: #10b981 !important;\r\n    animation: fe-spin 1.1s linear infinite !important;\r\n    flex-shrink: 0 !important;\r\n}\r\n\r\n@keyframes fe-spin {\r\n    from { transform: rotate(0deg); }\r\n    to   { transform: rotate(360deg); }\r\n}\r\n\r\n#flirteasy-status-bar .fe-transition-text {\r\n    font-size: 13px !important;\r\n    font-weight: 600 !important;\r\n    color: #059669 !important;\r\n    letter-spacing: -0.01em !important;\r\n    white-space: nowrap !important;\r\n}\r\n\r\n#flirteasy-status-bar .fe-transition-badge {\r\n    display: inline-flex !important;\r\n    align-items: center !important;\r\n    justify-content: center !important;\r\n    min-width: 32px !important;\r\n    height: 24px !important;\r\n    padding: 0 8px !important;\r\n    border-radius: 9999px !important;\r\n    background: #10b981 !important;\r\n    color: #ffffff !important;\r\n    font-size: 12px !important;\r\n    font-weight: 800 !important;\r\n    font-family: 'SF Mono', ui-monospace, monospace !important;\r\n    letter-spacing: 0.02em !important;\r\n    box-shadow: 0 2px 8px rgba(16,185,129,0.4), inset 0 1px 0 rgba(255,255,255,0.2) !important;\r\n    flex-shrink: 0 !important;\r\n}\r\n\r\n@keyframes fe-badge-tick {\r\n    0%   { transform: scale(1); }\r\n    50%  { transform: scale(1.25); }\r\n    100% { transform: scale(1); }\r\n}\r\n\r\n/* ── Preparing pill (no_likes — skip liking phase) ── */\r\n#flirteasy-status-bar .fe-preparing-pill {\r\n    background: linear-gradient(135deg, #faf5ff, #ede9fe) !important;\r\n    border: none !important;\r\n    box-shadow: inset 3px 3px 7px #d4c8f0, inset -2px -2px 5px rgba(0,0,0,0.08), 0 0 0 1.5px rgba(139,92,246,0.2) !important;\r\n    gap: 10px !important;\r\n}\r\n\r\n#flirteasy-status-bar .fe-preparing-icon {\r\n    display: inline-flex !important;\r\n    align-items: center !important;\r\n    justify-content: center !important;\r\n    color: #8b5cf6 !important;\r\n    animation: fe-preparing-pulse 1.4s ease-in-out infinite !important;\r\n    flex-shrink: 0 !important;\r\n}\r\n\r\n@keyframes fe-preparing-pulse {\r\n    0%, 100% { transform: scale(1);    opacity: 1;   }\r\n    50%       { transform: scale(1.18); opacity: 0.75; }\r\n}\r\n\r\n#flirteasy-status-bar .fe-preparing-text {\r\n    font-size: 13px !important;\r\n    font-weight: 600 !important;\r\n    color: #6d28d9 !important;\r\n    letter-spacing: -0.01em !important;\r\n    white-space: nowrap !important;\r\n}\r\n\r\n#flirteasy-status-bar .fe-preparing-badge {\r\n    display: inline-flex !important;\r\n    align-items: center !important;\r\n    justify-content: center !important;\r\n    min-width: 32px !important;\r\n    height: 24px !important;\r\n    padding: 0 8px !important;\r\n    border-radius: 9999px !important;\r\n    background: #8b5cf6 !important;\r\n    color: #ffffff !important;\r\n    font-size: 12px !important;\r\n    font-weight: 800 !important;\r\n    font-family: 'SF Mono', ui-monospace, monospace !important;\r\n    letter-spacing: 0.02em !important;\r\n    box-shadow: 0 2px 8px rgba(139,92,246,0.4), inset 0 1px 0 rgba(255,255,255,0.2) !important;\r\n    flex-shrink: 0 !important;\r\n}\r\n\r\n/* Orb: preparing state overrides (purple tint) */\r\n.control-center.is-transitioning.is-preparing .state-transitioning .orb-icon  { color: #8b5cf6 !important; }\r\n.control-center.is-transitioning.is-preparing .state-transitioning .orb-label { color: #6d28d9 !important; }\r\n.control-center.is-transitioning.is-preparing .state-transitioning .orb-timer { color: #6d28d9 !important; }\r\n.control-center.is-transitioning.is-preparing .master-orb                     { background: rgba(139,92,246,0.06) !important; }\r\n.control-center.is-transitioning.is-preparing .ring-inner                     { border-color: rgba(139,92,246,0.12) !important; }\r\n.control-center.is-transitioning.is-preparing .orb-pulse-ring                 { box-shadow: 0 0 0 1px rgba(139,92,246,0.32), 0 0 8px 3px rgba(139,92,246,0.22), 0 0 18px 7px rgba(139,92,246,0.09) !important; }\r\n\r\n/* ── Cooldown pill (App limits reached) ── */\r\n#flirteasy-status-bar .fe-cooldown-pill {\r\n    background: linear-gradient(135deg, #eff6ff, #dbeafe) !important;\r\n    border: none !important;\r\n    box-shadow: inset 3px 3px 7px #c2d4f0, inset -2px -2px 5px rgba(0,0,0,0.08), 0 0 0 1.5px rgba(59,130,246,0.2) !important;\r\n    gap: 10px !important;\r\n}\r\n\r\n#flirteasy-status-bar .fe-cooldown-icon {\r\n    display: inline-flex !important;\r\n    align-items: center !important;\r\n    justify-content: center !important;\r\n    color: #3b82f6 !important;\r\n    animation: fe-spin 1.4s linear infinite !important;\r\n    flex-shrink: 0 !important;\r\n}\r\n\r\n#flirteasy-status-bar .fe-cooldown-text {\r\n    font-size: 13px !important;\r\n    font-weight: 600 !important;\r\n    color: #1e40af !important;\r\n    letter-spacing: -0.01em !important;\r\n    white-space: nowrap !important;\r\n}\r\n\r\n#flirteasy-status-bar .fe-cooldown-badge {\r\n    display: inline-flex !important;\r\n    align-items: center !important;\r\n    justify-content: center !important;\r\n    min-width: 52px !important;\r\n    height: 24px !important;\r\n    padding: 0 10px !important;\r\n    border-radius: 9999px !important;\r\n    background: #3b82f6 !important;\r\n    color: #ffffff !important;\r\n    font-size: 12px !important;\r\n    font-weight: 800 !important;\r\n    font-family: 'SF Mono', ui-monospace, monospace !important;\r\n    letter-spacing: 0.02em !important;\r\n    box-shadow: 0 2px 8px rgba(59,130,246,0.4), inset 0 1px 0 rgba(255,255,255,0.2) !important;\r\n    flex-shrink: 0 !important;\r\n}\r\n\r\n/* ── Safety Lock pill (Internal rate limits) ── */\r\n#flirteasy-status-bar .fe-network-wait-pill {\r\n    background: linear-gradient(135deg, #fff7ed, #ffedd5) !important;\r\n    border: none !important;\r\n    box-shadow: inset 3px 3px 7px #fed7aa, inset -2px -2px 5px rgba(0,0,0,0.08), 0 0 0 1.5px rgba(249,115,22,0.25) !important;\r\n    gap: 10px !important;\r\n}\r\n\r\n#flirteasy-status-bar .fe-network-wait-pill .fe-cooldown-icon {\r\n    color: #ea580c !important;\r\n    animation: fe-wifi-pulse 1.4s ease-in-out infinite !important;\r\n}\r\n\r\n#flirteasy-status-bar .fe-network-wait-pill .fe-cooldown-text {\r\n    color: #9a3412 !important;\r\n    font-weight: 700 !important;\r\n}\r\n\r\n@keyframes fe-wifi-pulse {\r\n    0%, 100% { opacity: 1; transform: scale(1); }\r\n    50% { opacity: 0.45; transform: scale(0.92); }\r\n}\r\n\r\n#flirteasy-status-bar .fe-safety-lock-pill {\r\n    background: linear-gradient(135deg, #fffbeb, #fef3c7) !important;\r\n    border: none !important;\r\n    box-shadow: inset 3px 3px 7px #f8e1a0, inset -2px -2px 5px rgba(0,0,0,0.08), 0 0 0 1.5px rgba(245,158,11,0.25) !important;\r\n    gap: 10px !important;\r\n}\r\n\r\n#flirteasy-status-bar .fe-safety-lock-pill .fe-cooldown-icon {\r\n    color: #d97706 !important;\r\n    animation: fe-lock-wobble 2s ease-in-out infinite !important;\r\n}\r\n\r\n@keyframes fe-lock-wobble {\r\n    0%, 100% { transform: rotate(0deg); }\r\n    25% { transform: rotate(-8deg); }\r\n    75% { transform: rotate(8deg); }\r\n}\r\n\r\n#flirteasy-status-bar .fe-safety-lock-pill .fe-cooldown-text {\r\n    color: #92400e !important;\r\n    font-weight: 700 !important;\r\n}\r\n\r\n#flirteasy-status-bar .fe-safety-lock-pill .fe-cooldown-badge {\r\n    background: #f59e0b !important;\r\n    box-shadow: 0 2px 8px rgba(245,158,11,0.4), inset 0 1px 0 rgba(255,255,255,0.2) !important;\r\n}\r\n\r\n/* ── Trial Ended pill (purple — upgrade invitation) ── */\r\n#flirteasy-status-bar .fe-trial-ended-pill {\r\n    background: linear-gradient(135deg, #f5f3ff, #ede9fe) !important;\r\n    border: none !important;\r\n    box-shadow: inset 3px 3px 7px #ddd6fe, inset -2px -2px 5px rgba(0,0,0,0.06), 0 0 0 1.5px rgba(124,58,237,0.25) !important;\r\n    gap: 10px !important;\r\n}\r\n\r\n#flirteasy-status-bar .fe-trial-ended-pill .fe-cooldown-icon {\r\n    color: #7c3aed !important;\r\n}\r\n\r\n#flirteasy-status-bar .fe-trial-ended-pill .fe-cooldown-text {\r\n    color: #4c1d95 !important;\r\n    font-weight: 700 !important;\r\n}\r\n\r\n/* ── Countdown value ── */\r\n#flirteasy-status-bar .fe-countdown-val {\r\n    font-family: 'SF Mono', 'Fira Code', ui-monospace, 'Courier New', monospace !important;\r\n    font-size: 14px !important;\r\n    font-weight: 800 !important;\r\n    color: #1d4ed8 !important;\r\n    background: rgba(29, 78, 216, 0.08) !important;\r\n    border: 1px solid rgba(29, 78, 216, 0.15) !important;\r\n    border-radius: 5px !important;\r\n    padding: 1px 6px !important;\r\n    letter-spacing: 0.02em !important;\r\n    margin: 0 1px !important;\r\n}\r\n\r\n/* ── Typing dots ── */\r\n#flirteasy-status-bar .fe-typing-dots {\r\n    display: inline-flex !important;\r\n    align-items: center !important;\r\n    gap: 2px !important;\r\n    margin: 0 0 0 2px !important;\r\n    padding: 0 !important;\r\n    height: 16px !important;\r\n    vertical-align: middle !important;\r\n}\r\n\r\n#flirteasy-status-bar .fe-typing-dots span {\r\n    display: inline-block !important;\r\n    width: 4px !important;\r\n    height: 4px !important;\r\n    border-radius: 50% !important;\r\n    background: #e8197d !important;\r\n    animation: fe-typing-dot 1.4s ease-in-out infinite !important;\r\n    margin: 0 !important;\r\n    padding: 0 !important;\r\n}\r\n\r\n#flirteasy-status-bar .fe-typing-dots span:nth-child(2) { animation-delay: 0.2s !important; }\r\n#flirteasy-status-bar .fe-typing-dots span:nth-child(3) { animation-delay: 0.4s !important; }\r\n\r\n@keyframes fe-typing-dot {\r\n    0%, 100% { transform: translateY(0); opacity: 0.4; }\r\n    50%       { transform: translateY(-3px); opacity: 1; }\r\n}\r\n\r\n/* ── Right section ── */\r\n#flirteasy-status-bar .fe-right {\r\n    display: inline-flex !important;\r\n    align-items: center !important;\r\n    gap: 6px !important;\r\n    flex-shrink: 0 !important;\r\n    margin: 0 !important;\r\n    padding: 0 !important;\r\n    position: relative !important;\r\n    z-index: 2 !important;\r\n}\r\n\r\n/* Cooldown right section dimmed */\r\n#flirteasy-status-bar .fe-state-cooldown .fe-right { opacity: 0.6 !important; }\r\n\r\n/* ── Stat pills ── */\r\n#flirteasy-status-bar .fe-stat-pill {\r\n    display: inline-flex !important;\r\n    align-items: center !important;\r\n    gap: 5px !important;\r\n    padding: 4px 10px !important;\r\n    margin: 0 !important;\r\n    border-radius: 9999px !important;\r\n    background: #eef1f6 !important;\r\n    border: none !important;\r\n    box-shadow: 5px 5px 10px #c8ccd4, -3px -3px 7px rgba(0,0,0,0.12) !important;\r\n    transition: transform 0.15s ease, box-shadow 0.15s ease !important;\r\n    cursor: default !important;\r\n}\r\n\r\n#flirteasy-status-bar .fe-stat-pill:hover {\r\n    transform: scale(1.05) !important;\r\n    box-shadow: 6px 6px 12px #c0c4cc, -4px -4px 8px rgba(0,0,0,0.14) !important;\r\n}\r\n\r\n/* Active/Drafting stat pills: pink-tinted base */\r\n#flirteasy-status-bar .fe-state-active .fe-stat-pill,\r\n#flirteasy-status-bar .fe-state-drafting .fe-stat-pill {\r\n    background: #f5ecf3 !important;\r\n    box-shadow: 5px 5px 10px #d0c0cc, -3px -3px 7px rgba(0,0,0,0.12) !important;\r\n}\r\n\r\n/* Drafting msgs pill: extra highlighted */\r\n#flirteasy-status-bar .fe-state-drafting .fe-stat-pill-msgs {\r\n    background: #f0e0ee !important;\r\n    box-shadow: 5px 5px 10px #ccb8c8, -3px -3px 7px rgba(0,0,0,0.12), 0 0 0 1.5px rgba(232,25,125,0.3) !important;\r\n}\r\n\r\n/* Cooldown pills */\r\n#flirteasy-status-bar .fe-state-cooldown .fe-stat-pill {\r\n    background: #eef1f6 !important;\r\n    box-shadow: 5px 5px 10px #c4ccd8, -3px -3px 7px rgba(0,0,0,0.12) !important;\r\n}\r\n\r\n/* Match: translucent raised */\r\n#flirteasy-status-bar .fe-state-match .fe-stat-pill {\r\n    background: rgba(255,255,255,0.22) !important;\r\n    box-shadow: 4px 4px 10px rgba(0,0,0,0.18), -3px -3px 8px rgba(255,255,255,0.25) !important;\r\n    border: 1px solid rgba(255,255,255,0.35) !important;\r\n}\r\n\r\n/* Finished */\r\n#flirteasy-status-bar .fe-state-finished .fe-stat-pill {\r\n    background: #eef1f6 !important;\r\n    box-shadow: 5px 5px 10px #c4d4cc, -3px -3px 7px rgba(0,0,0,0.12) !important;\r\n}\r\n\r\n#flirteasy-status-bar .fe-pill-num {\r\n    font-size: 12px !important;\r\n    font-weight: 800 !important;\r\n    color: #e8197d !important;\r\n    letter-spacing: -0.01em !important;\r\n    line-height: 1 !important;\r\n    margin: 0 !important;\r\n    padding: 0 !important;\r\n}\r\n\r\n#flirteasy-status-bar .fe-pill-unit {\r\n    font-size: 10px !important;\r\n    font-weight: 700 !important;\r\n    color: #e8197d !important;\r\n    text-transform: uppercase !important;\r\n    letter-spacing: 0.07em !important;\r\n    line-height: 1 !important;\r\n    margin: 0 !important;\r\n    padding: 0 !important;\r\n    opacity: 0.75 !important;\r\n}\r\n\r\n#flirteasy-status-bar .fe-state-cooldown .fe-pill-num  { color: #64748b !important; opacity: 1 !important; }\r\n#flirteasy-status-bar .fe-state-cooldown .fe-pill-unit { color: #94a3b8 !important; opacity: 1 !important; }\r\n#flirteasy-status-bar .fe-state-match .fe-pill-num     { color: #ffffff !important; opacity: 1 !important; }\r\n#flirteasy-status-bar .fe-state-match .fe-pill-unit    { color: rgba(255,255,255,0.85) !important; opacity: 1 !important; }\r\n#flirteasy-status-bar .fe-state-finished .fe-pill-num  { color: #e8197d !important; opacity: 1 !important; }\r\n#flirteasy-status-bar .fe-state-finished .fe-pill-unit { color: #e8197d !important; opacity: 0.75 !important; }\r\n\r\n/* ── Trial limit warning pills ── */\r\n#flirteasy-status-bar .fe-stat-pill.fe-pill-caution {\r\n    background: #fff8ed !important;\r\n    box-shadow: 5px 5px 10px #e8d8b8, -3px -3px 7px rgba(0,0,0,0.10) !important;\r\n}\r\n#flirteasy-status-bar .fe-stat-pill.fe-pill-caution .fe-pill-num  { color: #d97706 !important; }\r\n#flirteasy-status-bar .fe-stat-pill.fe-pill-caution .fe-pill-unit { color: #d97706 !important; }\r\n\r\n#flirteasy-status-bar .fe-stat-pill.fe-pill-warn {\r\n    background: #fff1f2 !important;\r\n    box-shadow: 5px 5px 10px #f0c0c4, -3px -3px 7px rgba(0,0,0,0.10), 0 0 0 1.5px rgba(220,38,38,0.25) !important;\r\n}\r\n#flirteasy-status-bar .fe-stat-pill.fe-pill-warn .fe-pill-num  { color: #dc2626 !important; }\r\n#flirteasy-status-bar .fe-stat-pill.fe-pill-warn .fe-pill-unit { color: #dc2626 !important; }\r\n\r\n#flirteasy-status-bar .fe-stat-pill.fe-pill-trial-ended {\r\n    background: #f5f3ff !important;\r\n    box-shadow: 5px 5px 10px #ddd6fe, -3px -3px 7px rgba(0,0,0,0.08), 0 0 0 1.5px rgba(124,58,237,0.25) !important;\r\n}\r\n#flirteasy-status-bar .fe-stat-pill.fe-pill-trial-ended .fe-pill-unit { color: #7c3aed !important; font-weight: 600 !important; }\r\n\r\n/* ── Slot machine counter ── */\r\n@keyframes fe-slot-spin {\r\n    0%   { transform: translateY(-16px); filter: blur(4px); opacity: 0; }\r\n    50%  { filter: blur(1px); opacity: 0.6; }\r\n    100% { transform: translateY(0); filter: blur(0); opacity: 1; }\r\n}\r\n\r\n#flirteasy-status-bar .fe-slot-spin {\r\n    animation: fe-slot-spin 0.45s cubic-bezier(0.16, 1, 0.3, 1) forwards !important;\r\n}\r\n\r\n/* ── iOS-quality name transition (active state, name only) ── */\r\n@keyframes fe-name-exit {\r\n    0%   { opacity: 1; transform: translateY(0) scale(1);    filter: blur(0); }\r\n    100% { opacity: 0; transform: translateY(-9px) scale(0.82); filter: blur(5px); }\r\n}\r\n\r\n@keyframes fe-name-enter {\r\n    0%   { opacity: 0; transform: translateY(11px) scale(0.82); filter: blur(5px); }\r\n    55%  { opacity: 1; filter: blur(0);  transform: translateY(-2px) scale(1.06); }\r\n    75%  { transform: translateY(1px) scale(0.97); }\r\n    100% { opacity: 1; transform: translateY(0) scale(1);    filter: blur(0); }\r\n}\r\n\r\n#flirteasy-status-bar .fe-swipe-name {\r\n    display: inline-block !important;\r\n}\r\n\r\n/* ── Full pill transition (state changes) ── */\r\n@keyframes fe-pill-exit {\r\n    0%   { opacity: 1; transform: translateY(0) scale(1);    filter: blur(0); }\r\n    100% { opacity: 0; transform: translateY(-8px) scale(0.94); filter: blur(3px); }\r\n}\r\n\r\n@keyframes fe-pill-enter {\r\n    0%   { opacity: 0; transform: translateY(10px) scale(0.94); filter: blur(3px); }\r\n    60%  { opacity: 1; filter: blur(0); transform: translateY(-1px) scale(1.02); }\r\n    100% { opacity: 1; transform: translateY(0) scale(1);    filter: blur(0); }\r\n}\r\n\r\n/* keep old names as aliases for any inline style references */\r\n@keyframes fe-pill-slide-out {\r\n    0%   { opacity: 1; transform: translateY(0) scale(1);    filter: blur(0); }\r\n    100% { opacity: 0; transform: translateY(-8px) scale(0.94); filter: blur(3px); }\r\n}\r\n\r\n@keyframes fe-pill-slide-in {\r\n    0%   { opacity: 0; transform: translateY(10px) scale(0.94); filter: blur(3px); }\r\n    60%  { opacity: 1; filter: blur(0); transform: translateY(-1px) scale(1.02); }\r\n    100% { opacity: 1; transform: translateY(0) scale(1);    filter: blur(0); }\r\n}\r\n\r\n/* ── Action buttons ── */\r\n#flirteasy-status-bar .fe-action-btn {\r\n    display: inline-flex !important;\r\n    align-items: center !important;\r\n    gap: 5px !important;\r\n    padding: 5px 14px !important;\r\n    margin: 0 !important;\r\n    border-radius: 9999px !important;\r\n    font-size: 11px !important;\r\n    font-weight: 700 !important;\r\n    cursor: pointer !important;\r\n    border: none !important;\r\n    transition: transform 0.15s ease, opacity 0.15s ease !important;\r\n    white-space: nowrap !important;\r\n    letter-spacing: 0.02em !important;\r\n    outline: none !important;\r\n}\r\n\r\n#flirteasy-status-bar .fe-action-btn:hover  { transform: translateY(-1px) !important; opacity: 0.9 !important; }\r\n#flirteasy-status-bar .fe-action-btn:active { transform: translateY(0) !important; }\r\n\r\n/* View → button (match state) */\r\n#flirteasy-status-bar .fe-action-btn-view {\r\n    background: rgba(255,255,255,0.2) !important;\r\n    color: #ffffff !important;\r\n    border: 1.5px solid rgba(255,255,255,0.65) !important;\r\n    backdrop-filter: blur(4px) !important;\r\n    box-shadow: 0 2px 8px rgba(0,0,0,0.1) !important;\r\n    padding: 5px 14px 5px 16px !important;\r\n}\r\n\r\n/* View Full Report button (analyzing state) */\r\n#flirteasy-status-bar .fe-action-btn-purple {\r\n    background: #eef1f6 !important;\r\n    color: #6d28d9 !important;\r\n    border: none !important;\r\n    box-shadow: 4px 4px 8px #d4ceea, -2px -2px 5px rgba(0,0,0,0.10) !important;\r\n    font-size: 11px !important;\r\n}\r\n\r\n#flirteasy-status-bar .fe-action-btn-purple:hover {\r\n    transform: scale(1.05) translateY(0) !important;\r\n    opacity: 1 !important;\r\n}\r\n\r\n/* ── Divider ── */\r\n#flirteasy-status-bar .fe-ctrl-divider {\r\n    width: 1px !important;\r\n    height: 22px !important;\r\n    background: linear-gradient(to bottom, transparent, #c8cdd6, transparent) !important;\r\n    flex-shrink: 0 !important;\r\n    margin: 0 4px 0 10px !important;\r\n    padding: 0 !important;\r\n}\r\n\r\n/* ── Drag Handle ── */\r\n#flirteasy-status-bar .fe-drag-handle {\r\n    display: flex !important;\r\n    align-items: center !important;\r\n    justify-content: center !important;\r\n    flex-shrink: 0 !important;\r\n    align-self: stretch !important;\r\n    padding: 0 14px 0 10px !important;\r\n    background: transparent !important;\r\n    box-shadow: none !important;\r\n    border-radius: 0 !important;\r\n    color: #c4cad4 !important;\r\n    cursor: grab !important;\r\n    transition: color 0.15s ease !important;\r\n}\r\n\r\n#flirteasy-status-bar .fe-drag-handle:hover {\r\n    color: #94a3b8 !important;\r\n}\r\n\r\n#flirteasy-status-bar .fe-state-match .fe-drag-handle { color: rgba(255,255,255,0.45) !important; }\r\n#flirteasy-status-bar .fe-state-lead .fe-drag-handle  { color: rgba(120,53,15,0.35) !important; }\r\n\r\n/* ── Brand icon logo ── */\r\n#flirteasy-status-bar .fe-brand-icon {\r\n    width: 32px !important;\r\n    height: 32px !important;\r\n    border-radius: 8px !important;\r\n    object-fit: contain !important;\r\n    display: block !important;\r\n    flex-shrink: 0 !important;\r\n    margin: 0 !important;\r\n    padding: 0 !important;\r\n    transition: filter 0.3s ease !important;\r\n    pointer-events: none !important;\r\n}\r\n\r\n/* ── Chromatic rainbow glow on hover ── */\r\n#flirteasy-status-bar .fe-chromatic-glow {\r\n    position: absolute !important;\r\n    inset: 0 !important;\r\n    background: linear-gradient(45deg, #ff0000, #ff7300, #fffb00, #48ff00, #00ffd5, #002bff, #7a00ff, #ff00c8, #ff0000) !important;\r\n    border-radius: inherit !important;\r\n    filter: blur(10px) !important;\r\n    opacity: 0 !important;\r\n    z-index: 0 !important;\r\n    pointer-events: none !important;\r\n    transition: opacity 0.4s ease !important;\r\n    margin: 0 !important;\r\n    padding: 0 !important;\r\n}\r\n\r\n#flirteasy-status-bar .fe-bar-inner:hover .fe-chromatic-glow {\r\n    opacity: 0.1 !important;\r\n    animation: fe-chromatic-rotate 10s linear infinite !important;\r\n}\r\n\r\n#flirteasy-status-bar .fe-state-match .fe-chromatic-glow {\r\n    opacity: 0 !important;\r\n    animation: none !important;\r\n}\r\n\r\n@keyframes fe-chromatic-rotate {\r\n    0%   { filter: hue-rotate(0deg) blur(10px); }\r\n    100% { filter: hue-rotate(360deg) blur(10px); }\r\n}\r\n\r\n/* ── Brand row (name + platform badge) ── */\r\n#flirteasy-status-bar .fe-brand-row {\r\n    display: flex !important;\r\n    align-items: baseline !important;\r\n    gap: 5px !important;\r\n    margin: 0 !important;\r\n    padding: 0 !important;\r\n}\r\n\r\n/* ── Platform badge ── */\r\n#flirteasy-status-bar .fe-platform-badge {\r\n    font-size: 9px !important;\r\n    font-weight: 700 !important;\r\n    letter-spacing: 0.08em !important;\r\n    text-transform: uppercase !important;\r\n    color: #e8197d !important;\r\n    background: #eef1f6 !important;\r\n    border: none !important;\r\n    border-radius: 9999px !important;\r\n    padding: 2px 7px !important;\r\n    line-height: 1 !important;\r\n    margin: 0 0 1px 0 !important;\r\n    flex-shrink: 0 !important;\r\n    box-shadow: 3px 3px 6px #d0d4db, -2px -2px 5px rgba(255,255,255,0.9) !important;\r\n}\r\n\r\n#flirteasy-status-bar .fe-state-idle .fe-platform-badge,\r\n#flirteasy-status-bar .fe-state-cooldown .fe-platform-badge {\r\n    color: #94a3b8 !important;\r\n}\r\n\r\n#flirteasy-status-bar .fe-state-match .fe-platform-badge {\r\n    color: rgba(255,255,255,0.9) !important;\r\n    background: rgba(255,255,255,0.22) !important;\r\n    box-shadow: 2px 2px 5px rgba(0,0,0,0.15), -1px -1px 3px rgba(255,255,255,0.2) !important;\r\n}\r\n\r\n/* ── LIVE / OFF chip ── */\r\n#flirteasy-status-bar .fe-live-chip {\r\n    font-size: 9px !important;\r\n    font-weight: 700 !important;\r\n    letter-spacing: 0.08em !important;\r\n    text-transform: uppercase !important;\r\n    border: none !important;\r\n    border-radius: 9999px !important;\r\n    padding: 2px 7px !important;\r\n    line-height: 1.4 !important;\r\n    margin: 0 !important;\r\n    flex-shrink: 0 !important;\r\n    transition: background 0.25s ease, color 0.25s ease, box-shadow 0.25s ease !important;\r\n}\r\n\r\n#flirteasy-status-bar .fe-live-chip--on {\r\n    color: #059669 !important;\r\n    background: #eaf5f0 !important;\r\n    box-shadow: 3px 3px 6px #c8ddd5, -2px -2px 5px rgba(255,255,255,0.9) !important;\r\n    animation: fe-live-pulse 2.5s ease-in-out infinite !important;\r\n}\r\n\r\n#flirteasy-status-bar .fe-live-chip--off {\r\n    color: #94a3b8 !important;\r\n    background: #eef1f6 !important;\r\n    box-shadow: 3px 3px 6px #d0d4db, -2px -2px 5px rgba(255,255,255,0.9) !important;\r\n    animation: none !important;\r\n}\r\n\r\n@keyframes fe-live-pulse {\r\n    0%, 100% { opacity: 1; }\r\n    50%       { opacity: 0.55; }\r\n}\r\n\r\n#flirteasy-status-bar .fe-state-match .fe-live-chip--on {\r\n    color: rgba(255,255,255,0.95) !important;\r\n    background: rgba(255,255,255,0.22) !important;\r\n    box-shadow: 2px 2px 5px rgba(0,0,0,0.15), -1px -1px 3px rgba(255,255,255,0.2) !important;\r\n}\r\n\r\n/* ── Trial days pill — inherits base fe-pill-num/unit sizing ── */\r\n\r\n/* ── PRO plan pill ── */\r\n#flirteasy-status-bar .fe-stat-pill-pro {\r\n    background: linear-gradient(135deg, #f5f0ff, #ede9fe) !important;\r\n    box-shadow: 3px 3px 6px #d4ccea, -2px -2px 5px rgba(255,255,255,0.9) !important;\r\n}\r\n\r\n#flirteasy-status-bar .fe-pill-pro-label {\r\n    font-size: 10px !important;\r\n    font-weight: 800 !important;\r\n    letter-spacing: 0.12em !important;\r\n    color: #7c3aed !important;\r\n    background: linear-gradient(135deg, #7c3aed, #a855f7) !important;\r\n    -webkit-background-clip: text !important;\r\n    -webkit-text-fill-color: transparent !important;\r\n    background-clip: text !important;\r\n}\r\n\r\n#flirteasy-status-bar .fe-stat-pill-pro-expiry .fe-pill-num {\r\n    font-size: 11px !important;\r\n    font-weight: 700 !important;\r\n    letter-spacing: -0.02em !important;\r\n}\r\n\r\n#flirteasy-status-bar .fe-stat-pill-pro-expiry .fe-pill-unit {\r\n    font-size: 9px !important;\r\n    font-weight: 700 !important;\r\n    letter-spacing: 0.08em !important;\r\n    color: #7c3aed !important;\r\n}\r\n\r\n/* ══════════════════════════════════════════════════\r\n   BUMBLE DEAD STATE CARD\r\n   Injected into Bumble sidebar when user has no matches.\r\n   Liquid morph premium design.\r\n   ══════════════════════════════════════════════════ */\r\n\r\n.fe-bumble-dead-card {\r\n    position: relative !important;\r\n    width: 100% !important;\r\n    margin: 0 !important;\r\n    height: 76px !important;\r\n    border-radius: 0 !important;\r\n    cursor: pointer !important;\r\n    overflow: hidden !important;\r\n    background: #fff5f8 !important;\r\n    box-shadow: none !important;\r\n    border-bottom: 1px solid rgba(0,0,0,0.06) !important;\r\n    transition: background 0.2s ease !important;\r\n    animation: fe-bdc-enter 0.4s cubic-bezier(0.16,1,0.3,1) both !important;\r\n    font-family: 'Circular', 'Inter', -apple-system, BlinkMacSystemFont, sans-serif !important;\r\n    box-sizing: border-box !important;\r\n}\r\n\r\n.fe-bumble-dead-card * {\r\n    box-sizing: border-box !important;\r\n    font-family: inherit !important;\r\n}\r\n\r\n.fe-bumble-dead-card:hover {\r\n    background: #ffeef5 !important;\r\n}\r\n\r\n.fe-bumble-dead-card.fe-bdc-exit {\r\n    animation: fe-bdc-exit 0.28s ease forwards !important;\r\n}\r\n\r\n@keyframes fe-bdc-enter {\r\n    from { opacity: 0; transform: translateY(-10px) scale(0.97); }\r\n    to   { opacity: 1; transform: translateY(0) scale(1); }\r\n}\r\n\r\n@keyframes fe-bdc-exit {\r\n    to { opacity: 0; transform: translateY(-6px) scale(0.97); }\r\n}\r\n\r\n/* ── Gradient base layer ── */\r\n.fe-bumble-dead-card .fe-bdc-gradient-base {\r\n    position: absolute !important;\r\n    inset: 0 !important;\r\n    background: linear-gradient(135deg, rgba(252,231,243,0.5) 0%, rgba(237,233,254,0.5) 50%, rgba(255,237,213,0.5) 100%) !important;\r\n    z-index: 0 !important;\r\n}\r\n\r\n/* ── Morphing blobs ── */\r\n.fe-bumble-dead-card .fe-bdc-blob {\r\n    position: absolute !important;\r\n    border-radius: 50% !important;\r\n    mix-blend-mode: multiply !important;\r\n    filter: blur(18px) !important;\r\n    z-index: 1 !important;\r\n    pointer-events: none !important;\r\n}\r\n\r\n.fe-bumble-dead-card .fe-bdc-blob-1 {\r\n    width: 80px !important;\r\n    height: 80px !important;\r\n    top: -20px !important;\r\n    left: -16px !important;\r\n    background: rgba(249,168,212,0.5) !important;\r\n    animation: fe-bdc-blob 7s infinite !important;\r\n}\r\n\r\n.fe-bumble-dead-card .fe-bdc-blob-2 {\r\n    width: 90px !important;\r\n    height: 90px !important;\r\n    top: 4px !important;\r\n    right: -20px !important;\r\n    background: rgba(196,181,253,0.5) !important;\r\n    animation: fe-bdc-blob 7s infinite !important;\r\n    animation-delay: 2s !important;\r\n}\r\n\r\n.fe-bumble-dead-card .fe-bdc-blob-3 {\r\n    width: 80px !important;\r\n    height: 80px !important;\r\n    bottom: -28px !important;\r\n    left: 50px !important;\r\n    background: rgba(254,202,202,0.5) !important;\r\n    animation: fe-bdc-blob 7s infinite !important;\r\n    animation-delay: 4s !important;\r\n}\r\n\r\n@keyframes fe-bdc-blob {\r\n    0%   { transform: translate(0,0) scale(1); }\r\n    33%  { transform: translate(12px,-20px) scale(1.1); }\r\n    66%  { transform: translate(-8px,12px) scale(0.9); }\r\n    100% { transform: translate(0,0) scale(1); }\r\n}\r\n\r\n/* ── Glass overlay ── */\r\n.fe-bumble-dead-card .fe-bdc-glass {\r\n    position: absolute !important;\r\n    inset: 0 !important;\r\n    border-radius: 0 !important;\r\n    background: rgba(255,255,255,0.45) !important;\r\n    border: 1px solid rgba(255,255,255,0.85) !important;\r\n    z-index: 2 !important;\r\n    pointer-events: none !important;\r\n    backdrop-filter: blur(6px) !important;\r\n    -webkit-backdrop-filter: blur(6px) !important;\r\n}\r\n\r\n/* ── Content container ── */\r\n.fe-bumble-dead-card .fe-bdc-content {\r\n    position: relative !important;\r\n    z-index: 10 !important;\r\n    height: 100% !important;\r\n    padding: 10px 10px 10px 12px !important;\r\n    display: flex !important;\r\n    align-items: center !important;\r\n    gap: 10px !important;\r\n}\r\n\r\n/* ── Avatar section ── */\r\n.fe-bumble-dead-card .fe-bdc-avatar-wrap {\r\n    position: relative !important;\r\n    flex-shrink: 0 !important;\r\n    width: 52px !important;\r\n    height: 52px !important;\r\n}\r\n\r\n.fe-bumble-dead-card .fe-bdc-ripple {\r\n    position: absolute !important;\r\n    inset: 0 !important;\r\n    border-radius: 50% !important;\r\n    background: linear-gradient(135deg, #f472b6, #a78bfa) !important;\r\n    animation: fe-bdc-ripple 2s cubic-bezier(0,0.2,0.8,1) infinite !important;\r\n}\r\n\r\n.fe-bumble-dead-card .fe-bdc-ripple-2 {\r\n    animation-delay: 1s !important;\r\n}\r\n\r\n@keyframes fe-bdc-ripple {\r\n    0%   { transform: scale(0.8); opacity: 1; }\r\n    100% { transform: scale(1.65); opacity: 0; }\r\n}\r\n\r\n.fe-bumble-dead-card .fe-bdc-avatar-frame {\r\n    position: relative !important;\r\n    width: 52px !important;\r\n    height: 52px !important;\r\n    border-radius: 18px !important;\r\n    background: linear-gradient(135deg, #ffffff, #fce7f3) !important;\r\n    padding: 4px !important;\r\n    border: 1px solid rgba(255,255,255,0.9) !important;\r\n    display: flex !important;\r\n    align-items: center !important;\r\n    justify-content: center !important;\r\n    transition: transform 0.5s ease !important;\r\n}\r\n\r\n.fe-bumble-dead-card:hover .fe-bdc-avatar-frame {\r\n    transform: scale(1.03) !important;\r\n}\r\n\r\n.fe-bumble-dead-card .fe-bdc-avatar-inner {\r\n    width: 100% !important;\r\n    height: 100% !important;\r\n    border-radius: 14px !important;\r\n    background: #ffffff !important;\r\n    display: flex !important;\r\n    align-items: center !important;\r\n    justify-content: center !important;\r\n    overflow: hidden !important;\r\n    position: relative !important;\r\n}\r\n\r\n.fe-bumble-dead-card .fe-bdc-shine {\r\n    position: absolute !important;\r\n    top: -6px !important;\r\n    right: -6px !important;\r\n    width: 24px !important;\r\n    height: 24px !important;\r\n    background: rgba(244,114,182,0.2) !important;\r\n    border-radius: 50% !important;\r\n    filter: blur(6px) !important;\r\n}\r\n\r\n.fe-bumble-dead-card .fe-bdc-avatar-inner img {\r\n    width: 36px !important;\r\n    height: 36px !important;\r\n    object-fit: contain !important;\r\n    display: block !important;\r\n    position: relative !important;\r\n    z-index: 1 !important;\r\n}\r\n\r\n.fe-bumble-dead-card .fe-bdc-live-dot-wrap {\r\n    position: absolute !important;\r\n    bottom: -2px !important;\r\n    right: -2px !important;\r\n    width: 16px !important;\r\n    height: 16px !important;\r\n    border-radius: 50% !important;\r\n    background: #ffffff !important;\r\n    display: flex !important;\r\n    align-items: center !important;\r\n    justify-content: center !important;\r\n}\r\n\r\n.fe-bumble-dead-card .fe-bdc-live-dot {\r\n    width: 10px !important;\r\n    height: 10px !important;\r\n    border-radius: 50% !important;\r\n    background: #10b981 !important;\r\n    display: block !important;\r\n}\r\n\r\n/* ── Body text ── */\r\n.fe-bumble-dead-card .fe-bdc-body {\r\n    flex: 1 !important;\r\n    min-width: 0 !important;\r\n    display: flex !important;\r\n    flex-direction: column !important;\r\n    justify-content: center !important;\r\n    gap: 3px !important;\r\n    padding-top: 2px !important;\r\n}\r\n\r\n.fe-bumble-dead-card .fe-bdc-header {\r\n    display: flex !important;\r\n    align-items: center !important;\r\n    gap: 6px !important;\r\n    margin-bottom: 1px !important;\r\n}\r\n\r\n.fe-bumble-dead-card .fe-bdc-name {\r\n    font-size: 16px !important;\r\n    font-weight: 800 !important;\r\n    background: linear-gradient(135deg, #4c1d95, #9d174d) !important;\r\n    -webkit-background-clip: text !important;\r\n    -webkit-text-fill-color: transparent !important;\r\n    background-clip: text !important;\r\n    white-space: nowrap !important;\r\n    overflow: hidden !important;\r\n    text-overflow: ellipsis !important;\r\n    line-height: 1.2 !important;\r\n    letter-spacing: -0.02em !important;\r\n}\r\n\r\n/* LIVE badge */\r\n.fe-bumble-dead-card .fe-bdc-badge {\r\n    display: inline-flex !important;\r\n    align-items: center !important;\r\n    gap: 4px !important;\r\n    padding: 3px 8px 3px 5px !important;\r\n    border-radius: 9999px !important;\r\n    background: linear-gradient(135deg, #ecfdf5, #f0fdfa) !important;\r\n    border: 1px solid rgba(110,231,183,0.5) !important;\r\n    flex-shrink: 0 !important;\r\n    position: relative !important;\r\n}\r\n\r\n.fe-bumble-dead-card .fe-bdc-badge-ping {\r\n    position: absolute !important;\r\n    width: 6px !important;\r\n    height: 6px !important;\r\n    border-radius: 50% !important;\r\n    background: #34d399 !important;\r\n    left: 5px !important;\r\n    top: 50% !important;\r\n    transform: translateY(-50%) !important;\r\n    animation: fe-bdc-ping 1.2s cubic-bezier(0,0,0.2,1) infinite !important;\r\n}\r\n\r\n@keyframes fe-bdc-ping {\r\n    75%, 100% { transform: translateY(-50%) scale(2.2); opacity: 0; }\r\n}\r\n\r\n.fe-bumble-dead-card .fe-bdc-badge-dot {\r\n    width: 6px !important;\r\n    height: 6px !important;\r\n    border-radius: 50% !important;\r\n    background: #10b981 !important;\r\n    display: inline-block !important;\r\n    flex-shrink: 0 !important;\r\n    position: relative !important;\r\n    z-index: 1 !important;\r\n}\r\n\r\n.fe-bumble-dead-card .fe-bdc-badge-text {\r\n    font-size: 9px !important;\r\n    font-weight: 900 !important;\r\n    letter-spacing: 0.12em !important;\r\n    color: #059669 !important;\r\n    line-height: 1 !important;\r\n    padding-top: 1px !important;\r\n}\r\n\r\n.fe-bumble-dead-card .fe-bdc-msg {\r\n    font-size: 13px !important;\r\n    font-weight: 500 !important;\r\n    color: rgba(71,85,105,0.9) !important;\r\n    margin: 0 !important;\r\n    padding: 0 !important;\r\n    white-space: nowrap !important;\r\n    overflow: hidden !important;\r\n    text-overflow: ellipsis !important;\r\n    line-height: 1.4 !important;\r\n    padding-right: 4px !important;\r\n}\r\n\r\n/* ── Arrow button ── */\r\n.fe-bumble-dead-card .fe-bdc-arrow {\r\n    flex-shrink: 0 !important;\r\n    width: 32px !important;\r\n    height: 32px !important;\r\n    border-radius: 50% !important;\r\n    background: rgba(255,255,255,0.65) !important;\r\n    display: flex !important;\r\n    align-items: center !important;\r\n    justify-content: center !important;\r\n    color: #a78bfa !important;\r\n    transition: background 0.3s, color 0.3s, transform 0.3s !important;\r\n}\r\n\r\n.fe-bumble-dead-card:hover .fe-bdc-arrow {\r\n    background: rgba(255,255,255,0.95) !important;\r\n    color: #e8197d !important;\r\n    transform: translateX(3px) !important;\r\n}\r\n\r\n.fe-bumble-dead-card.fe-bdc-active .fe-bdc-arrow {\r\n    background: rgba(255,255,255,0.95) !important;\r\n    color: #e8197d !important;\r\n}\r\n\r\n/* ══════════════════════════════════════════════════\r\n   BUMBLE DEAD STATE — CONVERSATION PANEL\r\n   Opens when user clicks the AI Wingman card.\r\n   Overlays the main content area (right of sidebar).\r\n   ══════════════════════════════════════════════════ */\r\n\r\n#fe-bumble-panel {\r\n    position: fixed !important;\r\n    top: 0 !important;\r\n    left: 400px !important;\r\n    right: 0 !important;\r\n    bottom: 0 !important;\r\n    background: #fdf4f8 !important;\r\n    z-index: 9999990 !important;\r\n    display: flex !important;\r\n    flex-direction: column !important;\r\n    font-family: 'Circular', 'Inter', -apple-system, BlinkMacSystemFont, sans-serif !important;\r\n    animation: fe-panel-enter 0.28s cubic-bezier(0.16, 1, 0.3, 1) both !important;\r\n    border-left: 1px solid rgba(232,25,125,0.1) !important;\r\n}\r\n\r\n#fe-bumble-panel * {\r\n    box-sizing: border-box !important;\r\n    font-family: inherit !important;\r\n}\r\n\r\n@keyframes fe-panel-enter {\r\n    from { opacity: 0; transform: translateX(24px); }\r\n    to   { opacity: 1; transform: translateX(0); }\r\n}\r\n\r\n@keyframes fe-panel-exit {\r\n    from { opacity: 1; transform: translateX(0); }\r\n    to   { opacity: 0; transform: translateX(24px); }\r\n}\r\n\r\n#fe-bumble-panel.fe-panel-exiting {\r\n    animation: fe-panel-exit 0.22s ease forwards !important;\r\n    pointer-events: none !important;\r\n}\r\n\r\n/* ── Panel header ── */\r\n#fe-bumble-panel .fe-panel-header {\r\n    display: flex !important;\r\n    align-items: center !important;\r\n    gap: 14px !important;\r\n    padding: 18px 24px !important;\r\n    border-bottom: 1px solid rgba(232,25,125,0.1) !important;\r\n    background: linear-gradient(135deg, #fff0f6 0%, #f5f0ff 100%) !important;\r\n    flex-shrink: 0 !important;\r\n}\r\n\r\n#fe-bumble-panel .fe-panel-avatar {\r\n    position: relative !important;\r\n    width: 46px !important;\r\n    height: 46px !important;\r\n    flex-shrink: 0 !important;\r\n    border-radius: 15px !important;\r\n    background: linear-gradient(135deg, #ffffff, #fce7f3) !important;\r\n    padding: 4px !important;\r\n    border: 1px solid rgba(255,255,255,0.9) !important;\r\n    display: flex !important;\r\n    align-items: center !important;\r\n    justify-content: center !important;\r\n}\r\n\r\n#fe-bumble-panel .fe-panel-avatar-inner-wrap {\r\n    width: 100% !important;\r\n    height: 100% !important;\r\n    border-radius: 11px !important;\r\n    background: #ffffff !important;\r\n    display: flex !important;\r\n    align-items: center !important;\r\n    justify-content: center !important;\r\n    overflow: hidden !important;\r\n}\r\n\r\n#fe-bumble-panel .fe-panel-avatar img {\r\n    width: 30px !important;\r\n    height: 30px !important;\r\n    border-radius: 0 !important;\r\n    object-fit: contain !important;\r\n    border: none !important;\r\n    display: block !important;\r\n}\r\n\r\n#fe-bumble-panel .fe-panel-avatar .fe-panel-dot {\r\n    position: absolute !important;\r\n    bottom: -2px !important;\r\n    right: -2px !important;\r\n    width: 12px !important;\r\n    height: 12px !important;\r\n    border-radius: 50% !important;\r\n    background: #10b981 !important;\r\n    border: 2px solid #fff !important;\r\n}\r\n\r\n#fe-bumble-panel .fe-panel-title {\r\n    flex: 1 !important;\r\n}\r\n\r\n#fe-bumble-panel .fe-panel-name {\r\n    font-size: 17px !important;\r\n    font-weight: 800 !important;\r\n    background: linear-gradient(135deg, #4c1d95, #9d174d) !important;\r\n    -webkit-background-clip: text !important;\r\n    -webkit-text-fill-color: transparent !important;\r\n    background-clip: text !important;\r\n    display: block !important;\r\n    line-height: 1.2 !important;\r\n    letter-spacing: -0.02em !important;\r\n}\r\n\r\n#fe-bumble-panel .fe-panel-subtitle {\r\n    font-size: 12px !important;\r\n    color: #10b981 !important;\r\n    font-weight: 600 !important;\r\n    display: block !important;\r\n    margin-top: 3px !important;\r\n}\r\n\r\n#fe-bumble-panel .fe-panel-close {\r\n    width: 36px !important;\r\n    height: 36px !important;\r\n    border: none !important;\r\n    background: rgba(255,255,255,0.7) !important;\r\n    border-radius: 50% !important;\r\n    cursor: pointer !important;\r\n    display: flex !important;\r\n    align-items: center !important;\r\n    justify-content: center !important;\r\n    color: #6b7280 !important;\r\n    font-size: 18px !important;\r\n    line-height: 1 !important;\r\n    flex-shrink: 0 !important;\r\n    transition: background 0.15s, color 0.15s !important;\r\n    border: 1px solid rgba(0,0,0,0.06) !important;\r\n}\r\n\r\n#fe-bumble-panel .fe-panel-close:hover {\r\n    background: rgba(255,255,255,0.95) !important;\r\n    color: #e8197d !important;\r\n}\r\n\r\n/* ── Message area ── */\r\n#fe-bumble-panel .fe-panel-messages {\r\n    flex: 1 !important;\r\n    overflow-y: auto !important;\r\n    padding: 28px 24px !important;\r\n    display: flex !important;\r\n    flex-direction: column !important;\r\n    gap: 18px !important;\r\n    background: linear-gradient(160deg, #fff5fb 0%, #f9f5ff 50%, #fff8f0 100%) !important;\r\n}\r\n\r\n/* System label */\r\n#fe-bumble-panel .fe-panel-day-label {\r\n    text-align: center !important;\r\n    font-size: 11px !important;\r\n    font-weight: 700 !important;\r\n    color: #c084fc !important;\r\n    letter-spacing: 0.08em !important;\r\n    text-transform: uppercase !important;\r\n    margin: 0 !important;\r\n}\r\n\r\n/* Incoming message bubble */\r\n#fe-bumble-panel .fe-panel-bubble {\r\n    display: flex !important;\r\n    gap: 12px !important;\r\n    align-items: flex-start !important;\r\n    max-width: 82% !important;\r\n}\r\n\r\n#fe-bumble-panel .fe-panel-bubble-avatar {\r\n    width: 34px !important;\r\n    height: 34px !important;\r\n    border-radius: 11px !important;\r\n    flex-shrink: 0 !important;\r\n    overflow: hidden !important;\r\n    margin-top: 2px !important;\r\n    background: linear-gradient(135deg, #ffffff, #fce7f3) !important;\r\n    padding: 3px !important;\r\n    border: 1px solid rgba(232,25,125,0.15) !important;\r\n    display: flex !important;\r\n    align-items: center !important;\r\n    justify-content: center !important;\r\n}\r\n\r\n#fe-bumble-panel .fe-panel-bubble-avatar img {\r\n    width: 22px !important;\r\n    height: 22px !important;\r\n    object-fit: contain !important;\r\n    border-radius: 0 !important;\r\n}\r\n\r\n#fe-bumble-panel .fe-panel-bubble-text {\r\n    background: rgba(255,255,255,0.85) !important;\r\n    border: 1px solid rgba(232,25,125,0.1) !important;\r\n    border-radius: 4px 18px 18px 18px !important;\r\n    padding: 14px 18px !important;\r\n    font-size: 14px !important;\r\n    color: #1e1b4b !important;\r\n    line-height: 1.6 !important;\r\n    box-shadow: 0 2px 12px rgba(232,25,125,0.07) !important;\r\n    backdrop-filter: blur(4px) !important;\r\n    -webkit-backdrop-filter: blur(4px) !important;\r\n}\r\n\r\n/* Stats row inside bubble */\r\n#fe-bumble-panel .fe-panel-stats {\r\n    display: flex !important;\r\n    gap: 8px !important;\r\n    flex-wrap: wrap !important;\r\n    padding: 6px 0 0 !important;\r\n}\r\n\r\n#fe-bumble-panel .fe-panel-stat {\r\n    display: inline-flex !important;\r\n    align-items: center !important;\r\n    background: linear-gradient(135deg, #fdf2f8, #faf5ff) !important;\r\n    border: 1px solid rgba(232,25,125,0.18) !important;\r\n    border-radius: 9999px !important;\r\n    padding: 5px 14px !important;\r\n    font-size: 12px !important;\r\n    font-weight: 600 !important;\r\n    color: #6d28d9 !important;\r\n    white-space: nowrap !important;\r\n}\r\n\r\n#fe-bumble-panel .fe-panel-stat-icon {\r\n    display: inline-flex !important;\r\n    align-items: center !important;\r\n    justify-content: center !important;\r\n    color: #e8197d !important;\r\n    margin-right: 5px !important;\r\n    flex-shrink: 0 !important;\r\n}\r\n\r\n/* Tips list */\r\n#fe-bumble-panel .fe-panel-tips {\r\n    background: linear-gradient(135deg, rgba(255,240,246,0.9), rgba(245,240,255,0.9)) !important;\r\n    border: 1px solid rgba(232,25,125,0.18) !important;\r\n    border-radius: 16px !important;\r\n    padding: 16px 18px !important;\r\n    display: flex !important;\r\n    flex-direction: column !important;\r\n    gap: 9px !important;\r\n    max-width: 82% !important;\r\n    margin-left: 46px !important;\r\n    backdrop-filter: blur(4px) !important;\r\n    -webkit-backdrop-filter: blur(4px) !important;\r\n}\r\n\r\n#fe-bumble-panel .fe-panel-tips-title {\r\n    font-size: 11px !important;\r\n    font-weight: 800 !important;\r\n    background: linear-gradient(135deg, #7c3aed, #e8197d) !important;\r\n    -webkit-background-clip: text !important;\r\n    -webkit-text-fill-color: transparent !important;\r\n    background-clip: text !important;\r\n    text-transform: uppercase !important;\r\n    letter-spacing: 0.08em !important;\r\n}\r\n\r\n#fe-bumble-panel .fe-panel-tip-item {\r\n    font-size: 13px !important;\r\n    color: #4c1d95 !important;\r\n    line-height: 1.5 !important;\r\n    display: flex !important;\r\n    gap: 8px !important;\r\n    align-items: flex-start !important;\r\n    font-weight: 500 !important;\r\n}\r\n\r\n#fe-bumble-panel .fe-panel-tip-item::before {\r\n    content: '→' !important;\r\n    color: #e8197d !important;\r\n    flex-shrink: 0 !important;\r\n    font-weight: 700 !important;\r\n}\r\n\r\n/* ══════════════════════════════════════════════════\r\n   TINDER DEAD STATE CARD\r\n   Dark theme matching Tinder's UI.\r\n   ══════════════════════════════════════════════════ */\r\n\r\n.fe-tinder-dead-card {\r\n    position: relative !important;\r\n    width: 100% !important;\r\n    margin: 0 !important;\r\n    height: 102px !important;\r\n    border-radius: 0 !important;\r\n    cursor: pointer !important;\r\n    overflow: hidden !important;\r\n    background: #1e1e1e !important;\r\n    box-shadow: none !important;\r\n    border-bottom: 1px solid rgba(255,255,255,0.06) !important;\r\n    transition: background 0.2s ease !important;\r\n    animation: fe-tdc-enter 0.4s cubic-bezier(0.16,1,0.3,1) both !important;\r\n    font-family: 'Circular', 'Inter', -apple-system, BlinkMacSystemFont, sans-serif !important;\r\n    box-sizing: border-box !important;\r\n}\r\n\r\n.fe-tinder-dead-card * {\r\n    box-sizing: border-box !important;\r\n    font-family: inherit !important;\r\n}\r\n\r\n.fe-tinder-dead-card:hover {\r\n    background: #252525 !important;\r\n}\r\n\r\n.fe-tinder-dead-card.fe-tdc-exit {\r\n    animation: fe-tdc-exit 0.28s ease forwards !important;\r\n}\r\n\r\n@keyframes fe-tdc-enter {\r\n    from { opacity: 0; transform: translateY(-10px) scale(0.97); }\r\n    to   { opacity: 1; transform: translateY(0) scale(1); }\r\n}\r\n\r\n@keyframes fe-tdc-exit {\r\n    to { opacity: 0; transform: translateY(-6px) scale(0.97); }\r\n}\r\n\r\n/* ── Gradient base ── */\r\n.fe-tinder-dead-card .fe-tdc-gradient-base {\r\n    position: absolute !important;\r\n    inset: 0 !important;\r\n    background: linear-gradient(135deg, rgba(253,41,123,0.12) 0%, rgba(255,107,107,0.08) 50%, rgba(253,85,100,0.06) 100%) !important;\r\n    z-index: 0 !important;\r\n}\r\n\r\n/* ── Blobs ── */\r\n.fe-tinder-dead-card .fe-tdc-blob {\r\n    position: absolute !important;\r\n    border-radius: 50% !important;\r\n    filter: blur(18px) !important;\r\n    z-index: 1 !important;\r\n    pointer-events: none !important;\r\n}\r\n\r\n.fe-tinder-dead-card .fe-tdc-blob-1 {\r\n    width: 80px !important;\r\n    height: 80px !important;\r\n    top: -20px !important;\r\n    left: -16px !important;\r\n    background: rgba(253,41,123,0.35) !important;\r\n    animation: fe-tdc-blob 7s infinite !important;\r\n}\r\n\r\n.fe-tinder-dead-card .fe-tdc-blob-2 {\r\n    width: 90px !important;\r\n    height: 90px !important;\r\n    top: 4px !important;\r\n    right: -20px !important;\r\n    background: rgba(255,107,107,0.25) !important;\r\n    animation: fe-tdc-blob 7s infinite !important;\r\n    animation-delay: 2s !important;\r\n}\r\n\r\n.fe-tinder-dead-card .fe-tdc-blob-3 {\r\n    width: 80px !important;\r\n    height: 80px !important;\r\n    bottom: -28px !important;\r\n    left: 50px !important;\r\n    background: rgba(253,85,100,0.2) !important;\r\n    animation: fe-tdc-blob 7s infinite !important;\r\n    animation-delay: 4s !important;\r\n}\r\n\r\n@keyframes fe-tdc-blob {\r\n    0%   { transform: translate(0,0) scale(1); }\r\n    33%  { transform: translate(12px,-20px) scale(1.1); }\r\n    66%  { transform: translate(-8px,12px) scale(0.9); }\r\n    100% { transform: translate(0,0) scale(1); }\r\n}\r\n\r\n/* ── Glass overlay ── */\r\n.fe-tinder-dead-card .fe-tdc-glass {\r\n    position: absolute !important;\r\n    inset: 1px !important;\r\n    border-radius: 0 !important;\r\n    background: rgba(255,255,255,0.03) !important;\r\n    border: 1px solid rgba(255,255,255,0.06) !important;\r\n    z-index: 2 !important;\r\n    pointer-events: none !important;\r\n}\r\n\r\n/* ── Content ── */\r\n.fe-tinder-dead-card .fe-tdc-content {\r\n    position: relative !important;\r\n    z-index: 10 !important;\r\n    height: 100% !important;\r\n    padding: 14px 12px 14px 16px !important;\r\n    display: flex !important;\r\n    align-items: center !important;\r\n    gap: 12px !important;\r\n}\r\n\r\n/* ── Avatar ── */\r\n.fe-tinder-dead-card .fe-tdc-avatar-wrap {\r\n    position: relative !important;\r\n    flex-shrink: 0 !important;\r\n    width: 62px !important;\r\n    height: 62px !important;\r\n}\r\n\r\n.fe-tinder-dead-card .fe-tdc-ripple {\r\n    position: absolute !important;\r\n    inset: 0 !important;\r\n    border-radius: 50% !important;\r\n    background: linear-gradient(135deg, #fd297b, #ff6b6b) !important;\r\n    animation: fe-tdc-ripple 2s cubic-bezier(0,0.2,0.8,1) infinite !important;\r\n}\r\n\r\n.fe-tinder-dead-card .fe-tdc-ripple-2 {\r\n    animation-delay: 1s !important;\r\n}\r\n\r\n@keyframes fe-tdc-ripple {\r\n    0%   { transform: scale(0.8); opacity: 1; }\r\n    100% { transform: scale(1.65); opacity: 0; }\r\n}\r\n\r\n.fe-tinder-dead-card .fe-tdc-avatar-frame {\r\n    position: relative !important;\r\n    width: 62px !important;\r\n    height: 62px !important;\r\n    border-radius: 20px !important;\r\n    background: linear-gradient(135deg, #2a2a2a, #1a1a1a) !important;\r\n    padding: 4px !important;\r\n    border: 1px solid rgba(253,41,123,0.3) !important;\r\n    display: flex !important;\r\n    align-items: center !important;\r\n    justify-content: center !important;\r\n    transition: transform 0.5s ease !important;\r\n}\r\n\r\n.fe-tinder-dead-card:hover .fe-tdc-avatar-frame {\r\n    transform: scale(1.03) !important;\r\n}\r\n\r\n.fe-tinder-dead-card .fe-tdc-avatar-inner {\r\n    width: 100% !important;\r\n    height: 100% !important;\r\n    border-radius: 14px !important;\r\n    background: #ffffff !important;\r\n    display: flex !important;\r\n    align-items: center !important;\r\n    justify-content: center !important;\r\n    overflow: hidden !important;\r\n    position: relative !important;\r\n}\r\n\r\n.fe-tinder-dead-card .fe-tdc-shine {\r\n    position: absolute !important;\r\n    top: -6px !important;\r\n    right: -6px !important;\r\n    width: 24px !important;\r\n    height: 24px !important;\r\n    background: rgba(253,41,123,0.2) !important;\r\n    border-radius: 50% !important;\r\n    filter: blur(6px) !important;\r\n}\r\n\r\n.fe-tinder-dead-card .fe-tdc-avatar-inner img {\r\n    width: 36px !important;\r\n    height: 36px !important;\r\n    object-fit: contain !important;\r\n    display: block !important;\r\n    position: relative !important;\r\n    z-index: 1 !important;\r\n}\r\n\r\n.fe-tinder-dead-card .fe-tdc-live-dot-wrap {\r\n    position: absolute !important;\r\n    bottom: -2px !important;\r\n    right: -2px !important;\r\n    width: 16px !important;\r\n    height: 16px !important;\r\n    border-radius: 50% !important;\r\n    background: #1e1e1e !important;\r\n    display: flex !important;\r\n    align-items: center !important;\r\n    justify-content: center !important;\r\n}\r\n\r\n.fe-tinder-dead-card .fe-tdc-live-dot {\r\n    width: 10px !important;\r\n    height: 10px !important;\r\n    border-radius: 50% !important;\r\n    background: #10b981 !important;\r\n    display: block !important;\r\n}\r\n\r\n/* ── Body ── */\r\n.fe-tinder-dead-card .fe-tdc-body {\r\n    flex: 1 !important;\r\n    min-width: 0 !important;\r\n    display: flex !important;\r\n    flex-direction: column !important;\r\n    justify-content: center !important;\r\n    gap: 3px !important;\r\n    padding-top: 2px !important;\r\n}\r\n\r\n.fe-tinder-dead-card .fe-tdc-header {\r\n    display: flex !important;\r\n    align-items: center !important;\r\n    gap: 6px !important;\r\n    margin-bottom: 1px !important;\r\n}\r\n\r\n.fe-tinder-dead-card .fe-tdc-name {\r\n    font-size: 15px !important;\r\n    font-weight: 800 !important;\r\n    background: linear-gradient(135deg, #ff6b6b, #fd297b) !important;\r\n    -webkit-background-clip: text !important;\r\n    -webkit-text-fill-color: transparent !important;\r\n    background-clip: text !important;\r\n    white-space: nowrap !important;\r\n    overflow: hidden !important;\r\n    text-overflow: ellipsis !important;\r\n    line-height: 1.2 !important;\r\n    letter-spacing: -0.02em !important;\r\n}\r\n\r\n/* LIVE badge */\r\n.fe-tinder-dead-card .fe-tdc-badge {\r\n    display: inline-flex !important;\r\n    align-items: center !important;\r\n    gap: 4px !important;\r\n    padding: 3px 8px 3px 5px !important;\r\n    border-radius: 9999px !important;\r\n    background: rgba(16,185,129,0.1) !important;\r\n    border: 1px solid rgba(16,185,129,0.3) !important;\r\n    flex-shrink: 0 !important;\r\n    position: relative !important;\r\n}\r\n\r\n.fe-tinder-dead-card .fe-tdc-badge-ping {\r\n    position: absolute !important;\r\n    width: 6px !important;\r\n    height: 6px !important;\r\n    border-radius: 50% !important;\r\n    background: #34d399 !important;\r\n    left: 5px !important;\r\n    top: 50% !important;\r\n    transform: translateY(-50%) !important;\r\n    animation: fe-tdc-ping 1.2s cubic-bezier(0,0,0.2,1) infinite !important;\r\n}\r\n\r\n@keyframes fe-tdc-ping {\r\n    75%, 100% { transform: translateY(-50%) scale(2.2); opacity: 0; }\r\n}\r\n\r\n.fe-tinder-dead-card .fe-tdc-badge-dot {\r\n    width: 6px !important;\r\n    height: 6px !important;\r\n    border-radius: 50% !important;\r\n    background: #10b981 !important;\r\n    display: inline-block !important;\r\n    flex-shrink: 0 !important;\r\n    position: relative !important;\r\n    z-index: 1 !important;\r\n}\r\n\r\n.fe-tinder-dead-card .fe-tdc-badge-text {\r\n    font-size: 9px !important;\r\n    font-weight: 900 !important;\r\n    letter-spacing: 0.12em !important;\r\n    color: #10b981 !important;\r\n    line-height: 1 !important;\r\n    padding-top: 1px !important;\r\n}\r\n\r\n.fe-tinder-dead-card .fe-tdc-msg {\r\n    font-size: 13px !important;\r\n    font-weight: 400 !important;\r\n    color: rgba(255,255,255,0.55) !important;\r\n    margin: 0 !important;\r\n    padding: 0 !important;\r\n    white-space: nowrap !important;\r\n    overflow: hidden !important;\r\n    text-overflow: ellipsis !important;\r\n    line-height: 1.4 !important;\r\n    padding-right: 4px !important;\r\n}\r\n\r\n/* ── Arrow ── */\r\n.fe-tinder-dead-card .fe-tdc-arrow {\r\n    flex-shrink: 0 !important;\r\n    width: 32px !important;\r\n    height: 32px !important;\r\n    border-radius: 50% !important;\r\n    background: rgba(255,255,255,0.06) !important;\r\n    display: flex !important;\r\n    align-items: center !important;\r\n    justify-content: center !important;\r\n    color: rgba(255,255,255,0.3) !important;\r\n    transition: background 0.3s, color 0.3s, transform 0.3s !important;\r\n}\r\n\r\n.fe-tinder-dead-card:hover .fe-tdc-arrow {\r\n    background: rgba(253,41,123,0.15) !important;\r\n    color: #fd297b !important;\r\n    transform: translateX(3px) !important;\r\n}\r\n\r\n.fe-tinder-dead-card.fe-tdc-active .fe-tdc-arrow {\r\n    background: rgba(253,41,123,0.2) !important;\r\n    color: #fd297b !important;\r\n}\r\n\r\n/* ══════════════════════════════════════════════════\r\n   TINDER DEAD STATE — CONVERSATION PANEL\r\n   Dark theme, opens right of the Tinder sidebar.\r\n   ══════════════════════════════════════════════════ */\r\n\r\n#fe-tinder-panel {\r\n    position: fixed !important;\r\n    top: 0 !important;\r\n    left: 360px !important;\r\n    right: 0 !important;\r\n    bottom: 0 !important;\r\n    background: #111111 !important;\r\n    z-index: 9999990 !important;\r\n    display: flex !important;\r\n    flex-direction: column !important;\r\n    font-family: 'Circular', 'Inter', -apple-system, BlinkMacSystemFont, sans-serif !important;\r\n    animation: fe-tpanel-enter 0.28s cubic-bezier(0.16, 1, 0.3, 1) both !important;\r\n    border-left: 1px solid rgba(253,41,123,0.15) !important;\r\n}\r\n\r\n#fe-tinder-panel * {\r\n    box-sizing: border-box !important;\r\n    font-family: inherit !important;\r\n}\r\n\r\n@keyframes fe-tpanel-enter {\r\n    from { opacity: 0; transform: translateX(24px); }\r\n    to   { opacity: 1; transform: translateX(0); }\r\n}\r\n\r\n@keyframes fe-tpanel-exit {\r\n    from { opacity: 1; transform: translateX(0); }\r\n    to   { opacity: 0; transform: translateX(24px); }\r\n}\r\n\r\n#fe-tinder-panel.fe-tpanel-exiting {\r\n    animation: fe-tpanel-exit 0.25s ease forwards !important;\r\n    pointer-events: none !important;\r\n}\r\n\r\n/* ── Panel header ── */\r\n#fe-tinder-panel .fe-tpanel-header {\r\n    display: flex !important;\r\n    align-items: center !important;\r\n    gap: 14px !important;\r\n    padding: 18px 24px !important;\r\n    border-bottom: 1px solid rgba(253,41,123,0.12) !important;\r\n    background: linear-gradient(135deg, rgba(253,41,123,0.1) 0%, rgba(255,107,107,0.06) 100%) !important;\r\n    flex-shrink: 0 !important;\r\n}\r\n\r\n#fe-tinder-panel .fe-tpanel-avatar {\r\n    position: relative !important;\r\n    width: 46px !important;\r\n    height: 46px !important;\r\n    flex-shrink: 0 !important;\r\n    border-radius: 15px !important;\r\n    background: linear-gradient(135deg, #2a2a2a, #1a1a1a) !important;\r\n    padding: 4px !important;\r\n    border: 1px solid rgba(253,41,123,0.25) !important;\r\n    display: flex !important;\r\n    align-items: center !important;\r\n    justify-content: center !important;\r\n}\r\n\r\n#fe-tinder-panel .fe-tpanel-avatar-inner {\r\n    width: 100% !important;\r\n    height: 100% !important;\r\n    border-radius: 11px !important;\r\n    background: #ffffff !important;\r\n    display: flex !important;\r\n    align-items: center !important;\r\n    justify-content: center !important;\r\n    overflow: hidden !important;\r\n}\r\n\r\n#fe-tinder-panel .fe-tpanel-avatar img {\r\n    width: 30px !important;\r\n    height: 30px !important;\r\n    object-fit: contain !important;\r\n    display: block !important;\r\n    border: none !important;\r\n    border-radius: 0 !important;\r\n}\r\n\r\n#fe-tinder-panel .fe-tpanel-avatar .fe-tpanel-dot {\r\n    position: absolute !important;\r\n    bottom: -2px !important;\r\n    right: -2px !important;\r\n    width: 12px !important;\r\n    height: 12px !important;\r\n    border-radius: 50% !important;\r\n    background: #10b981 !important;\r\n    border: 2px solid #111 !important;\r\n}\r\n\r\n#fe-tinder-panel .fe-tpanel-title {\r\n    flex: 1 !important;\r\n}\r\n\r\n#fe-tinder-panel .fe-tpanel-name {\r\n    font-size: 17px !important;\r\n    font-weight: 800 !important;\r\n    background: linear-gradient(135deg, #ff6b6b, #fd297b) !important;\r\n    -webkit-background-clip: text !important;\r\n    -webkit-text-fill-color: transparent !important;\r\n    background-clip: text !important;\r\n    display: block !important;\r\n    line-height: 1.2 !important;\r\n    letter-spacing: -0.02em !important;\r\n}\r\n\r\n#fe-tinder-panel .fe-tpanel-subtitle {\r\n    font-size: 12px !important;\r\n    color: #10b981 !important;\r\n    font-weight: 600 !important;\r\n    display: block !important;\r\n    margin-top: 3px !important;\r\n}\r\n\r\n#fe-tinder-panel .fe-tpanel-close {\r\n    width: 36px !important;\r\n    height: 36px !important;\r\n    border: none !important;\r\n    background: rgba(255,255,255,0.06) !important;\r\n    border-radius: 50% !important;\r\n    cursor: pointer !important;\r\n    display: flex !important;\r\n    align-items: center !important;\r\n    justify-content: center !important;\r\n    color: rgba(255,255,255,0.4) !important;\r\n    font-size: 18px !important;\r\n    line-height: 1 !important;\r\n    flex-shrink: 0 !important;\r\n    transition: background 0.15s, color 0.15s !important;\r\n    border: 1px solid rgba(255,255,255,0.08) !important;\r\n}\r\n\r\n#fe-tinder-panel .fe-tpanel-close:hover {\r\n    background: rgba(253,41,123,0.15) !important;\r\n    color: #fd297b !important;\r\n}\r\n\r\n/* ── Messages area ── */\r\n#fe-tinder-panel .fe-tpanel-messages {\r\n    flex: 1 !important;\r\n    overflow-y: auto !important;\r\n    padding: 28px 24px !important;\r\n    display: flex !important;\r\n    flex-direction: column !important;\r\n    gap: 18px !important;\r\n    background: linear-gradient(160deg, #141414 0%, #0f0f0f 50%, #131313 100%) !important;\r\n}\r\n\r\n#fe-tinder-panel .fe-tpanel-day-label {\r\n    text-align: center !important;\r\n    font-size: 11px !important;\r\n    font-weight: 700 !important;\r\n    color: rgba(253,41,123,0.5) !important;\r\n    letter-spacing: 0.08em !important;\r\n    text-transform: uppercase !important;\r\n    margin: 0 !important;\r\n}\r\n\r\n/* ── Bubbles ── */\r\n#fe-tinder-panel .fe-tpanel-bubble {\r\n    display: flex !important;\r\n    gap: 12px !important;\r\n    align-items: flex-start !important;\r\n    max-width: 82% !important;\r\n}\r\n\r\n#fe-tinder-panel .fe-tpanel-bubble-avatar {\r\n    width: 34px !important;\r\n    height: 34px !important;\r\n    border-radius: 11px !important;\r\n    flex-shrink: 0 !important;\r\n    margin-top: 2px !important;\r\n    background: linear-gradient(135deg, #2a2a2a, #1a1a1a) !important;\r\n    padding: 3px !important;\r\n    border: 1px solid rgba(253,41,123,0.2) !important;\r\n    display: flex !important;\r\n    align-items: center !important;\r\n    justify-content: center !important;\r\n    overflow: hidden !important;\r\n}\r\n\r\n#fe-tinder-panel .fe-tpanel-bubble-avatar img {\r\n    width: 22px !important;\r\n    height: 22px !important;\r\n    object-fit: contain !important;\r\n    border-radius: 0 !important;\r\n}\r\n\r\n#fe-tinder-panel .fe-tpanel-bubble-text {\r\n    background: rgba(255,255,255,0.05) !important;\r\n    border: 1px solid rgba(253,41,123,0.12) !important;\r\n    border-radius: 4px 18px 18px 18px !important;\r\n    padding: 14px 18px !important;\r\n    font-size: 14px !important;\r\n    color: rgba(255,255,255,0.85) !important;\r\n    line-height: 1.6 !important;\r\n    box-shadow: 0 2px 12px rgba(0,0,0,0.3) !important;\r\n}\r\n\r\n/* ── Stats ── */\r\n#fe-tinder-panel .fe-tpanel-stats {\r\n    display: flex !important;\r\n    gap: 8px !important;\r\n    flex-wrap: wrap !important;\r\n    padding: 10px 0 0 !important;\r\n}\r\n\r\n#fe-tinder-panel .fe-tpanel-stat {\r\n    display: inline-flex !important;\r\n    align-items: center !important;\r\n    background: rgba(253,41,123,0.08) !important;\r\n    border: 1px solid rgba(253,41,123,0.2) !important;\r\n    border-radius: 9999px !important;\r\n    padding: 5px 14px !important;\r\n    font-size: 12px !important;\r\n    font-weight: 600 !important;\r\n    color: rgba(255,255,255,0.7) !important;\r\n    white-space: nowrap !important;\r\n}\r\n\r\n#fe-tinder-panel .fe-tpanel-stat-icon {\r\n    display: inline-flex !important;\r\n    align-items: center !important;\r\n    justify-content: center !important;\r\n    color: #fd297b !important;\r\n    margin-right: 5px !important;\r\n    flex-shrink: 0 !important;\r\n}\r\n\r\n/* ── Tips ── */\r\n#fe-tinder-panel .fe-tpanel-tips {\r\n    background: rgba(253,41,123,0.06) !important;\r\n    border: 1px solid rgba(253,41,123,0.18) !important;\r\n    border-radius: 16px !important;\r\n    padding: 16px 18px !important;\r\n    display: flex !important;\r\n    flex-direction: column !important;\r\n    gap: 9px !important;\r\n    max-width: 82% !important;\r\n    margin-left: 46px !important;\r\n}\r\n\r\n#fe-tinder-panel .fe-tpanel-tips-title {\r\n    font-size: 11px !important;\r\n    font-weight: 800 !important;\r\n    background: linear-gradient(135deg, #ff6b6b, #fd297b) !important;\r\n    -webkit-background-clip: text !important;\r\n    -webkit-text-fill-color: transparent !important;\r\n    background-clip: text !important;\r\n    text-transform: uppercase !important;\r\n    letter-spacing: 0.08em !important;\r\n}\r\n\r\n#fe-tinder-panel .fe-tpanel-tip-item {\r\n    font-size: 13px !important;\r\n    color: rgba(255,255,255,0.6) !important;\r\n    line-height: 1.5 !important;\r\n    display: flex !important;\r\n    gap: 8px !important;\r\n    align-items: flex-start !important;\r\n    font-weight: 500 !important;\r\n}\r\n\r\n#fe-tinder-panel .fe-tpanel-tip-item::before {\r\n    content: '→' !important;\r\n    color: #fd297b !important;\r\n    flex-shrink: 0 !important;\r\n    font-weight: 700 !important;\r\n}\r\n\r\n/* ── Tinder Login Modal Touch Defense (On-Device Mode) ── */\r\n#modal-manager, .modal-manager-portal {\r\n    position: relative !important;\r\n    z-index: 9999999 !important;\r\n    pointer-events: auto !important;\r\n}\r\n.modal-wrapper {\r\n    z-index: 9999999 !important;\r\n    pointer-events: auto !important;\r\n}\r\n[role=\"dialog\"]:not(#rebrand-mobile-menu),\r\ndiv[aria-modal=\"true\"]:not(#rebrand-mobile-menu),\r\n.modal:not(#rebrand-mobile-menu) {\r\n    z-index: 9999999 !important;\r\n    pointer-events: auto !important;\r\n}\r\n\r\n/* #rebrand-mobile-menu is Tinder's mobile nav drawer. It is also role=\"dialog\",\r\n   it stays in the DOM while closed, and it fills the viewport. Force-enabling\r\n   pointer-events on it (as the rule above used to do) makes the invisible closed\r\n   drawer sit on top of the login modal and swallow every tap — the 3 login\r\n   buttons paint fine but are completely untappable. Keep the container\r\n   transparent to touch; only re-enable its own controls when it is really open. */\r\n#rebrand-mobile-menu {\r\n    pointer-events: none !important;\r\n}\r\n#rebrand-mobile-menu[aria-modal=\"true\"] a,\r\n#rebrand-mobile-menu[aria-modal=\"true\"] button {\r\n    pointer-events: auto !important;\r\n}\r\n\r\n[role=\"dialog\"]:not(#rebrand-mobile-menu) button,\r\n[role=\"dialog\"]:not(#rebrand-mobile-menu) a,\r\n[role=\"dialog\"]:not(#rebrand-mobile-menu) input,\r\n[role=\"dialog\"]:not(#rebrand-mobile-menu) iframe {\r\n    pointer-events: auto !important;\r\n    touch-action: manipulation !important;\r\n    cursor: pointer !important;\r\n}\r\nbody.fe-modal-open .mobile-navigation-drawer,\r\nbody.fe-modal-open .mobile-navigation-drawer-overlay,\r\nbody:has([role=\"dialog\"]) .mobile-navigation-drawer,\r\nbody:has([role=\"dialog\"]) .mobile-navigation-drawer-overlay {\r\n    display: none !important;\r\n    pointer-events: none !important;\r\n    visibility: hidden !important;\r\n    z-index: -9999 !important;\r\n}\r\n.mobile-navigation-drawer-overlay, [class*=\"drawer-overlay\"] {\r\n    display: none !important;\r\n    opacity: 0 !important;\r\n    pointer-events: none !important;\r\n    visibility: hidden !important;\r\n    z-index: -9999 !important;\r\n    height: 0 !important;\r\n    width: 0 !important;\r\n}\r\n`;\n  (document.head || document.documentElement).appendChild(style);\n})();\n\n\n  \n// ═══════════════════════════════════════════════════════════════\n// [FlirtEasy Bundle] debug-config.js\n// ═══════════════════════════════════════════════════════════════\n/**\r\n * Global Debug Configuration\r\n * \r\n * IMPORTANT: Set DEBUG_ENABLED to true ONLY during development\r\n * Set to false before distributing to users to disable all console logs\r\n */\r\n\r\nconst DEBUG_ENABLED = false;\r\n\r\n\r\nconst getGlobal = () => {\r\n  if (typeof globalThis !== 'undefined') return globalThis;\r\n  if (typeof window !== 'undefined') return window;\r\n  if (typeof self !== 'undefined') return self;\r\n  if (typeof global !== 'undefined') return global;\r\n  return null;\r\n};\r\n\r\nconst _global = getGlobal();\r\n\r\nif (_global && _global.console) {\r\n  const originalLog = _global.console.log.bind(_global.console);\r\n  const originalWarn = _global.console.warn.bind(_global.console);\r\n  const originalError = _global.console.error.bind(_global.console);\r\n  const originalInfo = _global.console.info.bind(_global.console);\r\n  const originalDebug = _global.console.debug.bind(_global.console);\r\n\r\n  _global.__flirtEasyLogHistory = _global.__flirtEasyLogHistory || [];\r\n  const logHistoryArray = _global.__flirtEasyLogHistory;\r\n\r\n  const pushToHistory = (level, args) => {\r\n    let messageStr = '';\r\n    let dataObj = null;\r\n\r\n    if (args.length > 0) {\r\n      messageStr = (typeof args[0] === 'string') ? args[0] : JSON.stringify(args[0]);\r\n    }\r\n    if (args.length > 1) {\r\n      if (args.length === 2) {\r\n        dataObj = args[1];\r\n      } else {\r\n        dataObj = args.slice(1);\r\n      }\r\n    }\r\n\r\n    const logEntry = {\r\n      timestamp: new Date().toISOString(),\r\n      level: level,\r\n      message: messageStr,\r\n      data: (dataObj !== null && typeof dataObj === 'object') ? dataObj : (dataObj !== null ? { value: dataObj } : null)\r\n    };\r\n\r\n    logHistoryArray.push(logEntry);\r\n    if (logHistoryArray.length > 1000) {\r\n      logHistoryArray.shift();\r\n    }\r\n  };\r\n\r\n  // Rate-limit remote log forwarding (max 1000 per session) — runs in BOTH dev and prod\r\n  let _remoteLogCount = 0;\r\n  const MAX_REMOTE_LOGS = 2000;\r\n\r\n  const _FORWARD_PATTERN = /^\\[FlirtEasy|^\\[Background|^\\[Content|^\\[Message|^\\[Bumble|^\\[Tinder|^\\[AI|^\\[Storage|^\\[Network|^\\[Profile|^\\[Scanner|^\\[Achievement|^\\[Stripe|^\\[Account|^\\[Error|^\\[Warn|^->|^\\[FLIRTEASY/i;\r\n\r\n  let _lbatch = [];\r\n  let _lflushTimer = null;\r\n\r\n  const _storeLogsToStorage = (payloads) => {\r\n    try {\r\n      chrome.storage.local.get(['_lbuf'], (r) => {\r\n        const buf = r._lbuf || [];\r\n        buf.push(...payloads);\r\n        if (buf.length > 500) buf.splice(0, buf.length - 500);\r\n        chrome.storage.local.set({ _lbuf: buf });\r\n      });\r\n    } catch (_) { }\r\n  };\r\n\r\n  const _flushLogBatch = () => {\r\n    _lflushTimer = null;\r\n    if (_lbatch.length === 0) return;\r\n    if (typeof trackEvent === 'function') return;\r\n    const batch = _lbatch.splice(0);\r\n    try {\r\n      chrome.runtime.sendMessage({ action: 'remoteLogBatch', payloads: batch })\r\n        .catch(() => { _storeLogsToStorage(batch); });\r\n    } catch (_) {\r\n      _storeLogsToStorage(batch);\r\n    }\r\n  };\r\n\r\n  const forwardLog = (level, args) => {\r\n    if (level === 'DEBUG') return;\r\n    if (_remoteLogCount >= MAX_REMOTE_LOGS) return;\r\n    const msg = typeof args[0] === 'string' ? args[0] : (args[0] != null ? String(args[0]) : null);\r\n    if (!msg || msg.trim() === '') return;\r\n    if (level === 'INFO' && !_FORWARD_PATTERN.test(msg)) return;\r\n    _remoteLogCount++;\r\n    const raw1 = args.length > 1 ? args[1] : null;\r\n    const extra = raw1 != null ? (typeof raw1 === 'object' ? (() => { try { return JSON.stringify(raw1).slice(0, 300); } catch (_) { return String(raw1); } })() : String(raw1).slice(0, 300)) : null;\r\n    const payload = { level, message: msg.slice(0, 500), extra };\r\n    try {\r\n      if (typeof trackEvent === 'function') {\r\n        return;\r\n      }\r\n      _lbatch.push(payload);\r\n      if (!_lflushTimer) _lflushTimer = setTimeout(_flushLogBatch, 500);\r\n    } catch (_) { }\r\n  };\r\n\r\n  if (!DEBUG_ENABLED) {\r\n    _global.console.log = (...args) => forwardLog('INFO', args);\r\n    _global.console.info = (...args) => forwardLog('INFO', args);\r\n    _global.console.debug = (...args) => forwardLog('DEBUG', args);\r\n    _global.console.warn = (...args) => forwardLog('WARN', args);\r\n    _global.console.error = (...args) => forwardLog('ERROR', args);\r\n  } else {\r\n    const formatMessage = (args, type) => {\r\n      // ... keep the CSS formatting exactly as it is ...\r\n      if (args.length > 0 && typeof args[0] === 'string') {\r\n        const msg = args[0];\r\n        const match = msg.match(/^\\[(.*?)\\]/);\r\n\r\n        if (match) {\r\n          const module = match[1];\r\n          let bg = '#374151';\r\n          let text = '#f3f4f6';\r\n          let icon = '';\r\n          const moduleUpper = module.toUpperCase();\r\n          if (moduleUpper.includes('TINDER')) { bg = '#e11d48'; icon = ''; }\r\n          else if (moduleUpper.includes('BUMBLE')) { bg = '#f59e0b'; text = '#000000'; icon = ''; }\r\n          else if (moduleUpper.includes('AI') || moduleUpper.includes('OPENAI') || moduleUpper.includes('FLIRTEASY')) { bg = '#4f46e5'; text = '#ffffff'; icon = ''; }\r\n          else if (moduleUpper.includes('STORAGE') || moduleUpper.includes('DB')) { bg = '#0f766e'; text = '#ffffff'; icon = ''; }\r\n          else if (moduleUpper.includes('NETWORK') || moduleUpper.includes('API')) { bg = '#0369a1'; text = '#ffffff'; icon = ''; }\r\n          else if (moduleUpper.includes('BACKGROUND') || moduleUpper.includes('WORKER')) { bg = '#334155'; text = '#ffffff'; icon = ''; }\r\n          else if (moduleUpper.includes('ACHIEVEMENT') || moduleUpper.includes('MIGRATION')) { bg = '#b45309'; text = '#ffffff'; icon = ''; }\r\n          else if (moduleUpper.includes('POPUP') || moduleUpper.includes('UI')) { bg = '#be185d'; text = '#ffffff'; icon = ''; }\r\n          else if (moduleUpper.includes('STRIPE') || moduleUpper.includes('ACCOUNT')) { bg = '#4338ca'; text = '#ffffff'; icon = ''; }\r\n          else if (moduleUpper.includes('PROFILE') || moduleUpper.includes('PARSER') || moduleUpper.includes('CONTENT')) { bg = '#0f766e'; text = '#ffffff'; icon = ''; }\r\n          else if (moduleUpper.includes('MESSAGE') || moduleUpper.includes('CHAT')) { bg = '#1d4ed8'; text = '#ffffff'; icon = ''; }\r\n          else if (moduleUpper.includes('ERROR') || moduleUpper.includes('FAIL')) { bg = '#be123c'; text = '#ffffff'; icon = ''; }\r\n          else if (moduleUpper.includes('SCANNER') || moduleUpper.includes('TRACKER')) { bg = '#4d7c0f'; text = '#ffffff'; icon = ''; }\r\n          else if (moduleUpper.includes('HEADER') || moduleUpper.includes('PHASE')) { bg = '#1e293b'; text = '#ffffff'; icon = ''; }\r\n\r\n          let typeText = '';\r\n          let typeColor = 'inherit';\r\n\r\n          if (type === 'WARN') {\r\n\r\n\r\n            typeText = ' [WARN]';\r\n            typeColor = '#d97706';\r\n            bg = '#fffbeb';\r\n            text = '#92400e';\r\n          } else if (type === 'ERROR') {\r\n            typeText = ' [ERROR]';\r\n            typeColor = '#dc2626';\r\n            bg = '#fef2f2';\r\n            text = '#991b1b';\r\n          } else if (type === 'HEADER') {\r\n            bg = '#1e293b';\r\n            text = '#ffffff';\r\n            typeText = '';\r\n          }\r\n\r\n          const badgeCss = `background: ${bg}; color: ${text}; padding: 2px 7px; border-radius: 4px; font-weight: 700; font-size: 10px; margin-right: 6px; text-transform: uppercase; letter-spacing: 0.5px; border: 1px solid rgba(0,0,0,0.1);`;\r\n          const textCss = (type === 'WARN' || type === 'ERROR') ? `color: ${typeColor}; font-weight: 600;` : (type === 'HEADER' ? `color: #1e293b; font-weight: 800; font-size: 1.1em; text-decoration: underline;` : `color: inherit;`);\r\n\r\n          const plainMsg = msg.substring(match[0].length).trim();\r\n\r\n          args[0] = `%c${match[0]}%c${typeText} ${plainMsg}`;\r\n          args.splice(1, 0, badgeCss, textCss);\r\n        } else {\r\n          if (type === 'WARN') {\r\n            args[0] = `%c[WARN] ${args[0]}`;\r\n            args.splice(1, 0, `color: #d97706; font-weight: 600;`);\r\n          } else if (type === 'ERROR') {\r\n            args[0] = `%c[ERROR] ${args[0]}`;\r\n            args.splice(1, 0, `color: #dc2626; font-weight: 600;`);\r\n          } else if (type === 'INFO') {\r\n            args[0] = `%c[INFO] ${args[0]}`;\r\n            args.splice(1, 0, `color: #2563eb; font-weight: 500;`);\r\n          } else if (type === 'HEADER') {\r\n            args[0] = `%c${args[0]}`;\r\n            args.splice(1, 0, `color: #1e293b; font-weight: 800; font-size: 1.2em;`);\r\n          }\r\n        }\r\n      }\r\n      return args;\r\n    };\r\n\r\n    const cloneArgs = (args) => {\r\n      // Need to deeply copy objects if possible, but shallow array clone is fine for isolating formatMessage mutations\r\n      return [...args];\r\n    };\r\n\r\n    const _safeOriginalLog = (fn, args) => {\r\n      try {\r\n        fn.apply(null, args);\r\n      } catch (_) { }\r\n    };\r\n\r\n    _global.console.log = function (...args) {\r\n      pushToHistory('INFO', args);\r\n      forwardLog('INFO', args);\r\n      _safeOriginalLog(originalLog, formatMessage(cloneArgs(args), 'LOG'));\r\n    };\r\n    _global.console.info = function (...args) {\r\n      pushToHistory('INFO', args);\r\n      forwardLog('INFO', args);\r\n      _safeOriginalLog(originalInfo, formatMessage(cloneArgs(args), 'INFO'));\r\n    };\r\n    _global.console.warn = function (...args) {\r\n      pushToHistory('WARN', args);\r\n      forwardLog('WARN', args);\r\n      _safeOriginalLog(originalWarn, formatMessage(cloneArgs(args), 'WARN'));\r\n    };\r\n    _global.console.error = function (...args) {\r\n      pushToHistory('ERROR', args);\r\n      forwardLog('ERROR', args);\r\n      _safeOriginalLog(originalError, formatMessage(cloneArgs(args), 'ERROR'));\r\n    };\r\n    _global.console.debug = function (...args) {\r\n      pushToHistory('DEBUG', args);\r\n      forwardLog('DEBUG', args);\r\n      _safeOriginalLog(originalDebug, formatMessage(cloneArgs(args), 'DEBUG'));\r\n    };\r\n    _global.console.header = function (...args) {\r\n      pushToHistory('INFO', args);\r\n      _safeOriginalLog(originalLog, formatMessage(cloneArgs(args), 'HEADER'));\r\n    };\r\n\r\n    originalLog('%c[FlirtEasy] 🔥 Debug ON — content script console active', 'color:#e8197d;font-weight:700;font-size:13px');\r\n\r\n    // Global error listeners to ensure every error is logged and stylized appropriately\r\n    if (typeof _global.addEventListener === 'function') {\r\n      _global.addEventListener('error', (e) => {\r\n        // Prevent infinite loops if error logging causes an error\r\n        if (e && e.filename && e.filename.includes('debug-config.js')) return;\r\n        _global.console.error(`[Unhandled Error]`, e.filename ? `in ${e.filename}:${e.lineno} -` : '', e.message || e.error);\r\n      });\r\n\r\n      _global.addEventListener('unhandledrejection', (e) => {\r\n        _global.console.error(`[Promise Rejection]`, e.reason || 'No reason provided');\r\n      });\r\n    }\r\n  }\r\n}\r\n\n\n\n// ═══════════════════════════════════════════════════════════════\n// [FlirtEasy Bundle] config.js\n// ═══════════════════════════════════════════════════════════════\n/**\r\n * FlirtEasy Configuration File\r\n * \r\n * After changing settings, reload the Chrome extension:\r\n * 1. Go to chrome://extensions/\r\n * 2. Click the reload button on FlirtEasy extension\r\n */\r\n\r\nif (typeof CONFIG !== 'undefined') { /* Already loaded — skip re-declaration */ } else {\r\n\r\nvar CONFIG = {\r\n  // ========================================\r\n  // DEV MODE — Local bypass (no login/signup required)\r\n  // ========================================\r\n  // Set to true  → full pro access with no server calls (for local dev & team testing)\r\n  // Set to false → normal production auth flow\r\n  DEV_MODE: true,\r\n\r\n  // Identity shown in the UI while DEV_MODE is active\r\n  DEV_USER: {\r\n    email: 'dev@local.dev',\r\n    token: 'dev-bypass-token',\r\n    signedIn: true,\r\n    plan: 'pro',\r\n    lastAuth: 0   // written at runtime so it is always \"fresh\"\r\n  },\r\n\r\n  // ========================================\r\n  // System Configuration (PHASE 2)\r\n  // ========================================\r\n  API_BASE_URL: 'https://flirteasy-auth.shnaiderdm.workers.dev',\r\n  SERVER_ENDPOINTS: {\r\n    LOGIN: '/auth/login',\r\n    SIGNUP: '/auth/signup',\r\n    VERIFY: '/auth/verify',\r\n    STATUS: '/user/status',\r\n    UPGRADE: '/user/upgrade',\r\n    REFRESH: '/auth/refresh'\r\n  },\r\n\r\n  // ========================================\r\n  // Core Configuration\r\n  // ========================================\r\n  OPENAI_API_KEY: '',\r\n\r\n  // ========================================\r\n  // Prompt Templates (Advanced Customization)\r\n  // ========================================\r\n\r\n  // Custom AI System Prompt (Advanced)\r\n  // Leave empty to use auto-generated prompts based on settings below\r\n  // If provided, this overrides all auto-generated prompt logic\r\n  DEFAULT_CUSTOM_PROMPT: '',\r\n\r\n  // Auto-generated prompt template (used when custom prompt is empty)\r\n  // Variables available: {style}, {goal}, {emojiGuidance}\r\n  AUTO_PROMPT_TEMPLATE: `You are a {style} person on Tinder looking for {goal}. {emojiGuidance}\r\n\r\nIMPORTANT - Write like a REAL human texting:\r\n- Keep it SHORT (1-2 sentences max)\r\n- Be natural and conversational\r\n- Vary your style - don't be formulaic\r\n- Sometimes ask questions, sometimes make statements\r\n- Use casual language, contractions (I'm, you're, that's)\r\n- Don't be overly enthusiastic or try-hard\r\n- Match their energy level\r\n- Be authentic, not robotic`,\r\n\r\n  // Follow-up message prompt template\r\n  // Variables available: {style}, {goal}, {emojiGuidance}\r\n  FOLLOWUP_PROMPT_TEMPLATE: `You are a {style} person on Tinder looking for {goal}. {emojiGuidance} Generate a brief, natural follow-up message (1-2 sentences) that doesn't seem pushy. Keep it light and conversational, like a real person would text.`,\r\n\r\n  // ========================================\r\n  // Default Bot Behavior Settings\r\n  // ========================================\r\n\r\n  // Dating intentions (ignored if custom prompt is provided)\r\n  // Options: 'long_term', 'short_term', 'just_fun'\r\n  DEFAULT_INTENTIONS: 'short_term',\r\n\r\n  // Conversation style (ignored if custom prompt is provided)\r\n  // Options: 'freestyle', 'serious', 'gentle', 'flirty', 'confident'\r\n  DEFAULT_CHATTING_STYLE: 'freestyle',\r\n\r\n  // Use emojis in messages (ignored if custom prompt is provided)\r\n  // Options: true or false\r\n  DEFAULT_USE_EMOJIS: false,\r\n\r\n  // ========================================\r\n  // Cycle Settings\r\n  // ========================================\r\n\r\n  // Number of profiles to like per cycle\r\n  DEFAULT_LIKES_PER_CYCLE: 50,\r\n\r\n  // Number of messages to send per cycle\r\n  DEFAULT_MESSAGES_PER_CYCLE: 50,\r\n\r\n  // Schedule interval in minutes (how often the bot runs)\r\n  DEFAULT_SCHEDULE_INTERVAL: 30,\r\n\r\n  // ========================================\r\n  // Message Priority Settings (Hybrid System)\r\n  // ========================================\r\n\r\n  // Minimum guaranteed replies to old matches per cycle (as percentage)\r\n  // These matches always get priority (people who replied to you)\r\n  MIN_REPLY_SLOTS_PERCENT: 30,\r\n\r\n  // Maximum new match messages per cycle (as percentage)\r\n  // Caps how many opening messages are sent to prevent overwhelming backlog\r\n  MAX_NEW_MATCH_SLOTS_PERCENT: 70\r\n};\r\n\r\n} // end re-entry guard\r\n\n\n\n// ═══════════════════════════════════════════════════════════════\n// [FlirtEasy Bundle] utils/stop-conditions.js\n// ═══════════════════════════════════════════════════════════════\n/**\r\n * Advanced Stop Conditions & Lead Detection Engine (Production Grade)\r\n * Handles high-accuracy detection of leads (Moving Off-App)\r\n */\r\n\r\nasync function checkStopConditions(conversationHistory, stopConditions, apiKey, aiCaller = null) {\r\n  if (!conversationHistory || conversationHistory.length === 0) {\r\n    return { shouldStop: false, reason: null };\r\n  }\r\n\r\n  // Configuration check\r\n  if (!stopConditions || stopConditions.length === 0 || stopConditions.includes('never')) {\r\n    return { shouldStop: false, reason: null };\r\n  }\r\n\r\n  // ── Move-Off-App Goal Gate ──\r\n  // If a move_to_X goal is active, the AI handles redirecting — don't stop on\r\n  // the match mentioning OTHER platforms (WhatsApp, phone, etc.).\r\n  // Only let the MoveOffApp state machine handle the stop via its own mechanism.\r\n  const moveOffGoals = ['move_to_telegram', 'move_to_instagram', 'move_to_tango'];\r\n  const activeMoveGoal = stopConditions.find(s => moveOffGoals.includes(s));\r\n  if (activeMoveGoal) {\r\n    // Only stop on goal platform accepted — handled by the move-off-app state machine\r\n    // Don't fire here on WhatsApp/phone mentions from the match\r\n    return { shouldStop: false, reason: null };\r\n  }\r\n\r\n  // Phase 1: Context Sanitization\r\n  // We only care about the last 8 messages for context, but focus leads on the last 3 MATCH messages.\r\n  const contextWindow = conversationHistory.slice(-8);\r\n  const matchMessages = contextWindow.filter(m => m.sender !== 'user');\r\n\r\n  if (matchMessages.length === 0) {\r\n    return { shouldStop: false, reason: null };\r\n  }\r\n\r\n  // Prepare structured context for LLM\r\n  // We label speakers clearly to prevent the \"Daniel Loop\" (where user's mentions trigger leads)\r\n  const conversationText = contextWindow.map(m => {\r\n    const speaker = m.sender === 'user' ? 'ME (SENDER)' : 'MATCH (RECIPIENT)';\r\n    return `[${speaker}]: ${m.text}`;\r\n  }).join('\\n');\r\n\r\n  const systemPrompt = `You are a high-accuracy Lead Extraction Engine for a dating assistant.\r\n    Your task: Determine if the MATCH (not ME) has provided a concrete lead, requested a date, or expressed explicit intent.\r\n\r\n    STRICT CLASSIFICATION RULES:\r\n    1. INTENT: \r\n       - \"PROVIDING\": Match shared a handle, number, address, or explicit desire.\r\n       - \"REQUESTING\": Match asked for my handle/number or a specific meeting.\r\n       - \"MENTIONING\": Match talked about a platform or meeting generally (e.g. \"I saw it on Insta\", \"we should meet sometime\").\r\n    \r\n    2. EXTRACTION:\r\n       - Only \"shouldStop\" if INTENT is \"PROVIDING\" or \"REQUESTING\".\r\n       - IGNORE \"MENTIONING\" (False Positives).\r\n       - IGNORE anything said by \"ME (SENDER)\".\r\n\r\n    3. PLATFORM SPECIFICS:\r\n       - Phone: Look for digit patterns, \"WhatsApp\", \"my number\", \"text me\".\r\n       - Social: Look for @handles, or explicit handle text like \"my ig is...\", \"add me on snap\". Mentioning the app name without a handle is a MENTION, not a lead.\r\n       - Date: Look for specific logistics (\"Meet at 8\", \"Let's go to [Place]\", \"come over tonight\").\r\n       - Explicit: Match expresses direct sexual intent (\"drunk sex\", \"hook up\", \"lick me\", \"come to my place\").\r\n       - Location: Match shares a maps link or specific address.\r\n\r\n    OUTPUT FORMAT (JSON ONLY):\r\n    {\r\n      \"shouldStop\": boolean,\r\n      \"intent\": \"PROVIDING\" | \"REQUESTING\" | \"MENTIONING\",\r\n      \"category\": \"phone\" | \"date\" | \"instagram\" | \"explicit\" | \"location\" | \"none\",\r\n      \"extractedValue\": \"the handle, number, date logistics, or explicit phrase\",\r\n      \"snippet\": \"the match's specific sentence\"\r\n    }`;\r\n\r\n  const userPrompt = `Analyze this conversation:\\n\\n${conversationText}`;\r\n\r\n  try {\r\n    let resultText;\r\n\r\n    if (aiCaller && typeof aiCaller === 'function') {\r\n      // Use provided caller (e.g. background proxy)\r\n      resultText = await aiCaller(systemPrompt, userPrompt);\r\n    } else {\r\n      // Fallback: Direct OpenAI Call (Legacy)\r\n      const response = await fetch('https://api.openai.com/v1/chat/completions', {\r\n        method: 'POST',\r\n        headers: {\r\n          'Content-Type': 'application/json',\r\n          'Authorization': `Bearer ${apiKey}`\r\n        },\r\n        body: JSON.stringify({\r\n          model: 'gpt-4.1-mini',\r\n          messages: [\r\n            { role: 'system', content: systemPrompt },\r\n            { role: 'user', content: userPrompt }\r\n          ],\r\n          temperature: 0,\r\n          max_tokens: 150,\r\n          response_format: { type: \"json_object\" }\r\n        })\r\n      });\r\n\r\n      const data = await response.json();\r\n      if (!data.choices?.[0]?.message?.content) throw new Error('Invalid AI response');\r\n      resultText = data.choices[0].message.content;\r\n    }\r\n\r\n    const result = JSON.parse(resultText);\r\n\r\n    // Final Validation Gate\r\n    if (result.shouldStop) {\r\n      // Ensure it was a provision or request, not a casual mention\r\n      if (result.intent === 'MENTIONING') return { shouldStop: false, reason: null };\r\n\r\n      return {\r\n        shouldStop: true,\r\n        reason: result.category,\r\n        snippet: result.extractedValue || result.snippet,\r\n        intent: result.intent\r\n      };\r\n    }\r\n\r\n    return { shouldStop: false, reason: null };\r\n\r\n  } catch (error) {\r\n    console.warn('[LeadEngine] AI Analysis failed, falling back to Hard-Regex:', error.message);\r\n    return checkStopConditionsFallback(conversationHistory, stopConditions);\r\n  }\r\n}\r\n\r\n/**\r\n * Hardened Regex Fallback (Production Grade)\r\n * Uses word boundaries, entity validation, and negative lookaheads.\r\n */\r\nfunction checkStopConditionsFallback(conversationHistory, stopConditions) {\r\n  // Only process the last 3 MATCH messages for triggers to minimize noise\r\n  const matchMessages = conversationHistory\r\n    .slice(-5)\r\n    .filter(m => m.sender !== 'user')\r\n    .map(m => m.text.toLowerCase());\r\n\r\n  const allText = matchMessages.join(' | ');\r\n\r\n  if (!allText.trim()) return { shouldStop: false, reason: null };\r\n\r\n  // 1. PHONE / WHATSAPP (Resilient to spacing: \"0 5 4 ...\")\r\n  if (stopConditions.includes('phone')) {\r\n    const rawDigits = allText.replace(/[\\s.-]/g, '');\r\n    const phonePattern = /\\b(\\+?\\d{8,15})\\b/; // 8-15 digits\r\n    const whatsappPattern = /\\b(whatsapp|wa\\.me|telegram|signal|viber|call me|text me|my number)\\b/i;\r\n\r\n    if (phonePattern.test(rawDigits) && !allText.includes('year')) { // Avoid years like 2026\r\n      return { shouldStop: true, reason: 'phone', snippet: 'phone/whatsapp detected' };\r\n    }\r\n    if (whatsappPattern.test(allText)) {\r\n      return { shouldStop: true, reason: 'phone', snippet: allText.match(whatsappPattern)[0] };\r\n    }\r\n  }\r\n\r\n  // 2. SOCIAL HANDLES (Handle vs Reference detection)\r\n  if (stopConditions.includes('instagram')) {\r\n    // Negative lookahead: Trigger on handle context, ignore \"on insta\", \"saw on snap\"\r\n    const socialPatterns = [\r\n      /\\b(ig\\s*[:@]|insta\\s*[:@]|instagram\\s*[:@]|snap\\s*[:@]|snapchat\\s*[:@])\\b/i,\r\n      /@[\\w.]{3,20}\\b/ // Actual handle @user\r\n    ];\r\n\r\n    for (const pattern of socialPatterns) {\r\n      const match = allText.match(pattern);\r\n      if (match) {\r\n        // Context Check: exclude common referential phrases\r\n        const context = allText.toLowerCase();\r\n        const isReference = context.includes('on insta') || context.includes('on snap') || context.includes('on instagram');\r\n        if (isReference && !context.includes('@')) continue;\r\n\r\n        return { shouldStop: true, reason: 'instagram', snippet: match[0] };\r\n      }\r\n    }\r\n  }\r\n\r\n  // 3. DATE LOGISTICS (Explicit vs Vague)\r\n  if (stopConditions.includes('date')) {\r\n    const explicitDatePattern = /\\b(meet at|meet you at|grab (a )?coffee at|dinner at|lunch at|see you then|around [0-9]+|come over tonight|come to my (place|flat|apartment|house))\\b/i;\r\n    const match = allText.match(explicitDatePattern);\r\n    if (match) {\r\n      return { shouldStop: true, reason: 'date', snippet: match[0] };\r\n    }\r\n  }\r\n\r\n  // 4. EXPLICIT SEXUAL CONTENT — only when Stop After Goal is active\r\n  if (stopConditions.length > 0) {\r\n    const explicitPattern = /\\b(drunk sex|have sex|wanna fuck|want to fuck|lets? fuck|f[*u]ck me|lick me|suck me|sleep with me|hook ?up tonight|can we have sex|up for sex|dtf\\b|down to f[*u]ck)\\b/i;\r\n    const explicitMatch = allText.match(explicitPattern);\r\n    if (explicitMatch) {\r\n      return { shouldStop: true, reason: 'explicit', snippet: explicitMatch[0] };\r\n    }\r\n\r\n    // 5. LOCATION SHARING (URL-based)\r\n    const locationPattern = /maps\\.app\\.goo\\.gl\\/|goo\\.gl\\/maps|maps\\.google\\.com\\//i;\r\n    if (locationPattern.test(allText)) {\r\n      return { shouldStop: true, reason: 'location', snippet: 'Location shared' };\r\n    }\r\n  }\r\n\r\n  return { shouldStop: false, reason: null };\r\n}\r\n\r\n// Global Export\r\nif (typeof self !== 'undefined') {\r\n  self.checkStopConditions = checkStopConditions;\r\n  self.checkStopConditionsFallback = checkStopConditionsFallback;\r\n}\r\n\n\n\n// ═══════════════════════════════════════════════════════════════\n// [FlirtEasy Bundle] utils/language-detect.js\n// ═══════════════════════════════════════════════════════════════\n/**\r\n * FlirtEasy Language Detection Engine\r\n * \r\n * Detects conversation language from message text using:\r\n * 1. Unicode script range analysis (deterministic for non-Latin scripts)\r\n * 2. Word-pattern fingerprinting\r\n * \r\n * Used by both content scripts (UI button) and background (AI prompt override).\r\n */\r\n\r\n// Re-entry guard: prevent crashes from double content-script injection\r\nif (typeof LANG_SCRIPT_MAP !== 'undefined') { /* Already loaded */ } else {\r\n\r\n// Unicode script ranges → language mapping\r\nvar LANG_SCRIPT_MAP = [\r\n    { name: 'Hebrew', code: 'he', ranges: [[0x0590, 0x05FF], [0xFB1D, 0xFB4F]] },\r\n    { name: 'Arabic', code: 'ar', ranges: [[0x0600, 0x06FF], [0x0750, 0x077F], [0xFB50, 0xFDFF], [0xFE70, 0xFEFF]],\r\n      // Persian/Farsi uses these extra chars that pure Arabic doesn't: پ چ گ ژ ک ی\r\n      // If any of these appear, it's Persian not Arabic\r\n      excludeIfChars: /[\\u067E\\u0686\\u06AF\\u0698\\u06A9\\u06CC\\u06F0-\\u06F9]/ },\r\n    { name: 'Persian', code: 'fa', ranges: [[0x0600, 0x06FF]], requiredChars: /[\\u067E\\u0686\\u06AF\\u0698\\u06A9\\u06CC\\u06F0-\\u06F9]/ },\r\n    { name: 'Hindi', code: 'hi', ranges: [[0x0900, 0x097F]] },\r\n    { name: 'Bengali', code: 'bn', ranges: [[0x0980, 0x09FF]] },\r\n    { name: 'Tamil', code: 'ta', ranges: [[0x0B80, 0x0BFF]] },\r\n    { name: 'Telugu', code: 'te', ranges: [[0x0C00, 0x0C7F]] },\r\n    { name: 'Kannada', code: 'kn', ranges: [[0x0C80, 0x0CFF]] },\r\n    { name: 'Malayalam', code: 'ml', ranges: [[0x0D00, 0x0D7F]] },\r\n    { name: 'Thai', code: 'th', ranges: [[0x0E00, 0x0E7F]] },\r\n    { name: 'Korean', code: 'ko', ranges: [[0xAC00, 0xD7AF], [0x1100, 0x11FF], [0x3130, 0x318F]] },\r\n    { name: 'Japanese', code: 'ja', ranges: [[0x3040, 0x309F], [0x30A0, 0x30FF]] },\r\n    { name: 'Chinese', code: 'zh', ranges: [[0x4E00, 0x9FFF], [0x3400, 0x4DBF]] },\r\n    { name: 'Russian', code: 'ru', ranges: [[0x0400, 0x04FF]] },\r\n    { name: 'Ukrainian', code: 'uk', ranges: [[0x0400, 0x04FF]], \r\n      // Ukrainian-specific: і (і), ї (ї), є (є), ґ (ґ) - these DON'T exist in Russian\r\n      specificChars: /[іїєґ]/\r\n    },\r\n    { name: 'Greek', code: 'el', ranges: [[0x0370, 0x03FF]] },\r\n    { name: 'Georgian', code: 'ka', ranges: [[0x10A0, 0x10FF]] },\r\n    { name: 'Armenian', code: 'hy', ranges: [[0x0530, 0x058F]] },\r\n    { name: 'Gujarati', code: 'gu', ranges: [[0x0A80, 0x0AFF]] },\r\n    { name: 'Punjabi', code: 'pa', ranges: [[0x0A00, 0x0A7F]] },\r\n    { name: 'Sinhala', code: 'si', ranges: [[0x0D80, 0x0DFF]] },\r\n    { name: 'Lao', code: 'lo', ranges: [[0x0E80, 0x0EFF]] },\r\n    { name: 'Burmese', code: 'my', ranges: [[0x1000, 0x109F]] },\r\n    { name: 'Khmer', code: 'km', ranges: [[0x1780, 0x17FF]] },\r\n];\r\n\r\n// Latin-script languages via common word fingerprints + character patterns\r\nconst LANG_LATIN_PATTERNS = [\r\n    { \r\n        name: 'Romanian', \r\n        code: 'ro', \r\n        words: ['bună', 'buna', 'mulțumesc', 'multumesc', 'frumos', 'foarte', 'când', 'cand', 'pentru', 'acum', 'sunt', 'faci', 'iubire', 'întotdeauna', 'intotdeauna', 'și', 'si', 'tu', 'mai', 'din', 'ce', 'da', 'nu', 'salut', 'noapte', 'bine', 'stii', 'știi', 'merge', 'acolo', 'atunci', 'deci', 'esti', 'ești'],\r\n        // Romanian-specific: uses ă, â, î, ș, ț characters frequently\r\n        specialChars: /[ăâîșț]/i,\r\n        // Common Romanian suffixes\r\n        patterns: /\\b\\w+(ă|ește|esc|ațion|ților|ului)\\b/i\r\n    },\r\n    { name: 'Spanish', code: 'es', words: ['hola', 'holaa', 'holaaa', 'estás', 'estas', 'qué', 'gracias', 'también', 'tambien', 'pero', 'porque', 'tengo', 'quiero', 'mucho', 'amor', 'vale', 'bueno', 'nosotros', 'otra', 'otro', 'viviendo', 'vivo', 'vivir', 'argentina', 'español', 'española', 'pasarla', 'pasarlo', 'pasando', 'estar', 'estoy', 'soy', 'somos', 'desde', 'para', 'con', 'una', 'por', 'más', 'mas', 'está', 'esta', 'son', 'fue', 'hay', 'nos', 'mis', 'del', 'muy', 'así', 'asi', 'eso', 'ese', 'ser', 'sin', 'siempre', 'todo', 'nada', 'cuando', 'ahora', 'aquí', 'aqui', 'vida', 'algo', 'tiene', 'hacer', 'mal', 'solo', 'sola', 'quieres', 'vamos', 'venga', 'claro', 'oye', 'mira', 'pues', 'igual', 'mismo', 'misma', 'cuantos', 'cuántos', 'cuanto', 'cuánto', 'distancia', 'lejos', 'cerca', 'personas', 'bonita', 'bonitas', 'bonito', 'risueña', 'carismatica', 'carismática', 'graciosa', 'empatia', 'empatía', 'tiempos', 'busco', 'abierta', 'etapa', 'aportar', 'nueva', 'nuevo', 'difícil', 'dificil', 'sustituto', 'cosas',\r\n      // Spanish-exclusive words (do NOT exist in Portuguese)\r\n      'buenas', 'noches', 'buenos', 'dias', 'días', 'hombre', 'chico', 'chica', 'tío', 'tia', 'venga', 'jajaja', 'jaja', 'jeje', 'genial', 'guapo', 'guapa', 'bonito', 'lindo', 'linda', 'chévere', 'órale', 'andale', 'ahorita', 'dónde', 'donde', 'cuándo', 'viejo', 'vieja', 'papi', 'mami', 'qué tal', 'qué onda', 'cómo estás', 'poco', 'noche', 'virrey', 'estación', 'metro'],\r\n      specialChars: /[áéíóúñü]/i,\r\n      // Strong Spanish-exclusive chars/patterns — ñ is the most reliable signal\r\n      exclusiveChars: /[ñ]/ },\r\n    { name: 'Portuguese', code: 'pt', words: ['olá', 'ola', 'obrigado', 'obrigada', 'também', 'muito', 'quando', 'você', 'voce', 'aqui', 'tudo', 'boa', 'gostei', 'gosto', 'ainda', 'rsrs', 'kkk', 'kkkk', 'hahaha', 'né', 'né', 'tá', 'ta', 'pra', 'pro', 'gente', 'beleza', 'legal', 'bacana', 'cara', 'saudade', 'beijos', 'abraços', 'bjss', 'nossa', 'poxa', 'caramba', 'uai', 'bora', 'valeu', 'falou', 'tchau', 'oi', 'tamo', 'mano', 'mina', 'galera', 'pessoal', 'falar', 'fazer', 'estar', 'ficar', 'viver', 'amar', 'querer', 'poder', 'dever', 'saber', 'ver', 'dar', 'vir', 'ir', 'ter', 'ser',\r\n      // Portuguese-exclusive words (do NOT exist in Spanish)\r\n      'você', 'vocês', 'então', 'entao', 'também', 'tbm', 'aqui', 'isso', 'esse', 'essa', 'mesmo', 'mesma', 'bastante', 'demais', 'porém', 'porem', 'porquê', 'porque', 'enquanto', 'através', 'através', 'além', 'bem', 'bom', 'boa', 'ótimo', 'otimo', 'ótima', 'otima', 'dá', 'da', 'já', 'ja', 'né', 'tô', 'to', 'tá', 'ta', 'pra', 'nessa', 'nesse', 'naquele', 'naquela', 'mora', 'morar', 'encontrar', 'curtir', 'gostar', 'adorar'],\r\n      specialChars: /[áàâãçéêíóôõú]/i,\r\n      // Strong Portuguese-exclusive chars — ã, õ, ê, ô are the most reliable signals\r\n      exclusiveChars: /[ãõêô]/ },\r\n    { name: 'German', code: 'de', words: ['hallo', 'danke', 'bitte', 'nicht', 'auch', 'aber', 'schön', 'ich', 'sehr', 'warum', 'weil', 'gerne', 'heute', 'morgen', 'freut'], specialChars: /[äöüß]/i },\r\n    { name: 'Italian', code: 'it', words: ['ciao', 'come', 'grazie', 'anche', 'perché', 'molto', 'bene', 'buona', 'piacere', 'sempre', 'amore', 'tutto', 'niente', 'quando', 'ancora', 'meglio', 'vivo', 'sono', 'stai', 'bella', 'bello', 'mio', 'mia', 'vuoi', 'puoi', 'sai', 'hai', 'sei', 'vai', 'dai', 'poi', 'però', 'quello', 'questa', 'questo', 'davvero', 'magari', 'infatti', 'allora', 'quindi', 'oppure', 'adesso', 'subito', 'già', 'mai', 'solo', 'solo', 'forse', 'almeno', 'soprattutto'], specialChars: /[àèéìòù]/i },\r\n    { name: 'Dutch', code: 'nl', words: ['hallo', 'dank', 'ook', 'maar', 'waarom', 'goed', 'mooi', 'vandaag', 'leuk', 'gezellig', 'welkom', 'morgen', 'altijd'] },\r\n    { name: 'Turkish', code: 'tr', words: ['merhaba', 'teşekkür', 'evet', 'hayır', 'nasıl', 'güzel', 'çok', 'iyi', 'tamam', 'neden', 'selam', 'bugün'], specialChars: /[çğıöşü]/i },\r\n    { name: 'Polish', code: 'pl', words: ['cześć', 'dzień', 'dobry', 'dziękuję', 'proszę', 'bardzo', 'dobrze', 'dlaczego', 'kiedy', 'miło', 'piękny'], specialChars: /[ąćęłńóśźż]/i },\r\n    { name: 'Swedish', code: 'sv', words: ['tack', 'också', 'varför', 'vacker', 'alltid', 'underbar', 'gillar', 'gärna', 'jättefin', 'jättebra', 'mår', 'hur', 'din', 'profil', 'inte', 'från'], specialChars: /[åäö]/i },\r\n    { name: 'Norwegian', code: 'no', words: ['hei', 'takk', 'også', 'veldig', 'vakker', 'alltid', 'ikke', 'deg', 'seg', 'profilen', 'hyggelig', 'kjempebra', 'norsk'], specialChars: /[æøå]/i },\r\n    { name: 'Danish', code: 'da', words: ['meget', 'smuk', 'altid', 'dejlig', 'rigtig', 'hyggeligt', 'dansk', 'har'], specialChars: /[æøå]/i },\r\n    { name: 'Finnish', code: 'fi', words: ['kiitos', 'myös', 'miksi', 'kaunis', 'aina', 'tänään', 'miten', 'kuuluu', 'profiilisi', 'todella', 'hauska', 'mukava', 'suomi', 'olen'], specialChars: /[äö]/i },\r\n    { name: 'Czech', code: 'cs', words: ['ahoj', 'děkuji', 'prosím', 'dobře', 'proč', 'dnes', 'krásný', 'moc', 'rád', 'ráda'], specialChars: /[áčďéěíňóřšťúůýž]/i },\r\n    { name: 'Hungarian', code: 'hu', words: ['szia', 'köszönöm', 'kérem', 'szép', 'nagyon', 'miért', 'mindig', 'holnap', 'szeretem', 'jó'], specialChars: /[áéíóöőúüű]/i },\r\n    { name: 'Indonesian', code: 'id', words: ['halo', 'terima', 'kasih', 'bagaimana', 'bagus', 'sangat', 'kenapa', 'selalu', 'tidak', 'suka', 'kamu', 'aku', 'senang'] },\r\n    // Hinglish — Hindi written in Roman/Latin script (most common on Indian dating apps)\r\n    // Real examples: \"kese ho\", \"kya haal\", \"kya chal raha hai\", \"bhai\", \"yaar\", \"acha\"\r\n    { name: 'Hindi', code: 'hi', words: ['kese', 'kaise', 'haal', 'yaar', 'bhai', 'acha', 'achha', 'bilkul', 'sahi', 'nahi', 'nhi', 'bhi', 'bas', 'kal', 'aaj', 'agar', 'toh', 'mera', 'tera', 'kya', 'chal', 'raha', 'sab'] },\r\n    // Roman Urdu — Urdu written in Roman/Latin script (standard for Pakistani dating apps)\r\n    // Real examples: \"kya haal hai\", \"tum kese ho\", \"bilkul theek\", \"acha ji\"\r\n    { name: 'Urdu', code: 'ur', words: ['kya', 'kese', 'haal', 'yaar', 'bhai', 'acha', 'bilkul', 'sahi', 'nahi', 'tum', 'mujhe', 'apna', 'kitna', 'aur', 'hai', 'ho', 'haan', 'toh', 'bhi', 'theek', 'accha', 'phir', 'abhi'] },\r\n    // Finglish — Farsi/Persian written in Roman/Latin script (common in Iran)\r\n    // Real examples: \"chetori?\", \"khoobi?\", \"mersi kheili\", \"bahal\", \"vay\"\r\n    { name: 'Persian', code: 'fa', words: ['chetori', 'khoobi', 'mamnoon', 'bahal', 'baba', 'joon', 'kheili', 'vay', 'mersi', 'salamat', 'akheh', 'vali', 'che', 'koja', 'kojai', 'khob', 'doset', 'daram', 'eshgh'] },\r\n    { name: 'Swahili', code: 'sw', words: ['habari', 'asante', 'sana', 'nzuri', 'vipi', 'mambo', 'napenda', 'wasifu', 'sijambo', 'karibu', 'rafiki', 'kweli', 'nakupenda'] },\r\n    { name: 'Vietnamese', code: 'vi', words: ['xin', 'chào', 'cảm', 'ơn', 'rất', 'đẹp', 'tại', 'sao', 'luôn', 'không', 'hôm', 'ngày'], specialChars: /[àáảãạăằắẳẵặâầấẩẫậèéẻẽẹêềếểễệìíỉĩịòóỏõọôồốổỗộơờớởỡợùúủũụưừứửữựỳýỷỹỵđ]/i },\r\n];\r\n\r\n/**\r\n * Detect language from a text string.\r\n * @param {string} text - Text to analyze\r\n * @returns {{ name: string, code: string, confidence: number, source: string }}\r\n */\r\nglobalThis.detectLanguage = function(text) {\r\n    if (!text || text.trim().length === 0) {\r\n        return { name: 'Unknown', code: 'unknown', confidence: 0, source: 'empty' };\r\n    }\r\n\r\n    // Strip emojis, numbers, punctuation, whitespace\r\n    const cleaned = text\r\n        .replace(/[\\u{1F000}-\\u{1FFFF}]|[\\u{2600}-\\u{26FF}]|[\\u{2700}-\\u{27BF}]|[\\u{FE00}-\\u{FE0F}]|[\\u{1F900}-\\u{1F9FF}]|[\\u200D\\uFE0F]/gu, '')\r\n        .replace(/[0-9\\s.,!?;:'\"()\\-–—…@#$%^&*+=<>{}[\\]|\\\\\\/~`¿¡]/g, '')\r\n        .trim();\r\n\r\n    if (cleaned.length === 0) {\r\n        return { name: 'Unknown', code: 'unknown', confidence: 0, source: 'emoji_only' };\r\n    }\r\n\r\n    // Count characters per script\r\n    const scriptCounts = {};\r\n    let latinCount = 0;\r\n    let totalCounted = 0;\r\n\r\n    for (const char of cleaned) {\r\n        const cp = char.codePointAt(0);\r\n        let matched = false;\r\n\r\n        for (const script of LANG_SCRIPT_MAP) {\r\n            for (const [start, end] of script.ranges) {\r\n                if (cp >= start && cp <= end) {\r\n                    scriptCounts[script.name] = (scriptCounts[script.name] || 0) + 1;\r\n                    matched = true;\r\n                    totalCounted++;\r\n                    break;\r\n                }\r\n            }\r\n            if (matched) break;\r\n        }\r\n\r\n        // Latin characters (Basic Latin + Latin Extended)\r\n        if (!matched && ((cp >= 0x0041 && cp <= 0x007A) || (cp >= 0x00C0 && cp <= 0x024F))) {\r\n            latinCount++;\r\n            totalCounted++;\r\n        }\r\n    }\r\n\r\n    if (totalCounted === 0) {\r\n        return { name: 'Unknown', code: 'unknown', confidence: 0, source: 'no_text_chars' };\r\n    }\r\n\r\n    // Find dominant non-Latin script (>30% threshold)\r\n    let dominantScript = null;\r\n    let dominantCount = 0;\r\n    for (const [name, count] of Object.entries(scriptCounts)) {\r\n        if (count > dominantCount) {\r\n            dominantScript = name;\r\n            dominantCount = count;\r\n        }\r\n    }\r\n\r\n    if (dominantScript && dominantCount / totalCounted > 0.3) {\r\n        const scriptInfo = LANG_SCRIPT_MAP.find(s => s.name === dominantScript);\r\n        const confidence = Math.round((dominantCount / totalCounted) * 100);\r\n        \r\n        // Special case: Persian vs Arabic disambiguation\r\n        // Persian uses specific chars پ چ گ ژ ک ی that pure Arabic doesn't\r\n        if (dominantScript === 'Arabic') {\r\n            const persianScript = LANG_SCRIPT_MAP.find(s => s.name === 'Persian');\r\n            if (persianScript?.requiredChars && persianScript.requiredChars.test(text)) {\r\n                return { name: 'Persian', code: 'fa', confidence, source: 'unicode_script' };\r\n            }\r\n        }\r\n\r\n        // Special case: Ukrainian vs Russian disambiguation\r\n        // Primary signal: Ukrainian-unique chars і, ї, є, ґ that Russian doesn't use\r\n        // Secondary signal: vocabulary fingerprint — catches mixed-language speakers\r\n        // who use Russian words (like \"Хорошо\") but are fundamentally Ukrainian\r\n        if (dominantScript === 'Russian') {\r\n            const ukrainianScript = LANG_SCRIPT_MAP.find(s => s.name === 'Ukrainian');\r\n            if (ukrainianScript && ukrainianScript.specificChars && ukrainianScript.specificChars.test(text)) {\r\n                return { name: 'Ukrainian', code: 'uk', confidence, source: 'unicode_script' };\r\n            }\r\n            // Vocabulary fallback: distinctly Ukrainian words that don't exist in Russian\r\n            // Covers cases like \"каву\", \"спілкування\", \"зустрінемось\", \"будь ласка\", \"дякую\"\r\n            const ukrainianWords = /\\b(каву|каво|каф[еє]|кава|дякую|дяку|будь ласка|будьте|зустрін|зустріч|зустрітись|зустрімось|зустрінемось|спілку|спілкування|привіт|як справи|добре|добрий|добра|вечір|ранок|слухай|слухайте|звісно|звичайно|напевно|можемо|можете|хочу|хочеш|подобається|подобаєтьс|гарно|гарний|гарна|гарне|файно|чудово|чудовий|чудова|класно|окей|згода|погоджуюс|так|ні|хаха|їхати|їду|їдемо|тобто|взагалі|насправді)\\b/i;\r\n            if (ukrainianWords.test(text)) {\r\n                return { name: 'Ukrainian', code: 'uk', confidence: Math.max(confidence - 10, 50), source: 'unicode_script_vocab' };\r\n            }\r\n        }\r\n        \r\n        return {\r\n            name: scriptInfo.name,\r\n            code: scriptInfo.code,\r\n            confidence,\r\n            source: 'unicode_script'\r\n        };\r\n    }\r\n\r\n    // Latin-dominant text → word pattern matching + special character detection\r\n    if (latinCount / totalCounted > 0.5) {\r\n        const words = text.toLowerCase().split(/[\\s.,!?;:'\"()\\-–—…]+/).filter(w => w.length > 1);\r\n        let bestLang = null;\r\n        let bestScore = 0;\r\n\r\n        for (const lang of LANG_LATIN_PATTERNS) {\r\n            let score = 0;\r\n            \r\n            // Count word matches first\r\n            let wordMatches = 0;\r\n            for (const w of lang.words) {\r\n                if (words.includes(w)) wordMatches++;\r\n            }\r\n            score += wordMatches;\r\n            \r\n            // Special character bonus ONLY if:\r\n            // 1. The language has a specialChars pattern\r\n            // 2. At least 1 word also matched (prevents false positives from shared chars like é)\r\n            if (lang.specialChars && lang.specialChars.test(text) && wordMatches >= 1) {\r\n                score += 3;\r\n            }\r\n\r\n            // Exclusive character bonus — chars that ONLY exist in this language\r\n            // e.g. ñ is exclusively Spanish, ã/õ/ê/ô are exclusively Portuguese\r\n            // This is a decisive tiebreaker for Spanish vs Portuguese confusion\r\n            if (lang.exclusiveChars && lang.exclusiveChars.test(text)) {\r\n                score += 8; // strong signal — exclusive chars are unambiguous\r\n            }\r\n            \r\n            // Check for word patterns (Romanian suffixes, etc.)\r\n            if (lang.patterns && lang.patterns.test(text) && wordMatches >= 1) {\r\n                score += 2;\r\n            }\r\n            \r\n            const normalizedScore = score / Math.sqrt(lang.words.length);\r\n            if (normalizedScore > bestScore) {\r\n                bestScore = normalizedScore;\r\n                bestLang = lang;\r\n            }\r\n        }\r\n\r\n        // For very short text (1-2 words), lower the threshold — a single greeting word\r\n        // like \"Buna\", \"Hola\", \"Ciao\" is a strong enough signal on its own.\r\n        // For longer text, keep the 0.5 threshold to avoid false positives.\r\n        const shortTextWords = text.trim().split(/\\s+/).filter(Boolean);\r\n        const threshold = shortTextWords.length <= 2 ? 0.1 : 0.5;\r\n\r\n        if (bestLang && bestScore > threshold) {\r\n            return {\r\n                name: bestLang.name,\r\n                code: bestLang.code,\r\n                confidence: Math.min(Math.round(bestScore * 100), 95),\r\n                source: 'word_patterns'\r\n            };\r\n        }\r\n\r\n        // Fallback: Latin script with no strong word-pattern matches.\r\n        // For SHORT text (≤4 words): treat as unknown — bio fallback should be tried first.\r\n        // For LONGER text (>4 words): almost certainly English — no other Latin language\r\n        // would consistently produce multi-sentence text with zero word pattern matches.\r\n        const wordCount = text.trim().split(/\\s+/).filter(Boolean).length;\r\n        if (wordCount <= 4) {\r\n            return { name: 'Unknown', code: 'unknown', confidence: 0, source: 'latin_fallback' };\r\n        }\r\n        // Long Latin text with no other language signal = English\r\n        const confidence = Math.min(40 + Math.floor(wordCount / 3), 75);\r\n        return { name: 'English', code: 'en', confidence, source: 'latin_english_default' };\r\n    }\r\n\r\n    return { name: 'Unknown', code: 'unknown', confidence: 0, source: 'unrecognized' };\r\n}\r\n\r\n/**\r\n * Detect conversation language from an array of messages.\r\n * Only analyzes the match's messages (the language we want to reply in).\r\n * @param {Array<{sender: string, text: string}>} messages\r\n * @returns {{ name: string, code: string, confidence: number, source: string, messagesAnalyzed: number }}\r\n */\r\nglobalThis.detectConversationLanguage = function(messages) {\r\n    const matchMessages = messages.filter(m =>\r\n        m.sender === 'match' && \r\n        m.text && \r\n        m.text.trim().length >= 2 &&\r\n        !m.text.startsWith('[Link') &&   // Skip link previews\r\n        !m.text.startsWith('[Voice') &&  // Skip voice notes\r\n        !m.text.startsWith('[Video') &&  // Skip videos\r\n        !m.text.startsWith('[GIF') &&    // Skip GIFs\r\n        !m.text.startsWith('[Media') &&  // Skip media\r\n        !m.text.startsWith('[Image')     // Skip images\r\n    );\r\n\r\n    if (matchMessages.length === 0) {\r\n        return { name: 'Unknown', code: 'unknown', confidence: 0, source: 'no_match_messages', messagesAnalyzed: 0 };\r\n    }\r\n\r\n    // Primary: last 3 match messages = current conversation language\r\n    // Trust ANY known result — expanding the window only adds older messages that can contaminate\r\n    const recent3 = matchMessages.slice(-3);\r\n    const recent3Text = recent3.map(m => m.text).join(' ');\r\n    const recent3Result = detectLanguage(recent3Text);\r\n\r\n    // For Cyrillic scripts (Russian/Ukrainian/etc.), use ALL messages for disambiguation.\r\n    // A single Russian loanword like \"Хорошо\" in an otherwise Ukrainian conversation\r\n    // should not flip the detection. More data = more accurate Cyrillic disambiguation.\r\n    if (recent3Result.code === 'ru' || recent3Result.code === 'unknown') {\r\n        const allText = matchMessages.map(m => m.text).join(' ');\r\n        const allResult = detectLanguage(allText);\r\n        // If all-messages analysis disagrees (e.g. finds Ukrainian), trust that\r\n        if (allResult.code !== 'unknown' && allResult.code !== recent3Result.code) {\r\n            allResult.messagesAnalyzed = matchMessages.length;\r\n            allResult.source = `all_messages_${allResult.source}`;\r\n            return allResult;\r\n        }\r\n    }\r\n\r\n    if (recent3Result.code !== 'unknown') {\r\n        recent3Result.messagesAnalyzed = recent3.length;\r\n        recent3Result.source = `recent3_${recent3Result.source}`;\r\n        return recent3Result;\r\n    }\r\n\r\n    // Fallback: first message only (for very short single-message conversations)\r\n    const firstResult = detectLanguage(matchMessages[0].text);\r\n    firstResult.messagesAnalyzed = 1;\r\n    firstResult.source = `first_message_${firstResult.source}`;\r\n    return firstResult;\r\n}\r\n\r\n// Expose globally for both content scripts (window) and background (self/service worker)\r\nif (typeof window !== 'undefined') {\r\n    globalThis.detectLanguage = detectLanguage;\r\n    globalThis.detectConversationLanguage = detectConversationLanguage;\r\n}\r\n\r\nif (typeof self !== 'undefined') {\r\n    self.detectLanguage = detectLanguage;\r\n    self.detectConversationLanguage = detectConversationLanguage;\r\n}\r\n\r\n} // end re-entry guard\r\n\n\n\n// ═══════════════════════════════════════════════════════════════\n// [FlirtEasy Bundle] content/ui-alerts.js\n// ═══════════════════════════════════════════════════════════════\n/**\r\n * UI Alerts Module - Production Grade\r\n * Handles \"Time to Shine\" (Handoff) Takeover Toasts\r\n */\r\n\r\n// Re-entry guard: prevent crashes from double content-script injection\r\nif (typeof window.UIAlerts !== 'undefined') { /* Already loaded */ } else {\r\n\r\nvar UIAlerts = window.UIAlerts = {\r\n    injectStyles() {\r\n        if (document.getElementById('flirteasy-alerts-style')) return;\r\n\r\n        const style = document.createElement('style');\r\n        style.id = 'flirteasy-alerts-style';\r\n        style.textContent = `\r\n            .fe-alerts-wrapper {\r\n                position: fixed;\r\n                top: 24px;\r\n                left: 50%;\r\n                transform: translateX(-50%);\r\n                z-index: 2147483647;\r\n                display: flex;\r\n                flex-direction: column;\r\n                gap: 12px;\r\n                pointer-events: none;\r\n                width: 100%;\r\n                max-width: 400px;\r\n                align-items: center;\r\n            }\r\n            .fe-toast-item {\r\n                pointer-events: auto;\r\n                transform: translateY(-20px);\r\n                opacity: 0;\r\n                transition: all 0.5s cubic-bezier(0.19, 1, 0.22, 1);\r\n                width: fit-content;\r\n            }\r\n            .fe-toast-item.show {\r\n                transform: translateY(0);\r\n                opacity: 1;\r\n            }\r\n            .fe-toast-content {\r\n                background: rgba(13, 15, 24, 0.9);\r\n                backdrop-filter: blur(16px);\r\n                border: 1px solid rgba(251, 191, 36, 0.3);\r\n                border-radius: 16px;\r\n                padding: 12px 20px;\r\n                display: flex;\r\n                align-items: center;\r\n                gap: 12px;\r\n                box-shadow: 0 10px 40px rgba(0,0,0,0.5), 0 0 20px rgba(251, 191, 36, 0.1);\r\n                color: white;\r\n                font-family: -apple-system, BlinkMacSystemFont, \"Segoe UI\", Roboto, Helvetica, Arial, sans-serif;\r\n                min-width: 280px;\r\n            }\r\n            .fe-toast-icon {\r\n                font-size: 20px;\r\n                background: rgba(251, 191, 36, 0.1);\r\n                width: 36px;\r\n                height: 36px;\r\n                border-radius: 10px;\r\n                display: flex;\r\n                align-items: center;\r\n                justify-content: center;\r\n                color: #fbbf24;\r\n            }\r\n            .fe-toast-text {\r\n                display: flex;\r\n                flex-direction: column;\r\n                gap: 2px;\r\n            }\r\n            .fe-toast-title {\r\n                font-weight: 800;\r\n                font-size: 13px;\r\n                color: #fbbf24;\r\n                letter-spacing: 0.02em;\r\n            }\r\n            .fe-toast-detail {\r\n                font-size: 11px;\r\n                color: #94a3b8;\r\n                font-weight: 500;\r\n            }\r\n            .fe-toast-close {\r\n                margin-left: auto;\r\n                cursor: pointer;\r\n                opacity: 0.5;\r\n                transition: opacity 0.2s;\r\n                font-size: 18px;\r\n                padding: 4px;\r\n            }\r\n            .fe-toast-close:hover { opacity: 1; }\r\n            \r\n            @keyframes fe-shine {\r\n                0% { border-color: rgba(251, 191, 36, 0.3); box-shadow: 0 10px 40px rgba(0,0,0,0.5); }\r\n                50% { border-color: rgba(251, 191, 36, 0.6); box-shadow: 0 10px 40px rgba(0,0,0,0.5), 0 0 30px rgba(251, 191, 36, 0.2); }\r\n                100% { border-color: rgba(251, 191, 36, 0.3); box-shadow: 0 10px 40px rgba(0,0,0,0.5); }\r\n            }\r\n            .fe-toast-content.vip { animation: fe-shine 2s infinite; }\r\n        `;\r\n        document.head.appendChild(style);\r\n    },\r\n\r\n    getWrapper() {\r\n        let wrapper = document.querySelector('.fe-alerts-wrapper');\r\n        if (!wrapper) {\r\n            wrapper = document.createElement('div');\r\n            wrapper.className = 'fe-alerts-wrapper';\r\n            document.body.appendChild(wrapper);\r\n        }\r\n        return wrapper;\r\n    },\r\n\r\n    showHandoff(matchName, reason) {\r\n        // Disabled - status bar v2 handles all lead/handoff notifications\r\n        return;\r\n    }\r\n};\r\n\r\nwindow.UIAlerts = UIAlerts;\r\n\r\n} // end re-entry guard\r\n\n\n\n// ═══════════════════════════════════════════════════════════════\n// [FlirtEasy Bundle] content/tinder-dom.js\n// ═══════════════════════════════════════════════════════════════\nwindow.SELECTORS = window.SELECTORS || null;\r\n\r\n// Report DOM errors to background.js which forwards to the server\r\nfunction reportDomError(error_type, selector_key, error_message) {\r\n  try {\r\n    chrome.runtime.sendMessage({\r\n      action: 'reportDomError',\r\n      payload: { platform: 'tinder', error_type, selector_key, error_message, page_url: location.href }\r\n    });\r\n  } catch (_) {}\r\n}\r\n\r\nasync function loadSelectors() {\r\n  if (!window.SELECTORS) {\r\n    try {\r\n      const response = await fetch(chrome.runtime.getURL('config/selectors.json'));\r\n      window.SELECTORS = await response.json();\r\n    } catch (error) {\r\n      console.error('[FlirtEasy] Failed to load selectors:', error);\r\n      // Fallback defaults if load fails\r\n      window.SELECTORS = {\r\n        buttons: { like: [\"button[aria-label='Like']\"], pass: [\"button[aria-label='Pass']\"] },\r\n        navigation: { explore: [\"a[href='/app/recs']\"], messages: [\"a[href='/app/messages']\"] },\r\n        messaging: {\r\n          input: [\"textarea\"],\r\n          sendButton: [\"button[type='submit']\"],\r\n          messageBubble: [\".msg.BreakWord\", \"span.text\", \".msg-text\", \".message-content\", \".text-ellipsis\", \"[class*='messageText' i]\", \"[data-testid*='message']\", \"div[dir='auto']\"]\r\n        },\r\n        profile: { name: [\"h1\"], age: [\"span[itemprop='age']\"] }\r\n      };\r\n    }\r\n  }\r\n  return window.SELECTORS;\r\n}\r\n\r\n// Helper to find an element using an array of selectors\r\nfunction findElement(selectorArray, parent = document) {\r\n  if (!Array.isArray(selectorArray)) return parent.querySelector(selectorArray);\r\n\r\n  for (const selector of selectorArray) {\r\n    // Handle pseudo-selectors like :contains manually\r\n    if (selector.includes(':contains')) {\r\n      const parts = selector.split(':contains');\r\n      const tag = parts[0];\r\n      const text = parts[1].replace(/['\"()]/g, '');\r\n      const elements = parent.querySelectorAll(tag);\r\n      for (const el of elements) {\r\n        if (el.textContent.includes(text)) return el;\r\n      }\r\n    } else if (selector.includes(':text-equals')) {\r\n      const parts = selector.split(':text-equals');\r\n      const tag = parts[0];\r\n      const text = parts[1].replace(/['\"()]/g, '');\r\n      const elements = parent.querySelectorAll(tag);\r\n      for (const el of elements) {\r\n        if (el.textContent.trim() === text) return el;\r\n      }\r\n    } else {\r\n      const el = parent.querySelector(selector);\r\n      if (el) return el;\r\n    }\r\n  }\r\n  return null;\r\n}\r\n\r\n// Helper to find all elements using an array of selectors (merging results)\r\nfunction findElements(selectorArray, parent = document) {\r\n  if (!Array.isArray(selectorArray)) return parent.querySelectorAll(selectorArray);\r\n\r\n  let allElements = [];\r\n  for (const selector of selectorArray) {\r\n    if (selector.includes(':contains')) {\r\n      // Handle :contains for multiple elements\r\n      const parts = selector.split(':contains');\r\n      const tag = parts[0];\r\n      const text = parts[1].replace(/['\"()]/g, '');\r\n      const elements = parent.querySelectorAll(tag);\r\n      const matches = Array.from(elements).filter(el => el.textContent.includes(text));\r\n      allElements = [...allElements, ...matches];\r\n    } else if (selector.includes(':text-equals')) {\r\n      // Handle :text-equals for multiple elements\r\n      const parts = selector.split(':text-equals');\r\n      const tag = parts[0];\r\n      const text = parts[1].replace(/['\"()]/g, '');\r\n      const elements = parent.querySelectorAll(tag);\r\n      const matches = Array.from(elements).filter(el => el.textContent.trim() === text);\r\n      allElements = [...allElements, ...matches];\r\n    } else {\r\n      const elements = parent.querySelectorAll(selector);\r\n      if (elements.length > 0) {\r\n        allElements = [...allElements, ...Array.from(elements)];\r\n      }\r\n    }\r\n  }\r\n  return [...new Set(allElements)]; // Deduplicate\r\n}\r\n\r\n(async () => {\r\n  await loadSelectors();\r\n  console.log('[FlirtEasy] Selectors loaded');\r\n})();\r\n\r\nfunction findLikeButton() {\r\n  if (!window.SELECTORS) return null;\r\n  const btn = findElement(window.SELECTORS.buttons.like);\r\n  if (btn) return btn;\r\n  // Fallback: identify by SVG path fingerprint (Tinder removed aria-labels)\r\n  const svgBtn = _findActionButtonBySvgPath('M17.506 2q-.834 0-1.7.225');\r\n  if (svgBtn) return svgBtn;\r\n  // Both selector and SVG fallback failed — report so admin panel catches it\r\n  reportDomError('selector_miss', 'buttons.like', 'Like button not found via selectors or SVG fingerprint — Tinder may have changed the button markup');\r\n  return null;\r\n}\r\n\r\nfunction findPassButton() {\r\n  if (!window.SELECTORS) return null;\r\n  const btn = findElement(window.SELECTORS.buttons.pass);\r\n  if (btn) return btn;\r\n  // Fallback: identify by SVG path fingerprint (Tinder removed aria-labels)\r\n  const svgBtn = _findActionButtonBySvgPath('M21.974 4.171 19.97 2.17');\r\n  if (svgBtn) return svgBtn;\r\n  // Both selector and SVG fallback failed — report so admin panel catches it\r\n  reportDomError('selector_miss', 'buttons.pass', 'Pass/Nope button not found via selectors or SVG fingerprint — Tinder may have changed the button markup');\r\n  return null;\r\n}\r\n\r\nfunction findSuperLikeButton() {\r\n  if (!window.SELECTORS) return null;\r\n  const btn = findElement(window.SELECTORS.buttons.superLike);\r\n  if (btn) return btn;\r\n  // Fallback: identify by SVG path fingerprint (Tinder removed aria-labels)\r\n  return _findActionButtonBySvgPath('M16.296 8.04a1 1 0 0 1-.89-.65') || null;\r\n}\r\n\r\n// Find an action button by matching the start of its first SVG path's 'd' attribute.\r\n// Used as a resilient fallback when Tinder strips aria-labels from buttons.\r\nfunction _findActionButtonBySvgPath(pathPrefix) {\r\n  const candidates = [...document.querySelectorAll('button')].filter(btn => {\r\n    const r = btn.getBoundingClientRect();\r\n    return r.width >= 48 && r.width <= 70 && r.height >= 48 && r.top > 200;\r\n  });\r\n  return candidates.find(btn => {\r\n    const d = btn.querySelector('path')?.getAttribute('d') || '';\r\n    return d.startsWith(pathPrefix);\r\n  }) || null;\r\n}\r\n\r\nfunction closeSubscriptionPopup() {\r\n  if (!window.SELECTORS) return false;\r\n  const closeBtn = findElement(window.SELECTORS.buttons.closePopup);\r\n  if (closeBtn) {\r\n    closeBtn.click();\r\n    console.log('[FlirtEasy] Closed subscription popup');\r\n    return true;\r\n  }\r\n  return false;\r\n}\r\n\r\nfunction hasSubscriptionPopup() {\r\n  // Only match text that unambiguously signals the \"out of likes\" paywall,\r\n  // NOT subscription feature lists (which appear on the settings page for paid users too).\r\n  const text = (document.body.innerText || document.body.textContent).toLowerCase();\r\n  return text.includes('out of likes') ||\r\n    text.includes(\"you've run out of likes\") ||\r\n    text.includes('select a plan');\r\n}\r\n\r\nfunction detectTinderAccountTier() {\r\n  // Layer 1: API-sourced tier cached by api-interceptor.js (most reliable)\r\n  if (window.__flirtEasyAccountTier) return window.__flirtEasyAccountTier;\r\n\r\n  // Layer 2: DOM selectors — broad set covering current & historical Tinder markup\r\n  const paidSelectors = [\r\n    '[data-testid*=\"gold\"]',\r\n    '[data-testid*=\"platinum\"]',\r\n    '[data-testid*=\"tinder-u\"]',\r\n    '[class*=\"goldBadge\"]',\r\n    '[class*=\"platinumBadge\"]',\r\n    '[class*=\"premiumBadge\"]',\r\n    '[class*=\"subscriptionBadge\"]',\r\n    '[class*=\"subscription-badge\"]',\r\n    '.premium-icon',\r\n    'img[alt*=\"platinum\" i]',\r\n    'img[alt*=\"gold\" i]',\r\n    'svg[aria-label*=\"platinum\" i]',\r\n    'svg[aria-label*=\"gold\" i]',\r\n  ];\r\n  for (const sel of paidSelectors) {\r\n    if (document.querySelector(sel)) return 'paid';\r\n  }\r\n\r\n  // Layer 3: Text scan — sidebar/header subscription label visible for paid users\r\n  // Scope to a small subtree to avoid false matches in the swipe card stack\r\n  const navCandidates = [\r\n    document.querySelector('nav'),\r\n    document.querySelector('[class*=\"sidebar\" i]'),\r\n    document.querySelector('[class*=\"header\" i]'),\r\n    document.querySelector('[class*=\"profile\" i]'),\r\n  ].filter(Boolean);\r\n\r\n  for (const node of navCandidates) {\r\n    const t = node.textContent.toLowerCase();\r\n    if (t.includes('manage your subscription') ||\r\n        /tinder\\s*(platinum|gold|plus)/i.test(node.textContent)) {\r\n      return 'paid';\r\n    }\r\n  }\r\n\r\n  // Layer 4: Definitive free signal — paywall popup is visible\r\n  if (hasSubscriptionPopup()) return 'free';\r\n\r\n  return 'unknown';\r\n}\r\n\r\nfunction isStackEmpty() {\r\n  const text = (document.body.innerText || document.body.textContent || '').toLowerCase();\r\n  return text.includes(\"unable to find any potential matches\") ||\r\n    text.includes(\"checking out the profiles\") ||\r\n    text.includes(\"try changing your preferences\") ||\r\n    text.includes(\"people looking for\") ||\r\n    text.includes(\"we've run out of potential matches\") ||\r\n    text.includes(\"run out of potential matches\") ||\r\n    text.includes(\"there's no one new around you\") ||\r\n    text.includes(\"no one new around you\") ||\r\n    text.includes(\"go global\");\r\n}\r\n\r\nfunction isProfileVisible() {\r\n  if (!window.SELECTORS) return false;\r\n\r\n  // Layer 1: Check for \"No matches\" or \"Searching\" text\r\n  const bodyText = (document.body.innerText || document.body.textContent || '').toLowerCase();\r\n  if (\r\n    bodyText.includes(\"unable to find any potential matches\") ||\r\n    bodyText.includes(\"people looking for\") ||\r\n    bodyText.includes(\"out of likes\") ||\r\n    bodyText.includes(\"checking out the profiles\") ||\r\n    bodyText.includes(\"looking for people near you\") ||\r\n    bodyText.includes(\"searching for people\") ||\r\n    bodyText.includes(\"there's no one new around you\") ||\r\n    bodyText.includes(\"no one new around you\") ||\r\n    bodyText.includes(\"finding people near you\") ||\r\n    bodyText.includes(\"looking for potential matches\")\r\n  ) {\r\n    console.log('[FlirtEasy] No matches, searching, or out of likes screen detected in text');\r\n    return false;\r\n  }\r\n\r\n  // Layer 2: A valid Tinder candidate card MUST have either a candidate name or a photo URL\r\n  const cardName = typeof getSwipeCardName === 'function' ? getSwipeCardName() : null;\r\n  const photoUrl = typeof extractProfilePhotoUrl === 'function' ? extractProfilePhotoUrl() : null;\r\n\r\n  if (!cardName && !photoUrl) {\r\n    console.log('[FlirtEasy] No candidate card name or photo found — Tinder is in searching/radar state');\r\n    return false;\r\n  }\r\n\r\n  // Layer 3: Ensure card element is actually rendered and visible in viewport\r\n  const profileCard = findElement(window.SELECTORS.profile.card);\r\n  if (!profileCard) return false;\r\n\r\n  const rect = profileCard.getBoundingClientRect();\r\n  return rect.width >= 100 && rect.height >= 100;\r\n}\r\n\r\nfunction clickLikeButton() {\r\n  const likeBtn = findLikeButton();\r\n\r\n  if (likeBtn) {\r\n    likeBtn.click();\r\n    console.log('[FlirtEasy] Like button clicked');\r\n    return true;\r\n  }\r\n\r\n  // Keyboard shortcut fallback (Tinder web standard: ArrowRight = Like)\r\n  try {\r\n    const keyEvent = new KeyboardEvent('keydown', { key: 'ArrowRight', code: 'ArrowRight', keyCode: 39, which: 39, bubbles: true, cancelable: true });\r\n    window.dispatchEvent(keyEvent);\r\n    document.dispatchEvent(keyEvent);\r\n    console.log('[FlirtEasy] Dispatched ArrowRight keydown for Like');\r\n  } catch (_) {}\r\n\r\n  console.log('[FlirtEasy] Like button not found, attempting swipe right...');\r\n  return swipeRight();\r\n}\r\n\r\nfunction swipeRight() {\r\n  if (!window.SELECTORS) return false;\r\n  const profileCard = findElement(window.SELECTORS.profile.card);\r\n  if (!profileCard) {\r\n    console.log('[FlirtEasy] Profile card not found for swipe');\r\n    reportDomError('selector_miss', 'profile.card', 'Profile card not found for swipe — Tinder may have changed the card container selector');\r\n    return false;\r\n  }\r\n\r\n  // Ensure card is visible\r\n  const rect = profileCard.getBoundingClientRect();\r\n  if (rect.width === 0 || rect.height === 0) {\r\n    console.log('[FlirtEasy] Profile card found but hidden (0 dimensions), aborting swipe');\r\n    reportDomError('selector_miss', 'profile.card', 'Profile card found but has zero dimensions (hidden) — card stack may not be loaded yet');\r\n    return false;\r\n  }\r\n\r\n  const startX = rect.left + (rect.width / 2);\r\n  const startY = rect.top + (rect.height / 2);\r\n  const endX = startX + 200; // Swipe right\r\n  const endY = startY;\r\n\r\n  // Create start touch\r\n  const touchStartObj = new Touch({\r\n    identifier: 0,\r\n    target: profileCard,\r\n    clientX: startX,\r\n    clientY: startY,\r\n    screenX: startX,\r\n    screenY: startY,\r\n    pageX: startX,\r\n    pageY: startY\r\n  });\r\n\r\n  const touchStart = new TouchEvent('touchstart', {\r\n    bubbles: true,\r\n    cancelable: true,\r\n    view: window,\r\n    touches: [touchStartObj],\r\n    targetTouches: [touchStartObj],\r\n    changedTouches: [touchStartObj]\r\n  });\r\n\r\n  // Create move/end touch\r\n  const touchEndObj = new Touch({\r\n    identifier: 0,\r\n    target: profileCard,\r\n    clientX: endX,\r\n    clientY: endY,\r\n    screenX: endX,\r\n    screenY: endY,\r\n    pageX: endX,\r\n    pageY: endY\r\n  });\r\n\r\n  const touchMove = new TouchEvent('touchmove', {\r\n    bubbles: true,\r\n    cancelable: true,\r\n    view: window,\r\n    touches: [touchEndObj],\r\n    targetTouches: [touchEndObj],\r\n    changedTouches: [touchEndObj]\r\n  });\r\n\r\n  const touchEnd = new TouchEvent('touchend', {\r\n    bubbles: true,\r\n    cancelable: true,\r\n    view: window,\r\n    touches: [],\r\n    targetTouches: [],\r\n    changedTouches: [touchEndObj]\r\n  });\r\n\r\n  console.log('[FlirtEasy] Dispatching swipe sequence...');\r\n  profileCard.dispatchEvent(touchStart);\r\n\r\n  setTimeout(() => {\r\n    profileCard.dispatchEvent(touchMove);\r\n    setTimeout(() => {\r\n      profileCard.dispatchEvent(touchEnd);\r\n    }, 50);\r\n  }, 50);\r\n\r\n  return true;\r\n}\r\n\r\n// findPassButton is defined above (with SVG fallback + reportDomError)\r\n\r\nfunction clickPassButton() {\r\n  const passBtn = findPassButton();\r\n  if (passBtn) {\r\n    passBtn.click();\r\n    console.log('[FlirtEasy] Pass button clicked');\r\n    return true;\r\n  }\r\n\r\n  // Keyboard shortcut fallback (Tinder web standard: ArrowLeft = Pass)\r\n  try {\r\n    const keyEvent = new KeyboardEvent('keydown', { key: 'ArrowLeft', code: 'ArrowLeft', keyCode: 37, which: 37, bubbles: true, cancelable: true });\r\n    window.dispatchEvent(keyEvent);\r\n    document.dispatchEvent(keyEvent);\r\n    console.log('[FlirtEasy] Dispatched ArrowLeft keydown for Pass');\r\n  } catch (_) {}\r\n\r\n  console.log('[FlirtEasy] Pass button not found, attempting swipe left...');\r\n  return swipeLeft();\r\n}\r\n\r\nfunction swipeLeft() {\r\n  if (!window.SELECTORS) return false;\r\n  const profileCard = findElement(window.SELECTORS.profile.card);\r\n  if (!profileCard) {\r\n    reportDomError('selector_miss', 'profile.card', 'Profile card not found for swipe left');\r\n    return false;\r\n  }\r\n  const rect = profileCard.getBoundingClientRect();\r\n  if (rect.width === 0 || rect.height === 0) return false;\r\n\r\n  const startX = rect.left + (rect.width / 2);\r\n  const startY = rect.top + (rect.height / 2);\r\n  const endX = startX - 200;\r\n\r\n  const mkTouch = (x, y) => new Touch({ identifier: 1, target: profileCard, clientX: x, clientY: y, screenX: x, screenY: y, pageX: x, pageY: y });\r\n\r\n  profileCard.dispatchEvent(new TouchEvent('touchstart', { bubbles: true, cancelable: true, view: window, touches: [mkTouch(startX, startY)], targetTouches: [mkTouch(startX, startY)], changedTouches: [mkTouch(startX, startY)] }));\r\n  setTimeout(() => {\r\n    profileCard.dispatchEvent(new TouchEvent('touchmove', { bubbles: true, cancelable: true, view: window, touches: [mkTouch(endX, startY)], targetTouches: [mkTouch(endX, startY)], changedTouches: [mkTouch(endX, startY)] }));\r\n    setTimeout(() => {\r\n      profileCard.dispatchEvent(new TouchEvent('touchend', { bubbles: true, cancelable: true, view: window, touches: [], targetTouches: [], changedTouches: [mkTouch(endX, startY)] }));\r\n    }, 50);\r\n  }, 50);\r\n  return true;\r\n}\r\n\r\nfunction getProfileAge() {\r\n  if (!window.SELECTORS) return null;\r\n  const ageEl = findElement(window.SELECTORS.profile.age);\r\n  if (ageEl) {\r\n    const age = parseInt(ageEl.textContent.trim());\r\n    console.log('[FlirtEasy] Profile age:', age);\r\n    return age;\r\n  }\r\n  console.log('[FlirtEasy] Age not found');\r\n  return null;\r\n}\r\n\r\nfunction getCurrentProfile() {\r\n  if (!window.SELECTORS) return { name: null, bio: null, interests: [] };\r\n  const nameEl = findElement(window.SELECTORS.profile.name);\r\n  const bioEl = findElement(window.SELECTORS.profile.bio);\r\n  const interestEls = findElements(window.SELECTORS.profile.interests);\r\n\r\n  return {\r\n    name: nameEl?.textContent?.trim() || null,\r\n    bio: bioEl?.textContent?.trim() || null,\r\n    interests: Array.from(interestEls).map(el => el.textContent.trim())\r\n  };\r\n}\r\n\r\n\r\n\r\nfunction hasMatchModal() {\r\n  if (!window.SELECTORS) return false;\r\n  return !!document.querySelector(window.SELECTORS.overlays.matchModal);\r\n}\r\n\r\nfunction closeMatchModal() {\r\n  if (!window.SELECTORS) return false;\r\n  const modal = document.querySelector(window.SELECTORS.overlays.matchModal);\r\n  if (modal) {\r\n    const closeBtn = modal.querySelector('button');\r\n    if (closeBtn) closeBtn.click();\r\n    return true;\r\n  }\r\n  return false;\r\n}\r\n\r\nfunction hasLocationModal() {\r\n  const bodyText = (document.body.innerText || document.body.textContent || '').toLowerCase();\r\n  return (\r\n    bodyText.includes('enable location') ||\r\n    bodyText.includes('share location') ||\r\n    bodyText.includes('allow location') ||\r\n    bodyText.includes(\"you'll need to enable location\") ||\r\n    bodyText.includes('location services off') ||\r\n    bodyText.includes('allow location access')\r\n  );\r\n}\r\n\r\nfunction handleLocationModal() {\r\n  try {\r\n    const buttons = Array.from(document.querySelectorAll('button, [role=\"button\"]'));\r\n    for (let i = 0; i < buttons.length; i++) {\r\n      const btn = buttons[i];\r\n      const txt = (btn.innerText || btn.textContent || '').trim().toLowerCase();\r\n      const aria = (btn.getAttribute('aria-label') || '').toLowerCase();\r\n      if (\r\n        txt === 'allow' ||\r\n        txt === 'enable location' ||\r\n        txt === 'share location' ||\r\n        txt === 'allow location' ||\r\n        txt === 'continue' ||\r\n        txt === 'i understand' ||\r\n        aria.includes('enable location') ||\r\n        aria.includes('share location') ||\r\n        aria.includes('allow location')\r\n      ) {\r\n        console.log('[FlirtEasy] Auto-accepting Tinder location prompt button:', txt || aria);\r\n        btn.click();\r\n        return true;\r\n      }\r\n    }\r\n  } catch (e) {\r\n    console.warn('[FlirtEasy] handleLocationModal error:', e);\r\n  }\r\n  return false;\r\n}\r\n\r\nfunction isLoggedIn() {\r\n  if (!window.location.pathname.includes('/app')) {\r\n    return false;\r\n  }\r\n\r\n  // Never report logged in on login or landing paths\r\n  const path = window.location.pathname.toLowerCase();\r\n  if (path.includes('/app/login') || path === '/app' || path === '/app/') {\r\n    return false;\r\n  }\r\n\r\n  // If login form inputs or login dialogs are visible, user is NOT logged in\r\n  if (document.querySelector('input[type=\"tel\"], input[name=\"phone_number\"], input[autocomplete=\"one-time-code\"], input[name=\"code\"]')) {\r\n    return false;\r\n  }\r\n\r\n  const loginIndicators = [\r\n    () => window.SELECTORS?.navigation?.explore && findElement(window.SELECTORS.navigation.explore),\r\n    () => window.SELECTORS?.navigation?.messages && findElement(window.SELECTORS.navigation.messages),\r\n    () => window.SELECTORS?.buttons?.like && findElement(window.SELECTORS.buttons.like),\r\n    () => window.SELECTORS?.profile?.card && findElement(window.SELECTORS.profile.card),\r\n    () => document.querySelector('[data-testid=\"gamepad-like\"], button[aria-label*=\"Like\" i], a[href*=\"/app/recs\"], a[href*=\"/app/messages\"]'),\r\n    () => {\r\n      try {\r\n        const token = localStorage.getItem('TinderWeb/APIToken');\r\n        if (token && typeof token === 'string' && token.length > 20) return true;\r\n        const apiStore = localStorage.getItem('TinderWeb/APIStore');\r\n        if (apiStore) {\r\n          const parsed = JSON.parse(apiStore);\r\n          const tok = parsed && (parsed.token || parsed.auth_token || (parsed.user && parsed.user.api_token));\r\n          if (tok && typeof tok === 'string' && tok.length > 20) return true;\r\n        }\r\n        return false;\r\n      } catch(_) { return false; }\r\n    }\r\n  ];\r\n\r\n  return loginIndicators.some(check => {\r\n    try { return Boolean(check()); } catch(_) { return false; }\r\n  });\r\n}\r\n\r\nfunction waitRandom(min, max) {\r\n  const delay = Math.floor(Math.random() * (max - min + 1)) + min;\r\n  return new Promise(resolve => {\r\n    const t = setTimeout(resolve, delay);\r\n    const poll = setInterval(() => {\r\n      if (window.__flirteasy_stop === true) {\r\n        clearInterval(poll);\r\n        clearTimeout(t);\r\n        resolve();\r\n      }\r\n    }, 100);\r\n    setTimeout(() => clearInterval(poll), delay + 50);\r\n  });\r\n}\r\n\r\nasync function getSwipeDelay() {\r\n  try {\r\n    const result = await chrome.storage.local.get('remoteRateLimits');\r\n    const h = result.remoteRateLimits && result.remoteRateLimits.humanizer;\r\n    if (h && typeof h.swipe_min_ms === 'number' && typeof h.swipe_max_ms === 'number') {\r\n      return waitRandom(h.swipe_min_ms, h.swipe_max_ms);\r\n    }\r\n  } catch (_) {}\r\n  return waitRandom(2000, 4000);\r\n}\r\n\r\n\r\n\r\nfunction extractProfilePhotoUrl() {\r\n  console.log('[FlirtEasy] Searching for profile photo...');\r\n\r\n  const photoDivs = document.querySelectorAll('div[role=\"img\"][aria-hidden=\"false\"]');\r\n  console.log('[FlirtEasy] Found divs:', photoDivs.length);\r\n\r\n  for (const div of photoDivs) {\r\n    console.log('[FlirtEasy] Checking div:', div.getAttribute('aria-label'));\r\n    const style = div.style.backgroundImage;\r\n    if (style) {\r\n      console.log('[FlirtEasy] Background image style:', style);\r\n      const match = style.match(/url\\([\"']?([^\"')]+)[\"']?\\)/);\r\n      if (match && match[1]) {\r\n        console.log('[FlirtEasy] Extracted URL:', match[1]);\r\n        return match[1];\r\n      }\r\n    }\r\n  }\r\n\r\n  console.log('[FlirtEasy] No profile photo found');\r\n  return null;\r\n}\r\n\r\n/**\r\n * Extracts the profile name from the Tinder swipe card overlay.\r\n * getMatchName() uses the generic h1 selector which only works in chat view.\r\n * This function targets the card overlay where \"Shir 29\" is displayed.\r\n *\r\n * CRITICAL: Tinder pre-renders a STACK of cards in the DOM. The visible\r\n * (top) card is the LAST rendered element, so querySelectorAll + pick last\r\n * is required. querySelector returns the bottom card (wrong name).\r\n *\r\n * Multi-strategy approach (same resilience pattern as Bumble):\r\n *   1. itemprop=\"name\" span — last visible instance (top card)\r\n *   2. aria-label on profile photo div — last visible instance\r\n *   3. Card container text — parse \"Name Age\" from bottom overlay\r\n *   4. Fallback to getMatchName() for backwards compatibility\r\n */\r\nfunction getSwipeCardName() {\r\n  // Helper: from a NodeList, return the last element that is fully VISIBLE on screen\r\n  function pickTopCard(nodeList) {\r\n    if (!nodeList || nodeList.length === 0) return null;\r\n    // Walk backwards — top card is last in DOM order due to z-stacking\r\n    for (let i = nodeList.length - 1; i >= 0; i--) {\r\n      const el = nodeList[i];\r\n      const rect = el.getBoundingClientRect();\r\n      \r\n      // Strict Viewport Visibility Check:\r\n      // Tinder keeps recently swiped cards in the DOM for \"Rewind\" features, just translated\r\n      // off-screen horizontally. We MUST check both X and Y bounds to ignore swiped cards.\r\n      const isVisibleY = rect.width > 0 && rect.height > 0 && rect.top < window.innerHeight && rect.bottom > 0;\r\n      const isVisibleX = rect.left < window.innerWidth && rect.right > 0;\r\n      \r\n      if (isVisibleX && isVisibleY) {\r\n        return el;\r\n      }\r\n    }\r\n    // Fallback: lowest risk element\r\n    return nodeList[nodeList.length - 1];\r\n  }\r\n\r\n  // Strategy 1: itemprop=\"name\" — pick the TOP card's instance\r\n  const nameSpans = document.querySelectorAll('span[itemprop=\"name\"]');\r\n  if (nameSpans.length > 0) {\r\n    const topSpan = pickTopCard(nameSpans);\r\n    if (topSpan) {\r\n      const name = topSpan.textContent.trim();\r\n      if (name && name.length > 0 && name.length < 40) return name;\r\n    }\r\n  }\r\n\r\n  // Strategy 2: aria-label on profile photo div\r\n  // e.g., aria-label=\"Photo 1/4, Shir, 29 - scroll down for more\"\r\n  const photoDivs = document.querySelectorAll('div[role=\"img\"][aria-label]');\r\n  if (photoDivs.length > 0) {\r\n    const topDiv = pickTopCard(photoDivs);\r\n    if (topDiv) {\r\n      const label = topDiv.getAttribute('aria-label') || '';\r\n      const m = label.match(/Photo\\s+\\d+\\/\\d+,\\s*([^,]+)/i);\r\n      if (m && m[1].trim().length > 0 && m[1].trim().length < 40) return m[1].trim();\r\n    }\r\n  }\r\n\r\n  // Strategy 3: Parse visible card footer text \"Name Age\"\r\n  // Tinder's card info section at the bottom — contains name + age as siblings\r\n  const ageSpans = document.querySelectorAll('span[itemprop=\"age\"]');\r\n  if (ageSpans.length > 0) {\r\n    const topAge = pickTopCard(ageSpans);\r\n    if (topAge) {\r\n      // The name is a sibling span right before the age span\r\n      const parent = topAge.closest('h1, [class*=\"Typs\"], [class*=\"name\"]')\r\n                  || topAge.parentElement;\r\n      if (parent) {\r\n        const raw = parent.textContent.trim();\r\n        // Strip trailing age digits\r\n        const cleaned = raw.replace(/\\s*\\d{1,3}\\s*$/, '').trim();\r\n        if (cleaned && cleaned.length > 0 && cleaned.length < 40) return cleaned;\r\n      }\r\n    }\r\n  }\r\n\r\n  // Strategy 4: Generic h1 fallback (last one, skip \"match\" headers)\r\n  const allH1 = document.querySelectorAll('h1');\r\n  for (let i = allH1.length - 1; i >= 0; i--) {\r\n    const raw = allH1[i].textContent.trim();\r\n    const cleaned = raw.replace(/\\s*\\d{1,3}\\s*$/, '').trim();\r\n    if (cleaned && cleaned.length > 0 && cleaned.length < 40 &&\r\n        !cleaned.toLowerCase().includes('match') &&\r\n        !cleaned.toLowerCase().includes('tinder') &&\r\n        !cleaned.toLowerCase().includes('you matched')) {\r\n      return cleaned;\r\n    }\r\n  }\r\n\r\n  // Final fallback\r\n  return getMatchName() || null;\r\n}\r\n\r\n// ══════════════════════════════════════════════════\r\n// TINDER DEAD STATE CARD\r\n// Injected when user has no matches/conversations.\r\n// ══════════════════════════════════════════════════\r\n\r\nconst _FE_TINDER_CARD_ID = 'fe-tinder-wingman-card';\r\nconst _FE_TINDER_PANEL_ID = 'fe-tinder-panel';\r\nlet _feTinderCardObserver = null;\r\nlet _feTinderCardRetryTimer = null;\r\n\r\nconst _FE_TINDER_TIPS = [\r\n    'Watching for matches — I\\'ll message the moment one lands.',\r\n    'No convos yet. Swipe right on more people, let me handle the talking.',\r\n    'Your profile is live. I\\'ll craft a perfect opener for every new match.',\r\n    'Daily likes reset soon — come back for more swipes.',\r\n    'Ready and waiting. The second you match, I\\'m on it.',\r\n];\r\n\r\nfunction _buildTinderCardHtml() {\r\n    const iconUrl = (typeof chrome !== 'undefined' && chrome.runtime && chrome.runtime.getURL)\r\n        ? chrome.runtime.getURL('icons/icon_128.png') : '';\r\n    const tip = _FE_TINDER_TIPS[Math.floor(Date.now() / 60000) % _FE_TINDER_TIPS.length];\r\n    return `\r\n        <li id=\"${_FE_TINDER_CARD_ID}\" class=\"fe-tinder-dead-card\" data-fe-injected=\"true\" role=\"presentation\" style=\"list-style:none!important;padding:0!important;margin:0!important;\">\r\n            <div class=\"fe-tdc-gradient-base\"></div>\r\n            <div class=\"fe-tdc-blob fe-tdc-blob-1\"></div>\r\n            <div class=\"fe-tdc-blob fe-tdc-blob-2\"></div>\r\n            <div class=\"fe-tdc-blob fe-tdc-blob-3\"></div>\r\n            <div class=\"fe-tdc-glass\"></div>\r\n            <div class=\"fe-tdc-content\">\r\n                <div class=\"fe-tdc-avatar-wrap\">\r\n                    <div class=\"fe-tdc-ripple fe-tdc-ripple-1\"></div>\r\n                    <div class=\"fe-tdc-ripple fe-tdc-ripple-2\"></div>\r\n                    <div class=\"fe-tdc-avatar-frame\">\r\n                        <div class=\"fe-tdc-avatar-inner\">\r\n                            <div class=\"fe-tdc-shine\"></div>\r\n                            <img src=\"${iconUrl}\" alt=\"FlirtEasy\" draggable=\"false\">\r\n                        </div>\r\n                    </div>\r\n                    <div class=\"fe-tdc-live-dot-wrap\">\r\n                        <span class=\"fe-tdc-live-dot\"></span>\r\n                    </div>\r\n                </div>\r\n                <div class=\"fe-tdc-body\">\r\n                    <div class=\"fe-tdc-header\">\r\n                        <span class=\"fe-tdc-name\">AI Wingman</span>\r\n                        <div class=\"fe-tdc-badge\">\r\n                            <div class=\"fe-tdc-badge-ping\"></div>\r\n                            <span class=\"fe-tdc-badge-dot\"></span>\r\n                            <span class=\"fe-tdc-badge-text\">LIVE</span>\r\n                        </div>\r\n                    </div>\r\n                    <p class=\"fe-tdc-msg\">${tip}</p>\r\n                </div>\r\n                <div class=\"fe-tdc-arrow\">\r\n                    <svg width=\"15\" height=\"15\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2.5\" stroke-linecap=\"round\" stroke-linejoin=\"round\"><line x1=\"5\" y1=\"12\" x2=\"19\" y2=\"12\"/><polyline points=\"12 5 19 12 12 19\"/></svg>\r\n                </div>\r\n            </div>\r\n        </li>`;\r\n}\r\n\r\nfunction _buildTinderPanelHtml(iconUrl) {\r\n    const now = new Date();\r\n    const timeStr = now.toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' });\r\n    return `\r\n        <div id=\"${_FE_TINDER_PANEL_ID}\" role=\"dialog\" aria-label=\"AI Wingman\">\r\n            <div class=\"fe-tpanel-header\">\r\n                <div class=\"fe-tpanel-avatar\">\r\n                    <div class=\"fe-tpanel-avatar-inner\">\r\n                        <img src=\"${iconUrl}\" alt=\"AI Wingman\" draggable=\"false\">\r\n                    </div>\r\n                    <span class=\"fe-tpanel-dot\"></span>\r\n                </div>\r\n                <div class=\"fe-tpanel-title\">\r\n                    <span class=\"fe-tpanel-name\">AI Wingman</span>\r\n                    <span class=\"fe-tpanel-subtitle\">● Active &amp; watching</span>\r\n                </div>\r\n                <button class=\"fe-tpanel-close\" id=\"fe-tpanel-close-btn\" aria-label=\"Close\">✕</button>\r\n            </div>\r\n            <div class=\"fe-tpanel-messages\">\r\n                <p class=\"fe-tpanel-day-label\">Today · ${timeStr}</p>\r\n                <div class=\"fe-tpanel-bubble\">\r\n                    <div class=\"fe-tpanel-bubble-avatar\"><img src=\"${iconUrl}\" alt=\"\" draggable=\"false\"></div>\r\n                    <div class=\"fe-tpanel-bubble-text\">\r\n                        Scanned your matches — nothing new yet. The moment someone swipes right on you, I'll jump in with the perfect opener. Zero effort on your end.\r\n                    </div>\r\n                </div>\r\n                <div class=\"fe-tpanel-bubble\">\r\n                    <div class=\"fe-tpanel-bubble-avatar\"><img src=\"${iconUrl}\" alt=\"\" draggable=\"false\"></div>\r\n                    <div class=\"fe-tpanel-bubble-text\">\r\n                        Actively watching your inbox right now. Here's the deal:\r\n                        <div class=\"fe-tpanel-stats\">\r\n                            <div class=\"fe-tpanel-stat\">\r\n                                <span class=\"fe-tpanel-stat-icon\"><svg width=\"13\" height=\"13\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\"><path d=\"M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z\"/><circle cx=\"12\" cy=\"12\" r=\"3\"/></svg></span>Inbox monitored\r\n                            </div>\r\n                            <div class=\"fe-tpanel-stat\">\r\n                                <span class=\"fe-tpanel-stat-icon\"><svg width=\"13\" height=\"13\" viewBox=\"0 0 24 24\" fill=\"currentColor\"><polygon points=\"13 2 3 14 12 14 11 22 21 10 12 10 13 2\"/></svg></span>Auto-reply ready\r\n                            </div>\r\n                            <div class=\"fe-tpanel-stat\">\r\n                                <span class=\"fe-tpanel-stat-icon\"><svg width=\"13\" height=\"13\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\"><polyline points=\"23 4 23 10 17 10\"/><path d=\"M20.49 15a9 9 0 1 1-2.12-9.36L23 10\"/></svg></span>Checking every cycle\r\n                            </div>\r\n                        </div>\r\n                    </div>\r\n                </div>\r\n                <div class=\"fe-tpanel-tips\">\r\n                    <div class=\"fe-tpanel-tips-title\">\r\n                        <svg width=\"13\" height=\"13\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2.5\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:-1px;margin-right:5px\"><line x1=\"7\" y1=\"17\" x2=\"17\" y2=\"7\"/><polyline points=\"7 7 17 7 17 17\"/></svg>Get more matches while you wait\r\n                    </div>\r\n                    <div class=\"fe-tpanel-tip-item\">Add more photos — profiles with 4+ pics get way more right swipes.</div>\r\n                    <div class=\"fe-tpanel-tip-item\">Write a short specific bio. \"I hike on weekends\" beats \"I love to travel\" every time.</div>\r\n                    <div class=\"fe-tpanel-tip-item\">Swipe right on more people — I handle the talking, you just pick who you like.</div>\r\n                    <div class=\"fe-tpanel-tip-item\">Tinder Boost works best on Sunday evenings — peak active users.</div>\r\n                </div>\r\n            </div>\r\n        </div>`;\r\n}\r\n\r\nfunction _findTinderConversationListContainer() {\r\n    const firstLink = document.querySelector('a[href^=\"/app/messages/\"]');\r\n    if (firstLink) {\r\n        const li = firstLink.parentElement;\r\n        return (li && li.tagName === 'LI' && li.parentElement) ? li.parentElement : li;\r\n    }\r\n\r\n    const selectors = [\r\n        '[class*=\"messageList\"]',\r\n        '[data-testid=\"messageList\"]',\r\n        '[class*=\"chatList\"]',\r\n        '[class*=\"conversationList\"]',\r\n        '[class*=\"MatchList\"]',\r\n        '[class*=\"matchList\"]',\r\n    ];\r\n    for (const sel of selectors) {\r\n        const el = document.querySelector(sel);\r\n        if (el) return el;\r\n    }\r\n    return null;\r\n}\r\n\r\nfunction injectTinderDeadStateCard() {\r\n    if (document.getElementById(_FE_TINDER_CARD_ID)) return;\r\n\r\n    const container = _findTinderConversationListContainer();\r\n\r\n    if (!container) {\r\n        clearTimeout(_feTinderCardRetryTimer);\r\n        _feTinderCardRetryTimer = setTimeout(injectTinderDeadStateCard, 1500);\r\n        return;\r\n    }\r\n\r\n    const wrapper = document.createElement('div');\r\n    wrapper.innerHTML = _buildTinderCardHtml();\r\n    const card = wrapper.firstElementChild;\r\n    container.insertBefore(card, container.firstChild);\r\n\r\n    card.addEventListener('click', () => {\r\n        const isOpen = !!document.getElementById(_FE_TINDER_PANEL_ID);\r\n        if (isOpen) {\r\n            _closeTinderDeadStatePanel();\r\n            card.classList.remove('fe-tdc-active');\r\n        } else {\r\n            _openTinderDeadStatePanel();\r\n            card.classList.add('fe-tdc-active');\r\n        }\r\n    });\r\n\r\n    if (_feTinderCardObserver) _feTinderCardObserver.disconnect();\r\n    _feTinderCardObserver = new MutationObserver(() => {\r\n        if (!document.getElementById(_FE_TINDER_CARD_ID) && !_tinderHasConversations() && _tinderOnMessagesTab()) {\r\n            injectTinderDeadStateCard();\r\n        }\r\n    });\r\n    _feTinderCardObserver.observe(container, { childList: true });\r\n\r\n    console.log('[FlirtEasy] Tinder dead state card injected');\r\n}\r\n\r\nfunction removeTinderDeadStateCard() {\r\n    if (_feTinderCardObserver) {\r\n        _feTinderCardObserver.disconnect();\r\n        _feTinderCardObserver = null;\r\n    }\r\n    clearTimeout(_feTinderCardRetryTimer);\r\n    _closeTinderDeadStatePanel();\r\n    const card = document.getElementById(_FE_TINDER_CARD_ID);\r\n    if (card) {\r\n        card.style.animation = 'none';\r\n        void card.offsetHeight;\r\n        card.style.animation = '';\r\n        card.classList.add('fe-tdc-exit');\r\n        setTimeout(() => card.remove(), 300);\r\n    }\r\n}\r\n\r\nfunction _getTinderSidebarRight() {\r\n    const sidebarSelectors = ['aside', '[class*=\"Sidebar\"]', '[class*=\"sidebar\"]'];\r\n    for (const sel of sidebarSelectors) {\r\n        const el = document.querySelector(sel);\r\n        if (el) {\r\n            const rect = el.getBoundingClientRect();\r\n            if (rect.width > 50) return Math.round(rect.right);\r\n        }\r\n    }\r\n    return 360;\r\n}\r\n\r\nfunction _openTinderDeadStatePanel() {\r\n    if (document.getElementById(_FE_TINDER_PANEL_ID)) return;\r\n    const iconUrl = (typeof chrome !== 'undefined' && chrome.runtime && chrome.runtime.getURL)\r\n        ? chrome.runtime.getURL('icons/icon_128.png') : '';\r\n    const wrapper = document.createElement('div');\r\n    wrapper.innerHTML = _buildTinderPanelHtml(iconUrl);\r\n    const panel = wrapper.firstElementChild;\r\n    const sidebarRight = _getTinderSidebarRight();\r\n    panel.style.setProperty('left', sidebarRight + 'px', 'important');\r\n    document.body.appendChild(panel);\r\n    panel.querySelector('#fe-tpanel-close-btn').addEventListener('click', (e) => {\r\n        e.stopPropagation();\r\n        _closeTinderDeadStatePanel();\r\n        const card = document.getElementById(_FE_TINDER_CARD_ID);\r\n        if (card) card.classList.remove('fe-tdc-active');\r\n    });\r\n}\r\n\r\nfunction _closeTinderDeadStatePanel() {\r\n    const panel = document.getElementById(_FE_TINDER_PANEL_ID);\r\n    if (!panel) return;\r\n    panel.classList.add('fe-tpanel-exiting');\r\n    setTimeout(() => panel.remove(), 270);\r\n}\r\n\r\n// ── Passive dead-state detector ──\r\n// Auto-injects the card when user visits the messages tab with no conversations,\r\n// even when automation is not running. Watches URL changes (SPA navigation).\r\n\r\nlet _tinderDeadStatePassiveObserver = null;\r\nlet _tinderDeadStateCheckTimer = null;\r\nlet _tinderDeadStatePendingConfirm = false;\r\n\r\nfunction _tinderHasConversations() {\r\n    return document.querySelectorAll('a[href^=\"/app/messages/\"]').length >= 3;\r\n}\r\n\r\nfunction _tinderOnMessagesTab() {\r\n    if (window.location.pathname.includes('/app/messages') ||\r\n        window.location.pathname.includes('/app/matches')) return true;\r\n    // Tinder sidebar is always visible — check if message links OR the sidebar container are present in DOM\r\n    return !!document.querySelector(\r\n        'a[href^=\"/app/messages/\"], [class*=\"messageList\"], [class*=\"MatchList\"], [class*=\"chatList\"], [class*=\"conversationList\"]'\r\n    );\r\n}\r\n\r\nfunction _checkTinderDeadStatePassive(fromConfirm) {\r\n    clearTimeout(_tinderDeadStateCheckTimer);\r\n    const delay = fromConfirm ? 1800 : 2500;\r\n    _tinderDeadStateCheckTimer = setTimeout(() => {\r\n        if (!_tinderOnMessagesTab()) {\r\n            _tinderDeadStatePendingConfirm = false;\r\n            removeTinderDeadStateCard();\r\n            return;\r\n        }\r\n        if (_tinderHasConversations()) {\r\n            _tinderDeadStatePendingConfirm = false;\r\n            removeTinderDeadStateCard();\r\n            return;\r\n        }\r\n        if (!fromConfirm) {\r\n            _tinderDeadStatePendingConfirm = true;\r\n            _checkTinderDeadStatePassive(true);\r\n            return;\r\n        }\r\n        _tinderDeadStatePendingConfirm = false;\r\n        injectTinderDeadStateCard();\r\n    }, delay);\r\n}\r\n\r\nfunction _initTinderDeadStatePassiveDetector() {\r\n    if (_tinderDeadStatePassiveObserver) return;\r\n\r\n    let _lastPathname = window.location.pathname;\r\n    const origPushState = history.pushState.bind(history);\r\n    const origReplaceState = history.replaceState.bind(history);\r\n\r\n    history.pushState = function (...args) {\r\n        origPushState(...args);\r\n        if (window.location.pathname !== _lastPathname) {\r\n            _lastPathname = window.location.pathname;\r\n            _tinderDeadStatePendingConfirm = false;\r\n            _checkTinderDeadStatePassive();\r\n        }\r\n    };\r\n    history.replaceState = function (...args) {\r\n        origReplaceState(...args);\r\n        if (window.location.pathname !== _lastPathname) {\r\n            _lastPathname = window.location.pathname;\r\n            _tinderDeadStatePendingConfirm = false;\r\n            _checkTinderDeadStatePassive();\r\n        }\r\n    };\r\n    window.addEventListener('popstate', () => {\r\n        if (window.location.pathname !== _lastPathname) {\r\n            _lastPathname = window.location.pathname;\r\n            _tinderDeadStatePendingConfirm = false;\r\n            _checkTinderDeadStatePassive();\r\n        }\r\n    });\r\n\r\n    _tinderDeadStatePassiveObserver = new MutationObserver((mutations) => {\r\n        const pathChanged = window.location.pathname !== _lastPathname;\r\n        if (pathChanged) {\r\n            _lastPathname = window.location.pathname;\r\n            _tinderDeadStatePendingConfirm = false;\r\n            _checkTinderDeadStatePassive();\r\n            return;\r\n        }\r\n        // Only act on new conversation nodes\r\n        for (const m of mutations) {\r\n            for (const node of m.addedNodes) {\r\n                if (node.nodeType === 1) {\r\n                    if (node.matches('a[href^=\"/app/messages/\"]') ||\r\n                        node.querySelector('a[href^=\"/app/messages/\"]')) {\r\n                        if (_tinderHasConversations()) {\r\n                            _tinderDeadStatePendingConfirm = false;\r\n                            removeTinderDeadStateCard();\r\n                        } else if (_tinderOnMessagesTab() && !document.getElementById('fe-tinder-dead-card')) {\r\n                            // Sidebar re-rendered with < 3 convs — re-inject immediately\r\n                            _tinderDeadStatePendingConfirm = false;\r\n                            injectTinderDeadStateCard();\r\n                        }\r\n                        return;\r\n                    }\r\n                }\r\n            }\r\n        }\r\n    });\r\n    _tinderDeadStatePassiveObserver.observe(document.body, { childList: true, subtree: true });\r\n\r\n    _checkTinderDeadStatePassive();\r\n}\r\n\r\nif (document.readyState === 'loading') {\r\n    document.addEventListener('DOMContentLoaded', _initTinderDeadStatePassiveDetector);\r\n} else {\r\n    _initTinderDeadStatePassiveDetector();\r\n}\r\n\r\nwindow.injectTinderDeadStateCard = injectTinderDeadStateCard;\r\nwindow.removeTinderDeadStateCard = removeTinderDeadStateCard;\r\n\r\n// ── Distance Setting Automation ──\r\n// Navigates to Tinder's Discovery Settings, sets the distance slider, returns.\r\n\r\nasync function setTinderDistanceSetting(maxDistanceKm) {\r\n  const originalPath = window.location.pathname;\r\n  const onProfilePage = originalPath.includes('/app/profile');\r\n\r\n  if (!onProfilePage) {\r\n    window.history.pushState({}, '', '/app/profile');\r\n    window.dispatchEvent(new PopStateEvent('popstate', { state: {} }));\r\n    await new Promise(r => setTimeout(r, 3000));\r\n  }\r\n\r\n  // Poll for the distance slider (Discovery Settings may lazy-render)\r\n  let sliderHandle = null;\r\n  let legacySlider = null;\r\n  \r\n  for (let attempt = 0; attempt < 15; attempt++) {\r\n    sliderHandle = document.querySelector('[data-testid=\"distance-handle\"]') || document.querySelector('[role=\"slider\"][aria-label*=\"distance\" i]');\r\n    if (!sliderHandle) {\r\n      legacySlider = _findTinderDistanceSliderLegacy();\r\n    }\r\n    if (sliderHandle || legacySlider) break;\r\n    await new Promise(r => setTimeout(r, 600));\r\n  }\r\n\r\n  if (!sliderHandle && !legacySlider) {\r\n    console.warn('[FlirtEasy] Distance slider not found on Tinder profile page');\r\n    if (!onProfilePage) {\r\n      window.history.pushState({}, '', originalPath);\r\n      window.dispatchEvent(new PopStateEvent('popstate', { state: {} }));\r\n    }\r\n    return { success: false, error: 'Distance slider not found. Open Tinder → Profile → Discovery Settings and try again.' };\r\n  }\r\n\r\n  // 1. Handling the new custom ARIA slider\r\n  if (sliderHandle) {\r\n    const min = parseInt(sliderHandle.getAttribute('aria-valuemin'), 10) || 1;\r\n    const max = parseInt(sliderHandle.getAttribute('aria-valuemax'), 10) || 161;\r\n    const current = parseInt(sliderHandle.getAttribute('aria-valuenow'), 10);\r\n    \r\n    // Detect unit\r\n    const isMiles = document.body.textContent.toLowerCase().includes('mile') && !document.body.textContent.toLowerCase().includes('kilometer');\r\n    let targetValue = isMiles ? Math.round(maxDistanceKm / 1.60934) : maxDistanceKm;\r\n    targetValue = Math.max(min, Math.min(max, targetValue));\r\n\r\n    console.log(`[FlirtEasy] Custom slider found. Setting value to ${targetValue} (Current: ${current})`);\r\n\r\n    if (current !== targetValue) {\r\n      sliderHandle.focus();\r\n      let val = current;\r\n      let steps = 0;\r\n      const maxSteps = 300;\r\n\r\n      while (val !== targetValue && steps < maxSteps) {\r\n        const isIncrement = val < targetValue;\r\n        const key = isIncrement ? 'ArrowRight' : 'ArrowLeft';\r\n        const keyCode = isIncrement ? 39 : 37;\r\n\r\n        sliderHandle.dispatchEvent(new KeyboardEvent('keydown', { key: key, keyCode: keyCode, code: key, bubbles: true }));\r\n        sliderHandle.dispatchEvent(new KeyboardEvent('keyup', { key: key, keyCode: keyCode, code: key, bubbles: true }));\r\n\r\n        await new Promise(r => setTimeout(r, 15));\r\n        const newVal = parseInt(sliderHandle.getAttribute('aria-valuenow'), 10);\r\n        \r\n        if (newVal === val) {\r\n          // If keyboard navigation fails, attempt click simulation on the rail\r\n          console.warn(\"[FlirtEasy] Keyboard navigation stopped. Attempting rail click fallback...\");\r\n          const rail = document.querySelector('[data-testid=\"slider-rail\"]') || sliderHandle.closest('.wlw120x')?.querySelector('.rvv4owa');\r\n          if (rail) {\r\n            const rect = rail.getBoundingClientRect();\r\n            const pct = (targetValue - min) / (max - min);\r\n            const clientX = rect.left + pct * rect.width;\r\n            const clientY = rect.top + rect.height / 2;\r\n\r\n            rail.dispatchEvent(new MouseEvent('mousedown', { clientX, clientY, screenX: clientX, screenY: clientY, bubbles: true, cancelable: true }));\r\n            rail.dispatchEvent(new MouseEvent('mouseup', { clientX, clientY, screenX: clientX, screenY: clientY, bubbles: true, cancelable: true }));\r\n            rail.dispatchEvent(new MouseEvent('click', { clientX, clientY, screenX: clientX, screenY: clientY, bubbles: true, cancelable: true }));\r\n            await new Promise(r => setTimeout(r, 200));\r\n            val = parseInt(sliderHandle.getAttribute('aria-valuenow'), 10);\r\n          }\r\n          break;\r\n        }\r\n        val = newVal;\r\n        steps++;\r\n      }\r\n      targetValue = val;\r\n      \r\n      // IMPORTANT: Blur the slider or trigger a change event to force Tinder to save\r\n      sliderHandle.blur();\r\n      sliderHandle.dispatchEvent(new Event('change', { bubbles: true }));\r\n      document.body.click(); // Click somewhere else just in case to dismiss focus\r\n    }\r\n\r\n    // Wait for the API request to finish saving\r\n    await new Promise(r => setTimeout(r, 2000));\r\n\r\n    if (!onProfilePage) {\r\n      window.history.pushState({}, '', originalPath);\r\n      window.dispatchEvent(new PopStateEvent('popstate', { state: {} }));\r\n      await new Promise(r => setTimeout(r, 1200));\r\n    }\r\n\r\n    return { success: true, valueSet: targetValue, unit: isMiles ? 'miles' : 'km' };\r\n  }\r\n\r\n  // 2. Legacy Slider Fallback\r\n  if (legacySlider) {\r\n    const container = legacySlider.closest('div, section') || document.body;\r\n    const containerText = container.textContent.toLowerCase();\r\n    const isMiles = containerText.includes('mile') && !containerText.includes('kilometer');\r\n\r\n    let targetValue = isMiles ? Math.round(maxDistanceKm / 1.60934) : maxDistanceKm;\r\n    const sliderMin = parseInt(legacySlider.min) || 2;\r\n    const sliderMax = parseInt(legacySlider.max) || 161;\r\n    targetValue = Math.max(sliderMin, Math.min(sliderMax, targetValue));\r\n\r\n    const nativeSetter = Object.getOwnPropertyDescriptor(window.HTMLInputElement.prototype, 'value').set;\r\n    nativeSetter.call(legacySlider, targetValue);\r\n    legacySlider.dispatchEvent(new Event('input', { bubbles: true }));\r\n    legacySlider.dispatchEvent(new Event('change', { bubbles: true }));\r\n\r\n    await new Promise(r => setTimeout(r, 1000));\r\n\r\n    console.log(`[FlirtEasy] Legacy distance set to ${targetValue} ${isMiles ? 'miles' : 'km'}`);\r\n\r\n    if (!onProfilePage) {\r\n      window.history.pushState({}, '', originalPath);\r\n      window.dispatchEvent(new PopStateEvent('popstate', { state: {} }));\r\n      await new Promise(r => setTimeout(r, 1200));\r\n    }\r\n\r\n    return { success: true, valueSet: targetValue, unit: isMiles ? 'miles' : 'km' };\r\n  }\r\n}\r\n\r\nfunction _findTinderDistanceSliderLegacy() {\r\n  // Strategy 1: find a label/heading containing \"distance\" then grab nearest range input\r\n  const allEls = document.querySelectorAll('h3, h4, label, span, p, div');\r\n  for (const el of allEls) {\r\n    const text = el.textContent.trim().toLowerCase();\r\n    if ((text === 'distance preference' || text === 'maximum distance') && el.children.length === 0) {\r\n      let node = el.parentElement;\r\n      while (node && node !== document.body) {\r\n        const range = node.querySelector('input[type=\"range\"]');\r\n        if (range) return range;\r\n        node = node.parentElement;\r\n      }\r\n    }\r\n  }\r\n\r\n  // Strategy 2: any range input whose container mentions distance\r\n  const ranges = document.querySelectorAll('input[type=\"range\"]');\r\n  for (const range of ranges) {\r\n    let node = range.parentElement;\r\n    let depth = 0;\r\n    while (node && node !== document.body && depth < 6) {\r\n      if (node.textContent.toLowerCase().includes('distance')) return range;\r\n      node = node.parentElement;\r\n      depth++;\r\n    }\r\n  }\r\n\r\n  return null;\r\n}\r\n\r\nwindow.setTinderDistanceSetting = setTinderDistanceSetting;\r\n\r\n/**\r\n * Extracts the current match's distance in kilometers from the DOM.\r\n * Supports both kilometers and miles (converting miles to km).\r\n * Industry-grade robust implementation with container priority and global fallback.\r\n */\r\nfunction getMatchDistanceKm() {\r\n  try {\r\n    const distanceRegex = /~?\\s*(?:less\\s+than\\s+(?:a|1)\\s+|(\\d+)\\s*)(miles?|kilometers?|km)\\s+away/i;\r\n\r\n    // Search specifically within profile details panel first (usually right sidebar or active card)\r\n    const profileContainers = [\r\n      document.querySelector('[class*=\"profileCard\" i]'),\r\n      document.querySelector('.Expand'),\r\n      document.querySelector('[class*=\"Sidebar\" i]'),\r\n      document.querySelector('aside')\r\n    ].filter(Boolean);\r\n\r\n    for (const container of profileContainers) {\r\n      const elements = container.querySelectorAll('div, span, p, li');\r\n      for (const el of elements) {\r\n        if (el.children.length === 0 && el.textContent) {\r\n          const text = el.textContent.trim();\r\n          const match = text.match(distanceRegex);\r\n          if (match) {\r\n            const valStr = match[1];\r\n            const unit = match[2].toLowerCase();\r\n            let distance = valStr ? parseInt(valStr, 10) : 1; // Default to 1 for \"less than a/1\"\r\n            \r\n            if (unit.startsWith('mile')) {\r\n              distance = Math.round(distance * 1.60934);\r\n            }\r\n            console.log(`[FlirtEasy] getMatchDistanceKm (container search) found: ${distance} km (from: \"${text}\")`);\r\n            return distance;\r\n          }\r\n        }\r\n      }\r\n    }\r\n\r\n    // Fallback: search globally if container-specific search didn't find anything\r\n    const elements = document.querySelectorAll('div, span, p, li');\r\n    for (const el of elements) {\r\n      if (el.children.length === 0 && el.textContent) {\r\n        const text = el.textContent.trim();\r\n        // Ignore settings/sliders to avoid false positives\r\n        if (text.toLowerCase().includes('maximum') || text.toLowerCase().includes('preference')) {\r\n          continue;\r\n        }\r\n        const match = text.match(distanceRegex);\r\n        if (match) {\r\n          const valStr = match[1];\r\n          const unit = match[2].toLowerCase();\r\n          let distance = valStr ? parseInt(valStr, 10) : 1;\r\n          \r\n          if (unit.startsWith('mile')) {\r\n            distance = Math.round(distance * 1.60934);\r\n          }\r\n          console.log(`[FlirtEasy] getMatchDistanceKm (global search) found: ${distance} km (from: \"${text}\")`);\r\n          return distance;\r\n        }\r\n      }\r\n    }\r\n  } catch (err) {\r\n    console.error('[FlirtEasy] Exception in getMatchDistanceKm:', err);\r\n  }\r\n  \r\n  console.log('[FlirtEasy] getMatchDistanceKm: No distance found in DOM');\r\n  return null;\r\n}\r\n\r\nwindow.getMatchDistanceKm = getMatchDistanceKm;\r\n\r\n\r\n\n\n\n// ═══════════════════════════════════════════════════════════════\n// [FlirtEasy Bundle] content/profile-parser.js\n// ═══════════════════════════════════════════════════════════════\nfunction getMatchName() {\r\n  if (!window.SELECTORS) return null;\r\n  // Try profile panel h1 first — returns \"Ruxi37\", strip trailing digits\r\n  const h1 = document.querySelector('.profileContent h1');\r\n  if (h1) {\r\n    return h1.textContent.trim().replace(/\\d+$/, '').trim() || null;\r\n  }\r\n  const nameEl = findElement(window.SELECTORS.profile.name);\r\n  if (nameEl && nameEl.textContent.trim().length > 0) {\r\n    return nameEl.textContent.trim().replace(/\\d+$/, '').trim();\r\n  }\r\n  return null;\r\n}\r\n\r\nfunction getMatchAge() {\r\n  // Age is in a separate span: SPAN.Whs(nw).Typs(display-2-strong) = \"37\"\r\n  try {\r\n    const ageSpan = document.querySelector('span.Whs\\\\(nw\\\\).Typs\\\\(display-2-strong\\\\)');\r\n    if (ageSpan) {\r\n      const age = parseInt(ageSpan.textContent.trim());\r\n      if (!isNaN(age) && age >= 18 && age <= 99) return age;\r\n    }\r\n    // Fallback: extract digits from .profileContent h1 (e.g. \"Ruxi37\")\r\n    const h1 = document.querySelector('.profileContent h1');\r\n    if (h1) {\r\n      const match = h1.textContent.trim().match(/(\\d{2,3})$/);\r\n      if (match) {\r\n        const age = parseInt(match[1]);\r\n        if (age >= 18 && age <= 99) return age;\r\n      }\r\n    }\r\n  } catch (_) {}\r\n\r\n  // Fallback to original selector\r\n  if (!window.SELECTORS) return null;\r\n  const ageEl = findElement(window.SELECTORS.profile.age);\r\n  if (ageEl) {\r\n    const age = parseInt(ageEl.textContent.trim());\r\n    if (!isNaN(age) && age >= 18 && age <= 99) return age;\r\n  }\r\n  return null;\r\n}\r\n\r\nfunction getMatchBio() {\r\n  if (!window.SELECTORS) return null;\r\n\r\n  // Scope bio search to the profile panel on the right side of the chat\r\n  // This prevents matching chat message text which uses similar class names\r\n  const profilePanel = document.querySelector(\r\n    'div.P\\\\(24px\\\\).W\\\\(100\\\\%\\\\).Bgc\\\\(\\\\$c-ds-background-primary\\\\), ' +\r\n    '[data-testid=\"matchProfileContent\"], ' +\r\n    '.profileContent, ' +\r\n    'div[class*=\"profileContent\"], ' +\r\n    'aside, ' +\r\n    'div[class*=\"Sidebar\"]'\r\n  );\r\n  const searchRoot = profilePanel || document;\r\n\r\n  const bioSelectors = [\r\n    'div.C\\\\(\\\\$c-ds-text-primary\\\\).Typs\\\\(body-1-regular\\\\)',\r\n    '.BreakWord',\r\n    '[class*=\"bio\"]',\r\n  ];\r\n\r\n  for (const sel of bioSelectors) {\r\n    const el = searchRoot.querySelector(sel);\r\n    if (el) {\r\n      const text = el.textContent.trim();\r\n      if (text && text.length > 5) return text;\r\n    }\r\n  }\r\n\r\n  // Fallback to original selectors on full document\r\n  const bioEl = findElement(window.SELECTORS.profile.bio);\r\n  if (bioEl) {\r\n    const text = bioEl.textContent.trim();\r\n    if (text && text.length > 10) return text;\r\n  }\r\n  return null;\r\n}\r\n\r\nfunction getMatchInterests() {\r\n  const interests = [];\r\n\r\n  // Current Tinder DOM structure:\r\n  // SECTION → DIV (heading \"Interests\") + UL (interest chips)\r\n  try {\r\n    const headings = document.querySelectorAll('div, h2, span');\r\n    for (const el of headings) {\r\n      if (el.children.length <= 3 && el.innerText?.trim() === 'Interests') {\r\n        const ul = el.parentElement?.querySelector('ul') || el.nextElementSibling;\r\n        if (ul) {\r\n          ul.querySelectorAll('li').forEach(li => {\r\n            const text = li.innerText?.trim();\r\n            if (text && text.length > 1 && text.length < 50 && !interests.includes(text)) {\r\n              interests.push(text);\r\n            }\r\n          });\r\n          if (interests.length > 0) return interests;\r\n        }\r\n      }\r\n    }\r\n  } catch (_) {}\r\n\r\n  // Fallback to original selectors\r\n  if (!window.SELECTORS) return interests;\r\n  const interestEls = findElements(window.SELECTORS.profile.interests);\r\n  interestEls.forEach(el => {\r\n    const text = el.textContent.trim();\r\n    // Filter out UI noise — real interests are 2-30 chars, not numbers or long strings\r\n    if (text && text.length > 1 && text.length < 30 &&\r\n        !/^\\d+$/.test(text) &&\r\n        !['Send', 'Long-term', 'Still figuring', 'Short-term', 'Relationship', 'Friendship'].some(s => text.startsWith(s)) &&\r\n        !interests.includes(text)) {\r\n      interests.push(text);\r\n    }\r\n  });\r\n\r\n  return interests;\r\n}\r\n\r\nfunction getMatchQuestionAnswers() {\r\n  const answers = [];\r\n  const sections = document.querySelectorAll(\r\n    'div.P\\\\(24px\\\\).W\\\\(100\\\\%\\\\).Bgc\\\\(\\\\$c-ds-background-primary\\\\)'\r\n  );\r\n  sections.forEach(section => {\r\n    const labelEl = section.querySelector('h2, [class*=\"text-secondary\"]');\r\n    const answerEl = section.querySelector(\r\n      'div.C\\\\(\\\\$c-ds-text-primary\\\\).Typs\\\\(display-2-strong\\\\), ' +\r\n      '[class*=\"display-2-strong\"]'\r\n    );\r\n    if (labelEl && answerEl) {\r\n      const q = labelEl.textContent.trim();\r\n      const a = answerEl.textContent.trim();\r\n      // Skip emoji-only answers (no actual text content)\r\n      const aStripped = a.replace(/[\\u{1F000}-\\u{1FAFF}\\u{2600}-\\u{27BF}\\s]/gu, '');\r\n      if (q && a && q !== a && aStripped.length >= 2) {\r\n        answers.push({ question: q, answer: a });\r\n      }\r\n    }\r\n  });\r\n  return answers;\r\n}\r\n\r\nfunction parseCurrentProfile() {\r\n  return {\r\n    name: getMatchName(),\r\n    age: getMatchAge(),\r\n    bio: getMatchBio(),\r\n    interests: getMatchInterests(),\r\n    questionAnswers: getMatchQuestionAnswers(),\r\n    platform: 'tinder'\r\n  };\r\n}\r\n\r\n// Bridge to page context using CustomEvent\r\nfunction getInterceptedMessagesViaEvent(matchId) {\r\n  let receivedMessages = null;\r\n\r\n  // Set up one-time listener for response\r\n  const responseHandler = function (event) {\r\n    if (event.detail.matchId === matchId) {\r\n      receivedMessages = event.detail.messages;\r\n      document.removeEventListener('flirteasy:messagesResponse', responseHandler);\r\n    }\r\n  };\r\n\r\n  document.addEventListener('flirteasy:messagesResponse', responseHandler);\r\n\r\n  // Request messages from page context\r\n  console.log(`[FlirtEasy] Requesting messages via CustomEvent for ${matchId}`);\r\n  document.dispatchEvent(new CustomEvent('flirteasy:getMessages', {\r\n    detail: { matchId: matchId }\r\n  }));\r\n\r\n  // The event should fire synchronously, so we should have the result now\r\n  if (receivedMessages !== null) {\r\n    console.log(`[FlirtEasy] Received ${receivedMessages.length} messages via CustomEvent`);\r\n    return receivedMessages;\r\n  }\r\n\r\n  // Cleanup listener if something went wrong\r\n  document.removeEventListener('flirteasy:messagesResponse', responseHandler);\r\n  console.log(`[FlirtEasy] No messages received via CustomEvent`);\r\n  return [];\r\n}\r\n\r\nasync function getStoredMatchData(matchId) {\r\n  return new Promise((resolve) => {\r\n    chrome.runtime.sendMessage({ action: 'getMatchData', matchId }, (response) => {\r\n      if (chrome.runtime.lastError) {\r\n        console.warn('[FlirtEasy] Runtime error:', chrome.runtime.lastError.message);\r\n        resolve(null);\r\n      } else {\r\n        resolve(response && response.data);\r\n      }\r\n    });\r\n  });\r\n}\r\n\r\nasync function getConversationHistory(limit = 50) {\r\n  const matchId = extractMatchIdFromURL();\r\n  // console.log(`[FlirtEasy] getConversationHistory: matchId=${matchId}`);\r\n\r\n  if (matchId) {\r\n    const apiMessages = getInterceptedMessagesViaEvent(matchId);\r\n\r\n    try {\r\n      const storedData = await getStoredMatchData(matchId);\r\n      const storageMessages = storedData?.conversationHistory || [];\r\n\r\n      // MERGE: Start with API (source of truth for match replies), append storage-only messages\r\n      if (apiMessages.length > 0) {\r\n        const apiTexts = new Set(apiMessages.map(m => m.text));\r\n        const storageOnlyMessages = storageMessages.filter(m => !apiTexts.has(m.text));\r\n\r\n        if (storageOnlyMessages.length > 0) {\r\n          console.log(`[FlirtEasy] Merging: ${apiMessages.length} API + ${storageOnlyMessages.length} storage-only`);\r\n          return { messages: [...apiMessages, ...storageOnlyMessages].slice(-limit), apiMessages };\r\n        }\r\n\r\n        console.log(`[FlirtEasy] Using ${apiMessages.length} messages from API only`);\r\n        return { messages: apiMessages.slice(-limit), apiMessages };\r\n      }\r\n\r\n      // Fallback to storage if API empty\r\n      if (storageMessages.length > 0) {\r\n        console.log(`[FlirtEasy] Using ${storageMessages.length} messages from storage (API empty)`);\r\n        return { messages: storageMessages.slice(-limit), apiMessages: [] };\r\n      }\r\n    } catch (error) {\r\n      console.warn(`[FlirtEasy] Error loading from storage:`, error);\r\n      if (apiMessages && apiMessages.length > 0) {\r\n        console.log(`[FlirtEasy] Using ${apiMessages.length} messages from API (storage error)`);\r\n        return { messages: apiMessages.slice(-limit), apiMessages };\r\n      }\r\n    }\r\n\r\n    console.log(`[FlirtEasy] No messages in storage or API, falling back to DOM parsing`);\r\n  }\r\n\r\n  // Fallback to DOM parsing\r\n  const messageSelectors = window.SELECTORS?.messaging?.messageBubble || [\".msg-text\", \".message-content\", \".text-ellipsis\", \"[class*='messageText' i]\", \"[data-testid*='message']\", \"div[dir='auto']\"];\r\n\r\n  const messages = [];\r\n  const flatSelectors = Array.isArray(messageSelectors) ? messageSelectors : [messageSelectors];\r\n\r\n  for (const selector of flatSelectors) {\r\n    const messageEls = document.querySelectorAll(selector);\r\n\r\n    if (messageEls.length > 0) {\r\n      console.log(`[FlirtEasy] DOM Parsing: Found ${messageEls.length} elements with selector: ${selector}`);\r\n\r\n      Array.from(messageEls).slice(-limit).forEach((msgEl) => {\r\n        let text = msgEl.textContent.trim();\r\n\r\n        if (!text || text.length < 1) {\r\n          const mediaContainer = msgEl.closest('.msgHelper') || msgEl.parentElement;\r\n          if (mediaContainer) {\r\n            if (mediaContainer.querySelector('img[src*=\"giphy\"], [class*=\"gif\" i]')) {\r\n              text = '[GIF]';\r\n            } else if (mediaContainer.querySelector('audio, [class*=\"audio\" i]')) {\r\n              text = '[Voice Note]';\r\n            } else if (mediaContainer.querySelector('video')) {\r\n              text = '[Video]';\r\n            } else if (mediaContainer.querySelector('img:not([class*=\"avatar\"]):not([class*=\"photo\"]):not([class*=\"profile\"])')) {\r\n              text = '[Media/Image]';\r\n            }\r\n          }\r\n          if (!text || text.length < 1) return;\r\n        }\r\n\r\n        // Clean up common boilerplate from DOM text\r\n        text = text.replace(/^(You|null):?\\s*/i, '').trim();\r\n\r\n        // Robust Sender Detection for Tinder's Atomic CSS:\r\n        // 1. Ta(e) = Atomic CSS class on container (Right side, User)\r\n        // 2. Jc(fe) = Container justify-content flex-end (Right side, User)\r\n        // 3. Pink/Blue bubble color vs Grey\r\n        // NOTE: element-level textAlign is intentionally excluded — RTL languages\r\n        // (Hebrew, Arabic) render with textAlign 'right'/'end' in match bubbles,\r\n        // which would falsely classify them as user-sent messages.\r\n        const parent = msgEl.closest('.msgHelper') || msgEl.parentElement;\r\n        const container = parent?.parentElement;\r\n        const containerStyle = container ? window.getComputedStyle(container) : null;\r\n\r\n        const isRightAligned = container?.classList.contains('Ta(e)') ||\r\n          containerStyle?.justifyContent === 'flex-end' ||\r\n          containerStyle?.justifyContent === 'end';\r\n\r\n        const isUserColor = msgEl.classList.contains('C($c-ds-text-chat-bubble-send)') ||\r\n          msgEl.closest('[class*=\"Bgc($c-ds-background-brand-gradient)\"]') !== null;\r\n\r\n        const isFromUser = isRightAligned || isUserColor ||\r\n          msgEl.classList.contains('sent') ||\r\n          msgEl.classList.contains('user');\r\n\r\n        // Time Extraction (Tinder-specific DOM variants)\r\n        const timeEl = parent?.querySelector('[class*=\"timestamp\" i], [class*=\"time\" i], .Hidden');\r\n        const timeText = timeEl ? timeEl.textContent.trim() : \"\";\r\n\r\n        messages.push({\r\n          sender: isFromUser ? 'user' : 'match',\r\n          text: text,\r\n          timeText: timeText,\r\n          timestamp: Date.now() - ((messageEls.length - Array.from(messageEls).indexOf(msgEl)) * 5000) // Fallback\r\n        });\r\n      });\r\n      break;\r\n    }\r\n  }\r\n\r\n  // Secondary scan: catch media-only messages (GIF/Image/Audio/Video) that have\r\n  // no text child and are therefore invisible to all text selectors above.\r\n  // We only check the last .msgHelper in DOM order — if it's media-only and not\r\n  // already captured as the final message, it means the match replied with media\r\n  // and the AI would incorrectly think the user was last to send.\r\n  const allMsgHelpers = Array.from(document.querySelectorAll('.msgHelper'));\r\n  if (allMsgHelpers.length > 0) {\r\n    const lastHelper = allMsgHelpers[allMsgHelpers.length - 1];\r\n    const helperText = lastHelper.textContent.trim().replace(/^(You|null):?\\s*/i, '').trim();\r\n    const lastCaptured = messages[messages.length - 1];\r\n\r\n    if (!helperText || helperText.length < 1) {\r\n      let mediaText = null;\r\n      if (lastHelper.querySelector('img[src*=\"giphy\"], [class*=\"gif\" i]')) {\r\n        mediaText = '[GIF]';\r\n      } else if (lastHelper.querySelector('audio, [class*=\"audio\" i]')) {\r\n        mediaText = '[Voice Note]';\r\n      } else if (lastHelper.querySelector('video')) {\r\n        mediaText = '[Video]';\r\n      } else if (lastHelper.querySelector('img:not([class*=\"avatar\"]):not([class*=\"photo\"]):not([class*=\"profile\"])')) {\r\n        mediaText = '[Media/Image]';\r\n      }\r\n\r\n      if (mediaText && (!lastCaptured || lastCaptured.text !== mediaText)) {\r\n        const helperStyle = window.getComputedStyle(lastHelper.parentElement || lastHelper);\r\n        const isRight = lastHelper.parentElement?.classList.contains('Ta(e)') ||\r\n          helperStyle.justifyContent === 'flex-end' || helperStyle.justifyContent === 'end';\r\n        \r\n        // Final fallback timestamp\r\n        const lastTimeEl = lastHelper.querySelector('[class*=\"timestamp\" i], .Hidden');\r\n        const lastTimeText = lastTimeEl ? lastTimeEl.textContent.trim() : \"\";\r\n\r\n        messages.push({\r\n          sender: isRight ? 'user' : 'match',\r\n          text: mediaText,\r\n          timeText: lastTimeText,\r\n          timestamp: Date.now()\r\n        });\r\n      }\r\n    }\r\n  }\r\n\r\n  return { messages, apiMessages: [] };\r\n}\r\n\r\nfunction extractMatchIdFromURL() {\r\n  const match = window.location.pathname.match(/\\/app\\/messages\\/([^\\/]+)/);\r\n  return match ? match[1] : null;\r\n}\r\n\r\nasync function wasUserLastToReply() {\r\n  const result = await getConversationHistory(5);\r\n  const messages = result.messages || result;\r\n\r\n  if (messages.length === 0) {\r\n    return false;\r\n  }\r\n\r\n  const lastMessage = messages[messages.length - 1];\r\n  return lastMessage.sender === 'user';\r\n}\r\n\r\nconst handledTinderLeadsDuringScan = new Set();\r\n\r\nfunction getMatchesFromList() {\r\n  const matchItems = document.querySelectorAll('a[href^=\"/app/messages/\"]');\r\n  const matches = [];\r\n\r\n  matchItems.forEach((item, sidebarIndex) => {\r\n    // 1. IMPROVED NAME DETECTION (Robust Heuristic)\r\n    const nameEl = item.querySelector('h3, [class*=\"bold\" i], [class*=\"Primary\" i], .Ell');\r\n    let name = nameEl ? nameEl.textContent.trim() : \"\";\r\n\r\n    // Clean up or use innerText fallback if name is a badge or empty\r\n    if (!name || name.toLowerCase().includes('new message') || name.length > 40) {\r\n      name = item.innerText.split('\\n')[0].trim();\r\n    }\r\n\r\n    // Final safety check for names\r\n    if (!name) name = `Match ${sidebarIndex + 1}`;\r\n\r\n    // Extract snippet (usually the div right after the name area)\r\n    const snippetEl = item.querySelector('div[class*=\"snippet\" i], div.text-ellipsis + div, .message-content');\r\n    let snippet = snippetEl ? snippetEl.innerText.trim() : \"\";\r\n    if (!snippet) {\r\n      const lines = item.innerText.split('\\n').map(l => l.trim()).filter(Boolean);\r\n      if (lines.length >= 2) snippet = lines[1];\r\n    }\r\n\r\n    const unreadIndicator = item.querySelector('.unread, .badge, [data-testid=\"unread\"], [aria-label=\"New Message\"], .Bgc\\\\(\\\\$c-ds-background-brand\\\\)');\r\n    const hasUnread = !!unreadIndicator;\r\n\r\n    // 3. TINDER \"YOUR MOVE\" DETECTION (Cyan/Blue dot)\r\n    const isYourMove = item.innerText.toLowerCase().includes('your move') ||\r\n      !!item.querySelector('[class*=\"background-accent-cyan\" i]');\r\n\r\n    // 4. SHE REPLIED LAST DETECTION via span.Hidden accessibility text\r\n    // Format: \"Your last message was: ...\"      → bot sent last → sheRepliedLast = false\r\n    //         \"[Name]'s last message was: ...\"  → she sent last → sheRepliedLast = true\r\n    // This is the only reliable signal — sidebar ← arrow is an SVG (not in innerText).\r\n    const hiddenText = item.querySelector('span.Hidden')?.textContent || \"\";\r\n    const hiddenLower = hiddenText.toLowerCase();\r\n    const botSentLast = hiddenLower.includes('your last message was:');\r\n    const sheRepliedLast = hiddenText.length > 0 && !botSentLast && hiddenLower.includes(\"last message was:\");\r\n\r\n    const matchId = item.getAttribute('href').split('/').pop();\r\n\r\n    // Extract photoUrl from sidebar card\r\n    let photoUrl = null;\r\n    const imgEl = item.querySelector('img[src], [class*=\"avatar\" i] img, [class*=\"image\" i] img');\r\n    if (imgEl && imgEl.src) {\r\n      photoUrl = imgEl.src;\r\n    } else {\r\n      const thumbEl = item.querySelector('[class*=\"avatar\" i], [class*=\"image\" i], div[style*=\"background-image\"]');\r\n      if (thumbEl) {\r\n        const bg = window.getComputedStyle(thumbEl).backgroundImage;\r\n        const match = bg.match(/url\\([\"']?([^\"')]+)[\"']?\\)/);\r\n        if (match) photoUrl = match[1];\r\n      }\r\n    }\r\n\r\n    const matchObj = {\r\n      matchId,\r\n      name,\r\n      hasUnread,\r\n      isYourMove,\r\n      sheRepliedLast,\r\n      sidebarIndex,\r\n      snippet,\r\n      photoUrl,\r\n      element: item,\r\n      hiddenText: item.querySelector('span.Hidden')?.textContent || \"\"\r\n    };\r\n\r\n    // Instant Lead Detection on Tinder Sidebar (Attribution Shield)\r\n    // Scan EVERY snippet even if idle to apply shielding/passive detection\r\n    if (snippet) {\r\n      checkForTinderPassiveLead(matchObj);\r\n    }\r\n\r\n    matches.push(matchObj);\r\n  });\r\n\r\n  return matches;\r\n}\r\n\r\n/**\r\n * Tinder Passive Lead Scanner\r\n */\r\nasync function checkForTinderPassiveLead(match) {\r\n  if (!match.snippet || !match.matchId) return;\r\n\r\n  const text = match.snippet.toLowerCase();\r\n  let leadType = null;\r\n\r\n  // PHONE NUMBERS ONLY — Social handles are left to AI stop condition\r\n  // Regex on social keywords causes false positives: \"I use Insta for reference\" ≠ lead\r\n  // Digit-count validation prevents conversational numbers (\"15 07 1994\", large sums) from triggering\r\n  const _ph = text.match(/(\\+?[\\d][\\d\\s\\-\\(\\).]{5,}[\\d])/);\r\n  if (_ph) {\r\n    const _digits = _ph[0].replace(/\\D/g, '');\r\n    const _ctx = text.substring(Math.max(0, _ph.index - 25), Math.min(text.length, _ph.index + _ph[0].length + 25));\r\n    const _isDate = /\\b\\d{1,2}[\\s\\/\\-]\\d{1,2}[\\s\\/\\-]\\d{2,4}\\b/.test(_ph[0]);\r\n    const _isCurrency = /\\b(?:dollars?|euros?|pounds?|yen|millions?|billions?|thousands?)\\b/.test(_ctx);\r\n    if (_digits.length >= 9 && !_isDate && !_isCurrency) leadType = 'phone';\r\n  }\r\n  if (!leadType && text.match(/(?:whatsapp|wa\\b)/i)) {\r\n    const _wa = text.match(/(\\+?[\\d][\\d\\s\\-]{5,}[\\d])/);\r\n    if (_wa && _wa[0].replace(/\\D/g, '').length >= 7) leadType = 'phone';\r\n  }\r\n\r\n  if (leadType) {\r\n    const matchId = match.matchId;\r\n\r\n    // 1. Session-based deduplication\r\n    if (handledTinderLeadsDuringScan.has(matchId)) return;\r\n    handledTinderLeadsDuringScan.add(matchId);\r\n\r\n    console.log(`[FlirtEasy] ✨ TINDER PASSIVE LEAD: ${match.name} (\"${match.snippet.substring(0, 30)}...\")`);\r\n\r\n    // 2. Permanent Photo Check\r\n    let finalPhoto = match.photoUrl;\r\n    if (match.photoUrl && match.photoUrl.startsWith('http')) {\r\n      try {\r\n        finalPhoto = await tinderImageToBase64(match.photoUrl);\r\n      } catch (e) {\r\n        console.warn('[FlirtEasy] Photo conversion failed', e);\r\n      }\r\n    }\r\n\r\n    // Check if we've already handled this lead\r\n    chrome.runtime.sendMessage({ action: 'isChatStopped', matchId }, async (response) => {\r\n      if (response?.isStopped) return;\r\n\r\n      // --- ATTRIBUTION SHIELD ---\r\n      // 1. Check hidden accessibility text (most accurate)\r\n      // 2. Fallback to unread/yourMove logic\r\n      const hiddenText = match.hiddenText?.toLowerCase() || \"\";\r\n      const isUserLast = hiddenText.includes('your last message') ||\r\n        (!match.hasUnread && !match.isYourMove);\r\n\r\n      if (isUserLast) {\r\n        console.log(`[FlirtEasy] 🛡️ Tinder Attribution Shield: User sent contact info to ${match.name}. Shielding...`);\r\n        chrome.runtime.sendMessage({\r\n          action: 'markChatStopped',\r\n          matchId: match.matchId,\r\n          reason: '🚫 SHIELD (User Lead)',\r\n          matchName: match.name,\r\n          platform: 'tinder'\r\n        });\r\n        return;\r\n      }\r\n\r\n      console.log(`[FlirtEasy] ✨ TINDER PASSIVE LEAD: ${match.name} (\"${match.snippet.substring(0, 30)}...\")`);\r\n\r\n      // 1. Alert user\r\n      if (typeof window.UIAlerts !== 'undefined' && typeof window.UIAlerts.showHandoff === 'function') {\r\n        window.UIAlerts.showHandoff(match.name, leadType);\r\n      }\r\n\r\n      // 2. Mark as stopped + add to Calendar\r\n      chrome.runtime.sendMessage({\r\n        action: 'markChatStopped',\r\n        matchId: matchId,\r\n        reason: leadType,\r\n        matchName: match.name,\r\n        platform: 'tinder',\r\n        snippet: match.snippet,\r\n        photoUrl: finalPhoto\r\n      });\r\n    });\r\n  }\r\n}\r\n\r\n/**\r\n * Converts Tinder photos to permanent Base64\r\n * Uses fetch + FileReader for maximum robustness against CORS\r\n */\r\nasync function tinderImageToBase64(url) {\r\n  try {\r\n    const response = await fetch(url);\r\n    const blob = await response.blob();\r\n    return new Promise((resolve, reject) => {\r\n      const reader = new FileReader();\r\n      reader.onloadend = () => resolve(reader.result);\r\n      reader.onerror = reject;\r\n      reader.readAsDataURL(blob);\r\n    });\r\n  } catch (e) {\r\n    console.error('[FlirtEasy] Base64 conversion failed:', e);\r\n    return null;\r\n  }\r\n}\r\n\r\nfunction getNewMatchesFromGrid() {\r\n  const matchCards = document.querySelectorAll('a.matchListItem');\r\n  const matches = [];\r\n\r\n  matchCards.forEach((card, index) => {\r\n    const nameEl = card.querySelector('[aria-label]');\r\n    const name = nameEl ? nameEl.getAttribute('aria-label') : `Match ${index + 1}`;\r\n\r\n    const href = card.getAttribute('href');\r\n    if (name === '99+ likes' || href === '/app/likes-you' || href === '/app/my-likes' || (href && !href.includes('/app/messages/'))) {\r\n      return;\r\n    }\r\n\r\n    const matchId = href && href.includes('/app/messages/') ? href.split('/').pop() : null;\r\n\r\n    matches.push({\r\n      matchId,\r\n      name,\r\n      element: card,\r\n      isNew: true\r\n    });\r\n  });\r\n\r\n  return matches;\r\n}\r\n\r\nfunction navigateToMatch(matchId) {\r\n  const selectors = [\r\n    `a[href=\"/app/messages/${matchId}\"]`,\r\n    `a[href^=\"/app/messages/${matchId}\"]`,\r\n    `a[href*=\"${matchId}\"]`\r\n  ];\r\n\r\n  for (const selector of selectors) {\r\n    const matchLink = document.querySelector(selector);\r\n    if (matchLink) {\r\n      console.log(`[FlirtEasy] Found match link with selector: ${selector}`);\r\n      matchLink.click();\r\n      return true;\r\n    }\r\n  }\r\n\r\n  console.log(`[FlirtEasy] Link not found for matchId: ${matchId}`);\r\n  return false;\r\n}\r\n\r\nfunction isOnMatchesPage() {\r\n  return window.location.pathname.includes('/app/messages') ||\r\n    window.location.pathname.includes('/app/recs');\r\n}\r\n\r\nfunction isOnChatPage() {\r\n  return window.location.pathname.includes('/app/messages/') &&\r\n    window.location.pathname.split('/').length > 4;\r\n}\r\n\r\nasync function getUserOwnProfile() {\r\n  const currentUrl = window.location.href;\r\n  const isOnProfilePage = window.location.pathname.includes('/app/profile');\r\n\r\n  console.log(`[FlirtEasy] getUserOwnProfile called, isOnProfilePage: ${isOnProfilePage}, url: ${currentUrl}`);\r\n\r\n  if (!isOnProfilePage) {\r\n    console.warn('[FlirtEasy] Not on profile page, cannot read profile');\r\n    return null;\r\n  }\r\n\r\n  // Wait up to 4 seconds for React to hydrate and render profile components\r\n  for (let attempt = 0; attempt < 12; attempt++) {\r\n    const hasContent = document.querySelector('textarea') || \r\n                       document.querySelector('h2') || \r\n                       (document.body.innerText && (document.body.innerText.includes('ABOUT') || document.body.innerText.includes('PASSIONS')));\r\n    if (hasContent) break;\r\n    await new Promise(r => setTimeout(r, 300));\r\n  }\r\n\r\n  const profile = {\r\n    name: null,\r\n    age: null,\r\n    bio: null,\r\n    interests: [],\r\n    height: null,\r\n    job: null,\r\n    school: null,\r\n    city: null,\r\n    gender: null,\r\n    sexualOrientation: null,\r\n    location: null,\r\n    distancePreference: null,\r\n    interestedIn: null,\r\n    lookingFor: null,\r\n    relationshipType: null,\r\n    languages: [],\r\n    preferredLanguages: [],\r\n    zodiac: null,\r\n    education: null,\r\n    familyPlans: null,\r\n    communicationStyle: null,\r\n    loveStyle: null,\r\n    pets: null,\r\n    userPets: null,\r\n    drinking: null,\r\n    smoking: null,\r\n    workout: null,\r\n    sleepingHabits: null,\r\n    dietaryPreference: null,\r\n    socialMedia: null\r\n  };\r\n\r\n  // ── LAYER 1: Parse modern Tinder /app/profile/edit page directly ──\r\n  const isEditPage = window.location.pathname.includes('/app/profile/edit');\r\n  if (isEditPage || document.querySelector('textarea') || (document.body.innerText && document.body.innerText.includes('ABOUT'))) {\r\n    // 1. Bio from textarea\r\n    const textareas = Array.from(document.querySelectorAll('textarea'));\r\n    if (textareas.length > 0 && textareas[0].value) {\r\n      profile.bio = textareas[0].value.trim();\r\n      console.log(`[FlirtEasy] Found bio from textarea: ${profile.bio.substring(0, 40)}...`);\r\n    }\r\n\r\n    const pageText = document.body.innerText || '';\r\n\r\n    // 2. Name from \"ABOUT <NAME>\"\r\n    const nameMatch = pageText.match(/ABOUT\\s+([A-Za-z0-9_ -]+)\\s*\\n/i);\r\n    if (nameMatch) {\r\n      profile.name = nameMatch[1].trim();\r\n      console.log(`[FlirtEasy] Found name from ABOUT header: ${profile.name}`);\r\n    }\r\n\r\n    // 3. Name & Age from page text (e.g. \"Sanket,\\n27\")\r\n    const nameAgeMatch = pageText.match(/([A-Z][a-z]+),\\s*\\n?\\s*(\\d{2})/);\r\n    if (nameAgeMatch) {\r\n      if (!profile.name) profile.name = nameAgeMatch[1].trim();\r\n      if (!profile.age) profile.age = parseInt(nameAgeMatch[2]);\r\n      console.log(`[FlirtEasy] Found name/age from text: ${profile.name}, ${profile.age}`);\r\n    }\r\n\r\n    // 4. Passions\r\n    const passionsMatch = pageText.match(/PASSIONS\\s*\\n([^\\n]+)/i);\r\n    if (passionsMatch && !passionsMatch[1].includes('Update your passions')) {\r\n      profile.interests = passionsMatch[1].split(/[,]+/).map(s => s.trim()).filter(Boolean);\r\n      console.log(`[FlirtEasy] Found passions: ${profile.interests.join(', ')}`);\r\n    }\r\n\r\n    // 5. Height\r\n    const heightMatch = pageText.match(/HEIGHT\\s*\\n([^\\n]+)/i);\r\n    if (heightMatch && !heightMatch[1].includes('RELATIONSHIP')) {\r\n      profile.height = heightMatch[1].trim();\r\n    }\r\n\r\n    // 6. Relationship Goals & Type\r\n    const relGoalsMatch = pageText.match(/RELATIONSHIP GOALS\\s*\\n(?:Looking for\\s*\\n)?([^\\n]+)/i);\r\n    if (relGoalsMatch) {\r\n      profile.lookingFor = relGoalsMatch[1].trim();\r\n    }\r\n    const relTypeMatch = pageText.match(/RELATIONSHIP TYPE\\s*\\n(?:Open to\\.\\.\\.\\s*\\n)?([^\\n]+)/i);\r\n    if (relTypeMatch) {\r\n      profile.relationshipType = relTypeMatch[1].trim();\r\n    }\r\n\r\n    // 7. Languages\r\n    const langMatch = pageText.match(/LANGUAGES I KNOW\\s*\\n(?:Add languages\\s*\\n)?([^\\n]+)/i);\r\n    if (langMatch && !langMatch[1].includes('BASICS')) {\r\n      profile.languages = langMatch[1].split(/[,]+/).map(s => s.trim()).filter(Boolean);\r\n    }\r\n\r\n    // 8. Basics\r\n    const basicsMatch = pageText.match(/BASICS\\s*\\n([\\s\\S]*?)LIFESTYLE/i);\r\n    if (basicsMatch) {\r\n      const bText = basicsMatch[1];\r\n      const zodiacM = bText.match(/Zodiac\\s*\\n([^\\n]+)/i);\r\n      if (zodiacM) profile.zodiac = zodiacM[1].trim();\r\n      const eduM = bText.match(/Education\\s*\\n([^\\n]+)/i);\r\n      if (eduM) profile.education = eduM[1].trim();\r\n      const familyM = bText.match(/Family Plans\\s*\\n([^\\n]+)/i);\r\n      if (familyM) profile.familyPlans = familyM[1].trim();\r\n      const commM = bText.match(/Communication Style\\s*\\n([^\\n]+)/i);\r\n      if (commM) profile.communicationStyle = commM[1].trim();\r\n      const loveM = bText.match(/Love Style\\s*\\n([^\\n]+)/i);\r\n      if (loveM) profile.loveStyle = loveM[1].trim();\r\n    }\r\n\r\n    // 9. Lifestyle\r\n    const lifeMatch = pageText.match(/LIFESTYLE\\s*\\n([\\s\\S]*?)JOB TITLE/i);\r\n    if (lifeMatch) {\r\n      const lText = lifeMatch[1];\r\n      const petsM = lText.match(/Pets\\s*\\n([^\\n]+)/i);\r\n      if (petsM) { profile.pets = petsM[1].trim(); profile.userPets = profile.pets; }\r\n      const drinkM = lText.match(/Drinking\\s*\\n([^\\n]+)/i);\r\n      if (drinkM) profile.drinking = drinkM[1].trim();\r\n      const smokeM = lText.match(/Smoking\\s*\\n([^\\n]+)/i);\r\n      if (smokeM) profile.smoking = smokeM[1].trim();\r\n      const workoutM = lText.match(/Workout\\s*\\n([^\\n]+)/i);\r\n      if (workoutM) profile.workout = workoutM[1].trim();\r\n      const socialM = lText.match(/Social Media\\s*\\n([^\\n]+)/i);\r\n      if (socialM) profile.socialMedia = socialM[1].trim();\r\n    }\r\n\r\n    // 10. Gender\r\n    const genderMatch = pageText.match(/GENDER\\s*\\n([^\\n]+)/i);\r\n    if (genderMatch && !genderMatch[1].includes('Update your gender')) {\r\n      profile.gender = genderMatch[1].trim();\r\n    }\r\n\r\n    // 11. Living In / City\r\n    const cityMatch = pageText.match(/LIVING IN\\s*\\n([^\\n]+)/i);\r\n    if (cityMatch && !cityMatch[1].includes('Add City')) {\r\n      profile.city = cityMatch[1].trim();\r\n    }\r\n\r\n    // If we got valid bio/name from the edit page, return immediately\r\n    if (profile.bio || profile.name || profile.interests.length > 0) {\r\n      console.log('[FlirtEasy] Successfully extracted profile from edit page:', profile);\r\n      return profile;\r\n    }\r\n  }\r\n\r\n  // ── LAYER 2: Auto-click Edit button and switch to Preview tab (legacy fallback) ──\r\n  let editButton = null;\r\n  for (let i = 0; i < 6; i++) {\r\n    editButton = document.querySelector('button[aria-label=\"Edit profile\"]') ||\r\n      Array.from(document.querySelectorAll('button, a')).find(btn => btn.textContent.trim() === 'Edit' || btn.textContent.trim() === 'Edit Profile');\r\n    if (editButton) break;\r\n    await new Promise(resolve => setTimeout(resolve, 300));\r\n  }\r\n\r\n  if (editButton) {\r\n    console.log('[FlirtEasy] Clicking Edit button to open profile editor');\r\n    editButton.click();\r\n    await new Promise(resolve => setTimeout(resolve, 1000));\r\n\r\n    // Click Preview tab using role=\"tab\" selector\r\n    let previewTab = null;\r\n    for (let i = 0; i < 6; i++) {\r\n      previewTab = document.querySelector('button[role=\"tab\"][aria-selected=\"false\"]') ||\r\n        Array.from(document.querySelectorAll('button[role=\"tab\"]')).find(btn =>\r\n          btn.textContent.trim() === 'Preview'\r\n        );\r\n      if (previewTab) break;\r\n      await new Promise(resolve => setTimeout(resolve, 300));\r\n    }\r\n\r\n    if (previewTab) {\r\n      console.log('[FlirtEasy] Clicking Preview tab');\r\n      previewTab.click();\r\n      await new Promise(resolve => setTimeout(resolve, 1500));\r\n\r\n      const viewAllButtons = document.querySelectorAll('button');\r\n      const expandButtons = Array.from(viewAllButtons).filter(btn => {\r\n        const text = btn.textContent.toLowerCase();\r\n        return text.includes('view all') || text.includes('view more');\r\n      });\r\n\r\n      for (const btn of expandButtons) {\r\n        btn.click();\r\n        await new Promise(resolve => setTimeout(resolve, 200));\r\n      }\r\n    }\r\n  }\r\n\r\n  // Extract name and age from profile preview\r\n  const nameAgeSection = document.querySelector('h1');\r\n  if (nameAgeSection) {\r\n    const text = nameAgeSection.textContent.trim();\r\n    const match = text.match(/^(.+?)(\\d+)$/);\r\n    if (match) {\r\n      profile.name = match[1].trim();\r\n      profile.age = parseInt(match[2]);\r\n      console.log(`[FlirtEasy] Found name: ${profile.name}, age: ${profile.age}`);\r\n    }\r\n  }\r\n\r\n  // Extract bio from Preview panel - look for \"About me\" section\r\n  const aboutMeSection = Array.from(document.querySelectorAll('div')).find(el =>\r\n    el.textContent.startsWith('About me') && el.children.length > 0\r\n  );\r\n  if (aboutMeSection) {\r\n    const bioText = aboutMeSection.textContent.replace('About me', '').trim();\r\n    if (bioText && bioText.length > 10) {\r\n      profile.bio = bioText;\r\n      console.log(`[FlirtEasy] Found bio: ${profile.bio.substring(0, 50)}...`);\r\n    }\r\n  } else {\r\n    console.log('[FlirtEasy] About me section not found');\r\n  }\r\n\r\n  // Extract interests - look for section with h2 containing \"Interests\"\r\n  const interestsSection = Array.from(document.querySelectorAll('section')).find(section => {\r\n    const h2 = section.querySelector('h2');\r\n    return h2 && h2.textContent.trim() === 'Interests';\r\n  });\r\n\r\n  if (interestsSection) {\r\n    // Get all span elements with the passion text class\r\n    const interestSpans = interestsSection.querySelectorAll('span[class*=\"passions\"]');\r\n    profile.interests = Array.from(interestSpans).map(span => span.textContent.trim()).filter(t => t.length > 0);\r\n    console.log(`[FlirtEasy] Found interests: ${profile.interests.join(', ')}`);\r\n  } else {\r\n    console.log('[FlirtEasy] Interests section not found');\r\n  }\r\n\r\n  // Extract essentials (height, job, school) from Preview panel OR profile card\r\n  let essentialsSection = Array.from(document.querySelectorAll('div')).find(el =>\r\n    el.textContent.startsWith('Essentials')\r\n  );\r\n\r\n  if (!essentialsSection) {\r\n    // Try from profile card\r\n    const profileCard = document.querySelector('[class*=\"profileCard\"]');\r\n    if (profileCard) {\r\n      essentialsSection = Array.from(profileCard.querySelectorAll('div')).find(el =>\r\n        el.textContent.includes('Essentials')\r\n      );\r\n    }\r\n  }\r\n\r\n  if (essentialsSection) {\r\n    // Get all child divs that contain actual data\r\n    const childDivs = Array.from(essentialsSection.querySelectorAll('div'))\r\n      .map(div => div.textContent.trim())\r\n      .filter(text => text.length > 2 && text.length < 150 && text !== 'Essentials');\r\n\r\n    console.log(`[FlirtEasy] Essentials child divs:`, childDivs);\r\n\r\n    // Extract height\r\n    const heightDiv = childDivs.find(text => text.match(/^\\d+\\s*cm$/));\r\n    if (heightDiv) {\r\n      profile.height = heightDiv;\r\n      console.log(`[FlirtEasy] Found height: ${profile.height}`);\r\n    }\r\n\r\n    // Extract job - usually comes after height, doesn't contain keywords\r\n    const jobDiv = childDivs.find(text =>\r\n      !text.includes('cm') &&\r\n      !text.includes('English') &&\r\n      !text.includes('Hebrew') &&\r\n      !text.includes('Russian') &&\r\n      !text.includes('Persian') &&\r\n      !text.includes('School') &&\r\n      !text.includes('University') &&\r\n      !text.includes('College') &&\r\n      text.length > 5\r\n    );\r\n    if (jobDiv) {\r\n      profile.job = jobDiv;\r\n      console.log(`[FlirtEasy] Found job: ${profile.job}`);\r\n    }\r\n\r\n    // Extract school - contains School/University/College keywords\r\n    const schoolDiv = childDivs.find(text =>\r\n      (text.includes('School') || text.includes('University') || text.includes('College')) &&\r\n      text !== profile.job\r\n    );\r\n    if (schoolDiv) {\r\n      profile.school = schoolDiv;\r\n      console.log(`[FlirtEasy] Found school: ${profile.school}`);\r\n    }\r\n\r\n    // Extract languages from Essentials (user's own languages)\r\n    const languagesDiv = childDivs.find(text =>\r\n      (text.includes('English') || text.includes('Hebrew') || text.includes('Russian') ||\r\n        text.includes('Spanish') || text.includes('French') || text.includes('German')) &&\r\n      !text.includes('cm') && text !== profile.job && text !== profile.school\r\n    );\r\n    if (languagesDiv) {\r\n      profile.languages = languagesDiv.split(',').map(l => l.trim()).filter(l => l.length > 0);\r\n      console.log(`[FlirtEasy] Found languages: ${profile.languages.join(', ')}`);\r\n    }\r\n  } else {\r\n    console.log('[FlirtEasy] Essentials section not found');\r\n  }\r\n\r\n  // Extract Basics and Lifestyle sections from Preview panel\r\n  const sections = document.querySelectorAll('section');\r\n  console.log(`[FlirtEasy] Found ${sections.length} sections in Preview panel`);\r\n  sections.forEach(section => {\r\n    const h2 = section.querySelector('h2');\r\n    if (!h2) return;\r\n\r\n    const sectionName = h2.textContent.trim();\r\n    console.log(`[FlirtEasy] Processing section: ${sectionName}`);\r\n    if (sectionName === 'Basics' || sectionName === 'Lifestyle') {\r\n      const items = section.querySelectorAll('li');\r\n      console.log(`[FlirtEasy] Found ${items.length} items in ${sectionName} section`);\r\n      items.forEach(item => {\r\n        const h3 = item.querySelector('h3');\r\n        const valueDiv = item.querySelector('div.Typs\\\\(body-1-regular\\\\).C\\\\(\\\\$c-ds-text-primary\\\\)');\r\n\r\n        console.log(`[FlirtEasy] Item h3: ${h3 ? h3.textContent.trim() : 'null'}, valueDiv: ${valueDiv ? valueDiv.textContent.trim() : 'null'}`);\r\n\r\n        if (h3 && valueDiv) {\r\n          const fieldName = h3.textContent.trim();\r\n          const fieldValue = valueDiv.textContent.trim();\r\n\r\n          // Map to profile fields\r\n          if (fieldName === 'Education' && !profile.education) {\r\n            profile.education = fieldValue;\r\n            console.log(`[FlirtEasy] Found from ${sectionName}: ${fieldName} = ${fieldValue}`);\r\n          } else if (fieldName === 'Communication Style' && !profile.communicationStyle) {\r\n            profile.communicationStyle = fieldValue;\r\n            console.log(`[FlirtEasy] Found from ${sectionName}: ${fieldName} = ${fieldValue}`);\r\n          } else if (fieldName === 'Zodiac' && !profile.zodiac) {\r\n            profile.zodiac = fieldValue;\r\n            console.log(`[FlirtEasy] Found from ${sectionName}: ${fieldName} = ${fieldValue}`);\r\n          } else if (fieldName === 'Workout' && !profile.workout) {\r\n            profile.workout = fieldValue;\r\n            console.log(`[FlirtEasy] Found from ${sectionName}: ${fieldName} = ${fieldValue}`);\r\n          } else if (fieldName === 'Drinking' && !profile.drinking) {\r\n            profile.drinking = fieldValue;\r\n            console.log(`[FlirtEasy] Found from ${sectionName}: ${fieldName} = ${fieldValue}`);\r\n          } else if (fieldName === 'Smoking' && !profile.smoking) {\r\n            profile.smoking = fieldValue;\r\n            console.log(`[FlirtEasy] Found from ${sectionName}: ${fieldName} = ${fieldValue}`);\r\n          } else if (fieldName === 'Sleeping Habits' && !profile.sleepingHabits) {\r\n            profile.sleepingHabits = fieldValue;\r\n            console.log(`[FlirtEasy] Found from ${sectionName}: ${fieldName} = ${fieldValue}`);\r\n          } else if (fieldName === 'Pets' && !profile.userPets) {\r\n            profile.userPets = fieldValue;\r\n            console.log(`[FlirtEasy] Found from ${sectionName}: ${fieldName} = ${fieldValue}`);\r\n          }\r\n        }\r\n      });\r\n    }\r\n  });\r\n\r\n  // Extract all menu items with values\r\n  // Use centralized selector from config\r\n  let menuItems = [];\r\n  if (window.SELECTORS && window.SELECTORS.profile.menuItems) {\r\n    menuItems = findElements(window.SELECTORS.profile.menuItems);\r\n  } else {\r\n    // Fallback if selectors not loaded\r\n    menuItems = document.querySelectorAll('.menuItem__contents');\r\n  }\r\n\r\n  console.log(`[FlirtEasy] Found ${menuItems.length} menu items`);\r\n\r\n  menuItems.forEach(item => {\r\n    const ariaLabel = item.getAttribute('aria-label') || '';\r\n\r\n    // Extract height\r\n    if (ariaLabel.includes('cm') && ariaLabel.includes('Height')) {\r\n      const heightMatch = ariaLabel.match(/(\\d+)\\s*cm/);\r\n      if (heightMatch) {\r\n        profile.height = heightMatch[1] + ' cm';\r\n        console.log(`[FlirtEasy] Found height: ${profile.height}`);\r\n      }\r\n    }\r\n\r\n    // Extract city\r\n    const labelEl = item.querySelector('span.Maw\\\\(80\\\\%\\\\)');\r\n    if (labelEl && labelEl.textContent.includes('Add City')) {\r\n      // City not set, skip\r\n    } else if (labelEl && item.querySelector('#living_in')) {\r\n      const cityText = labelEl.textContent.trim();\r\n      if (cityText && !cityText.includes('Add City')) {\r\n        profile.city = cityText;\r\n        console.log(`[FlirtEasy] Found city: ${profile.city}`);\r\n      }\r\n    }\r\n\r\n    // Extract gender\r\n    if (ariaLabel.includes('gender') || item.querySelector('#gender')) {\r\n      const genderEl = item.querySelector('span.Maw\\\\(80\\\\%\\\\)');\r\n      if (genderEl) {\r\n        const genderText = genderEl.textContent.replace('Update your gender', '').trim();\r\n        if (genderText) {\r\n          profile.gender = genderText;\r\n          console.log(`[FlirtEasy] Found gender: ${profile.gender}`);\r\n        }\r\n      }\r\n    }\r\n\r\n    // Fallback: extract gender from the dedicated gender edit link (current Tinder UI)\r\n    if (!profile.gender) {\r\n      const genderLink = document.querySelector('a[href=\"/app/profile/edit/gender\"]');\r\n      if (genderLink) {\r\n        const raw = genderLink.innerText?.trim() || '';\r\n        const genderText = raw.replace(/update your gender/i, '').trim();\r\n        if (genderText) {\r\n          profile.gender = genderText;\r\n          console.log(`[FlirtEasy] Found gender (link fallback): ${profile.gender}`);\r\n        }\r\n      }\r\n    }\r\n\r\n    // Extract sexual orientation\r\n    if (ariaLabel.includes('sexual orientation') || item.querySelector('#sexual_orientation')) {\r\n      const orientationEl = item.querySelector('span.Maw\\\\(80\\\\%\\\\)');\r\n      if (orientationEl) {\r\n        const orientationText = orientationEl.textContent.replace('Update your sexual orientation', '').trim();\r\n        if (orientationText) {\r\n          profile.sexualOrientation = orientationText;\r\n          console.log(`[FlirtEasy] Found sexual orientation: ${profile.sexualOrientation}`);\r\n        }\r\n      }\r\n    }\r\n\r\n    // Get label - try multiple selectors\r\n    let label = '';\r\n    const labelEl1 = item.querySelector('span.Maw\\\\(80\\\\%\\\\)');\r\n    const labelEl2 = item.querySelector('span.Ov\\\\(h\\\\)');\r\n\r\n    if (labelEl1) {\r\n      label = labelEl1.textContent.trim();\r\n    } else if (labelEl2) {\r\n      label = labelEl2.textContent.trim();\r\n    }\r\n\r\n    if (!label) {\r\n      return;\r\n    }\r\n\r\n    // Get value from menuItem__select span\r\n    const valueEl = item.querySelector('.menuItem__select span[aria-hidden=\"true\"]');\r\n    if (!valueEl) return;\r\n\r\n    const value = valueEl.textContent.trim();\r\n\r\n    // Skip if value is \"Select\" or \"Add\" (not filled)\r\n    if (value === 'Select' || value === 'Add' || !value) return;\r\n\r\n    console.log(`[FlirtEasy] Found: ${label} = ${value}`);\r\n\r\n    // Map to profile fields\r\n    if (label.includes('Location')) {\r\n      profile.location = value;\r\n    } else if (label.includes('Distance Preference')) {\r\n      if (value && value.trim() && value !== 'km.' && value !== 'km') {\r\n        profile.distancePreference = value;\r\n        console.log(`[FlirtEasy] Saved distancePreference: ${value}`);\r\n      } else {\r\n        console.log(`[FlirtEasy] Skipped invalid distance value: ${value}`);\r\n      }\r\n    } else if (label.includes('Interested In') || (label.includes('Looking for') && value === 'Women')) {\r\n      profile.interestedIn = value;\r\n    } else if (label.includes('Looking for') && value !== 'Women') {\r\n      profile.lookingFor = value;\r\n    } else if (label.includes('language')) {\r\n      profile.preferredLanguages = value.split(',').map(l => l.trim());\r\n    } else if (label.includes('Zodiac')) {\r\n      profile.zodiac = value;\r\n    } else if (label.includes('Education')) {\r\n      profile.education = value;\r\n    } else if (label.includes('Family')) {\r\n      profile.familyPlans = value;\r\n    } else if (label.includes('Communication')) {\r\n      profile.communicationStyle = value;\r\n    } else if (label.includes('Love')) {\r\n      profile.loveStyle = value;\r\n    } else if (label.includes('Pets')) {\r\n      profile.pets = value;\r\n    } else if (label.includes('Drinking')) {\r\n      profile.drinking = value;\r\n    } else if (label.includes('Smoking')) {\r\n      profile.smoking = value;\r\n    } else if (label.includes('Workout')) {\r\n      profile.workout = value;\r\n    } else if (label.includes('Sleeping')) {\r\n      profile.sleepingHabits = value;\r\n    } else if (label.includes('Dietary')) {\r\n      profile.dietaryPreference = value;\r\n    } else if (label.includes('Social')) {\r\n      profile.socialMedia = value;\r\n    }\r\n  });\r\n\r\n  // Gender fallback: the Edit tab has a direct link to the gender edit page\r\n  // with the current value as its text content. This works even when the\r\n  // Preview tab sections don't expose gender explicitly.\r\n  if (!profile.gender) {\r\n    const genderLink = document.querySelector('a[href=\"/app/profile/edit/gender\"]');\r\n    if (genderLink) {\r\n      const raw = genderLink.innerText?.trim() || '';\r\n      const genderText = raw.replace(/update your gender/i, '').trim();\r\n      if (genderText) {\r\n        profile.gender = genderText;\r\n        console.log('[FlirtEasy] Found gender (link fallback):', profile.gender);\r\n      }\r\n    }\r\n  }\r\n\r\n  console.log('[FlirtEasy] Profile extraction result:', profile);\r\n  return profile;\r\n}\r\n\r\nasync function updateTinderBio(newBio) {\r\n  try {\r\n    console.log('[FlirtEasy] updateTinderBio called with bio:', newBio);\r\n    console.log('[FlirtEasy] Current URL:', window.location.href);\r\n\r\n    // 1. Ensure on edit profile page\r\n    if (!window.location.pathname.includes('/app/profile/edit')) {\r\n      console.log('[FlirtEasy] Navigating to /app/profile/edit...');\r\n      window.location.href = 'https://tinder.com/app/profile/edit';\r\n      await new Promise(r => setTimeout(r, 2500));\r\n    }\r\n\r\n    // 2. Wait for bio textarea to load\r\n    console.log('[FlirtEasy] Looking for bio textarea...');\r\n    let bioTextarea = null;\r\n    for (let i = 0; i < 20; i++) {\r\n      bioTextarea = document.querySelector('textarea[maxlength=\"500\"]') || document.querySelector('textarea');\r\n      if (bioTextarea) break;\r\n      await new Promise(resolve => setTimeout(resolve, 300));\r\n    }\r\n\r\n    if (!bioTextarea) {\r\n      console.error('[FlirtEasy] Bio textarea not found after retries');\r\n      return { success: false, error: 'Bio field not found' };\r\n    }\r\n\r\n    console.log('[FlirtEasy] Found bio textarea, updating value...');\r\n\r\n    // 3. React-compatible native value setter\r\n    bioTextarea.focus();\r\n    const proto = window.HTMLTextAreaElement.prototype;\r\n    const nativeSetter = Object.getOwnPropertyDescriptor(proto, 'value')?.set;\r\n    if (nativeSetter) {\r\n      nativeSetter.call(bioTextarea, newBio);\r\n    } else {\r\n      bioTextarea.value = newBio;\r\n    }\r\n\r\n    bioTextarea.dispatchEvent(new Event('input', { bubbles: true }));\r\n    bioTextarea.dispatchEvent(new Event('change', { bubbles: true }));\r\n    await new Promise(resolve => setTimeout(resolve, 300));\r\n    bioTextarea.blur();\r\n\r\n    // 4. Save via Tinder Web API\r\n    console.log('[FlirtEasy] Visual update complete, saving via API...');\r\n    let authToken = null;\r\n    try {\r\n      authToken = localStorage.getItem('TinderWeb/APIToken');\r\n      if (!authToken) {\r\n        const tinderData = localStorage.getItem('TinderWeb/APIStore');\r\n        if (tinderData) {\r\n          const parsed = JSON.parse(tinderData);\r\n          authToken = parsed?.token || parsed?.auth_token || parsed?.user?.api_token;\r\n        }\r\n      }\r\n    } catch (e) { }\r\n\r\n    if (!authToken) {\r\n      try {\r\n        authToken = sessionStorage.getItem('authToken') || sessionStorage.getItem('x-auth-token');\r\n      } catch (e) { }\r\n    }\r\n\r\n    if (!authToken) {\r\n      authToken = document.cookie.split('; ').find(row => row.startsWith('x-auth-token='))?.split('=')[1];\r\n    }\r\n\r\n    let apiSuccess = false;\r\n    if (authToken) {\r\n      try {\r\n        console.log('[FlirtEasy] Making profile API update call...');\r\n        const response = await fetch('https://api.gotinder.com/v2/profile?locale=en', {\r\n          method: 'POST',\r\n          headers: {\r\n            'Content-Type': 'application/json',\r\n            'x-auth-token': authToken,\r\n            'app-version': '1064501',\r\n            'platform': 'web',\r\n            'tinder-version': '6.45.1'\r\n          },\r\n          body: JSON.stringify({ user: { bio: newBio } })\r\n        });\r\n        const data = await response.json();\r\n        apiSuccess = data?.meta?.status === 200;\r\n        console.log('[FlirtEasy] API update result:', apiSuccess);\r\n      } catch (apiErr) {\r\n        console.warn('[FlirtEasy] API update warning:', apiErr.message);\r\n      }\r\n    }\r\n\r\n    // 5. Commit UI by clicking 'Done' button\r\n    const doneBtn = Array.from(document.querySelectorAll('button')).find(b => b.innerText && b.innerText.trim() === 'Done');\r\n    if (doneBtn) {\r\n      console.log('[FlirtEasy] Clicking Done button to commit...');\r\n      doneBtn.click();\r\n    }\r\n\r\n    // 6. Update local extension storage\r\n    try {\r\n      const s = (await chrome.storage.local.get('settings')).settings || {};\r\n      s.userProfile = s.userProfile || {};\r\n      s.userProfile.bio = newBio;\r\n      await chrome.storage.local.set({ settings: s });\r\n    } catch (_) {}\r\n\r\n    return { success: true, apiSuccess, bio: newBio };\r\n  } catch (e) {\r\n    console.error('[FlirtEasy] updateTinderBio error:', e);\r\n    return { success: false, error: e.message };\r\n  }\r\n}\n\n\n\n// ═══════════════════════════════════════════════════════════════\n// [FlirtEasy Bundle] content/message-sender.js\n// ═══════════════════════════════════════════════════════════════\n// Toggle console output (set to false for production)\r\nconst DEBUG_MODE = false;\r\nconst log = (...args) => DEBUG_MODE && console.log(...args);\r\nconst warn = (...args) => console.warn(...args);\r\nconst error = (...args) => console.error(...args);\r\n\r\nfunction findChatInput() {\r\n  if (!window.SELECTORS) {\r\n    warn('[FlirtEasy] Selectors not loaded for findChatInput');\r\n    return null;\r\n  }\r\n  const el = findElement(window.SELECTORS.messaging.input);\r\n  if (!el) {\r\n    try { chrome.runtime.sendMessage({ action: 'reportDomError', payload: { platform: 'tinder', error_type: 'selector_miss', selector_key: 'messaging.input', error_message: 'Chat input field not found — Tinder may have changed its messaging UI', page_url: location.href } }); } catch (_) {}\r\n  }\r\n  return el;\r\n}\r\n\r\nfunction findSendButton() {\r\n  if (!window.SELECTORS) {\r\n    warn('[FlirtEasy] Selectors not loaded for findSendButton');\r\n    return null;\r\n  }\r\n  const el = findElement(window.SELECTORS.messaging.sendButton);\r\n  if (!el) {\r\n    try { chrome.runtime.sendMessage({ action: 'reportDomError', payload: { platform: 'tinder', error_type: 'selector_miss', selector_key: 'messaging.sendButton', error_message: 'Send button not found — Tinder may have changed its messaging UI', page_url: location.href } }); } catch (_) {}\r\n  }\r\n  return el;\r\n}\r\n\r\nfunction insertMessage(text) {\r\n  const input = findChatInput();\r\n\r\n  if (!input) {\r\n    error('[FlirtEasy] Chat input not found');\r\n    return false;\r\n  }\r\n\r\n  if (input.tagName === 'TEXTAREA' || input.tagName === 'INPUT') {\r\n    const proto = input.tagName === 'TEXTAREA' ? window.HTMLTextAreaElement.prototype : window.HTMLInputElement.prototype;\r\n    const nativeInputSetter = Object.getOwnPropertyDescriptor(proto, 'value')?.set;\r\n    if (nativeInputSetter) {\r\n      nativeInputSetter.call(input, text);\r\n    } else {\r\n      input.value = text;\r\n    }\r\n    input.dispatchEvent(new Event('input', { bubbles: true }));\r\n    input.dispatchEvent(new Event('change', { bubbles: true }));\r\n\r\n  } else if (input.hasAttribute('contenteditable') || input.getAttribute('role') === 'textbox') {\r\n    input.focus();\r\n    input.textContent = '';\r\n    const inserted = document.execCommand('insertText', false, text);\r\n    if (!inserted) {\r\n      input.textContent = text;\r\n      input.dispatchEvent(new InputEvent('input', { bubbles: true, data: text, inputType: 'insertText' }));\r\n    }\r\n    input.dispatchEvent(new Event('input', { bubbles: true }));\r\n  }\r\n\r\n  return true;\r\n}\r\n\r\nfunction clickSendButton() {\r\n  const sendBtn = findSendButton();\r\n\r\n  if (!sendBtn) {\r\n    console.error('[FlirtEasy] Send button not found');\r\n    return false;\r\n  }\r\n\r\n  if (sendBtn.disabled) {\r\n    console.warn('[FlirtEasy] Send button is disabled');\r\n    return false;\r\n  }\r\n\r\n  sendBtn.click();\r\n  return true;\r\n}\r\n\r\nasync function sendMessage(message) {\r\n  console.log('[FlirtEasy] Attempting to send message:', message);\r\n\r\n  if (!navigator.onLine) {\r\n    console.warn('[FlirtEasy] Network offline — aborting send');\r\n    return { success: false, networkOffline: true, error: 'Network offline' };\r\n  }\r\n\r\n  // Strip prefixes like \"You:\", \"null:\", \"Name:\" from the message\r\n  let cleanMessage = message;\r\n  const prefixPattern = /^(You|null|[A-Z][a-z]+):\\s*/;\r\n  if (prefixPattern.test(cleanMessage)) {\r\n    cleanMessage = cleanMessage.replace(prefixPattern, '');\r\n    console.log('[FlirtEasy] Stripped prefix, clean message:', cleanMessage);\r\n  }\r\n\r\n  const inserted = insertMessage(cleanMessage);\r\n  if (!inserted) {\r\n    return { success: false, error: 'Failed to insert message into input field' };\r\n  }\r\n\r\n  await wait(1000); // Increased from 500\r\n\r\n  const sent = clickSendButton();\r\n  if (!sent) {\r\n    return { success: false, error: 'Failed to click send button' };\r\n  }\r\n\r\n  await wait(1500);\r\n\r\n  console.log('[FlirtEasy] Message sent successfully');\r\n  return { success: true };\r\n}\r\n\r\nfunction wait(ms) {\r\n  return new Promise(resolve => setTimeout(resolve, ms));\r\n}\r\n\r\n\r\n\n\n\n// ═══════════════════════════════════════════════════════════════\n// [FlirtEasy Bundle] content/content.js\n// ═══════════════════════════════════════════════════════════════\n// Auto-redirect away from /faq directly to Tinder home\r\nif (window.location.pathname.startsWith('/faq') || window.location.href.includes('/faq')) {\r\n  window.location.replace('https://tinder.com/');\r\n}\r\n\r\n// ─── Automated Cookie Consent & Backdrop Auto-Dismiss ───\r\n// This is driven by a MutationObserver on a React app that mutates constantly,\r\n// so it MUST stay cheap and MUST stop once it has done its job. The text scan\r\n// below reads innerText on every button in the document, which forces a\r\n// synchronous layout per element. Running that on every mutation record is\r\n// enough to saturate the WebView main thread on a phone: the page still paints\r\n// but taps never reach React's click handlers, so buttons look dead.\r\nlet _cookieConsentDone = false;\r\nlet _cookieScanCount = 0;\r\n\r\nfunction autoDismissCookies() {\r\n  if (_cookieConsentDone) return;\r\n  try {\r\n    // 1. OneTrust / standard accept button targets (cheap id + attribute lookups)\r\n    const otBtn = document.getElementById('onetrust-accept-btn-handler') ||\r\n      document.querySelector('#onetrust-consent-sdk button') ||\r\n      document.querySelector('[aria-label=\"Accept all\"]') ||\r\n      document.querySelector('[data-testid=\"cookie-accept\"]');\r\n    if (otBtn) {\r\n      otBtn.click();\r\n      _cookieConsentDone = true;\r\n    }\r\n\r\n    // 2. Clear any sticky backdrop filter that intercepts touches\r\n    const filter = document.querySelector('.onetrust-pc-dark-filter');\r\n    if (filter) {\r\n      filter.style.pointerEvents = 'none';\r\n      filter.style.display = 'none';\r\n    }\r\n\r\n    // 3. Text-based fallback — expensive, so hard-capped instead of unbounded\r\n    if (!_cookieConsentDone && _cookieScanCount < 15) {\r\n      _cookieScanCount++;\r\n      const btns = Array.from(document.querySelectorAll('button, a, div[role=\"button\"]'));\r\n      for (let i = 0; i < btns.length; i++) {\r\n        const b = btns[i];\r\n        const txt = (b.innerText || b.textContent || '').trim().toLowerCase();\r\n        if (txt === 'i accept' || txt === 'accept all' || txt === 'accept' || txt === 'i agree' || txt === 'got it') {\r\n          b.click();\r\n          _cookieConsentDone = true;\r\n          break;\r\n        }\r\n      }\r\n    }\r\n  } catch (_) {}\r\n}\r\n\r\nif (typeof document !== 'undefined') {\r\n  autoDismissCookies();\r\n\r\n  let cookieObserver = null;\r\n  let cookieTick = null;\r\n\r\n  const stopCookieObserver = () => {\r\n    if (cookieTick) { clearTimeout(cookieTick); cookieTick = null; }\r\n    if (cookieObserver) { cookieObserver.disconnect(); cookieObserver = null; }\r\n  };\r\n\r\n  // Coalesce bursts of mutations into at most one scan per 400ms.\r\n  const scheduleCookieScan = () => {\r\n    if (_cookieConsentDone) { stopCookieObserver(); return; }\r\n    if (cookieTick) return;\r\n    cookieTick = setTimeout(() => {\r\n      cookieTick = null;\r\n      autoDismissCookies();\r\n      if (_cookieConsentDone) stopCookieObserver();\r\n    }, 400);\r\n  };\r\n\r\n  cookieObserver = new MutationObserver(scheduleCookieScan);\r\n\r\n  const startCookieObserver = () => {\r\n    if (document.body && cookieObserver) {\r\n      cookieObserver.observe(document.body, { childList: true, subtree: true });\r\n      // Never watch forever — a consent banner that never appears must not keep\r\n      // the observer (and its scans) alive for the whole session.\r\n      setTimeout(stopCookieObserver, 30000);\r\n    }\r\n  };\r\n\r\n  if (document.body) {\r\n    startCookieObserver();\r\n  } else {\r\n    document.addEventListener('DOMContentLoaded', startCookieObserver);\r\n  }\r\n}\r\n\r\n// Inject interceptor into page context (only once, Chrome Extension context only)\r\nif (window.chrome?.runtime?.id && window.chrome.runtime.id !== 'flirteasy-on-device') {\r\n  if (!document.querySelector('script[data-flirteasy-interceptor]')) {\r\n    const script = document.createElement('script');\r\n    script.src = chrome.runtime.getURL('content/api-interceptor.js');\r\n    script.setAttribute('data-flirteasy-interceptor', 'true');\r\n    script.onload = function () {\r\n      this.remove();\r\n    };\r\n    script.onerror = function () {\r\n      console.error('[FlirtEasy] Failed to inject API interceptor');\r\n    };\r\n    (document.head || document.documentElement).appendChild(script);\r\n  }\r\n\r\n  // Inject Achievement System\r\n  const achievementScripts = [\r\n    'features/achievements/achievements-core.js',\r\n    'features/achievements/achievements-tracker.js',\r\n    'features/achievements/achievements-tinder.js'\r\n  ];\r\n\r\n  // Inject CSS\r\n  const link = document.createElement('link');\r\n  link.rel = 'stylesheet';\r\n  link.href = chrome.runtime.getURL('features/achievements/achievements-styles.css');\r\n  (document.head || document.documentElement).appendChild(link);\r\n\r\n  // Inject JS Sequentially\r\n  function injectNextScript(index = 0) {\r\n    if (index >= achievementScripts.length) return;\r\n\r\n    const script = document.createElement('script');\r\n    script.src = chrome.runtime.getURL(achievementScripts[index]);\r\n    script.onload = () => injectNextScript(index + 1);\r\n    (document.head || document.documentElement).appendChild(script);\r\n  }\r\n  injectNextScript();\r\n}\r\n\r\n// --- Achievement Bridge (Content Script <-> Page Context) ---\r\nwindow.addEventListener('achievement:getData', async () => {\r\n  if (!chrome.runtime?.id) return; // Context lost\r\n  try {\r\n    const data = await chrome.runtime.sendMessage({ action: 'getAchievementData' });\r\n    // Background returns { achievementData: ... }, so we pass it directly\r\n    window.dispatchEvent(new CustomEvent('achievement:dataResponse', { detail: data }));\r\n  } catch (err) {\r\n    if (err.message.includes('Extension context invalidated')) return;\r\n    console.error('[AchievementBridge] Failed to get data:', err);\r\n  }\r\n});\r\n\r\nwindow.addEventListener('achievement:saveData', async (event) => {\r\n  if (!chrome.runtime?.id) return; // Context lost\r\n  if (event.detail && event.detail.achievementData) {\r\n    chrome.runtime.sendMessage({\r\n      action: 'saveAchievementData',\r\n      data: event.detail.achievementData\r\n    });\r\n  }\r\n});\r\n\r\n// --- GLOBAL UTILITY BRIDGE ---\r\nwindow.addEventListener('lead:refresh', async () => {\r\n  if (!chrome.runtime?.id) return; // Context lost\r\n  console.log('[FlirtEasy] Lead refresh triggered via event...');\r\n  try {\r\n    await chrome.runtime.sendMessage({ action: 'clearStoppedChats' });\r\n  } catch (e) {\r\n    // Ignore context invalidation\r\n  }\r\n  if (typeof handledTinderLeadsDuringScan !== 'undefined') {\r\n    handledTinderLeadsDuringScan.clear();\r\n  }\r\n  console.log('[FlirtEasy] Memory wiped. Rescan will start soon.');\r\n});\r\n\r\n// Explicit Sweep Trigger (Production Grade - Accessible from Page Context)\r\nwindow.addEventListener('lead:sweep', async () => {\r\n  console.log('[FlirtEasy] Deep Sweep triggered via event...');\r\n  if (typeof getUnreadMatches === 'function') {\r\n    await getUnreadMatches();\r\n    console.log('[FlirtEasy] Deep Sweep complete.');\r\n  }\r\n});\r\n\r\n// Forward badge unlocks from background to page context\r\n// FLOW: Background (Stats Update) -> Content (Runtime Msg) -> Page (Custom Event) -> Tracker (Check Rules) -> UI (Show Overlay)\r\nchrome.runtime.onMessage.addListener((message) => {\r\n  if (!chrome.runtime?.id) return; // Context lost\r\n\r\n  if (message.action === 'badgeUnlocked') {\r\n    window.dispatchEvent(new CustomEvent('achievement:unlocked', { detail: { badge: message.badge } }));\r\n  }\r\n  if (message.action === 'achievementStatsUpdated') {\r\n    // Fetch latest stats and tell tracker to re-check\r\n    chrome.runtime.sendMessage({ action: 'getAchievementData' }, (data) => {\r\n      if (chrome.runtime.lastError) return;\r\n      if (data && data.achievementData && data.achievementData.userStats) {\r\n        window.dispatchEvent(new CustomEvent('achievement:check', { detail: { stats: data.achievementData.userStats } }));\r\n      }\r\n    });\r\n  }\r\n});\r\n\r\nconsole.log('[FlirtEasy] Content script loaded on Tinder');\r\n\r\nconst FOLLOWUP_SOFT_STOP_COUNT = 9; // 3 per day for 3 days\r\nconst FOLLOWUP_HARD_STOP_COUNT = 10; // Final message after a week pause\r\nconst FOLLOWUP_PAUSE_DURATION = 7 * 24 * 60 * 60 * 1000;\r\n\r\nlet backgroundPort = null;\r\nlet keepAliveInterval = null;\r\nlet autoLikeRunning = false; // Mutex for autoLike\r\nlet processChatsRunning = false; // Mutex for processChats\r\nlet _autoLikeGen = 0;      // Monotonic generation counter for autoLike instances\r\nlet _processChatsGen = 0;  // Monotonic generation counter for processChats instances\r\n\r\nfunction stopAllAutomation() {\r\n  console.log(`[FlirtEasy] ⛔ STOP command executed at ${new Date().toISOString()}`);\r\n  console.log(`[FlirtEasy] ⛔ Kill state: autoLikeRunning=${autoLikeRunning}, processChatsRunning=${processChatsRunning}`);\r\n  autoLikeRunning = false;\r\n  processChatsRunning = false;\r\n  _autoLikeGen++;\r\n  _processChatsGen++;\r\n  window.__flirteasy_stop = true;\r\n  if (typeof removeTinderDeadStateCard === 'function') removeTinderDeadStateCard();\r\n}\r\n\r\n// Global hooks for direct invocation via React Native WebView bridge\r\nwindow.__flirteasyStopAutomation = stopAllAutomation;\r\nwindow.__linksyStopSwiping = stopAllAutomation;\r\nwindow.__flirteasyStartAutomation = function(count) {\r\n  return autoLike(count || 50);\r\n};\r\nwindow.__linksyStartSwiping = window.__flirteasyStartAutomation;\r\nwindow.__flirteasyContentScriptReady = true;\r\nlet tinderSessionSentIds = new Set(); // FAST session-based double-send lock\r\nlet tinderNetworkOfflineReported = false;\r\nlet lastMatchId = null;\r\nlet globalMatchesCreated = 0;\r\nlet userSettings = {};\r\nlet currentMatchIndex = 0;\r\nlet totalMatchesToProcess = 0;\r\n\r\n// --- PROFESSIONAL LOGGING HELPERS ---\r\nfunction logPhaseHeader(title) {\r\n  if (typeof console.header === 'function') {\r\n    console.header(`[PHASE: ${title}]`);\r\n  } else {\r\n    console.log(`%c[PHASE: ${title}]`, 'font-weight: bold; font-size: 1.2em; color: #1e293b; text-decoration: underline;');\r\n  }\r\n}\r\n\r\nfunction logMatchHeader(name, count, total) {\r\n  const countStr = total ? ` (${count}/${total})` : '';\r\n  console.log(`%c------------------- [MATCH: ${name}]${countStr} -------------------`, 'color: #1d4ed8; font-weight: bold;');\r\n}\r\n\r\nfunction logAction(message, data = null) {\r\n  const prefix = '  -> ';\r\n  if (data) console.log(`${prefix}${message}`, data);\r\n  else console.log(`${prefix}${message}`);\r\n}\r\n\r\n// Load settings immediately\r\nchrome.storage.local.get('userSettings', (data) => {\r\n  userSettings = data.userSettings || {};\r\n});\r\n\r\n// Update settings when changed in popup\r\nchrome.storage.onChanged.addListener((changes, area) => {\r\n  if (area === 'local' && changes.userSettings) {\r\n    userSettings = changes.userSettings.newValue || {};\r\n    // If blocking enabled, immediately remove UI\r\n    if (userSettings.blockMessages) {\r\n      const existingBtn = document.querySelector('#flirteasy-stop-btn');\r\n      if (existingBtn) existingBtn.remove();\r\n      const existingLangBtn = document.querySelector('#flirteasy-lang-btn');\r\n      if (existingLangBtn) existingLangBtn.remove();\r\n    }\r\n  }\r\n});\r\n\r\n// Inject Tinder global styles (Design Parity with Bumble)\r\nif (!document.getElementById('flirteasy-tinder-styles')) {\r\n  const style = document.createElement('style');\r\n  style.id = 'flirteasy-tinder-styles';\r\n  style.textContent = `\r\n    .flirteasy-glow-btn {\r\n      margin-right: 12px !important;\r\n      padding: 8px 16px !important;\r\n      border-radius: 24px !important;\r\n      font-size: 13px !important;\r\n      font-weight: 700 !important;\r\n      border: none !important;\r\n      cursor: pointer !important;\r\n      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1) !important;\r\n      position: relative !important;\r\n      overflow: hidden !important;\r\n      color: white !important;\r\n      white-space: nowrap !important;\r\n      z-index: 1000 !important;\r\n      outline: none !important;\r\n      display: inline-flex !important;\r\n      align-items: center !important;\r\n      justify-content: center !important;\r\n      flex-shrink: 0 !important;\r\n    }\r\n    .flirteasy-glow-btn:hover {\r\n      transform: translateY(-2px) scale(1.05) !important;\r\n      filter: brightness(1.2) saturate(1.3) !important;\r\n    }\r\n    .flirteasy-glow-btn[data-stopped=\"false\"][data-type=\"stop\"]:hover {\r\n      box-shadow: 0 0 30px #ef4444, 0 0 60px #ef4444, 0 0 90px #ef444499, 0 4px 15px rgba(0,0,0,0.4) !important;\r\n    }\r\n    .flirteasy-glow-btn[data-stopped=\"true\"][data-type=\"stop\"]:hover {\r\n      box-shadow: 0 0 30px #f59e0b, 0 0 60px #f59e0b, 0 0 90px #f59e0b99, 0 4px 15px rgba(0,0,0,0.4) !important;\r\n    }\r\n    .flirteasy-glow-btn[data-type=\"lang\"]:hover {\r\n        box-shadow: 0 0 30px #6366f1, 0 0 60px #6366f1, 0 0 90px #6366f199, 0 4px 15px rgba(0,0,0,0.4) !important;\r\n    }\r\n    .flirteasy-glow-btn:active {\r\n      transform: translateY(0) scale(0.97) !important;\r\n      filter: brightness(0.9) !important;\r\n      box-shadow: none !important;\r\n    }\r\n    #flirteasy-tinder-group-container {\r\n      display: flex !important;\r\n      \r\n      align-items: center !important;\r\n      gap: 8px !important;\r\n      margin-right: 12px !important;\r\n      overflow: visible !important;\r\n    }\r\n  `;\r\n  (document.head || document.documentElement).appendChild(style);\r\n}\r\n\r\ndocument.addEventListener('flirteasy:matchDetected', (event) => {\r\n  console.log('[FlirtEasy] Match detected via API interceptor!', event.detail);\r\n  globalMatchesCreated++;\r\n\r\n  try {\r\n    if (window.ReactNativeWebView && window.ReactNativeWebView.postMessage) {\r\n      window.ReactNativeWebView.postMessage(JSON.stringify({\r\n        type: 'FE_MATCH',\r\n        matchCount: globalMatchesCreated,\r\n        matchName: (event.detail && event.detail.name) || null,\r\n      }));\r\n    }\r\n  } catch (_) {}\r\n\r\n  chrome.runtime.sendMessage({\r\n    action: 'updateCycleStats',\r\n    stats: { matchesCreated: globalMatchesCreated, currentName: (event.detail && event.detail.name) || null }\r\n  }, () => {\r\n    if (chrome.runtime.lastError) {\r\n      console.warn('[FlirtEasy] Failed to update match stats:', chrome.runtime.lastError.message);\r\n    }\r\n  });\r\n});\r\n\r\nasync function injectStopChatButton() {\r\n  if (!chrome.runtime?.id) return; // Context lost\r\n  if (userSettings && userSettings.blockMessages) {\r\n    const existingBtn = document.querySelector('#flirteasy-stop-btn');\r\n    if (existingBtn) existingBtn.remove();\r\n    return;\r\n  }\r\n\r\n  const matchId = extractMatchIdFromURL();\r\n  if (!matchId) return;\r\n\r\n  if (matchId !== lastMatchId) {\r\n    lastMatchId = matchId;\r\n  }\r\n\r\n  // Check if button already exists (Guard)\r\n  if (document.getElementById('flirteasy-stop-btn')) return;\r\n\r\n  const response = await chrome.runtime.sendMessage({ action: 'isChatstopped', matchId });\r\n\r\n  // Re-check existence after await and context\r\n  if (!chrome.runtime?.id) return;\r\n  if (document.getElementById('flirteasy-stop-btn')) return;\r\n\r\n  // Re-verify container after await\r\n  let groupContainer = document.getElementById('flirteasy-tinder-group-container');\r\n  if (!groupContainer) {\r\n    const injectionSelector = window.SELECTORS?.injectionPoints?.headerRight || 'div.Mend\\\\(16px\\\\)--ml';\r\n    const rightSection = document.querySelector(injectionSelector);\r\n    if (!rightSection) return;\r\n\r\n    groupContainer = document.createElement('div');\r\n    groupContainer.id = 'flirteasy-tinder-group-container';\r\n    rightSection.prepend(groupContainer);\r\n  }\r\n\r\n  const stopBtn = document.createElement('button');\r\n  stopBtn.id = 'flirteasy-stop-btn';\r\n  stopBtn.type = 'button';\r\n  stopBtn.className = 'flirteasy-glow-btn';\r\n  stopBtn.dataset.type = 'stop';\r\n\r\n  if (response && response.isStopped) {\r\n    stopBtn.textContent = 'Unblock AI';\r\n    stopBtn.style.backgroundColor = '#f59e0b';\r\n    stopBtn.dataset.stopped = 'true';\r\n  } else {\r\n    stopBtn.textContent = 'Block AI';\r\n    stopBtn.style.backgroundColor = '#ef4444';\r\n    stopBtn.dataset.stopped = 'false';\r\n  }\r\n\r\n  stopBtn.addEventListener('click', async (e) => {\r\n    e.stopPropagation();\r\n    if (!chrome.runtime?.id) return;\r\n    const isStopped = stopBtn.dataset.stopped === 'true';\r\n    if (isStopped) {\r\n      const confirmed = confirm('Unblock this match?\\n\\nAI will resume messaging them.');\r\n      if (!confirmed) return;\r\n      await chrome.runtime.sendMessage({ action: 'unblockChat', matchId });\r\n      stopBtn.style.backgroundColor = '#ef4444';\r\n      stopBtn.textContent = 'Block AI';\r\n      stopBtn.dataset.stopped = 'false';\r\n    } else {\r\n      const confirmed = confirm('Block AI from messaging this match?\\n\\nYou can still message manually.');\r\n      if (!confirmed) return;\r\n      const h1Raw = document.querySelector('h1')?.textContent?.trim() || '';\r\n\r\n      // Multi-language name extraction from Tinder notification h1 text\r\n      // Handles: English, Italian, Spanish, French, German, Portuguese, Dutch, Swedish, etc.\r\n      const h1Extracted = (\r\n        h1Raw.match(/You matched with (.+?) on /i)?.[1] ||             // EN\r\n        h1Raw.match(/Hai fatto match con (.+?) il /i)?.[1] ||          // IT\r\n        h1Raw.match(/Hai messo un Super Like a (.+?) il /i)?.[1] ||    // IT SuperLike\r\n        h1Raw.match(/Hiciste match con (.+?) el /i)?.[1] ||            // ES\r\n        h1Raw.match(/Vous avez un match avec (.+?)[\\s,]+(le|!)/i)?.[1] || // FR\r\n        h1Raw.match(/Du hast ein Match mit (.+?) am /i)?.[1] ||        // DE\r\n        h1Raw.match(/Você deu match com (.+?) em /i)?.[1] ||           // PT\r\n        h1Raw.match(/Je hebt een match met (.+?) op /i)?.[1] ||        // NL\r\n        h1Raw.match(/Du matchade med (.+?) den /i)?.[1] ||             // SV\r\n        null\r\n      );\r\n\r\n      const matchName = getMatchName() || h1Extracted || null;\r\n\r\n      if (typeof UIAlerts !== 'undefined') UIAlerts.showHandoff(matchName, 'manual');\r\n      await chrome.runtime.sendMessage({\r\n        action: 'markChatStopped',\r\n        matchId,\r\n        reason: 'Manually stopped by user',\r\n        matchName,\r\n        photoUrl: typeof extractProfilePhotoUrl === 'function' ? extractProfilePhotoUrl() : null\r\n      });\r\n      stopBtn.style.backgroundColor = '#f59e0b';\r\n      stopBtn.textContent = 'Unblock AI';\r\n      stopBtn.dataset.stopped = 'true';\r\n    }\r\n  });\r\n\r\n  if (groupContainer) groupContainer.appendChild(stopBtn);\r\n}\r\n\r\nconst uiInjectionInterval = setInterval(() => {\r\n  // If the extension is reloaded/updated, the context becomes invalidated.\r\n  // We must stop the interval to prevent \"Extension context invalidated\" errors.\r\n  if (!chrome.runtime?.id) {\r\n    clearInterval(uiInjectionInterval);\r\n    return;\r\n  }\r\n\r\n  if (window.location.pathname.includes('/app/messages/')) {\r\n    injectStopChatButton();\r\n    injectLanguageButton();\r\n  } else {\r\n    lastMatchId = null;\r\n  }\r\n}, 1000);\r\n\r\n// ========== LANGUAGE DETECTION BUTTON ==========\r\n\r\nconst LANG_GLOBE_SVG = `<svg width=\"14\" height=\"14\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"vertical-align: middle; margin-right: 8px; fill: none !important;\"><circle cx=\"12\" cy=\"12\" r=\"10\"/><line x1=\"2\" y1=\"12\" x2=\"22\" y2=\"12\"/><path d=\"M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z\"/></svg>`;\r\n\r\nconst LANG_LIST = [\r\n  { name: 'English', code: 'en' }, { name: 'Hebrew', code: 'he' },\r\n  { name: 'Arabic', code: 'ar' }, { name: 'Spanish', code: 'es' },\r\n  { name: 'French', code: 'fr' }, { name: 'German', code: 'de' },\r\n  { name: 'Portuguese', code: 'pt' }, { name: 'Italian', code: 'it' },\r\n  { name: 'Russian', code: 'ru' }, { name: 'Hindi', code: 'hi' },\r\n  { name: 'Turkish', code: 'tr' }, { name: 'Dutch', code: 'nl' },\r\n  { name: 'Japanese', code: 'ja' }, { name: 'Korean', code: 'ko' },\r\n  { name: 'Chinese', code: 'zh' }, { name: 'Polish', code: 'pl' },\r\n  { name: 'Thai', code: 'th' }, { name: 'Swedish', code: 'sv' },\r\n  { name: 'Romanian', code: 'ro' }, { name: 'Czech', code: 'cs' },\r\n  { name: 'Greek', code: 'el' }, { name: 'Hungarian', code: 'hu' },\r\n  { name: 'Indonesian', code: 'id' }, { name: 'Vietnamese', code: 'vi' },\r\n];\r\n\r\nconst LANG_GLOW_COLOR = '#6366f1';\r\nlet lastLangMatchId = null;\r\n\r\nasync function injectLanguageButton() {\r\n  if (!chrome.runtime?.id) return; // Context lost\r\n  // If blocking enabled, remove and skip\r\n  if (userSettings && userSettings.blockMessages) {\r\n    const existing = document.querySelector('#flirteasy-lang-btn');\r\n    if (existing) existing.remove();\r\n    return;\r\n  }\r\n\r\n  const path = window.location.pathname;\r\n  const parts = path.split('/');\r\n  const matchId = parts.length >= 4 && path.includes('/app/messages/') ? parts[3] : null;\r\n  if (!matchId) return;\r\n\r\n  // Already injected for this match\r\n  const existingBtn = document.querySelector('#flirteasy-lang-btn');\r\n  if (existingBtn && existingBtn.dataset.matchId === matchId) return;\r\n  if (existingBtn) existingBtn.remove();\r\n\r\n  // Find the same header container as Block AI\r\n  const injectionSelector = window.SELECTORS?.injectionPoints?.headerRight || 'div.Mend\\\\(16px\\\\)--ml';\r\n  const rightSection = document.querySelector(injectionSelector);\r\n  if (!rightSection) return;\r\n\r\n  // Use shared group container pattern for Tinder\r\n  let groupContainer = document.getElementById('flirteasy-tinder-group-container');\r\n  if (!groupContainer) {\r\n    groupContainer = document.createElement('div');\r\n    groupContainer.id = 'flirteasy-tinder-group-container';\r\n    rightSection.prepend(groupContainer);\r\n  }\r\n\r\n  const langBtn = document.createElement('button');\r\n  langBtn.id = 'flirteasy-lang-btn';\r\n  langBtn.dataset.matchId = matchId;\r\n  langBtn.className = 'flirteasy-glow-btn';\r\n  langBtn.dataset.type = 'lang';\r\n  langBtn.innerHTML = LANG_GLOBE_SVG + 'English';\r\n  langBtn.style.backgroundColor = LANG_GLOW_COLOR;\r\n\r\n  langBtn.addEventListener('click', (e) => {\r\n    e.stopPropagation();\r\n    showTinderLangPicker(langBtn, matchId);\r\n  });\r\n\r\n  // Prepend inside group (Language on left, Stop on right)\r\n  if (groupContainer) groupContainer.prepend(langBtn);\r\n\r\n  // ASYNC UPDATE\r\n  try {\r\n    const storedLang = await new Promise(resolve => {\r\n      const timeout = setTimeout(() => resolve(null), 3000); // increased from 1500ms\r\n      chrome.runtime.sendMessage({ action: 'getMatchLanguage', matchId }, r => {\r\n        clearTimeout(timeout);\r\n        resolve(r?.language || null);\r\n      });\r\n    });\r\n\r\n    if (storedLang && storedLang.source === 'manual') {\r\n      // Manual override — always use it, never run fresh detection\r\n      langBtn.innerHTML = LANG_GLOBE_SVG + storedLang.name;\r\n      langBtn.dataset.langCode = storedLang.code;\r\n      langBtn.dataset.langName = storedLang.name;\r\n      return; // stop here — do not run any detection\r\n    } else {\r\n      const history = await getConversationHistory(10);\r\n      if (history && history.messages && history.messages.length > 0) {\r\n        const detected = detectConversationLanguage(history.messages);\r\n        let finalDetected = detected;\r\n\r\n        // Bio fallback — if conversation is ambiguous OR low confidence, try profile bio\r\n        if (detected.code === 'unknown' || detected.source?.includes('fallback') ||\r\n            detected.source?.includes('latin_english_default') ||\r\n            (detected.confidence < 50 && detected.source?.includes('word_patterns'))) {\r\n          try {\r\n            const profile = parseCurrentProfile();\r\n            const bioText = [\r\n              profile?.bio || '',\r\n              (profile?.questionAnswers || []).map(qa => qa.answer || qa).join(' '),\r\n            ].join(' ').trim();\r\n\r\n            if (bioText.length >= 4 && typeof detectLanguage === 'function') {\r\n              const bioResult = detectLanguage(bioText);\r\n              if (bioResult.code !== 'unknown' && bioResult.code !== 'en') {\r\n                // Only use bio language if the conversation doesn't have clear English signals\r\n                const conversationHasEnglish = history.messages\r\n                  .filter(m => m.sender === 'match')\r\n                  .some(m => /\\b(the|and|you|are|have|good|can|talk|text|sounds|like|want|nice|meet|ok|cool|yeah|yes|no)\\b/i.test(m.text || ''));\r\n                if (!conversationHasEnglish) {\r\n                  finalDetected = { ...bioResult, source: `bio→${bioResult.source}` };\r\n                  console.log(`[FlirtEasy] Language from bio: ${bioResult.name} (${bioResult.confidence}%)`);\r\n                }\r\n              }\r\n            }\r\n          } catch (_) {}\r\n        }\r\n\r\n        if (finalDetected.code !== 'unknown' && !finalDetected.source?.includes('latin_fallback')) {\r\n          langBtn.innerHTML = LANG_GLOBE_SVG + finalDetected.name;\r\n          langBtn.dataset.langCode = finalDetected.code;\r\n          langBtn.dataset.langName = finalDetected.name;\r\n          // Always overwrite stale auto-detected language\r\n          if (!storedLang || storedLang.source !== 'manual' || storedLang.code !== finalDetected.code) {\r\n            chrome.runtime.sendMessage({\r\n              action: 'setMatchLanguage',\r\n              matchId,\r\n              langData: { code: finalDetected.code, name: finalDetected.name, confidence: finalDetected.confidence, source: 'detected' }\r\n            });\r\n          }\r\n        }\r\n      }\r\n    }\r\n  } catch (e) {\r\n    console.warn('[FlirtEasy] Lang update failed:', e);\r\n  }\r\n}\r\n\r\nfunction showTinderLangPicker(btn, matchId) {\r\n  const existing = document.getElementById('flirteasy-lang-picker');\r\n  if (existing) { existing.remove(); return; }\r\n\r\n  const rect = btn.getBoundingClientRect();\r\n  const picker = document.createElement('div');\r\n  picker.id = 'flirteasy-lang-picker';\r\n\r\n  // Appending to BODY to avoid overflow issues\r\n  picker.style.cssText = `\r\n    position: fixed; \r\n    top: ${rect.bottom + 12}px; \r\n    left: ${rect.left + rect.width / 2}px; \r\n    transform: translateX(-50%); \r\n    background: rgba(30, 30, 40, 0.98); \r\n    border: 1px solid rgba(99, 102, 241, 0.4); \r\n    border-radius: 16px; \r\n    padding: 8px; \r\n    z-index: 2147483647; \r\n    min-width: 190px; \r\n    max-height: 350px; \r\n    overflow-y: auto; \r\n    box-shadow: 0 10px 40px rgba(0,0,0,0.6), 0 0 25px rgba(99,102,241,0.2); \r\n    backdrop-filter: blur(25px); \r\n    scrollbar-width: thin; \r\n    scrollbar-color: rgba(99,102,241,0.4) transparent;\r\n  `;\r\n\r\n  // Auto-detect option\r\n  const autoDiv = document.createElement('div');\r\n  autoDiv.innerHTML = LANG_GLOBE_SVG + ' Auto-Detect';\r\n  autoDiv.style.cssText = 'padding: 10px 14px; border-radius: 10px; cursor: pointer; color: #a5b4fc; font-size: 13px; font-weight: 600; transition: all 0.2s; border-bottom: 1px solid rgba(255,255,255,0.08); margin-bottom: 6px;';\r\n  autoDiv.onmouseenter = () => { autoDiv.style.background = 'rgba(99,102,241,0.2)'; };\r\n  autoDiv.onmouseleave = () => { autoDiv.style.background = 'none'; };\r\n  autoDiv.addEventListener('click', async (e) => {\r\n    e.stopPropagation();\r\n    picker.remove();\r\n    const result = await getConversationHistory(10);\r\n    const messages = result.messages || [];\r\n    let detected = { code: 'unknown', confidence: 0 };\r\n    if (typeof detectConversationLanguage === 'function' && messages.length > 0) {\r\n      detected = detectConversationLanguage(messages);\r\n    }\r\n\r\n    // Bio fallback — if conversation detection failed or is low confidence, try profile bio\r\n    if (detected.code === 'unknown' || detected.source?.includes('fallback') ||\r\n        detected.source?.includes('latin_english_default') ||\r\n        (detected.confidence < 50 && detected.source?.includes('word_patterns'))) {\r\n      try {\r\n        if (typeof parseCurrentProfile === 'function' && typeof detectLanguage === 'function') {\r\n          const profile = parseCurrentProfile();\r\n          const bioText = [\r\n            profile?.bio || '',\r\n            (profile?.questionAnswers || []).map(qa => qa.answer || qa).join(' '),\r\n            (profile?.interests || []).join(' '),\r\n          ].join(' ').trim();\r\n          if (bioText.length >= 4) {\r\n            const bioResult = detectLanguage(bioText);\r\n            if (bioResult.code !== 'unknown' && bioResult.code !== 'en') {\r\n              const conversationHasEnglish = messages\r\n                .filter(m => m.sender === 'match')\r\n                .some(m => /\\b(the|and|you|are|have|good|can|talk|text|sounds|like|want|nice|meet|ok|cool|yeah|yes|no)\\b/i.test(m.text || ''));\r\n              if (!conversationHasEnglish) {\r\n                detected = { ...bioResult, source: `bio→${bioResult.source}` };\r\n                console.log(`[FlirtEasy] Auto-detect bio fallback: ${bioResult.name} (${bioResult.confidence}%)`);\r\n              }\r\n            }\r\n          }\r\n        }\r\n      } catch (_) {}\r\n    }\r\n\r\n    const langName = detected.code !== 'unknown' ? detected.name : 'English';\r\n    const langCode = detected.code !== 'unknown' ? detected.code : 'en';\r\n    btn.innerHTML = LANG_GLOBE_SVG + langName;\r\n    btn.dataset.langCode = langCode;\r\n    btn.dataset.langName = langName;\r\n    chrome.runtime.sendMessage({\r\n      action: 'setMatchLanguage',\r\n      matchId,\r\n      langData: { code: langCode, name: langName, confidence: detected.confidence || 0, source: 'detected' }\r\n    });\r\n  });\r\n  picker.appendChild(autoDiv);\r\n\r\n  const currentCode = btn.dataset.langCode || 'en';\r\n  for (const lang of LANG_LIST) {\r\n    const item = document.createElement('div');\r\n    const isActive = currentCode === lang.code;\r\n    item.textContent = lang.name;\r\n    item.style.cssText = `padding: 9px 14px; border-radius: 10px; cursor: pointer; color: ${isActive ? '#fff' : '#cbd5e1'}; font-size: 13px; font-weight: ${isActive ? '700' : '500'}; background: ${isActive ? 'rgba(99,102,241,0.35)' : 'none'}; transition: all 0.2s; margin-bottom: 2px;`;\r\n\r\n    if (!isActive) {\r\n      item.onmouseenter = () => { item.style.background = 'rgba(99,102,241,0.2)'; };\r\n      item.onmouseleave = () => { item.style.background = 'none'; };\r\n    }\r\n\r\n    item.addEventListener('click', (e) => {\r\n      e.stopPropagation();\r\n      picker.remove();\r\n      btn.innerHTML = LANG_GLOBE_SVG + lang.name;\r\n      btn.dataset.langCode = lang.code;\r\n      btn.dataset.langName = lang.name;\r\n      chrome.runtime.sendMessage({\r\n        action: 'setMatchLanguage',\r\n        matchId,\r\n        langData: { code: lang.code, name: lang.name, confidence: 100, source: 'manual' }\r\n      });\r\n      console.log(`[FlirtEasy] ✨ MATCH INTEREST DETECTED for ${matchId.substring(0, 8)}***`);\r\n    });\r\n    picker.appendChild(item);\r\n  }\r\n\r\n  document.body.appendChild(picker);\r\n\r\n  // Prevent parent button from triggering again when clicking inside picker\r\n  picker.addEventListener('click', (e) => e.stopPropagation());\r\n\r\n  setTimeout(() => {\r\n    const close = (e) => {\r\n      if (!picker.contains(e.target)) {\r\n        picker.remove();\r\n        document.removeEventListener('click', close);\r\n      }\r\n    };\r\n    document.addEventListener('click', close);\r\n  }, 100);\r\n}\r\n\r\nfunction connectToBackground() {\r\n  try {\r\n    if (!chrome.runtime?.id) {\r\n      console.log('[FlirtEasy] Extension context invalidated, stopping reconnection attempts');\r\n      return;\r\n    }\r\n\r\n    if (backgroundPort) {\r\n      backgroundPort.disconnect();\r\n    }\r\n\r\n    if (keepAliveInterval) {\r\n      clearInterval(keepAliveInterval);\r\n    }\r\n\r\n    backgroundPort = chrome.runtime.connect({ name: 'flirtEasy' });\r\n\r\n    backgroundPort.onMessage.addListener((message) => {\r\n      handlePortMessage(message);\r\n    });\r\n\r\n    backgroundPort.onDisconnect.addListener(() => {\r\n      console.log('[FlirtEasy] Port disconnected, reconnecting...');\r\n      backgroundPort = null;\r\n      setTimeout(connectToBackground, 100);\r\n    });\r\n\r\n    keepAliveInterval = setInterval(() => {\r\n      if (backgroundPort) {\r\n        try {\r\n          backgroundPort.postMessage({ type: 'keepalive' });\r\n        } catch (e) {\r\n          console.warn('[FlirtEasy] Keepalive failed:', e);\r\n        }\r\n      }\r\n    }, 5000);\r\n\r\n    console.log('[FlirtEasy] Connected to background');\r\n  } catch (error) {\r\n    if (error.message.includes('Extension context invalidated')) {\r\n      console.log('[FlirtEasy] Extension was reloaded, page refresh required');\r\n      return;\r\n    }\r\n    console.error('[FlirtEasy] Connection error:', error);\r\n    setTimeout(connectToBackground, 1000);\r\n  }\r\n}\r\n\r\nconnectToBackground();\r\n\r\nwindow.addEventListener('pageshow', (event) => {\r\n  if (event.persisted) {\r\n    console.log('[FlirtEasy] Page restored from bfcache, reconnecting...');\r\n    connectToBackground();\r\n  }\r\n});\r\n\r\nasync function handlePortMessage(message) {\r\n  const { messageId, action } = message;\r\n  let response = null;\r\n\r\n  try {\r\n    if (action === 'autoLike') {\r\n      response = await autoLike(message.count || 50);\r\n    } else if (action === 'checkLogin') {\r\n      response = { loggedIn: isLoggedIn() };\r\n    }\r\n  } catch (error) {\r\n    console.error('[FlirtEasy] Port message handler error:', error);\r\n    response = { success: false, error: error.message };\r\n  }\r\n\r\n  if (backgroundPort && messageId) {\r\n    backgroundPort.postMessage({ messageId, data: response });\r\n  }\r\n}\r\n\r\nfunction sendMessageToBackground(message) {\r\n  return new Promise((resolve) => {\r\n    if (!backgroundPort) {\r\n      connectToBackground();\r\n    }\r\n\r\n    chrome.runtime.sendMessage(message, (response) => {\r\n      if (chrome.runtime.lastError) {\r\n        console.warn('[FlirtEasy] Runtime error:', chrome.runtime.lastError.message);\r\n        resolve(null);\r\n      } else {\r\n        resolve(response);\r\n      }\r\n    });\r\n  });\r\n}\r\n\r\nchrome.runtime.onMessage.addListener((request, sender, sendResponse) => {\r\n  console.log('[FlirtEasy] Received message:', request.action);\r\n\r\n  try {\r\n    if (request.action === 'stopAutomation') {\r\n      stopAllAutomation();\r\n      sendResponse({ success: true });\r\n      return false;\r\n    }\r\n\r\n    if (request.action === 'testCelebration') {\r\n      console.log('[FlirtEasy] Relay testCelebration to page context');\r\n      window.dispatchEvent(new CustomEvent('achievement:testCelebration', {\r\n        detail: { badge: request.badge }\r\n      }));\r\n      sendResponse({ success: true });\r\n      return false;\r\n    }\r\n\r\n    if (request.action === 'checkAchievements') {\r\n      console.log('[FlirtEasy] checkAchievements request received, stats:', request.stats);\r\n      // If tracker is ready, dispatch event to page context\r\n      if (achievementTrackerReady) {\r\n        console.log('[FlirtEasy] Tracker ready, dispatching achievement:check event');\r\n        window.dispatchEvent(new CustomEvent('achievement:check', {\r\n          detail: { stats: request.stats }\r\n        }));\r\n        sendResponse({ success: true });\r\n        return false;\r\n      }\r\n\r\n      // Otherwise, queue the request (will be processed when tracker loads)\r\n      console.log('[FlirtEasy] Achievement tracker not ready yet, queuing request');\r\n      achievementQueue.push({ stats: request.stats });\r\n      sendResponse({ success: true, queued: true });\r\n      return false;\r\n    }\r\n\r\n    if (request.action === 'autoLike') {\r\n      autoLike(request.count || 50).then(sendResponse).catch(err => {\r\n        console.error('[FlirtEasy] autoLike error:', err);\r\n        sendResponse({ success: false, error: err.message });\r\n      });\r\n      return true;\r\n    }\r\n\r\n    if (request.action === 'navigateToExplore') {\r\n      navigateToSwipingPage().then(success => {\r\n        sendResponse({ success });\r\n      }).catch(err => {\r\n        console.error('[FlirtEasy] navigateToExplore error:', err);\r\n        sendResponse({ success: false, error: err.message });\r\n      });\r\n      return true;\r\n    }\r\n\r\n    if (request.action === 'getMatches') {\r\n      getUnreadMatches().then(sendResponse).catch(err => {\r\n        console.error('[FlirtEasy] getMatches error:', err);\r\n        sendResponse({ success: false, error: err.message });\r\n      });\r\n      return true;\r\n    }\r\n\r\n    if (request.action === 'processChats') {\r\n      processChats(request.settings, request.maxMessages).then(sendResponse).catch(err => {\r\n        console.error('[FlirtEasy] processChats error:', err);\r\n        sendResponse({ success: false, error: err.message });\r\n      });\r\n      return true;\r\n    }\r\n\r\n    if (request.action === 'navigateToMatch') {\r\n      const success = navigateToMatch(request.matchId);\r\n      sendResponse({ success });\r\n      return true;\r\n    }\r\n\r\n    if (request.action === 'sendMessageToChat') {\r\n      sendMessageToCurrentChat(request.message).then(sendResponse).catch(err => {\r\n        console.error('[FlirtEasy] sendMessageToChat error:', err);\r\n        sendResponse({ success: false, error: err.message });\r\n      });\r\n      return true;\r\n    }\r\n\r\n    if (request.action === 'getProfileData') {\r\n      (async () => {\r\n        const profile = parseCurrentProfile();\r\n        const history = await getConversationHistory();\r\n        sendResponse({ success: true, profile, history });\r\n      })();\r\n      return true;\r\n    }\r\n\r\n    if (request.action === 'checkLogin') {\r\n      const loggedIn = isLoggedIn();\r\n      sendResponse({ loggedIn });\r\n      return true;\r\n    }\r\n\r\n    if (request.action === 'checkAccountTier') {\r\n      (async () => {\r\n        let tier = detectTinderAccountTier();\r\n        if (tier === 'unknown') {\r\n          // API tier may not have arrived yet — wait up to 1.5s for the interceptor to populate it\r\n          tier = await new Promise(resolve => {\r\n            const deadline = setTimeout(() => resolve('unknown'), 1500);\r\n            const handler = (e) => {\r\n              clearTimeout(deadline);\r\n              document.removeEventListener('flirteasy:accountTierDetected', handler);\r\n              resolve(e.detail.tier);\r\n            };\r\n            if (window.__flirtEasyAccountTier) {\r\n              clearTimeout(deadline);\r\n              resolve(window.__flirtEasyAccountTier);\r\n            } else {\r\n              document.addEventListener('flirteasy:accountTierDetected', handler);\r\n            }\r\n          });\r\n        }\r\n        sendResponse({ tier });\r\n      })();\r\n      return true;\r\n    }\r\n\r\n    if (request.action === 'getUserProfile') {\r\n      (async () => {\r\n        const profile = await getUserOwnProfile();\r\n        sendResponse(profile);\r\n      })();\r\n      return true;\r\n    }\r\n\r\n    if (request.action === 'updateTinderBio' || request.action === 'pushBio') {\r\n      console.log(`[FlirtEasy] Received ${request.action} request, bio: [bio-` + (request.bio?.length || 0) + '-chars]');\r\n      (async () => {\r\n        try {\r\n          const result = await updateTinderBio(request.bio);\r\n          console.log(`[FlirtEasy] ${request.action} result:`, result);\r\n          sendResponse(result);\r\n        } catch (err) {\r\n          console.error(`[FlirtEasy] ${request.action} exception:`, err);\r\n          sendResponse({ success: false, error: err.message });\r\n        }\r\n      })();\r\n      return true;\r\n    }\r\n\r\n    if (request.action === 'startVisualTraining') {\r\n      (async () => {\r\n        try {\r\n          const result = await startVisualTrainingMode();\r\n          sendResponse(result);\r\n        } catch (err) {\r\n          console.error('[FlirtEasy] startVisualTraining exception:', err);\r\n          sendResponse({ success: false, error: err.message });\r\n        }\r\n      })();\r\n      return true;\r\n    }\r\n\r\n    if (request.action === 'isTrainingActive') {\r\n      sendResponse({ active: !!document.getElementById('flirteasy-training-overlay') });\r\n      return true;\r\n    }\r\n\r\n    if (request.action === 'stopVisualTraining') {\r\n      const overlay = document.getElementById('flirteasy-training-overlay');\r\n      if (overlay) {\r\n        overlay.firstElementChild && (overlay.firstElementChild.style.opacity = '0');\r\n        setTimeout(() => overlay.remove(), 300);\r\n      }\r\n      sendResponse({ success: true });\r\n      return true;\r\n    }\r\n\r\n    if (request.action === 'setTinderDistance') {\r\n      (async () => {\r\n        try {\r\n          if (typeof setTinderDistanceSetting !== 'function') {\r\n            sendResponse({ success: false, error: 'setTinderDistanceSetting not available' });\r\n            return;\r\n          }\r\n          const result = await setTinderDistanceSetting(request.maxDistanceKm);\r\n          sendResponse(result);\r\n        } catch (err) {\r\n          console.error('[FlirtEasy] setTinderDistance exception:', err);\r\n          sendResponse({ success: false, error: err.message });\r\n        }\r\n      })();\r\n      return true;\r\n    }\r\n\r\n    if (request.action === 'getMatchDistance') {\r\n      try {\r\n        if (typeof getMatchDistanceKm !== 'function') {\r\n          sendResponse({ success: false, error: 'getMatchDistanceKm not available' });\r\n        } else {\r\n          const dist = getMatchDistanceKm();\r\n          sendResponse({ success: true, distance: dist });\r\n        }\r\n      } catch (err) {\r\n        sendResponse({ success: false, error: err.message });\r\n      }\r\n      return true;\r\n    }\r\n  } catch (error) {\r\n    console.error('[FlirtEasy] Message handler error:', error);\r\n    sendResponse({ success: false, error: error.message });\r\n  }\r\n\r\n  return false;\r\n});\r\n\r\nasync function autoLike(count) {\r\n  if (autoLikeRunning) {\r\n    console.log('[FlirtEasy] Auto-like already running, skipping duplicate request');\r\n    return { success: false, likesCompleted: 0, errors: ['Auto-like already in progress'] };\r\n  }\r\n\r\n  window.__flirteasy_stop = false;\r\n  autoLikeRunning = true;\r\n  const _myAutoLikeGen = ++_autoLikeGen;\r\n  const isAutoLikeAborted = () => !autoLikeRunning || _autoLikeGen !== _myAutoLikeGen || window.__flirteasy_stop === true;\r\n  try {\r\n    console.log(`[FlirtEasy] Starting auto-like for ${count} profiles`);\r\n    console.log('[FlirtEasy] Current URL:', window.location.href);\r\n\r\n    await loadSelectors();\r\n\r\n    // Get age filter settings\r\n    const settings = await new Promise((resolve) => {\r\n      chrome.runtime.sendMessage({ action: 'getSettings' }, (response) => {\r\n        resolve(response || {});\r\n      });\r\n    });\r\n\r\n    const ageFilter = settings.ageFilter || { enabled: false, minAge: 18, maxAge: 99 };\r\n    const _ah = settings.activeHours || {};\r\n    const _vp = settings.visualPreferences || {};\r\n    const _sc = settings.stopConditions || [];\r\n    const _sc_display = _sc.length === 0 || _sc.includes('never') ? 'Off (Keep Engaging)' : _sc.join(', ');\r\n    const stopEnabled = _sc.length > 0 && settings.stopAfterGoalEnabled !== false;\r\n\r\n    console.log('%c[FlirtEasy] ══════════ USER SETTINGS ══════════', 'color:#e91e8c;font-weight:bold;');\r\n\r\n    console.log('%c[FlirtEasy] ── SWIPING ──', 'color:#667085;font-weight:bold;');\r\n    console.log(`[FlirtEasy]   Likes Per Cycle      : ${count}`);\r\n    console.log(`[FlirtEasy]   Age Filter           : ${ageFilter.enabled ? `ENABLED (${ageFilter.minAge}–${ageFilter.maxAge} yrs)` : 'DISABLED (all ages)'}`);\r\n    console.log(`[FlirtEasy]   Visual Preferences   : ${_vp.enabled ? `ON (${(_vp.likedPhotos || []).length} trained photos)` : 'OFF'}`);\r\n\r\n    console.log('%c[FlirtEasy] ── MESSAGING ──', 'color:#667085;font-weight:bold;');\r\n    console.log(`[FlirtEasy]   Messages Per Cycle   : ${settings.messagesPerCycle || 50}`);\r\n    console.log(`[FlirtEasy]   Reply Slot %         : ${settings.minReplyPercent || 30}% replies / ${settings.maxNewMatchPercent || 70}% new matches`);\r\n    console.log(`[FlirtEasy]   Intentions           : ${settings.intentions || 'short_term'}`);\r\n    console.log(`[FlirtEasy]   Chatting Style       : ${settings.chattingStyle || 'freestyle'}`);\r\n    console.log(`[FlirtEasy]   Language             : ${settings.conversationLanguage || 'en'}`);\r\n    console.log(`[FlirtEasy]   Use Emojis           : ${settings.useEmojis !== false ? `YES (${settings.emojiProbability ?? 30}% chance per message)` : 'NO'}`);\r\n    console.log(`[FlirtEasy]   Smart Reactions       : ${settings.randomHearts ? `YES (${settings.randomHeartsProbability || 30}% chance)` : 'NO'}`);\r\n\r\n    console.log('%c[FlirtEasy] ── GOAL & STOP ──', 'color:#667085;font-weight:bold;');\r\n    console.log(`[FlirtEasy]   Stop After Goal      : ${_sc_display}`);\r\n\r\n    console.log('%c[FlirtEasy] ── SCHEDULE ──', 'color:#667085;font-weight:bold;');\r\n    console.log(`[FlirtEasy]   Cycle Interval       : Every ${settings.scheduleInterval || 120} min`);\r\n    console.log(`[FlirtEasy]   Active Hours         : ${_ah.enabled ? `${_ah.startTime || '09:00'} – ${_ah.endTime || '23:00'} (${_ah.preset || 'custom'})` : 'Always On (24/7)'}`);\r\n\r\n    console.log('%c[FlirtEasy] ── SAFETY & AI ──', 'color:#667085;font-weight:bold;');\r\n    console.log(`[FlirtEasy]   Safety Mode          : ${settings.safetyMode !== false ? 'ON' : 'OFF'}`);\r\n    console.log(`[FlirtEasy]   API Key              : ${settings.apiKey ? '✓ Set' : '✗ Not set'}`);\r\n    console.log(`[FlirtEasy]   About Myself         : ${settings.aboutMyself ? `\"${settings.aboutMyself.substring(0, 60)}${settings.aboutMyself.length > 60 ? '...' : ''}\"` : 'Not set'}`);\r\n    console.log(`[FlirtEasy]   Custom Prompt        : ${settings.customPrompt ? `\"${settings.customPrompt.substring(0, 60)}${settings.customPrompt.length > 60 ? '...' : ''}\"` : 'Not set'}`);\r\n\r\n    console.log('%c[FlirtEasy] ════════════════════════════════════', 'color:#e91e8c;font-weight:bold;');\r\n\r\n    if (!isLoggedIn()) {\r\n      console.warn('[FlirtEasy] Please log into Tinder first before starting AI.');\r\n      return { success: false, likesCompleted: 0, errors: ['Please log into Tinder to start AI swiping'] };\r\n    }\r\n\r\n    if (!window.location.pathname.includes('/app/recs')) {\r\n      console.log('[FlirtEasy] Not on explore page, auto-navigating to /app/recs...');\r\n      const navigated = await navigateToSwipingPage();\r\n      if (!navigated && !window.location.pathname.includes('/app/recs')) {\r\n        const swipingLink = document.querySelector('a[href*=\"/app/recs\"], a[href*=\"/recs\"], [aria-label*=\"Explore\" i]');\r\n        if (swipingLink) {\r\n          swipingLink.click();\r\n          await waitRandom(2500, 3500);\r\n        } else if (window.location.pathname.includes('/app') && !window.location.pathname.includes('/app/login')) {\r\n          window.location.href = 'https://tinder.com/app/recs';\r\n          await waitRandom(3000, 4500);\r\n        } else {\r\n          console.warn('[FlirtEasy] Please log into Tinder first before starting AI.');\r\n          return { success: false, likesCompleted: 0, errors: ['Please log into Tinder to start AI swiping'] };\r\n        }\r\n      }\r\n    }\r\n\r\n    await waitRandom(2000, 3000);\r\n\r\n    let likesCompleted = 0;\r\n    let errors = [];\r\n    let profilesChecked = 0;\r\n    const maxProfilesToCheck = count * 3;\r\n\r\n    while (likesCompleted < count && profilesChecked < maxProfilesToCheck) {\r\n      if (isAutoLikeAborted()) {\r\n        console.log('[FlirtEasy] Auto-like interrupted by stopAutomation signal');\r\n        break;\r\n      }\r\n      try {\r\n        profilesChecked++;\r\n\r\n        // Network check per iteration — pause up to 90s, abort if still offline\r\n        if (!navigator.onLine) {\r\n          const resumed = await tinderWaitForNetworkOrAbort(90000, 'liking');\r\n          if (!resumed) {\r\n            console.warn('[FlirtEasy] Network timeout during liking — aborting loop');\r\n            errors.push('Network timeout - stopped at profile ' + profilesChecked);\r\n            break;\r\n          }\r\n        }\r\n\r\n        const stateCheck = await new Promise((resolve) => {\r\n          chrome.runtime.sendMessage({ action: 'getAgentState' }, (response) => {\r\n            if (chrome.runtime.lastError) {\r\n              console.warn('[FlirtEasy] Runtime error:', chrome.runtime.lastError.message);\r\n              resolve(null);\r\n            } else {\r\n              resolve(response);\r\n            }\r\n          });\r\n        });\r\n\r\n        if (!stateCheck || !stateCheck.isRunning) {\r\n          console.log('[FlirtEasy] Agent stopped, cancelling auto-like');\r\n          break;\r\n        }\r\n\r\n        console.log(`[FlirtEasy] Checking profile ${profilesChecked} (${likesCompleted}/${count} likes completed)`);\r\n\r\n        if (hasMatchModal()) {\r\n          console.log('[FlirtEasy] Match modal detected (backup detection), closing...');\r\n          globalMatchesCreated++;\r\n\r\n          try {\r\n            if (window.ReactNativeWebView && window.ReactNativeWebView.postMessage) {\r\n              window.ReactNativeWebView.postMessage(JSON.stringify({\r\n                type: 'FE_MATCH',\r\n                matchCount: globalMatchesCreated,\r\n              }));\r\n            }\r\n          } catch (_) {}\r\n\r\n          chrome.runtime.sendMessage({\r\n            action: 'updateCycleStats',\r\n            stats: { matchesCreated: globalMatchesCreated }\r\n          }, () => {\r\n            if (chrome.runtime.lastError) {\r\n              console.warn('[FlirtEasy] Failed to update match stats:', chrome.runtime.lastError.message);\r\n            }\r\n          });\r\n\r\n          closeMatchModal();\r\n          await waitRandom(1000, 2000);\r\n        }\r\n\r\n        if (typeof hasLocationModal === 'function' && hasLocationModal()) {\r\n          console.log('[FlirtEasy] Location prompt modal detected during swiping cycle, auto-accepting...');\r\n          handleLocationModal();\r\n          await waitRandom(800, 1500);\r\n        }\r\n\r\n        if (hasSubscriptionPopup()) {\r\n          console.log('[FlirtEasy] Subscription popup / Out of likes detected, stopping cycle');\r\n          errors.push('Out of likes / Subscription popup');\r\n          break;\r\n        }\r\n\r\n        if (isStackEmpty()) {\r\n          console.log('[FlirtEasy] Stack is empty (no more matches), stopping cycle');\r\n          break;\r\n        }\r\n\r\n        let profileVisible = isProfileVisible();\r\n        console.log(`[FlirtEasy] Profile visible: ${profileVisible}`);\r\n\r\n        if (!profileVisible) {\r\n          console.warn(`[FlirtEasy] No profile card loaded at check ${profilesChecked} — Tinder is searching/loading recs`);\r\n          try {\r\n            chrome.runtime.sendMessage({\r\n              action: 'updateAgentState',\r\n              state: { activeSubPhase: 'lead_scan' }\r\n            });\r\n          } catch (_) {}\r\n\r\n          // Wait up to 12 seconds in 2.5s increments for candidate cards to populate\r\n          let attempts = 0;\r\n          while (!profileVisible && attempts < 5) {\r\n            await waitRandom(2000, 3000);\r\n            attempts++;\r\n            profileVisible = isProfileVisible();\r\n            if (isStackEmpty()) {\r\n              console.log('[FlirtEasy] Stack confirmed empty while waiting for profiles, stopping cycle');\r\n              break;\r\n            }\r\n          }\r\n\r\n          if (!profileVisible) {\r\n            if (isStackEmpty()) {\r\n              console.log('[FlirtEasy] No more profiles in deck, ending swipe cycle');\r\n              break;\r\n            }\r\n            console.log(`[FlirtEasy] Still no profile loaded after wait at check ${profilesChecked}, skipping click`);\r\n            errors.push(`Deck loading timeout at check ${profilesChecked}`);\r\n            continue;\r\n          }\r\n        }\r\n\r\n        // Final guard: NEVER click or count swipes if no real card is present\r\n        if (!isProfileVisible()) {\r\n          console.warn('[FlirtEasy] Strict Guard: Card not visible, skipping swipe to prevent false likes');\r\n          continue;\r\n        }\r\n\r\n        // Check age filter BEFORE liking\r\n        if (ageFilter.enabled) {\r\n          const age = getProfileAge();\r\n          console.log(`[FlirtEasy] Age filter check: age=${age}, enabled=${ageFilter.enabled}, range=${ageFilter.minAge}-${ageFilter.maxAge}`);\r\n\r\n          if (age === null) {\r\n            console.warn(`[FlirtEasy] Could not detect age, skipping profile for safety`);\r\n            const passed = clickPassButton();\r\n            if (passed) {\r\n              console.log(`[FlirtEasy] Passed profile (age unknown)`);\r\n            }\r\n            await getSwipeDelay();\r\n            continue;\r\n          }\r\n\r\n          if (age < ageFilter.minAge || age > ageFilter.maxAge) {\r\n            console.log(`[FlirtEasy] Age ${age} OUTSIDE range ${ageFilter.minAge}-${ageFilter.maxAge}, PASSING`);\r\n            const passed = clickPassButton();\r\n            if (passed) {\r\n              console.log(`[FlirtEasy] Successfully passed profile (age ${age})`);\r\n            } else {\r\n              console.warn(`[FlirtEasy] Failed to pass profile`);\r\n            }\r\n            await getSwipeDelay();\r\n            continue;\r\n          }\r\n\r\n          console.log(`[FlirtEasy] Age ${age} WITHIN range ${ageFilter.minAge}-${ageFilter.maxAge}, proceeding to like`);\r\n        }\r\n\r\n        // Check visual preferences BEFORE liking\r\n        const visualPrefs = settings.visualPreferences || { enabled: false };\r\n        if (visualPrefs.enabled && visualPrefs.likedPhotos && visualPrefs.likedPhotos.length >= 20) {\r\n          const rawPhotoUrl = extractProfilePhotoUrl();\r\n          if (rawPhotoUrl) {\r\n            console.log(`[FlirtEasy] Checking visual preferences...`);\r\n\r\n            let photoUrl = rawPhotoUrl;\r\n            if (typeof tinderImageToBase64 === 'function') {\r\n              const b64 = await tinderImageToBase64(rawPhotoUrl);\r\n              if (b64) photoUrl = b64;\r\n            }\r\n\r\n            const matchResult = await chrome.runtime.sendMessage({\r\n              action: 'analyzeVisualMatch',\r\n              photoUrl,\r\n              threshold: visualPrefs.threshold || 75\r\n            });\r\n\r\n            if (matchResult && matchResult.score < (visualPrefs.threshold || 75)) {\r\n              console.log(`[FlirtEasy] Visual match score ${matchResult.score}% below threshold ${visualPrefs.threshold}%, PASSING`);\r\n              const passed = clickPassButton();\r\n              if (passed) {\r\n                console.log(`[FlirtEasy] Successfully passed profile (visual mismatch)`);\r\n              }\r\n              await getSwipeDelay();\r\n              continue;\r\n            }\r\n\r\n            console.log(`[FlirtEasy] Visual match score ${matchResult?.score || 'N/A'}%, proceeding to like`);\r\n          }\r\n        }\r\n\r\n        // Final candidate validation before liking\r\n        if (!isProfileVisible()) {\r\n          console.warn('[FlirtEasy] Candidate disappeared before click, aborting like');\r\n          continue;\r\n        }\r\n\r\n        // STREAMING: Get candidate details for personalized feed BEFORE clicking\r\n        const profileInfo = typeof getCurrentProfile === 'function' ? getCurrentProfile() : {};\r\n        const currentAge = typeof getProfileAge === 'function' ? getProfileAge() : null;\r\n        const currentName = profileInfo.name || (typeof getSwipeCardName === 'function' ? getSwipeCardName() : getMatchName()) || 'Someone';\r\n        const profileDetail = currentAge\r\n          ? `Age ${currentAge} · Verified Profile`\r\n          : (profileInfo.bio ? profileInfo.bio.slice(0, 42).trim() : 'AI Target Match · Safe Paced');\r\n\r\n        const clicked = clickLikeButton();\r\n        console.log(`[FlirtEasy] Click result: ${clicked}`);\r\n\r\n        if (clicked) {\r\n          likesCompleted++;\r\n          console.log(`[FlirtEasy] Liked profile ${likesCompleted}/${count}`);\r\n\r\n          try {\r\n            if (window.ReactNativeWebView && window.ReactNativeWebView.postMessage) {\r\n              window.ReactNativeWebView.postMessage(JSON.stringify({\r\n                type: 'FE_SWIPE',\r\n                swipeCount: likesCompleted,\r\n                total: count,\r\n                name: currentName,\r\n                age: currentAge,\r\n                detail: profileDetail,\r\n              }));\r\n            }\r\n          } catch (_) {}\r\n\r\n          chrome.runtime.sendMessage({\r\n            action: 'updateCycleStats',\r\n            stats: { \r\n              likesCompleted, \r\n              currentName,\r\n              age: currentAge,\r\n              detail: profileDetail,\r\n            }\r\n          }, (response) => {\r\n            if (chrome.runtime.lastError) {\r\n              console.warn('[FlirtEasy] Failed to update stats:', chrome.runtime.lastError.message);\r\n            } else if (response && response.trialStatus === 'expired') {\r\n              console.log('[FlirtEasy] Trial limit reached during auto-like, stopping.');\r\n              autoLikeRunning = false;\r\n            }\r\n          });\r\n        } else {\r\n          console.warn(`[FlirtEasy] Failed to click like button`);\r\n          errors.push(`Failed to click like button at check ${profilesChecked}`);\r\n        }\r\n\r\n        await getSwipeDelay();\r\n\r\n      } catch (error) {\r\n        console.error(`[FlirtEasy] Error at check ${profilesChecked}:`, error);\r\n        errors.push(`Error at ${profilesChecked}: ${error.message}`);\r\n      }\r\n    }\r\n\r\n    console.log(`[FlirtEasy] Auto-like completed: ${likesCompleted}/${count} successful`);\r\n\r\n    // Only post FE_CYCLE_DONE if the cycle completed naturally (reached target count or stack empty),\r\n    // NEVER when aborted or interrupted by stopAutomation!\r\n    if (!isAutoLikeAborted() && (likesCompleted >= count || (typeof isStackEmpty === 'function' && isStackEmpty()))) {\r\n      try {\r\n        if (window.ReactNativeWebView && window.ReactNativeWebView.postMessage) {\r\n          window.ReactNativeWebView.postMessage(JSON.stringify({\r\n            type: 'FE_CYCLE_DONE',\r\n            count: likesCompleted,\r\n          }));\r\n        }\r\n      } catch (_) {}\r\n    }\r\n\r\n    return {\r\n      success: true,\r\n      likesCompleted,\r\n      errors\r\n    };\r\n  } finally {\r\n    autoLikeRunning = false;\r\n  }\r\n}\r\n\r\nasync function getUnreadMatches(isAborted = () => false) {\r\n  console.log('[FlirtEasy] Getting unread matches');\r\n\r\n  const pathParts = window.location.pathname.split('/');\r\n  const onSpecificMatch = pathParts.length >= 4 && pathParts[3] !== '';\r\n\r\n  if (!window.location.pathname.includes('/app/messages') || onSpecificMatch) {\r\n    console.log('[FlirtEasy] Navigating to main messages list page...');\r\n\r\n    const navigated = await navigateToMessagesPage();\r\n    if (!navigated) {\r\n      console.error('[FlirtEasy] Failed to navigate to messages page');\r\n      return { success: false, matches: [] };\r\n    }\r\n\r\n    await waitRandom(2000, 3000);\r\n  }\r\n\r\n  await waitRandom(1000, 2000);\r\n\r\n  console.log('[FlirtEasy] Checking Matches tab for new matches...');\r\n  const tabsElements = Array.from(document.querySelectorAll('button[role=\"tab\"]'));\r\n  const matchesTabButton = tabsElements.length > 0 ? tabsElements[0] : null;\r\n\r\n  // Reset passive lead deduplication for new scan\r\n  if (typeof handledTinderLeadsDuringScan !== 'undefined') {\r\n    handledTinderLeadsDuringScan.clear();\r\n  }\r\n\r\n  let newMatches = [];\r\n  if (matchesTabButton) {\r\n    matchesTabButton.click();\r\n    await waitRandom(1000, 1500);\r\n\r\n    newMatches = getNewMatchesFromGrid();\r\n    console.log(`[FlirtEasy] Found ${newMatches.length} new matches in Matches tab`);\r\n  }\r\n\r\n  console.log('[FlirtEasy] Switching to Messages tab...');\r\n  const messagesTabButton = tabsElements.length > 1 ? tabsElements[1] : null;\r\n  if (messagesTabButton) {\r\n    messagesTabButton.click();\r\n    await waitRandom(1000, 1500);\r\n  }\r\n\r\n  chrome.runtime.sendMessage({\r\n    action: 'updateCycleStats',\r\n    stats: { phase: 'scanning dates...' }\r\n  });\r\n  console.log('[FlirtEasy] Scanning messages for potential dates...');\r\n  let lastHeight = 0;\r\n  let stationaryCount = 0;\r\n  const maxStationary = 3;\r\n\r\n  // Find sidebar scroll container robustly (Tinder uses atomic CSS classes that break querySelector)\r\n  let scrollContainer = null;\r\n  try {\r\n    // Strategy 1: Find a match link and walk up to the scrollable parent\r\n    const matchLink = document.querySelector('a[href^=\"/app/messages/\"]');\r\n    if (matchLink) {\r\n      let el = matchLink.parentElement;\r\n      while (el && el !== document.body) {\r\n        if (el.scrollHeight > el.clientHeight + 50) {\r\n          scrollContainer = el;\r\n          break;\r\n        }\r\n        el = el.parentElement;\r\n      }\r\n    }\r\n    // Strategy 2: Look for the aside element's scrollable child\r\n    if (!scrollContainer) {\r\n      const aside = document.querySelector('aside');\r\n      if (aside) {\r\n        const divs = aside.querySelectorAll('div');\r\n        for (const div of divs) {\r\n          const style = window.getComputedStyle(div);\r\n          if ((style.overflowY === 'auto' || style.overflowY === 'scroll' || style.overflow === 'hidden') && div.scrollHeight > div.clientHeight + 50) {\r\n            scrollContainer = div;\r\n            break;\r\n          }\r\n        }\r\n      }\r\n    }\r\n  } catch (e) {\r\n    console.warn('[FlirtEasy] Scroll container detection failed:', e.message);\r\n  }\r\n\r\n  const matchesMap = new Map();\r\n  let globalDiscoveryIndex = 0;\r\n  let lastScrollTop = -1;\r\n\r\n  // PRODUCTION FIX: Wait for Tinder to fully load sidebar contacts before scanning.\r\n  // When Chrome is minimized, IntersectionObserver is suppressed — only ~10-15 contacts\r\n  // are rendered in the DOM. Background focuses the window first, then we poll here until\r\n  // scrollHeight grows beyond 2× clientHeight (30+ contacts) or stabilises (small list).\r\n  if (scrollContainer) {\r\n    const _needsHydration = scrollContainer.scrollHeight <= scrollContainer.clientHeight * 2;\r\n    if (_needsHydration) {\r\n      console.log(`[FlirtEasy] Tinder sidebar may not be fully loaded (${scrollContainer.scrollHeight}px vs ${scrollContainer.clientHeight}px) — waiting...`);\r\n      let _prevH = -1;\r\n      let _stableRounds = 0;\r\n      const _hydrateStart = Date.now();\r\n      while (Date.now() - _hydrateStart < 8000) {\r\n        if (isAborted()) {\r\n          console.log('[FlirtEasy] Tinder hydration wait aborted by kill-switch.');\r\n          break;\r\n        }\r\n        const _curH = scrollContainer.scrollHeight;\r\n        if (_curH > scrollContainer.clientHeight * 2) {\r\n          console.log(`[FlirtEasy] ✓ Tinder sidebar fully loaded at ${_curH}px (${Date.now() - _hydrateStart}ms)`);\r\n          break;\r\n        }\r\n        if (_curH === _prevH) {\r\n          _stableRounds++;\r\n          if (_stableRounds >= 3) {\r\n            console.log(`[FlirtEasy] ✓ Tinder sidebar stable at ${_curH}px after ${Date.now() - _hydrateStart}ms`);\r\n            break;\r\n          }\r\n        } else {\r\n          _stableRounds = 0;\r\n        }\r\n        _prevH = _curH;\r\n        await new Promise(r => setTimeout(r, 500));\r\n      }\r\n    }\r\n  }\r\n\r\n  // Initial scroll to top\r\n  if (scrollContainer) scrollContainer.scrollTop = 0;\r\n  await waitRandom(1000, 1500);\r\n\r\n  // Initial Scan\r\n  getMatchesFromList().forEach(m => {\r\n    if (m.matchId && !matchesMap.has(m.matchId)) {\r\n      m.sidebarIndex = globalDiscoveryIndex++;\r\n      matchesMap.set(m.matchId, m);\r\n    }\r\n  });\r\n\r\n  const MAX_SCAN_LIMIT = 150;\r\n\r\n  for (let i = 0; i < 40; i++) { // Increased limit for deep scans\r\n    const scrolled = await scrollMatchesList(700);\r\n\r\n    // Scan current batch\r\n    const initialSize = matchesMap.size;\r\n    getMatchesFromList().forEach(m => {\r\n      if (m.matchId && !matchesMap.has(m.matchId)) {\r\n        m.sidebarIndex = globalDiscoveryIndex++;\r\n        matchesMap.set(m.matchId, m);\r\n      }\r\n    });\r\n\r\n    // Strategy 1: If we found NEW and never-before-seen IDs, we are definitely making progress\r\n    const foundNewMatches = matchesMap.size > initialSize;\r\n    if (foundNewMatches) {\r\n      stationaryCount = 0; // Reset stationary count if we found new people\r\n    }\r\n\r\n    console.log(`[FlirtEasy] 📜 Scanning messages for potential dates: ${matchesMap.size}/${MAX_SCAN_LIMIT} found... i=${i}`);\r\n\r\n    if (foundNewMatches) {\r\n      // We found NEW matches in this scroll, but might still have more to load. \r\n      // If we hit the cap, stop. Otherwise keep going.\r\n    }\r\n\r\n    if (matchesMap.size >= MAX_SCAN_LIMIT) {\r\n      console.log(`[FlirtEasy] Scan limit reached (${MAX_SCAN_LIMIT}), stopping discovery.`);\r\n      break;\r\n    }\r\n\r\n    if (!scrolled) {\r\n      console.log(`[FlirtEasy] Discovery ended (bottom reached or scroll stationary).`);\r\n      break;\r\n    }\r\n\r\n    // Check if we hit the bottom using scrollTop progress (better for virtualized lists)\r\n    const currentScrollTop = scrollContainer ? scrollContainer.scrollTop : 0;\r\n    const currentHeight = scrollContainer ? scrollContainer.scrollHeight : 0;\r\n\r\n    // If we scrolled but found no NEW matches, we might need to wait or scroll more\r\n    // But ONLY stop if the scroll definitely didn't move AND height is constant\r\n    if (currentScrollTop === lastScrollTop && currentHeight === lastHeight) {\r\n      stationaryCount++;\r\n    } else {\r\n      stationaryCount = 0;\r\n    }\r\n\r\n    lastScrollTop = currentScrollTop;\r\n    lastHeight = currentHeight;\r\n\r\n    if (stationaryCount >= maxStationary) {\r\n      console.log(`[FlirtEasy] Sidebar scan stationary for ${maxStationary} cycles, finishing.`);\r\n      break;\r\n    }\r\n    await waitRandom(900, 1300);\r\n  }\r\n\r\n  const existingMatches = Array.from(matchesMap.values());\r\n  console.log(`[FlirtEasy] 📜 Scanning messages for potential dates: Discovered ${existingMatches.length} (Cap: ${MAX_SCAN_LIMIT})`);\r\n  console.log(`[FlirtEasy] Found ${existingMatches.length} existing conversations in Messages tab`);\r\n\r\n  const allMatches = [...newMatches, ...existingMatches.map(m => ({\r\n    ...m,\r\n    isNew: false\r\n  }))];\r\n\r\n  // Deduplicate by matchId (same match can appear in both Matches and Messages tabs)\r\n  const seenIds = new Set();\r\n  const uniqueMatches = allMatches.filter(match => {\r\n    if (seenIds.has(match.matchId)) {\r\n      console.log(`[FlirtEasy] Deduplicating match: ${match.name} (${match.matchId}) - already in list`);\r\n      return false;\r\n    }\r\n    seenIds.add(match.matchId);\r\n    return true;\r\n  });\r\n\r\n  console.log(`[FlirtEasy] Total matches to scan: ${uniqueMatches.length} (${allMatches.length - uniqueMatches.length} duplicates removed)`);\r\n\r\n  return {\r\n    success: true,\r\n    matches: uniqueMatches\r\n  };\r\n}\r\n\r\nasync function navigateToSwipingPage() {\r\n  if (window.location.pathname.includes('/app/recs')) {\r\n    console.log('[FlirtEasy] Already on explore page');\r\n    return true;\r\n  }\r\n\r\n  const swipingLinkSelectors = window.SELECTORS?.navigation?.explore || [\r\n    'a[href=\"/app/recs\"]',\r\n    'button[aria-label*=\"Explore\" i]',\r\n    'a[aria-label*=\"Explore\" i]'\r\n  ];\r\n\r\n  for (let selectorAttempt = 0; selectorAttempt < 2; selectorAttempt++) {\r\n    for (const selector of swipingLinkSelectors) {\r\n      let link = document.querySelector(selector);\r\n\r\n      if (link && link.tagName === 'svg') {\r\n        link = link.closest('a') || link.closest('button');\r\n      }\r\n\r\n      if (link) {\r\n        console.log(`[FlirtEasy] Found swiping link with selector: ${selector} (attempt ${selectorAttempt + 1})`);\r\n        link.click();\r\n\r\n        let retries = 0;\r\n        while (!window.location.pathname.includes('/app/recs') && retries < 20) {\r\n          console.log(`[FlirtEasy] Waiting for navigation to complete... (${retries + 1}/20)`);\r\n          await waitRandom(400, 700);\r\n          retries++;\r\n        }\r\n\r\n        if (window.location.pathname.includes('/app/recs')) {\r\n          console.log('[FlirtEasy] Successfully navigated to swiping page');\r\n          await waitRandom(1500, 2500);\r\n          return true;\r\n        }\r\n      }\r\n    }\r\n\r\n    if (selectorAttempt === 0) {\r\n      console.log('[FlirtEasy] First attempt failed, waiting and retrying...');\r\n      await waitRandom(2000, 3000);\r\n    }\r\n  }\r\n\r\n  logAction('Click navigation failed, will let background handle direct URL navigation', {}, 'warn');\r\n  return false;\r\n}\r\n\r\nasync function navigateToMessagesPage() {\r\n  // Check if messages list is already visible in sidebar\r\n  const hasMessagesVisible = !!document.querySelector('a[href^=\"/app/messages/\"]');\r\n  if (hasMessagesVisible || window.location.pathname.includes('/app/messages')) {\r\n    logAction('Messages list already visible or on messages path');\r\n    return true;\r\n  }\r\n\r\n  const messagesLinkSelectors = window.SELECTORS?.navigation?.messages || [\r\n    'button[aria-label*=\"Messages\" i]',\r\n    'a[aria-label*=\"Messages\" i]',\r\n    'a[href=\"/app/messages\"]'\r\n  ];\r\n\r\n  for (let selectorAttempt = 0; selectorAttempt < 2; selectorAttempt++) {\r\n    for (const selector of messagesLinkSelectors) {\r\n      let link = document.querySelector(selector);\r\n\r\n      if (link && link.tagName === 'svg') {\r\n        link = link.closest('a') || link.closest('button');\r\n      }\r\n\r\n      if (link) {\r\n        logAction(`Found messages link with selector: ${selector} (attempt ${selectorAttempt + 1})`);\r\n        link.click();\r\n\r\n        let retries = 0;\r\n        // Navigation successful if URL changes OR message links appear\r\n        while (!window.location.pathname.includes('/app/messages') &&\r\n          !document.querySelector('a[href^=\"/app/messages/\"]') &&\r\n          retries < 15) {\r\n          logAction(`Waiting for messages to appear... (${retries + 1}/15)`);\r\n          await waitRandom(400, 700);\r\n          retries++;\r\n        }\r\n\r\n        if (window.location.pathname.includes('/app/messages') || document.querySelector('a[href^=\"/app/messages/\"]')) {\r\n          logAction('Successfully reached messages view');\r\n          await waitRandom(1000, 2000);\r\n          return true;\r\n        }\r\n      }\r\n    }\r\n\r\n    if (selectorAttempt === 0) {\r\n      await waitRandom(1500, 2500);\r\n    }\r\n  }\r\n\r\n  logAction('Click navigation failed, using Recs page as fallback (messages are usually in the sidebar)...', {}, 'warn');\r\n  if (!window.location.pathname.includes('/app/recs')) {\r\n    window.location.href = 'https://tinder.com/app/recs';\r\n  }\r\n\r\n  // Poll for message links to actually appear in the sidebar — do NOT return true just\r\n  // because the URL is /app/recs. The sidebar must be confirmed populated.\r\n  let fallbackRetries = 0;\r\n  const FALLBACK_MAX_RETRIES = 20; // up to ~10s total\r\n  while (!document.querySelector('a[href^=\"/app/messages/\"]') && fallbackRetries < FALLBACK_MAX_RETRIES) {\r\n    logAction(`Fallback: waiting for message links in sidebar... (${fallbackRetries + 1}/${FALLBACK_MAX_RETRIES})`);\r\n    await waitRandom(400, 600);\r\n    fallbackRetries++;\r\n  }\r\n\r\n  if (document.querySelector('a[href^=\"/app/messages/\"]')) {\r\n    logAction('Fallback succeeded: message links visible in sidebar');\r\n    await waitRandom(500, 1000);\r\n    return true;\r\n  }\r\n\r\n  logAction('navigateToMessagesPage: all navigation strategies exhausted, no message links found', {}, 'error');\r\n  return false;\r\n}\r\n\r\nconst MESSAGING_LOG_PREFIX = '[FlirtEasy][Messaging]';\r\n\r\nfunction shortMessagingId(matchId) {\r\n  if (!matchId || typeof matchId !== 'string') return 'unknown';\r\n  return `${matchId.substring(0, 8)}***`;\r\n}\r\n\r\nfunction previewMessagingText(text, max = 80) {\r\n  if (!text || typeof text !== 'string') return '';\r\n  return text.length > max ? `${text.substring(0, max)}...` : text;\r\n}\r\n\r\nfunction humanizeMessagingDuration(ms) {\r\n  if (!Number.isFinite(ms) || ms < 0) return '0s';\r\n  if (ms < 1000) return `${ms}ms`;\r\n  const totalSec = Math.floor(ms / 1000);\r\n  const min = Math.floor(totalSec / 60);\r\n  const sec = totalSec % 60;\r\n  return min > 0 ? `${min}m ${sec}s` : `${sec}s`;\r\n}\r\n\r\nfunction logMessaging(event, data = {}, level = 'log') {\r\n  const safeLevel = (level && typeof console[level] === 'function') ? level : 'log';\r\n  const payload = (data && typeof data === 'object') ? data : { value: data };\r\n  console[safeLevel](`${MESSAGING_LOG_PREFIX} ${event}`, payload);\r\n}\r\n\r\nasync function tinderWaitForNetworkOrAbort(maxMs = 90000, networkPhase = 'messaging') {\r\n  if (navigator.onLine) return true;\r\n\r\n  const POLL_INTERVAL = 5000;\r\n  const deadline = Date.now() + maxMs;\r\n\r\n  if (!tinderNetworkOfflineReported) {\r\n    tinderNetworkOfflineReported = true;\r\n    console.warn('[FlirtEasy] 🔴 Network offline — pausing Tinder cycle, waiting up to', maxMs / 1000, 's for reconnect');\r\n    chrome.runtime.sendMessage({\r\n      action: 'updateCycleStats',\r\n      stats: { networkLost: true, phase: 'network_wait' }\r\n    });\r\n  }\r\n\r\n  while (Date.now() < deadline) {\r\n    await new Promise(resolve => setTimeout(resolve, POLL_INTERVAL));\r\n    if (navigator.onLine) {\r\n      console.log('[FlirtEasy] ✅ Network restored — resuming Tinder cycle');\r\n      tinderNetworkOfflineReported = false;\r\n      chrome.runtime.sendMessage({\r\n        action: 'updateCycleStats',\r\n        stats: { networkRestored: true, networkPhase: networkPhase, phase: networkPhase }\r\n      });\r\n      return true;\r\n    }\r\n    console.log('[FlirtEasy] 🔴 Still offline… waiting');\r\n  }\r\n\r\n  console.warn('[FlirtEasy] ⏱ Network timeout — aborting Tinder cycle');\r\n  tinderNetworkOfflineReported = false;\r\n  chrome.runtime.sendMessage({\r\n    action: 'updateCycleStats',\r\n    stats: { networkTimedOut: true, phase: 'network_wait' }\r\n  });\r\n  return false;\r\n}\r\n\r\nasync function processChats(settings, maxMessagesOverride = null) {\r\n  if (processChatsRunning) {\r\n    logAction('processChats already running, skipping duplicate request', {}, 'warn');\r\n    return { success: false, skipped: true, processed: 0, followUps: 0, errors: ['Chat processing already in progress'] };\r\n  }\r\n  window.__flirteasy_stop = false;\r\n  processChatsRunning = true;\r\n  const _myProcessChatsGen = ++_processChatsGen;\r\n  const isProcessChatsAborted = () => !processChatsRunning || _processChatsGen !== _myProcessChatsGen || window.__flirteasy_stop === true;\r\n  const startTime = Date.now();\r\n  logPhaseHeader('MESSAGING CYCLE START');\r\n\r\n  const processed = [];\r\n  const followUps = [];\r\n  const skipped = [];\r\n  const errors = [];\r\n  const stopped = [];\r\n  const processedMatchIds = new Set();\r\n  let _authErrorOccurred = false;\r\n  const _sc = settings.stopConditions || [];\r\n  const stopEnabled = _sc.length > 0 && settings.stopAfterGoalEnabled !== false;\r\n\r\n  // Get timestamp of when likes were completed (if available)\r\n  const likesCompletedAt = settings.likesCompletedAt || 0;\r\n  logAction(`Likes cycle completed: ${likesCompletedAt ? new Date(likesCompletedAt).toISOString() : 'N/A'}`);\r\n\r\n  try {\r\n    // Check if agent is still running\r\n    const stateCheck = await new Promise((resolve) => {\r\n      chrome.runtime.sendMessage({ action: 'getAgentState' }, (response) => {\r\n        resolve(response);\r\n      });\r\n    });\r\n\r\n    if (!stateCheck || !stateCheck.isRunning) {\r\n      logAction('Agent stopped, cancelling chat processing');\r\n      return { success: true, processed: 0, followUps: 0, errors: [] };\r\n    }\r\n\r\n    // Cleanup old pending messages (>24h) to prevent storage bloat\r\n    await cleanupOldPendingMessages();\r\n\r\n    // Signal scanning phase — parity with Bumble so UI shows radar/searching state\r\n    chrome.runtime.sendMessage({\r\n      action: 'updateCycleStats',\r\n      stats: { phase: 'scanning dates...', isMessagingScanning: true }\r\n    });\r\n\r\n    const matchesResult = await getUnreadMatches(isProcessChatsAborted);\r\n\r\n    // Transition to active messaging phase\r\n    chrome.runtime.sendMessage({\r\n      action: 'updateCycleStats',\r\n      stats: { phase: 'messaging', isMessagingScanning: false }\r\n    });\r\n\r\n    if (!matchesResult.success) {\r\n      logAction('Failed to retrieve matches (navigation failure)', {}, 'error');\r\n      return { success: false, processed: 0, followUps: 0, errors: ['Navigation to messages page failed'] };\r\n    }\r\n\r\n    if (matchesResult.matches.length === 0) {\r\n      logAction('No matches to process');\r\n      if (typeof injectTinderDeadStateCard === 'function') injectTinderDeadStateCard();\r\n      return { success: true, processed: 0, followUps: 0, errors: [] };\r\n    }\r\n\r\n    if (typeof removeTinderDeadStateCard === 'function') removeTinderDeadStateCard();\r\n    logAction(`Discovered matches: ${matchesResult.matches.length} profiles`);\r\n\r\n    // Filter matches based on when they were created vs when we completed likes\r\n    // Matches created AFTER likesCompletedAt are from this cycle\r\n    const newMatches = [];\r\n    const existingMatches = [];\r\n\r\n    for (const match of matchesResult.matches) {\r\n      // Check if match has metadata with creation time\r\n      const matchData = await getStoredMatchData(match.matchId);\r\n      const matchCreatedAt = matchData?.createdAt || 0;\r\n\r\n      // If match was created after we completed likes, it's new from this cycle\r\n      // OR if it's marked as isNew from Matches tab and we have a recent likes timestamp\r\n      const isNewFromThisCycle = match.isNew && likesCompletedAt > 0 && matchCreatedAt === 0;\r\n\r\n      if (isNewFromThisCycle) {\r\n        // Store creation time for future reference\r\n        await saveMatchMetadata(match.matchId, { createdAt: Date.now() });\r\n        newMatches.push(match);\r\n      } else if (match.isNew && likesCompletedAt === 0) {\r\n        // No timestamp available, trust isNew flag\r\n        await saveMatchMetadata(match.matchId, { createdAt: Date.now() });\r\n        newMatches.push(match);\r\n      } else {\r\n        existingMatches.push(match);\r\n      }\r\n    }\r\n\r\n    logAction(`Match pool split: ${newMatches.length} new / ${existingMatches.length} existing`);\r\n    logAction(`Time since last like: ${Math.round((Date.now() - likesCompletedAt) / 60000)} minutes`);\r\n\r\n    // Filter existing matches to only include those READY to message\r\n    const followupDelay = (settings.promptModes?.followup?.delay || 24) * 60 * 60 * 1000;\r\n\r\n    const readyMatches = [];\r\n    for (const match of existingMatches) {\r\n      const storedData = await getStoredMatchData(match.matchId);\r\n      const lastMessageTime = storedData?.lastMessageTimestamp || 0;\r\n      const followupCount = storedData?.followupCount || 0;\r\n      // Only calculate timeSince if we ACTUALLY know the last message time. Otherwise 0.\r\n      const timeSince = lastMessageTime > 0 ? (Date.now() - lastMessageTime) : 0;\r\n      const hardStopped = storedData?.hardStopped || false;\r\n      const pausedUntil = storedData?.pausedUntil || 0;\r\n      const isPaused = pausedUntil > Date.now();\r\n\r\n      // Match is ready if:\r\n      // 1. Has unread message (IMMEDIATE PRIORITY)\r\n      // 2. She replied last (no \"←\" prefix in snippet) — catches cases where Tinder cleared the\r\n      //    unread dot after the bot opened the conversation, but she still sent the most recent msg.\r\n      // 3. No stored history AND Tinder \"Your Move\" badge (new match, first opener needed)\r\n      //    — DO NOT use lastMessageTime===0 alone: ghost matches with no DOM signal would flood queue.\r\n      // 4. Enough time passed for follow-up, not hard-stopped, not in 1-week pause\r\n      const isFollowupReady = (lastMessageTime > 0 && !hardStopped && !isPaused && timeSince >= followupDelay && followupCount < FOLLOWUP_HARD_STOP_COUNT);\r\n      const isReady = match.hasUnread || match.sheRepliedLast || (lastMessageTime === 0 && match.isYourMove) || isFollowupReady;\r\n      if (isReady) {\r\n        match.lastScannedAt = storedData?.lastScannedAt || 0;\r\n        readyMatches.push(match);\r\n      }\r\n\r\n      // Update scan time even if not messaged so they rotate to the back of the queue\r\n      await saveMatchMetadata(match.matchId, { lastScannedAt: Date.now() });\r\n    }\r\n\r\n    // Advanced 3-Tier Recency Sort (Tinder Edition):\r\n    // TIER 1: Unread (She replied) -> sorted by RECENCY (sidebar position)\r\n    // TIER 2: Your Move (Her turn/badge) -> sorted by RECENCY\r\n    // TIER 3: Followups -> Fairness rotation + 14-day Stale Guard\r\n    const STALE_THRESHOLD_MS = 14 * 24 * 60 * 60 * 1000; // 14 days\r\n\r\n    readyMatches.sort((a, b) => {\r\n      const isUnreadA = a.hasUnread;\r\n      const isUnreadB = b.hasUnread;\r\n      const isYourMoveA = !a.hasUnread && a.isYourMove;\r\n      const isYourMoveB = !b.hasUnread && b.isYourMove;\r\n      const isFollowupA = !a.hasUnread && !a.isYourMove;\r\n      const isFollowupB = !b.hasUnread && !b.isYourMove;\r\n\r\n      // Tier 1 vs others\r\n      if (isUnreadA && !isUnreadB) return -1;\r\n      if (!isUnreadA && isUnreadB) return 1;\r\n\r\n      // Tier 2 vs Tier 3\r\n      if (isYourMoveA && isFollowupB) return -1;\r\n      if (isFollowupA && isYourMoveB) return 1;\r\n\r\n      // Within Tier 1/2: Sort by sidebar position (recency)\r\n      if ((isUnreadA && isUnreadB) || (isYourMoveA && isYourMoveB)) {\r\n        return (a.sidebarIndex || 0) - (b.sidebarIndex || 0);\r\n      }\r\n\r\n      // Tier 3: Fairness rotation (oldest scanned first)\r\n      const lastA = a.lastScannedAt || 0;\r\n      const lastB = b.lastScannedAt || 0;\r\n      return lastA - lastB;\r\n    });\r\n\r\n    const filteredReadyMatches = [];\r\n    for (const match of readyMatches) {\r\n      const isFollowup = !match.hasUnread && !match.isYourMove;\r\n      if (!isFollowup) {\r\n        filteredReadyMatches.push(match);\r\n        continue;\r\n      }\r\n\r\n      const storedData = await getStoredMatchData(match.matchId);\r\n      const lastActivity = storedData?.lastMessageTimestamp || storedData?.matchedAt || 0;\r\n      const daysSince = lastActivity > 0 ? Math.floor((Date.now() - lastActivity) / (24 * 60 * 60 * 1000)) : 0;\r\n\r\n      if (lastActivity > 0 && daysSince > 90) {\r\n        logAction(`Tinder Stale Guard: Skipping ${match.name} (no activity in ${daysSince}d)`);\r\n        skipped.push({ name: match.name, reason: `Stale (${daysSince}d)` });\r\n        continue;\r\n      }\r\n\r\n      const timeSinceScanned = Date.now() - (match.lastScannedAt || 0);\r\n      if (timeSinceScanned > STALE_THRESHOLD_MS) {\r\n        logAction(`Tinder Stale Guard: Skipping ${match.name} (no activity in ${Math.round(timeSinceScanned / 86400000)}d)`);\r\n        skipped.push({ name: match.name, reason: `Stale (scanned ${Math.round(timeSinceScanned / 86400000)}d ago)` });\r\n        continue;\r\n      }\r\n      filteredReadyMatches.push(match);\r\n    }\r\n\r\n    logAction(`Ready for messaging: ${filteredReadyMatches.length}/${existingMatches.length} (Unread: ${filteredReadyMatches.filter(m => m.hasUnread).length})`);\r\n\r\n    // HYBRID PRIORITY SYSTEM (with percentage-based allocation)\r\n    const maxMessages = maxMessagesOverride !== null ? maxMessagesOverride : (settings.messagesPerCycle || 50);\r\n    const minReplyPercent = settings.minReplyPercent || 30;\r\n    const maxNewMatchPercent = settings.maxNewMatchPercent || 70;\r\n\r\n    // Calculate actual slots from percentages\r\n    const minReplySlots = Math.floor(maxMessages * (minReplyPercent / 100));\r\n    const maxNewMatchSlots = Math.floor(maxMessages * (maxNewMatchPercent / 100));\r\n\r\n    logAction('Messaging allocation:', { maxMessages, minReplySlots, maxNewMatchSlots });\r\n\r\n    let matchesToProcess = [];\r\n\r\n    // Pool is built up to maxMessages * 2 so skipped conversations have backups.\r\n    // The loop below enforces the real limit by counting only actual sends.\r\n    const poolSize = maxMessages * 2;\r\n\r\n    // PHASE 1: New matches first (bot always sends first = establishes bot ID)\r\n    // Cap at percentage limit only — overall quota enforced in the send loop\r\n    const newMatchSlots = Math.min(newMatches.length, maxNewMatchSlots);\r\n    for (let i = 0; i < newMatchSlots; i++) {\r\n      matchesToProcess.push(newMatches[i]);\r\n    }\r\n    logAction(`Phase 1: Added ${newMatchSlots} new matches`);\r\n\r\n    // PHASE 2: Guaranteed replies to old matches (minimum) - USE READY MATCHES\r\n    const guaranteedReplies = Math.min(filteredReadyMatches.length, minReplySlots);\r\n    for (let i = 0; i < guaranteedReplies; i++) {\r\n      matchesToProcess.push(filteredReadyMatches[i]);\r\n    }\r\n    logAction(`Phase 2: Added ${guaranteedReplies} guaranteed replies`);\r\n\r\n    // PHASE 3 & 4: Fill remaining pool slots while respecting percentage caps\r\n    let bonusNewMatches = 0;\r\n    let additionalReplies = 0;\r\n    let remainingSlots = poolSize - matchesToProcess.length;\r\n\r\n    while (remainingSlots > 0) {\r\n      const currentNew = newMatchSlots + bonusNewMatches;\r\n      const currentReplies = guaranteedReplies + additionalReplies;\r\n\r\n      const hasMoreNew = (newMatchSlots + bonusNewMatches) < newMatches.length;\r\n      const hasMoreReplies = (guaranteedReplies + additionalReplies) < filteredReadyMatches.length;\r\n\r\n      const newMatchCapReached = currentNew >= maxNewMatchSlots;\r\n      const replyMinMet = currentReplies >= minReplySlots;\r\n\r\n      let added = false;\r\n\r\n      // Priority 1: Fill new matches up to percentage cap\r\n      if (hasMoreNew && !newMatchCapReached) {\r\n        matchesToProcess.push(newMatches[newMatchSlots + bonusNewMatches]);\r\n        bonusNewMatches++;\r\n        added = true;\r\n      }\r\n      // Priority 2: Fill replies if available\r\n      else if (hasMoreReplies) {\r\n        matchesToProcess.push(filteredReadyMatches[guaranteedReplies + additionalReplies]);\r\n        additionalReplies++;\r\n        added = true;\r\n      }\r\n      // Priority 3: Both pools exhausted or capped\r\n      else if (hasMoreNew && newMatchCapReached && replyMinMet) {\r\n        break;\r\n      }\r\n\r\n      if (!added) break;\r\n      remainingSlots--;\r\n    }\r\n\r\n    logAction(`Phase 3: Added ${bonusNewMatches} bonus new matches`);\r\n    logAction(`Phase 4: Added ${additionalReplies} additional replies`);\r\n\r\n    logAction(`Final processing pool: ${matchesToProcess.length} matches`);\r\n    \r\n    currentMatchIndex = 0;\r\n    totalMatchesToProcess = matchesToProcess.length;\r\n\r\n    for (const match of matchesToProcess) {\r\n      // ── Local volatile kill-switch (instant abort) ──\r\n      if (isProcessChatsAborted()) {\r\n        logAction('Kill-switch active - aborting messaging session');\r\n        break;\r\n      }\r\n\r\n      // Stop when quota of actual sends is reached — skips do NOT count against this\r\n      if (processed.length + followUps.length >= maxMessages) {\r\n        logAction(`Send quota reached (${maxMessages}) - ending session`);\r\n        break;\r\n      }\r\n\r\n      // Check if agent is still running before each match\r\n      const runCheck = await new Promise((resolve) => {\r\n        chrome.runtime.sendMessage({ action: 'getAgentState' }, (response) => {\r\n          resolve(response);\r\n        });\r\n      });\r\n\r\n      if (!runCheck || !runCheck.isRunning) {\r\n        logAction('Agent manually stopped - cancelling session');\r\n        break;\r\n      }\r\n\r\n      // Network guard — pause up to 90s before each contact\r\n      if (!navigator.onLine) {\r\n        const recovered = await tinderWaitForNetworkOrAbort(90000);\r\n        if (!recovered) {\r\n          logAction('Network timeout — aborting Tinder messaging cycle');\r\n          break;\r\n        }\r\n      }\r\n\r\n      try {\r\n        currentMatchIndex++;\r\n        logMatchHeader(match.name, currentMatchIndex, totalMatchesToProcess);\r\n\r\n        // Push early event so the status bar updates immediately when we start on this contact\r\n        // (Parity with Bumble — eliminates the \"drafting\" label delay)\r\n        // Also resets draftingStep so any stale step from a skipped previous match is cleared.\r\n        chrome.runtime.sendMessage({\r\n          action: 'updateCycleStats',\r\n          stats: { phase: 'messaging', currentName: match.name || 'Match', draftingStep: '' }\r\n        });\r\n        window.dispatchEvent(new CustomEvent('fe:draftingStep', { detail: { step: '' } }));\r\n        try {\r\n          const _pfStore = await chrome.storage.local.get('progressFeedEvents');\r\n          const _pfEvents = _pfStore.progressFeedEvents || [];\r\n          _pfEvents.unshift({ type: 'contact_started', name: match.name, timestamp: Date.now() });\r\n          if (_pfEvents.length > 50) _pfEvents.length = 50;\r\n          await chrome.storage.local.set({ progressFeedEvents: _pfEvents });\r\n        } catch (_) {}\r\n\r\n        let matchId = match.matchId;\r\n\r\n        if (processedMatchIds.has(matchId)) {\r\n          logAction(`Already processed ${match.name} in this cycle, skipping`);\r\n          continue;\r\n        }\r\n\r\n        if (match.isNew) {\r\n          logAction(`Navigating to new match from Matches grid...`);\r\n\r\n          const tabsElements = Array.from(document.querySelectorAll('button[role=\"tab\"]'));\r\n          const matchesTabButton = tabsElements.length > 0 ? tabsElements[0] : null;\r\n          if (matchesTabButton) {\r\n            matchesTabButton.click();\r\n            await waitRandom(1000, 1500);\r\n          }\r\n\r\n          const matchCards = getNewMatchesFromGrid();\r\n\r\n          // Prefer matchId-based lookup (robust), fall back to name match\r\n          let targetCard = match.matchId\r\n            ? matchCards.find(m => m.matchId === match.matchId)\r\n            : null;\r\n          if (!targetCard) {\r\n            targetCard = matchCards.find(m => m.name === match.name);\r\n          }\r\n\r\n          if (!targetCard || !targetCard.element) {\r\n            logAction(`Could not find card for ${match.name}`, {}, 'error');\r\n            errors.push(`${match.name}: Card not found`);\r\n            continue;\r\n          }\r\n\r\n          // If we already know the matchId from the card href, navigate directly\r\n          if (targetCard.matchId) {\r\n            matchId = targetCard.matchId;\r\n            const directLink = document.querySelector(`a[href=\"/app/messages/${matchId}\"]`);\r\n            if (directLink) {\r\n              directLink.click();\r\n            } else {\r\n              targetCard.element.click();\r\n            }\r\n          } else {\r\n            targetCard.element.click();\r\n          }\r\n\r\n          await waitRandom(2000, 3000);\r\n\r\n          const currentPath = window.location.pathname;\r\n          const pathParts = currentPath.split('/');\r\n          if (!matchId) {\r\n            matchId = pathParts.length >= 4 && currentPath.includes('/app/messages/') ? pathParts[3] : null;\r\n          }\r\n\r\n          if (!matchId || !currentPath.includes('/app/messages/')) {\r\n            logAction(`Failed to extract matchId for ${match.name}, wrong page: ${currentPath}`, {}, 'error');\r\n            errors.push(`${match.name}: Navigation failed`);\r\n            continue;\r\n          }\r\n\r\n          logAction(`Navigated to new match, id: ${matchId.substring(0, 8)}...`);\r\n\r\n          const { isStopped: isStoppedNew, reason: stopReasonNew } = await checkIfChatStopped(matchId);\r\n          if (isStoppedNew && (stopReasonNew === 'Manually stopped by user' || stopEnabled)) {\r\n            logAction(`Match is blocked, skipping`);\r\n            stopped.push(match.name);\r\n            continue;\r\n          }\r\n        } else {\r\n          const { isStopped: isStoppedAlready, reason: stopReasonAlready } = await checkIfChatStopped(matchId);\r\n          if (isStoppedAlready && (stopReasonAlready === 'Manually stopped by user' || stopEnabled)) {\r\n            logAction(`Match is already blocked, skipping`);\r\n            stopped.push(match.name);\r\n            continue;\r\n          }\r\n\r\n          const navigated = await navigateToMatchWithRetry(matchId, match.name);\r\n          if (!navigated) {\r\n            logAction(`Could not navigate to ${match.name}`, {}, 'warn');\r\n            errors.push(`${match.name}: Navigation failed`);\r\n            continue;\r\n          }\r\n        }\r\n\r\n        logAction(`Waiting for conversation thread load...`);\r\n\r\n        await waitForTinderChatReady(matchId);\r\n\r\n        chrome.runtime.sendMessage({\r\n          action: 'updateCycleStats',\r\n          stats: { phase: `Checking: ${match.name}`, currentName: match.name || 'Match', draftingStep: 'reading' }\r\n        });\r\n        window.dispatchEvent(new CustomEvent('fe:draftingStep', { detail: { step: 'reading' } }));\r\n\r\n        // ── DISTANCE FILTER ──\r\n        if (settings.distanceFilter?.enabled) {\r\n          const maxDistance = settings.distanceFilter.maxDistance || 50;\r\n          const distance = typeof getMatchDistanceKm === 'function' ? getMatchDistanceKm() : null;\r\n          \r\n          if (distance !== null && distance > maxDistance) {\r\n            logAction(`Distance filter: Skipping ${match.name} (${distance} km away exceeds limit of ${maxDistance} km)`);\r\n            skipped.push(match.name);\r\n            processedMatchIds.add(matchId);\r\n            \r\n            chrome.runtime.sendMessage({\r\n              action: 'updateCycleStats',\r\n              stats: { skippedMessages: skipped.length, currentName: match.name || 'Match' }\r\n            });\r\n            \r\n            // Push progress feed event for transparency\r\n            chrome.runtime.sendMessage({\r\n              action: 'progressFeedUpdate',\r\n              event: {\r\n                type: 'rate_limit',\r\n                timestamp: Date.now(),\r\n                name: 'tinder',\r\n                detail: `Skipped ${match.name}: Match is ${distance} km away (limit: ${maxDistance} km)`\r\n              }\r\n            }).catch(() => {});\r\n            \r\n            continue; // Skip messaging this contact\r\n          }\r\n        }\r\n\r\n        const result = await getConversationHistory();\r\n        let conversationHistory = result.messages || result;\r\n        const apiMessages = result.apiMessages || [];\r\n\r\n        logAction(`Thread loaded: ${conversationHistory.length} messages found`);\r\n        if (conversationHistory.length > 0) {\r\n          logAction(`Recent activity snippet detected`);\r\n        }\r\n\r\n        await saveMatchMetadata(matchId, { conversationHistory: conversationHistory.slice(-100) });\r\n\r\n        // ── DEEP LEAD SCANNER (Tinder) ──\r\n        // Scans ALL messages (not just sidebar snippet) for phone/social shared by the MATCH\r\n        if (conversationHistory.length > 0 && matchId) {\r\n          const { isStopped: isStoppedForLead, reason: stopReasonForLead } = await checkIfChatStopped(matchId);\r\n          const shouldSkipLeadScan = isStoppedForLead && (stopReasonForLead === 'Manually stopped by user' || stopEnabled);\r\n          if (!shouldSkipLeadScan) {\r\n            let deepLeadType = null;\r\n            let deepLeadSnippet = '';\r\n\r\n            for (const msg of conversationHistory) {\r\n              if (msg.sender !== 'match') continue;\r\n              const text = (msg.text || '').toLowerCase();\r\n\r\n              const _ph = text.match(/(\\+?[\\d][\\d\\s\\-\\(\\).]{5,}[\\d])/);\r\n              if (_ph) {\r\n                const _digits = _ph[0].replace(/\\D/g, '');\r\n                const _ctx = text.substring(Math.max(0, _ph.index - 25), Math.min(text.length, _ph.index + _ph[0].length + 25));\r\n                const _isDate = /\\b\\d{1,2}[\\s\\/\\-]\\d{1,2}[\\s\\/\\-]\\d{2,4}\\b/.test(_ph[0]);\r\n                const _isCurrency = /\\b(?:dollars?|euros?|pounds?|yen|millions?|billions?|thousands?)\\b/.test(_ctx);\r\n                if (_digits.length >= 9 && !_isDate && !_isCurrency) {\r\n                  deepLeadType = 'phone'; deepLeadSnippet = msg.text;\r\n                }\r\n              }\r\n              if (!deepLeadType && text.match(/(?:whatsapp|wa\\b)/i)) {\r\n                const _wa = text.match(/(\\+?[\\d][\\d\\s\\-]{5,}[\\d])/);\r\n                if (_wa && _wa[0].replace(/\\D/g, '').length >= 7) {\r\n                  deepLeadType = 'phone'; deepLeadSnippet = msg.text;\r\n                }\r\n              }\r\n              if (!deepLeadType) {\r\n                if (\r\n                  text.match(/(?:instagram|snapchat|telegram)\\s*:\\s*@?([a-z0-9._]{4,})/i) ||\r\n                  text.match(/(?:instagram|snapchat|telegram)\\s+@([a-z0-9._]{4,})/i) ||\r\n                  text.match(/\\b(?:ig|snap|insta|tg)\\s*:\\s*@?([a-z0-9._]{4,})/i) ||\r\n                  text.match(/\\b(?:ig|snap|insta|tg)\\s+@([a-z0-9._]{4,})/i) ||\r\n                  text.match(/(?<!\\w)@([a-z][a-z0-9._]{4,})/i)\r\n                ) {\r\n                  deepLeadType = 'social'; deepLeadSnippet = msg.text;\r\n                }\r\n              }\r\n              if (deepLeadType) break;\r\n            }\r\n\r\n            if (deepLeadType) {\r\n              logAction(`Lead detected (${deepLeadType}): \"${deepLeadSnippet.substring(0, 40)}...\"`);\r\n\r\n              if (stopEnabled) {\r\n                const rawPhoto = extractProfilePhotoUrl();\r\n                const permanentPhoto = typeof tinderImageToBase64 === 'function' ? await tinderImageToBase64(rawPhoto) : rawPhoto;\r\n                await markChatAsStopped(matchId, deepLeadType, match.name, permanentPhoto, deepLeadSnippet);\r\n                if (typeof UIAlerts !== 'undefined') UIAlerts.showHandoff(match.name, deepLeadType);\r\n                stopped.push(match.name);\r\n                processedMatchIds.add(matchId);\r\n                continue;\r\n              } else {\r\n                logAction(`Lead detected but \"Stop After Goal\" is off - continuing`);\r\n              }\r\n            } else {\r\n              logAction(`Conversation analyzed - no lead found`);\r\n            }\r\n          }\r\n        }\r\n\r\n        const stopCheck = stopEnabled ? (await sendMessageToBackground({\r\n          action: 'checkStopCondition',\r\n          conversationHistory,\r\n          stopConditions: settings.stopConditions\r\n        }) || { shouldStop: false }) : { shouldStop: false };\r\n\r\n        if (stopEnabled && stopCheck.shouldStop) {\r\n          logAction(`Goal reached: ${stopCheck.reason}`);\r\n\r\n          // PREMIUM HANDOFF ALERT (Time to Shine ✨)\r\n          if (typeof UIAlerts !== 'undefined') {\r\n            UIAlerts.showHandoff(match.name, stopCheck.reason);\r\n          }\r\n\r\n          const rawPhoto = extractProfilePhotoUrl();\r\n          const permanentPhoto = typeof tinderImageToBase64 === 'function' ? await tinderImageToBase64(rawPhoto) : rawPhoto;\r\n\r\n          await markChatAsStopped(matchId, stopCheck.reason, match.name, permanentPhoto);\r\n          stopped.push(match.name);\r\n          processedMatchIds.add(matchId);\r\n          continue;\r\n        }\r\n\r\n        const hasMessages = conversationHistory.length > 0;\r\n        let userWasLast = hasMessages ? (conversationHistory[conversationHistory.length - 1].sender === 'user') : false;\r\n\r\n        let pendingMessage = await getPendingMessage(matchId);\r\n        if (pendingMessage) {\r\n          const pendingAge = Date.now() - pendingMessage.timestamp;\r\n          const PENDING_TIMEOUT = 10 * 60 * 1000; // 10 minutes\r\n\r\n          logAction(`Recent pending message found (${Math.round(pendingAge / 1000)}s old)`);\r\n\r\n          // If pending is very recent (< 10 min), always trust it to avoid double messaging\r\n          if (pendingAge < PENDING_TIMEOUT) {\r\n            const pendingInAPI = apiMessages.some(msg =>\r\n              msg.sender === 'user' && msg.text === pendingMessage.text\r\n            );\r\n\r\n            if (pendingInAPI) {\r\n              logAction(`Confirmed pending message in API, clearing status`);\r\n              await clearPendingMessage(matchId);\r\n              pendingMessage = null;\r\n              const freshResult = await getConversationHistory();\r\n              conversationHistory = freshResult.messages || freshResult;\r\n              userWasLast = conversationHistory.length > 0 ? (conversationHistory[conversationHistory.length - 1].sender === 'user') : false;\r\n            } else {\r\n              // Pending exists but not in API yet - check if match replied with text\r\n              const lastMessage = hasMessages ? conversationHistory[conversationHistory.length - 1] : null;\r\n\r\n              const matchRepliedWithText = hasMessages &&\r\n                !!lastMessage &&\r\n                lastMessage.sender === 'match' &&\r\n                (lastMessage.text || '').trim().length > 0;\r\n\r\n              if (matchRepliedWithText) {\r\n                logAction(`Detected match reply, clearing pending status`);\r\n                await clearPendingMessage(matchId);\r\n                pendingMessage = null;\r\n                const freshResult = await getConversationHistory();\r\n                conversationHistory = freshResult.messages || freshResult;\r\n                userWasLast = conversationHistory.length > 0 ? (conversationHistory[conversationHistory.length - 1].sender === 'user') : false;\r\n                logAction(`Reloaded after match reply for ${match.name}, userWasLast=${userWasLast}`);\r\n              } else {\r\n                logAction(`Recent pending active - skipping to avoid double messaging`);\r\n                userWasLast = true;\r\n              }\r\n            }\r\n          } else {\r\n            // Pending is old (> 10 min), clear it and re-evaluate who was last\r\n            logAction(`Stale pending message (${Math.round(pendingAge / 60000)}min old), clearing for ${match.name}`);\r\n\r\n            await clearPendingMessage(matchId);\r\n            pendingMessage = null;\r\n            \r\n            // Re-evaluate userWasLast purely from history now that pending is gone\r\n            userWasLast = hasMessages ? (conversationHistory[conversationHistory.length - 1].sender === 'user') : false;\r\n            logAction(`Re-evaluated userWasLast correctly: ${userWasLast}`);\r\n          }\r\n        }\r\n\r\n        logAction(`State check: userWasLast=${userWasLast}, historyCount=${conversationHistory.length}, hasPending=${!!pendingMessage}`);\r\n\r\n        // ── FINAL ATTRIBUTION SHIELD (Tinder) ──\r\n        // If user was last AND user shared contact info, stop the chat (shielded)\r\n        if (userWasLast && hasMessages) {\r\n          const lastMsg = conversationHistory[conversationHistory.length - 1];\r\n          const text = (lastMsg.text || '').toLowerCase();\r\n          const hasLeadPattern = text.match(/(\\+?\\d[\\d\\s\\-\\(\\).]{8,}\\d)/) ||\r\n            text.match(/(?:ig|insta|instagram|snap|snapchat|telegram|tg)[:\\s]+@?([a-z0-9._]{3,})/i) ||\r\n            (text.match(/(?:whatsapp|wa\\b)/i) && text.match(/\\+?\\d[\\d\\s\\-]{6,}/)) ||\r\n            text.match(/@[a-z0-9._]{3,}/i);\r\n\r\n          if (hasLeadPattern) {\r\n            logAction(`SHIELD: User sent contact info - marking as stopped`);\r\n\r\n            const rawPhoto = extractProfilePhotoUrl();\r\n            const permanentPhoto = typeof tinderImageToBase64 === 'function' ? await tinderImageToBase64(rawPhoto) : rawPhoto;\r\n\r\n            await markChatAsStopped(matchId, '🚫 SHIELD (User Lead)', match.name, permanentPhoto);\r\n            stopped.push(match.name);\r\n            processedMatchIds.add(matchId);\r\n            continue;\r\n          }\r\n        }\r\n\r\n        // Check if this is a follow-up scenario (user sent last message, no reply after delay)\r\n        const followupDelay = (settings.promptModes?.followup?.delay || 24) * 60 * 60 * 1000; // Convert hours to ms\r\n\r\n        if (userWasLast && hasMessages) {\r\n          const lastMessage = conversationHistory[conversationHistory.length - 1];\r\n          const timeSinceLastMessage = Date.now() - (lastMessage.timestamp || 0);\r\n\r\n          // Get follow-up count from storage\r\n          const storedData = await getStoredMatchData(matchId);\r\n          const followupCount = storedData?.followupCount || 0;\r\n          const hardStopped = storedData?.hardStopped || false;\r\n          const pausedUntil = storedData?.pausedUntil || 0;\r\n          const isPaused = pausedUntil > Date.now();\r\n\r\n          logAction(`Follow-up check: wait=${Math.round(timeSinceLastMessage / 3600000)}h, count=${followupCount}, hardStopped=${hardStopped}`);\r\n\r\n          if (hardStopped) {\r\n            logAction(`Match permanently stopped (follow-up limit)`);\r\n          } else if (isPaused) {\r\n            logAction(`Match paused (soft stop active)`);\r\n          } else if (timeSinceLastMessage >= followupDelay && followupCount < FOLLOWUP_HARD_STOP_COUNT) {\r\n            logAction(`Match qualifies for follow-up (${followupCount + 1}/${FOLLOWUP_HARD_STOP_COUNT})`);\r\n\r\n            const profile = parseCurrentProfile();\r\n            const messageData = { ...profile, conversationHistory: conversationHistory };\r\n\r\n            // ── Kill-switch check before expensive AI call ──\r\n            if (isProcessChatsAborted()) {\r\n              logAction('Kill-switch active - discarding generation');\r\n              break;\r\n            }\r\n\r\n            const followupResult = await generateAndSendMessage(messageData, settings, matchId, true, isProcessChatsAborted);\r\n            if (followupResult.success) {\r\n              followUps.push(match.name);\r\n              processedMatchIds.add(matchId);\r\n\r\n              const newCount = followupCount + 1;\r\n              const metaUpdate = { followupCount: newCount, lastFollowupTimestamp: Date.now() };\r\n              if (newCount >= FOLLOWUP_HARD_STOP_COUNT) {\r\n                metaUpdate.hardStopped = true;\r\n                logAction(`${match.name} reached ${FOLLOWUP_HARD_STOP_COUNT} unanswered messages — hard stopped permanently`);\r\n              } else if (newCount >= FOLLOWUP_SOFT_STOP_COUNT) {\r\n                metaUpdate.pausedUntil = Date.now() + FOLLOWUP_PAUSE_DURATION;\r\n                logAction(`${match.name} reached ${FOLLOWUP_SOFT_STOP_COUNT} unanswered messages — pausing for 1 week`);\r\n              }\r\n              await saveMatchMetadata(matchId, metaUpdate);\r\n\r\n              await savePendingMessage(matchId, followupResult.message, Date.now());\r\n\r\n              chrome.runtime.sendMessage({\r\n                action: 'updateCycleStats',\r\n                stats: { followUpsSent: followUps.length, currentName: match.name || 'Match' }\r\n              });\r\n\r\n              if (followupResult.isTrialLimit) {\r\n                logAction('Trial limit reached (final message sent), stopping Chat Processing.');\r\n                break;\r\n              }\r\n            } else if (followupResult.networkOffline) {\r\n              logAction(`Network offline after follow-up send for ${match.name} — waiting for reconnect`);\r\n              const recovered = await tinderWaitForNetworkOrAbort(90000);\r\n              if (!recovered) break;\r\n            } else {\r\n              errors.push(`${match.name}: Follow-up failed - ${followupResult.error}`);\r\n\r\n              chrome.runtime.sendMessage({\r\n                action: 'updateCycleStats',\r\n                stats: { errors }\r\n              });\r\n\r\n              if (followupResult.isAuthError) {\r\n                logAction('Session expired — stopping Chat Processing.');\r\n                _authErrorOccurred = true;\r\n                break;\r\n              }\r\n              if (followupResult.isTrialLimit) {\r\n                logAction('Trial limit reached during follow-up, stopping Chat Processing.');\r\n                break;\r\n              }\r\n            }\r\n            continue;\r\n          } else {\r\n            logAction(`${match.name} not ready for follow-up yet (need ${Math.round((followupDelay - timeSinceLastMessage) / 3600000)}h more)`);\r\n          }\r\n\r\n          skipped.push(match.name);\r\n          processedMatchIds.add(matchId);\r\n\r\n          chrome.runtime.sendMessage({\r\n            action: 'updateCycleStats',\r\n            stats: { skippedMessages: skipped.length, currentName: match.name || 'Match' }\r\n          });\r\n\r\n          continue;\r\n        }\r\n\r\n        if (!hasMessages) {\r\n          logAction(`AI: Generating opening message...`);\r\n\r\n          const profile = parseCurrentProfile();\r\n          const messageData = { ...profile, id: matchId, conversationHistory: [] };\r\n\r\n          // Detect language for new match (check stored language)\r\n          const storedLangNew = await new Promise(resolve => {\r\n            chrome.runtime.sendMessage({ action: 'getMatchLanguage', matchId }, r => {\r\n              if (chrome.runtime.lastError) { resolve(null); return; }\r\n              resolve(r?.language);\r\n            });\r\n          });\r\n          if (storedLangNew && storedLangNew.source === 'manual') {\r\n            messageData.detectedLanguage = { code: storedLangNew.code, name: storedLangNew.name };\r\n            logAction(`AI: Using manual language override: ${storedLangNew.name}`);\r\n          }\r\n\r\n          chrome.runtime.sendMessage({ action: 'saveLastProfileData', profileData: profile });\r\n\r\n          // ── Kill-switch check before expensive AI call ──\r\n          if (isProcessChatsAborted()) {\r\n            logAction('Kill-switch active - discarding generation');\r\n            break;\r\n          }\r\n\r\n          const result = await generateAndSendMessage(messageData, settings, matchId, false, isProcessChatsAborted);\r\n          if (result.success) {\r\n            processed.push(match.name);\r\n            processedMatchIds.add(matchId);\r\n\r\n            await savePendingMessage(matchId, result.message, Date.now());\r\n\r\n            // Achievement tracking removed from here - handled by generateAndSendMessage\r\n\r\n            chrome.runtime.sendMessage({\r\n              action: 'updateCycleStats',\r\n              stats: { messagesProcessed: processed.length, currentName: match.name || 'Match', currentMessage: (result.message || '').trim() }\r\n            });\r\n\r\n            if (result.isTrialLimit) {\r\n              logAction('Trial limit reached (final message sent), stopping Chat Processing.');\r\n              break;\r\n            }\r\n          } else if (result.networkOffline) {\r\n            logAction(`Network offline after opening send for ${match.name} — waiting for reconnect`);\r\n            const recovered = await tinderWaitForNetworkOrAbort(90000);\r\n            if (!recovered) break;\r\n          } else {\r\n            errors.push(`${match.name}: Opening message failed - ${result.error}`);\r\n\r\n            chrome.runtime.sendMessage({\r\n              action: 'updateCycleStats',\r\n              stats: { errors }\r\n            });\r\n\r\n            if (result.isAuthError) {\r\n              logAction('Session expired — stopping Chat Processing.');\r\n              _authErrorOccurred = true;\r\n              break;\r\n            }\r\n            if (result.isTrialLimit) {\r\n              logAction('Trial limit reached during opening message, stopping Chat Processing.');\r\n              break;\r\n            }\r\n            if (result.isRateLimit) {\r\n              logAction('Rate limit reached during opening message, stopping Chat Processing.');\r\n              chrome.runtime.sendMessage({ action: 'messagingRateLimitReached' }).catch(() => {});\r\n              break;\r\n            }\r\n          }\r\n          continue;\r\n        }\r\n\r\n        logAction(`AI: Generating reply...`);\r\n\r\n        // ── Move Off App State Machine ──\r\n        // Handles Telegram/Instagram/Tango goal escalation with intent classification\r\n        const _anyMoveOffGoal = (settings.stopConditions || []).some(g => g.startsWith('move_to_'));\r\n        const _stopAfterGoal  = settings.stopAfterGoalEnabled !== false;\r\n        if (_anyMoveOffGoal) {\r\n          // Build priority list of platforms the user has configured\r\n          const _cd = settings.contactDetails || {};\r\n          const _goalPlatformMap = { move_to_telegram: 'telegram', move_to_instagram: 'instagram', move_to_tango: 'tango' };\r\n          const _priorityPlatforms = (settings.stopConditions || [])\r\n            .filter(g => _goalPlatformMap[g])\r\n            .map(g => _goalPlatformMap[g])\r\n            .filter(p => _cd[p]?.enabled && _cd[p]?.value?.trim());\r\n\r\n          if (_priorityPlatforms.length > 0) {\r\n            // Get current state for this match\r\n            const _moveState = await new Promise(resolve => {\r\n              chrome.runtime.sendMessage({ action: 'getMoveOffAppState', matchId }, r => resolve(r?.state || { state: 'idle', offeredPlatforms: [], persuasionCount: 0, lastOfferedPlatform: null }));\r\n            });\r\n\r\n            // Find if any handle was shared and match replied after\r\n            let _lastOfferedHandle = null;\r\n            let _lastOfferedPlatform = null;\r\n            let _matchReplyAfterOffer = null;\r\n\r\n            for (const platform of _priorityPlatforms) {\r\n              const handle = _cd[platform]?.value?.trim();\r\n              if (!handle) continue;\r\n              const handleIdx = conversationHistory.findLastIndex(\r\n                m => m.sender === 'user' && m.text && m.text.includes(handle)\r\n              );\r\n              if (handleIdx >= 0) {\r\n                const repliesAfter = conversationHistory.slice(handleIdx + 1).filter(m => m.sender !== 'user');\r\n                if (repliesAfter.length > 0) {\r\n                  _lastOfferedHandle = handle;\r\n                  _lastOfferedPlatform = platform;\r\n                  _matchReplyAfterOffer = repliesAfter[repliesAfter.length - 1]?.text || '';\r\n                  break;\r\n                }\r\n              }\r\n            }\r\n\r\n            if (_lastOfferedHandle && _matchReplyAfterOffer) {\r\n              // Classify the match's intent\r\n              const _intentResult = await new Promise(resolve => {\r\n                chrome.runtime.sendMessage({\r\n                  action: 'classifyMoveOffAppIntent',\r\n                  matchReply: _matchReplyAfterOffer,\r\n                  offeredPlatform: _lastOfferedPlatform,\r\n                  settings\r\n                }, r => resolve(r?.intent || 'NEUTRAL'));\r\n              });\r\n\r\n              logAction(`AI: Move-off-app intent for ${match.name}: ${_intentResult} (${_lastOfferedPlatform})`);\r\n\r\n              if (_intentResult === 'ACCEPTED') {\r\n                // Mission accomplished — stop chat\r\n                if (_stopAfterGoal) {\r\n                  const rawPhoto = extractProfilePhotoUrl?.();\r\n                  const permanentPhoto = typeof tinderImageToBase64 === 'function' && rawPhoto\r\n                    ? await tinderImageToBase64(rawPhoto).catch(() => rawPhoto) : rawPhoto;\r\n                  await markChatAsStopped(matchId, `${_lastOfferedPlatform}_goal_complete`, match.name, permanentPhoto);\r\n                  chrome.runtime.sendMessage({ action: 'resetMoveOffAppState', matchId });\r\n                  stopped.push(match.name);\r\n                  processedMatchIds.add(matchId);\r\n                  continue;\r\n                }\r\n              } else if (_intentResult === 'REJECTED') {\r\n                const maxPersuasion = settings.moveOffAppMaxPersuasion ?? 2;\r\n                const offeredPlatforms = _moveState.offeredPlatforms || [];\r\n\r\n                // Mark this platform as tried\r\n                if (!offeredPlatforms.includes(_lastOfferedPlatform)) {\r\n                  offeredPlatforms.push(_lastOfferedPlatform);\r\n                }\r\n\r\n                // Find next untried platform\r\n                const nextPlatform = _priorityPlatforms.find(p => !offeredPlatforms.includes(p));\r\n\r\n                if (nextPlatform) {\r\n                  // Escalate to next platform — update state, let AI offer it\r\n                  const nextPlatformState = { state: `offering_${nextPlatform}`, offeredPlatforms, persuasionCount: 0, lastOfferedPlatform: nextPlatform };\r\n                  await new Promise(resolve => chrome.runtime.sendMessage({\r\n                    action: 'saveMoveOffAppState', matchId,\r\n                    stateData: nextPlatformState\r\n                  }, resolve));\r\n                  _moveState.state = `offering_${nextPlatform}`;\r\n                  _moveState.lastOfferedPlatform = nextPlatform;\r\n                  matchData.moveOffAppState = _moveState; // Pass updated state to AI call\r\n                  // Fall through to AI generation — openai.js will offer the next platform\r\n                } else {\r\n                  // All platforms exhausted — enter persuasion mode\r\n                  const persuasionCount = (_moveState.persuasionCount || 0) + 1;\r\n                  if (persuasionCount > maxPersuasion) {\r\n                    // Give up — stop chat\r\n                    if (_stopAfterGoal) {\r\n                      const rawPhoto = extractProfilePhotoUrl?.();\r\n                      const permanentPhoto = typeof tinderImageToBase64 === 'function' && rawPhoto\r\n                        ? await tinderImageToBase64(rawPhoto).catch(() => rawPhoto) : rawPhoto;\r\n                      await markChatAsStopped(matchId, 'move_off_app_exhausted', match.name, permanentPhoto);\r\n                      chrome.runtime.sendMessage({ action: 'resetMoveOffAppState', matchId });\r\n                      stopped.push(match.name);\r\n                      processedMatchIds.add(matchId);\r\n                      continue;\r\n                    }\r\n                  } else {\r\n                    // Still persuading — update count, fall through to AI\r\n                    const newPersuadingState = { state: 'persuading', offeredPlatforms, persuasionCount, lastOfferedPlatform: _lastOfferedPlatform };\r\n                    await new Promise(resolve => chrome.runtime.sendMessage({\r\n                      action: 'saveMoveOffAppState', matchId,\r\n                      stateData: newPersuadingState\r\n                    }, resolve));\r\n                    _moveState.state = 'persuading'; // Update in-memory so AI call uses correct state\r\n                    _moveState.persuasionCount = persuasionCount;\r\n                    matchData.moveOffAppState = _moveState; // Pass updated state to AI call\r\n                    // Fall through — openai.js will send persuasion message\r\n                  }\r\n                }\r\n              }\r\n              // NEUTRAL — just fall through, AI chats normally\r\n            }\r\n          }\r\n        }\r\n\r\n        // Reset follow-up state since match replied\r\n        await saveMatchMetadata(matchId, { followupCount: 0, pausedUntil: 0, hardStopped: false });\r\n\r\n        // Random heart on match's last message if enabled\r\n        if (settings.randomHearts) {\r\n          const heartProbability = settings.randomHeartsProbability || 30;\r\n          const shouldSendHeart = Math.random() * 100 < heartProbability;\r\n          if (shouldSendHeart) {\r\n            logAction(`AI: Random heart reaction triggered`);\r\n            await randomHeartMessage();\r\n          }\r\n        }\r\n\r\n        const profile = parseCurrentProfile();\r\n        const messageData = { ...profile, id: matchId, conversationHistory: conversationHistory };\r\n\r\n        // 1. Check for stored language first (prioritize MANUAL overrides)\r\n        const storedLang = await new Promise(resolve => {\r\n          chrome.runtime.sendMessage({ action: 'getMatchLanguage', matchId }, r => {\r\n            if (chrome.runtime.lastError) { resolve(null); return; }\r\n            resolve(r?.language);\r\n          });\r\n        });\r\n\r\n        if (storedLang && storedLang.source === 'manual') {\r\n          messageData.detectedLanguage = { code: storedLang.code, name: storedLang.name };\r\n          logAction(`AI: Using manual language override: ${storedLang.name}`);\r\n        } else if (typeof detectConversationLanguage === 'function') {\r\n          // 2. Auto-detect from conversation for reply (no manual override)\r\n          const langResult = detectConversationLanguage(conversationHistory);\r\n          const isLowConfidence = langResult.source?.includes('fallback') ||\r\n            langResult.source?.includes('latin_english_default') ||\r\n            (langResult.confidence < 50 && langResult.source?.includes('word_patterns'));\r\n\r\n          if (langResult.code !== 'unknown' && !isLowConfidence) {\r\n            // High confidence conversation detection — use it directly\r\n            messageData.detectedLanguage = { code: langResult.code, name: langResult.name };\r\n            logAction(`AI: Detected language: ${langResult.name} (${langResult.confidence}%)`);\r\n            chrome.runtime.sendMessage({\r\n              action: 'setMatchLanguage',\r\n              matchId,\r\n              langData: { code: langResult.code, name: langResult.name, confidence: langResult.confidence, source: 'detected' }\r\n            });\r\n          } else if (typeof detectLanguage === 'function') {\r\n            // Low confidence or unknown — try bio as secondary signal\r\n            const bioText = [\r\n              messageData.bio || '',\r\n              (messageData.questionAnswers || []).map(qa => qa.answer || qa).join(' '),\r\n              (messageData.interests || []).join(' '),\r\n              (messageData.languages || []).join(' '),\r\n            ].join(' ').trim();\r\n\r\n            if (bioText.length >= 8) {\r\n              const bioResult = detectLanguage(bioText);\r\n              if (bioResult.code !== 'unknown' && bioResult.code !== 'en') {\r\n                // Only use bio language if the conversation doesn't have clear English signals.\r\n                // Bio detection of non-Latin scripts (Hebrew, Arabic) from the owner's profile\r\n                // should NOT override an English-speaking match's messages.\r\n                const conversationHasEnglish = conversationHistory\r\n                  .filter(m => m.sender !== 'user')\r\n                  .some(m => /\\b(the|and|you|are|have|good|can|talk|text|instagram|sounds|like|want|nice|meet|ok|cool)\\b/i.test(m.text || ''));\r\n                if (!conversationHasEnglish) {\r\n                  messageData.detectedLanguage = { code: bioResult.code, name: bioResult.name };\r\n                  logAction(`AI: Language from profile bio: ${bioResult.name} (${bioResult.confidence}%)`);\r\n                  chrome.runtime.sendMessage({\r\n                    action: 'setMatchLanguage',\r\n                    matchId,\r\n                    langData: { code: bioResult.code, name: bioResult.name, confidence: bioResult.confidence, source: 'bio_detected' }\r\n                  });\r\n                } else {\r\n                  logAction(`AI: Bio suggests ${bioResult.name} but conversation is English — using English.`);\r\n                }\r\n              }\r\n            }\r\n            // If bio is also unknown/English: leave detectedLanguage unset → AI detects from history\r\n          }\r\n        }\r\n\r\n        // ── Kill-switch check before expensive AI call ──\r\n        if (isProcessChatsAborted()) {\r\n          logAction('Kill-switch active - discarding generation');\r\n          break;\r\n        }\r\n\r\n        const msgResult = await generateAndSendMessage(messageData, settings, matchId, false, isProcessChatsAborted);\r\n        if (msgResult.success) {\r\n          processed.push(match.name);\r\n          processedMatchIds.add(matchId);\r\n\r\n          await savePendingMessage(matchId, msgResult.message, Date.now());\r\n\r\n          if (msgResult.isTrialLimit) {\r\n            logAction('Trial limit reached during messaging, stopping.');\r\n            break;\r\n          }\r\n\r\n          chrome.runtime.sendMessage({\r\n            action: 'updateCycleStats',\r\n            stats: { messagesProcessed: processed.length, currentName: match.name || 'Match', currentMessage: (msgResult.message || '').trim() }\r\n          });\r\n        } else if (msgResult.networkOffline) {\r\n          logAction(`Network offline after reply send for ${match.name} — waiting for reconnect`);\r\n          const recovered = await tinderWaitForNetworkOrAbort(90000);\r\n          if (!recovered) break;\r\n        } else {\r\n          errors.push(`${match.name}: Message failed - ${msgResult.error}`);\r\n\r\n          chrome.runtime.sendMessage({\r\n            action: 'updateCycleStats',\r\n            stats: { errors }\r\n          });\r\n\r\n          if (msgResult.isAuthError) {\r\n            logAction('Session expired — stopping Chat Processing.');\r\n            _authErrorOccurred = true;\r\n            break;\r\n          }\r\n          if (msgResult.isTrialLimit) {\r\n            logAction('Trial limit reached during reply, stopping Chat Processing.');\r\n            break;\r\n          }\r\n          if (msgResult.isRateLimit) {\r\n            logAction('Rate limit reached during reply, stopping Chat Processing.');\r\n            chrome.runtime.sendMessage({ action: 'messagingRateLimitReached' }).catch(() => {});\r\n            break;\r\n          }\r\n        }\r\n\r\n      } catch (error) {\r\n        logAction(`Error processing match ${match.name}: ${error.message}`, {}, 'error');\r\n        errors.push(`${match.name}: ${error.message}`);\r\n\r\n        chrome.runtime.sendMessage({\r\n          action: 'updateCycleStats',\r\n          stats: { errors }\r\n        });\r\n      }\r\n\r\n      await waitRandom(1000, 2000);\r\n    }\r\n\r\n    window.dispatchEvent(new CustomEvent('fe:draftingStep', { detail: { step: '' } }));\r\n\r\n    // ── WATCHDOG LOOP ──\r\n    // If we've processed all immediate targets but haven't reached our message quota,\r\n    // enter Watchdog mode to wait for fresh replies instead of exiting immediately.\r\n    let cycleTimeLimitMs = 15 * 60 * 1000; // 15 minutes max per cycle phase\r\n    let elapsedMs = Date.now() - startTime;\r\n    \r\n    if (processed.length + followUps.length < maxMessages && elapsedMs < cycleTimeLimitMs) {\r\n      logPhaseHeader('WATCHDOG MODE ACTIVE');\r\n      logAction(`Awaiting new replies for up to ${Math.round((cycleTimeLimitMs - elapsedMs) / 60000)} minutes...`);\r\n      \r\n      // Update UI to reflect Watchdog status — switch orb to polling state (parity with Bumble)\r\n      chrome.runtime.sendMessage({\r\n        action: 'updateCycleStats',\r\n        stats: { watchdogActive: true }\r\n      });\r\n      try {\r\n        const _pfStore = await chrome.storage.local.get('progressFeedEvents');\r\n        const _pfEvents = _pfStore.progressFeedEvents || [];\r\n        _pfEvents.unshift({ type: 'watchdog_active', name: 'Watchdog', timestamp: Date.now() });\r\n        if (_pfEvents.length > 50) _pfEvents.length = 50;\r\n        await chrome.storage.local.set({ progressFeedEvents: _pfEvents });\r\n      } catch (_) {}\r\n\r\n      // Keep polling until quota or time limit is reached, or user stops\r\n      while (!isProcessChatsAborted() && (processed.length + followUps.length < maxMessages) && (elapsedMs < cycleTimeLimitMs)) {\r\n        logAction(`[Watchdog] Polling inbox...`);\r\n        \r\n        // Quick visual check of the sidebar for new replies\r\n        const currentMatches = getMatchesFromList();\r\n        let foundNewUnread = false;\r\n\r\n        for (const m of currentMatches) {\r\n          if ((m.hasUnread || m.sheRepliedLast) && !processedMatchIds.has(m.matchId)) {\r\n            logAction(`[Watchdog] 🔔 NEW REPLY DETECTED from ${m.name}!`);\r\n            matchesToProcess.push(m);\r\n            foundNewUnread = true;\r\n            break;\r\n          }\r\n        }\r\n\r\n        if (foundNewUnread) {\r\n          // A new match was added to matchesToProcess, break the watchdog wait sequence\r\n          // and let the main functionality handle processing it.\r\n          // Wait, we are already outside the main `for...of` loop.\r\n          // Since we want to process it, we should just let the function return and get called next cycle?\r\n          // No, we can just fetch the history and reply right here, or we can restart the loop.\r\n          // Restarting the loop from inside here is messy. Let's just break the Watchdog and \r\n          // let the background script queue another cycle if it wants, OR we can just end the cycle early \r\n          // to trigger the next phase naturally. \r\n          // Wait, the easiest way is to push it and let the outer logic see it. But the outer `for` loop is done.\r\n          // Let's just return early with success so the cycle finishes, and the next cycle picks it up?\r\n          // No, Watchdog means staying IN this phase and replying.\r\n          // Let's implement a simple \"click and reply\" for the watchdog:\r\n          // Actually, to avoid code duplication, we can just wrap the `for (const match of matchesToProcess)` in a `do { ... } while(watchdogHasNewTarget)`\r\n          logAction(`[Watchdog] Ending polling to process new match immediately on next cycle.`);\r\n          break; // Break the watchdog loop to finish the cycle; it'll be picked up immediately in the next run.\r\n        }\r\n\r\n        // Wait 30 seconds before polling again\r\n        let waitTime = 30000;\r\n        while(waitTime > 0 && !isProcessChatsAborted()) {\r\n           await waitRandom(1000, 1000);\r\n           waitTime -= 1000;\r\n        }\r\n        elapsedMs = Date.now() - startTime;\r\n      }\r\n      \r\n      logAction(`[Watchdog] Escaped Watchdog mode.`);\r\n    }\r\n\r\n    logPhaseHeader('MESSAGING CYCLE COMPLETE');\r\n    logAction(`Results: ${processed.length} sent, ${followUps.length} follow-ups, ${skipped.length} skipped`, { errors: errors.length });\r\n\r\n    return {\r\n      success: true,\r\n      processed: processed.length,\r\n      skipped: skipped.length,\r\n      followUps: followUps.length,\r\n      processedNames: processed,\r\n      skippedNames: skipped,\r\n      followUpNames: followUps,\r\n      stoppedChats: stopped,\r\n      errors,\r\n      isAuthError: _authErrorOccurred\r\n    };\r\n  } catch (error) {\r\n    logAction(`Error in processChats: ${error.message}`, {}, 'error');\r\n    errors.push(error.message);\r\n    return {\r\n      success: false,\r\n      processed: processed.length,\r\n      skipped: skipped.length,\r\n      followUps: followUps.length,\r\n      processedNames: processed,\r\n      skippedNames: skipped,\r\n      followUpNames: followUps,\r\n      stoppedChats: stopped,\r\n      errors,\r\n      isAuthError: _authErrorOccurred\r\n    };\r\n  } finally {\r\n    processChatsRunning = false;\r\n  }\r\n}\r\n\r\nasync function generateAndSendMessage(matchData, settings, matchId, isFollowUp, isAborted = () => !processChatsRunning) {\r\n  try {\r\n    // ── STAGE 0.1: FAST SESSION LOCK ──\r\n    if (matchId && tinderSessionSentIds.has(matchId)) {\r\n        logAction(`GUARD: Session lock active - already messaged this profile`);\r\n        return { success: true };\r\n    }\r\n\r\n    // ── Retroactive handle scan: count handles in existing history (once per matchId) ──\r\n    if (matchId && matchData?.conversationHistory?.length > 0) {\r\n      try {\r\n        const _scanned = await new Promise(resolve => {\r\n          chrome.storage.local.get(['handleSentScannedIds'], r => resolve(r.handleSentScannedIds || {}));\r\n        });\r\n        if (!_scanned[matchId]) {\r\n          const _cd = settings?.contactDetails || {};\r\n          const _retroPlatforms = { telegram: _cd.telegram?.value?.trim(), instagram: _cd.instagram?.value?.trim(), tango: _cd.tango?.value?.trim() };\r\n          const _userMessages = matchData.conversationHistory.filter(m => m.sender === 'user');\r\n          for (const [platform, handle] of Object.entries(_retroPlatforms)) {\r\n            if (handle && _userMessages.some(m => m.text && m.text.includes(handle))) {\r\n              chrome.runtime.sendMessage({ action: 'trackHandleSent', platform });\r\n              logAction(`[HANDLE] Retroactive: found ${platform} handle in history`);\r\n            }\r\n          }\r\n          _scanned[matchId] = true;\r\n          chrome.storage.local.set({ handleSentScannedIds: _scanned });\r\n        }\r\n      } catch (_e) { /* non-critical */ }\r\n    }\r\n\r\n    // ── STAGE 0.2: DOM RECENCY GUARD (Hardened) ──\r\n    if (matchData.conversationHistory && matchData.conversationHistory.length > 0) {\r\n        const lastMsg = matchData.conversationHistory[matchData.conversationHistory.length - 1];\r\n        const lastTimeText = (lastMsg.timeText || \"\").toLowerCase();\r\n        const isVeryRecent = lastTimeText.includes('now') || /\\d+m($|[^a-z])/.test(lastTimeText);\r\n        \r\n        if (lastMsg.sender === 'user' && isVeryRecent) {\r\n          logAction(`GUARD: Recency lock active - message sent ${lastTimeText || 'just now'}`);\r\n          return { success: true };\r\n        }\r\n    }\r\n\r\n    // Trial Pre-Check\r\n    const trialStatus = await sendMessageToBackground({ action: 'getTrialStatus' });\r\n    if (trialStatus && (trialStatus.status === 'expired' || trialStatus.messagesExhausted)) {\r\n      logAction(`[AI] Trial limit reached - generation cancelled`);\r\n      if (trialStatus.likesRemaining <= 0) {\r\n        chrome.runtime.sendMessage({ action: 'trialMessageLimitReached' }).catch(() => {});\r\n      }\r\n      return { success: false, error: 'Trial message limit reached', isTrialLimit: true };\r\n    }\r\n\r\n    const rateLimitResponse = await sendMessageToBackground({ action: 'canSendMessage' });\r\n    if (rateLimitResponse && rateLimitResponse.allowed === false) {\r\n      logAction(`[AI] Rate limit active - generation cancelled`);\r\n      return { success: false, error: 'Rate limit reached', isRateLimit: true };\r\n    }\r\n\r\n    chrome.runtime.sendMessage({ action: 'updateCycleStats', stats: { draftingStep: 'crafting' } });\r\n    window.dispatchEvent(new CustomEvent('fe:draftingStep', { detail: { step: 'crafting' } }));\r\n\r\n    const response = await sendMessageToBackground({\r\n      action: 'generateMessage',\r\n      matchData,\r\n      settings,\r\n      isFollowUp\r\n    });\r\n\r\n    if (response && response.success) {\r\n      // ── Kill-switch check before sending (last chance to abort) ──\r\n      if (isAborted()) {\r\n        logAction(`[AI] Generation discarded - kill-switch active`);\r\n        return { success: false, error: 'Stopped by user' };\r\n      }\r\n\r\n      chrome.runtime.sendMessage({ action: 'updateCycleStats', stats: { draftingStep: 'sending' } });\r\n      window.dispatchEvent(new CustomEvent('fe:draftingStep', { detail: { step: 'sending' } }));\r\n\r\n      // ── Consecutive messages: send 2-3 parts with human-like delays ──\r\n      const messageParts = response.messages && Array.isArray(response.messages) && response.messages.length > 1\r\n        ? response.messages\r\n        : [response.message];\r\n\r\n      let lastSentMessage = messageParts[0];\r\n      let allSentOk = true;\r\n\r\n      for (let _pi = 0; _pi < messageParts.length; _pi++) {\r\n        const part = messageParts[_pi];\r\n        if (!part?.trim()) continue;\r\n\r\n        if (_pi > 0) {\r\n          // Human-like pause between consecutive messages\r\n          const prevLen = messageParts[_pi - 1]?.length || 20;\r\n          const pauseMs = Math.min(12000, Math.max(3000, prevLen * 50 + Math.random() * 3000));\r\n          logAction(`[SEND] Consecutive pause ${Math.round(pauseMs / 1000)}s before part ${_pi + 1}`);\r\n          await new Promise(r => setTimeout(r, pauseMs));\r\n          if (isAborted()) { allSentOk = false; break; }\r\n        }\r\n\r\n        const sendResult = await sendMessage(part);\r\n        if (sendResult?.networkOffline) {\r\n          return { success: false, networkOffline: true, error: 'Network offline' };\r\n        }\r\n        if (!sendResult?.success) {\r\n          allSentOk = false;\r\n          logAction(`[SEND] Part ${_pi + 1} failed`);\r\n          break;\r\n        }\r\n        lastSentMessage = part;\r\n        logAction(`[SEND] Part ${_pi + 1}/${messageParts.length} delivered`);\r\n      }\r\n\r\n      if (allSentOk) {\r\n        // Add to session lock immediately to prevent double-triggers\r\n        if (matchId) tinderSessionSentIds.add(matchId);\r\n\r\n        // Update language button to reflect the language actually used\r\n        try {\r\n          const usedLang = matchData?.detectedLanguage;\r\n          if (usedLang?.code && usedLang?.name) {\r\n            const btn = document.getElementById('flirteasy-lang-btn');\r\n            if (btn) {\r\n              btn.innerHTML = LANG_GLOBE_SVG + usedLang.name;\r\n              btn.dataset.langCode = usedLang.code;\r\n              btn.dataset.langName = usedLang.name;\r\n            }\r\n          }\r\n        } catch (_) {}\r\n        \r\n        let isTrialLimit = false;\r\n        // Tracking is now unified via updateCycleStats in the processChats loop\r\n        // These manual calls were causing double/triple counting.\r\n\r\n        try {\r\n          const storedData = await getStoredMatchData(matchId);\r\n          const existingHistory = storedData?.conversationHistory || [];\r\n\r\n          // Add all sent parts to history\r\n          const newMessages = messageParts.map((part, idx) => ({\r\n            id: `sent_${Date.now() + idx}`,\r\n            text: part,\r\n            sender: 'user',\r\n            timestamp: Date.now() + idx\r\n          }));\r\n          const updatedHistory = [...existingHistory, ...newMessages].slice(-100);\r\n\r\n          await saveMatchMetadata(matchId, {\r\n            lastMessageTimestamp: Date.now(),\r\n            lastMessage: lastSentMessage,\r\n            conversationHistory: updatedHistory\r\n          });\r\n          logAction(`[SUCCESS] Message sent and persisted to storage`);\r\n\r\n          // ── Track handle sends (Telegram / Instagram / Tango counter) ──\r\n          try {\r\n            const _cd = settings?.contactDetails || {};\r\n            const _handlePlatforms = { telegram: _cd.telegram?.value?.trim(), instagram: _cd.instagram?.value?.trim(), tango: _cd.tango?.value?.trim() };\r\n            const _allSentText = messageParts.join(' ');\r\n            for (const [platform, handle] of Object.entries(_handlePlatforms)) {\r\n              if (handle && _allSentText.includes(handle)) {\r\n                chrome.runtime.sendMessage({ action: 'trackHandleSent', platform });\r\n                logAction(`[HANDLE] Tracked ${platform} handle send`);\r\n              }\r\n            }\r\n          } catch (_) { /* non-critical */ }\r\n\r\n          // If we just sent a persuasion message, increment the persuasion count\r\n          if (matchData?.moveOffAppState?.state === 'persuading') {\r\n            const prevCount = matchData.moveOffAppState.persuasionCount || 0;\r\n            const newCount = prevCount + 1;\r\n            chrome.runtime.sendMessage({\r\n              action: 'saveMoveOffAppState', matchId,\r\n              stateData: { ...matchData.moveOffAppState, persuasionCount: newCount }\r\n            });\r\n            logAction(`[PERSUASION] Count incremented to ${newCount}`);\r\n          }\r\n        } catch (storageError) {\r\n          console.error(`[FlirtEasy] Failed to save to storage:`, storageError);\r\n        }\r\n\r\n        return { success: true, message: lastSentMessage, isTrialLimit };\r\n      } else {\r\n        console.error(`[FlirtEasy] Consecutive message send failed`);\r\n        return { success: false, error: 'Send failed' };\r\n      }\r\n    } else {\r\n      const errorMsg = response?.error || 'Failed to generate message';\r\n      const isTrialLimit = response?.isTrialLimit || errorMsg.includes('trial usage limit') || errorMsg.includes('trial has ended') || errorMsg.includes('messages have been used up');\r\n      if (isTrialLimit) {\r\n        const ts = await sendMessageToBackground({ action: 'getTrialStatus' }).catch(() => null);\r\n        if (!ts || ts.likesRemaining <= 0) {\r\n          chrome.runtime.sendMessage({ action: 'trialMessageLimitReached' }).catch(() => {});\r\n        }\r\n      }\r\n      const isAuthError = response?.isAuthError || false;\r\n      console.error(`[FlirtEasy] generateMessage failed: ${errorMsg}`);\r\n      return { success: false, error: errorMsg, isTrialLimit, isAuthError };\r\n    }\r\n  } catch (error) {\r\n    console.error('[FlirtEasy] Error in generateAndSendMessage:', error);\r\n    try {\r\n      chrome.runtime.sendMessage({ action: 'reportDomError', payload: { platform: window.CURRENT_PLATFORM?.toLowerCase() || 'tinder', error_type: 'send_exception', selector_key: 'generateAndSendMessage', error_message: `${error.name}: ${error.message}`, page_url: location.href } });\r\n    } catch (_) {}\r\n    return { success: false, error: error.message };\r\n  } finally {\r\n    chrome.runtime.sendMessage({ action: 'updateCycleStats', stats: { draftingStep: '' } });\r\n    window.dispatchEvent(new CustomEvent('fe:draftingStep', { detail: { step: '' } }));\r\n  }\r\n}\r\n\r\nasync function getStoredMatchData(matchId) {\r\n  return new Promise((resolve) => {\r\n    chrome.runtime.sendMessage({ action: 'getMatchData', matchId }, (response) => {\r\n      if (chrome.runtime.lastError) {\r\n        console.warn('[FlirtEasy] Runtime error:', chrome.runtime.lastError.message);\r\n        resolve(null);\r\n      } else {\r\n        resolve(response && response.data);\r\n      }\r\n    });\r\n  });\r\n}\r\n\r\nasync function saveMatchMetadata(matchId, data) {\r\n  return new Promise((resolve) => {\r\n    chrome.runtime.sendMessage({ action: 'saveMatchData', matchId, data }, () => {\r\n      if (chrome.runtime.lastError) {\r\n        console.warn('[FlirtEasy] Runtime error:', chrome.runtime.lastError.message);\r\n      }\r\n      resolve();\r\n    });\r\n  });\r\n}\r\n\r\nasync function checkIfChatStopped(matchId) {\r\n  return new Promise((resolve) => {\r\n    chrome.runtime.sendMessage({ action: 'isChatstopped', matchId }, (response) => {\r\n      if (chrome.runtime.lastError) {\r\n        console.warn('[FlirtEasy] Runtime error:', chrome.runtime.lastError.message);\r\n        resolve({ isStopped: false, reason: null });\r\n      } else {\r\n        resolve({ isStopped: !!(response && response.isStopped), reason: (response && response.reason) || null });\r\n      }\r\n    });\r\n  });\r\n}\r\n\r\nasync function markChatAsStopped(matchId, reason, matchName = null, photoUrl = null, snippet = null) {\r\n  return new Promise((resolve) => {\r\n    chrome.runtime.sendMessage({\r\n      action: 'markChatStopped',\r\n      matchId,\r\n      reason,\r\n      matchName,\r\n      photoUrl,\r\n      snippet,\r\n      platform: 'tinder'\r\n    }, () => {\r\n      if (chrome.runtime.lastError) {\r\n        console.warn('[FlirtEasy] Runtime error:', chrome.runtime.lastError.message);\r\n      }\r\n      resolve();\r\n    });\r\n  });\r\n}\r\n\r\nasync function savePendingMessage(matchId, messageText, timestamp) {\r\n  return new Promise((resolve) => {\r\n    chrome.storage.local.get(['pendingMessages'], (result) => {\r\n      const pendingMessages = result.pendingMessages || {};\r\n      pendingMessages[matchId] = { text: messageText, timestamp: timestamp };\r\n      chrome.storage.local.set({ pendingMessages }, () => {\r\n        if (chrome.runtime.lastError) {\r\n          console.warn('[FlirtEasy] Error saving pending message:', chrome.runtime.lastError.message);\r\n        }\r\n        console.log(`[FlirtEasy] Saved pending message for ${matchId.substring(0, 8)}***: \"${messageText.substring(0, 20)}***\"`);\r\n        resolve();\r\n      });\r\n    });\r\n  });\r\n}\r\n\r\nasync function getPendingMessage(matchId) {\r\n  return new Promise((resolve) => {\r\n    chrome.storage.local.get(['pendingMessages'], (result) => {\r\n      const pendingMessages = result.pendingMessages || {};\r\n      resolve(pendingMessages[matchId] || null);\r\n    });\r\n  });\r\n}\r\n\r\nasync function clearPendingMessage(matchId) {\r\n  return new Promise((resolve) => {\r\n    chrome.storage.local.get(['pendingMessages'], (result) => {\r\n      const pendingMessages = result.pendingMessages || {};\r\n      delete pendingMessages[matchId];\r\n      chrome.storage.local.set({ pendingMessages }, () => {\r\n        if (chrome.runtime.lastError) {\r\n          console.warn('[FlirtEasy] Error clearing pending message:', chrome.runtime.lastError.message);\r\n        }\r\n        console.log(`[FlirtEasy] Cleared pending message for ${matchId.substring(0, 8)}***`);\r\n        resolve();\r\n      });\r\n    });\r\n  });\r\n}\r\n\r\nasync function cleanupOldPendingMessages() {\r\n  return new Promise((resolve) => {\r\n    chrome.storage.local.get(['pendingMessages'], (result) => {\r\n      const pendingMessages = result.pendingMessages || {};\r\n      const maxAge = 24 * 60 * 60 * 1000; // 24 hours\r\n      const now = Date.now();\r\n      let cleaned = 0;\r\n\r\n      for (const matchId in pendingMessages) {\r\n        const age = now - pendingMessages[matchId].timestamp;\r\n        if (age > maxAge) {\r\n          delete pendingMessages[matchId];\r\n          cleaned++;\r\n        }\r\n      }\r\n\r\n      if (cleaned > 0) {\r\n        chrome.storage.local.set({ pendingMessages }, () => {\r\n          console.log(`[FlirtEasy] Cleaned up ${cleaned} old pending messages (>24h)`);\r\n          resolve(cleaned);\r\n        });\r\n      } else {\r\n        resolve(0);\r\n      }\r\n    });\r\n  });\r\n}\r\n\r\nasync function waitForTinderChatReady(matchId, timeoutMs = 6000) {\r\n  const pollInterval = 400;\r\n  const maxAttempts = Math.ceil(timeoutMs / pollInterval);\r\n\r\n  for (let i = 0; i < maxAttempts; i++) {\r\n    const urlReady = window.location.pathname.includes(matchId);\r\n\r\n    const apiReady = (() => {\r\n      let result = [];\r\n      const handler = (e) => { if (e.detail.matchId === matchId) result = e.detail.messages; };\r\n      document.addEventListener('flirteasy:messagesResponse', handler);\r\n      document.dispatchEvent(new CustomEvent('flirteasy:getMessages', { detail: { matchId } }));\r\n      document.removeEventListener('flirteasy:messagesResponse', handler);\r\n      return result.length > 0;\r\n    })();\r\n\r\n    const domReady = !!document.querySelector('.msg-text, [class*=\"messageText\"], .message-content');\r\n\r\n    if (urlReady && (apiReady || domReady)) {\r\n      console.log(`[FlirtEasy] Chat ready for ${matchId.substring(0, 8)}*** after ${(i + 1) * pollInterval}ms (api:${apiReady} dom:${domReady})`);\r\n      return;\r\n    }\r\n\r\n    await new Promise(r => setTimeout(r, pollInterval));\r\n  }\r\n\r\n  console.warn(`[FlirtEasy] Chat ready timeout for ${matchId.substring(0, 8)}***, proceeding anyway`);\r\n  try { chrome.runtime.sendMessage({ action: 'reportDomError', payload: { platform: 'tinder', error_type: 'dom_timeout', selector_key: 'messaging.chatReady', error_message: `Chat UI did not become ready within ${timeoutMs}ms for match ${matchId.substring(0, 8)}***`, page_url: location.href } }); } catch (_) {}\r\n}\r\n\r\nasync function navigateToMatchWithRetry(matchId, matchName, maxRetries = 5) {\r\n  console.log(`[FlirtEasy] Navigating to main messages page to clear cached state`);\r\n  const tabsElements = Array.from(document.querySelectorAll('button[role=\"tab\"]'));\r\n  const messagesTabButton = tabsElements.length > 1 ? tabsElements[1] : null;\r\n  if (messagesTabButton) {\r\n    messagesTabButton.click();\r\n    await waitRandom(500, 800);\r\n  }\r\n\r\n  const currentPath = window.location.pathname;\r\n  const pathParts = currentPath.split('/');\r\n  const currentMatchId = pathParts.length >= 4 ? pathParts[3] : null;\r\n\r\n  console.log(`[FlirtEasy] Current path: ${currentPath}, target matchId: ${matchId}, current matchId: ${currentMatchId}`);\r\n\r\n  if (currentMatchId && currentMatchId === matchId) {\r\n    console.log(`[FlirtEasy] Navigating away to clear cached page_token for ${matchName}`);\r\n    window.history.pushState({}, '', '/app/recs');\r\n    await waitRandom(300, 500);\r\n  }\r\n\r\n  for (let attempt = 1; attempt <= maxRetries; attempt++) {\r\n    console.log(`[FlirtEasy] Navigation attempt ${attempt}/${maxRetries} for ${matchName}`);\r\n\r\n    await waitRandom(500, 1000);\r\n\r\n    const navigated = navigateToMatch(matchId);\r\n    if (navigated) {\r\n      console.log(`[FlirtEasy] Click successful, waiting for page to load and API call...`);\r\n      await waitRandom(3000, 4000);\r\n\r\n      const newPath = window.location.pathname;\r\n      if (newPath.includes(matchId)) {\r\n        console.log(`[FlirtEasy] Successfully navigated to ${matchName}`);\r\n        return true;\r\n      }\r\n    }\r\n\r\n    if (attempt < maxRetries) {\r\n      console.log(`[FlirtEasy] Navigation failed, scrolling match list to find ${matchName}...`);\r\n\r\n      const scrolled = await scrollMatchesList(300);\r\n      if (scrolled) {\r\n        console.log('[FlirtEasy] Scrolled matches list, waiting for new matches to load...');\r\n        await waitRandom(1500, 2000);\r\n      } else {\r\n        console.warn(`[FlirtEasy] Scroll failed during navigation retry for ${matchName} - match may not be visible`);\r\n      }\r\n    }\r\n  }\r\n\r\n  console.error(`[FlirtEasy] Failed to navigate to ${matchName} after ${maxRetries} attempts`);\r\n  return false;\r\n}\r\n\r\nasync function scrollMatchesList(scrollAmount = 300) {\r\n  console.log('[FlirtEasy] Attempting to scroll matches list...');\r\n\r\n  const scrollCandidates = [\r\n    () => {\r\n      const aside = document.querySelector('aside');\r\n      if (!aside) return null;\r\n      const allDivs = aside.querySelectorAll('div');\r\n      let maxScrollDiv = null;\r\n      let maxScrollHeight = 0;\r\n      for (let div of allDivs) {\r\n        if (div.scrollHeight > maxScrollHeight) {\r\n          maxScrollHeight = div.scrollHeight;\r\n          maxScrollDiv = div;\r\n        }\r\n      }\r\n      return maxScrollDiv;\r\n    },\r\n    () => document.querySelector('aside > div > div'),\r\n    () => document.querySelector('aside > div'),\r\n    () => document.querySelector('aside div[class*=\"Ov\"]'),\r\n    () => {\r\n      const aside = document.querySelector('aside');\r\n      if (!aside) return null;\r\n      return Array.from(aside.querySelectorAll('div')).find(div => {\r\n        const style = window.getComputedStyle(div);\r\n        return (style.overflowY === 'auto' || style.overflowY === 'scroll') && div.scrollHeight > div.clientHeight;\r\n      });\r\n    }\r\n  ];\r\n\r\n  for (let i = 0; i < scrollCandidates.length; i++) {\r\n    const element = scrollCandidates[i]();\r\n    if (element) {\r\n      console.log(`[FlirtEasy] Trying scroll candidate ${i + 1}: ${element.tagName}.${element.className?.slice(0, 60)}`);\r\n      console.log(`[FlirtEasy] Element metrics - scrollHeight: ${element.scrollHeight}, clientHeight: ${element.clientHeight}, scrollTop: ${element.scrollTop}`);\r\n\r\n      const initialScrollTop = element.scrollTop;\r\n      element.scrollBy({ top: scrollAmount, behavior: 'auto' }); // Use auto/instant for virtualization\r\n\r\n      await new Promise(resolve => setTimeout(resolve, 800)); // Increase wait for virtualization to catch up\r\n\r\n      if (element.scrollTop !== initialScrollTop) {\r\n        console.log(`[FlirtEasy] Successfully scrolled! New scrollTop: ${element.scrollTop}`);\r\n        return true;\r\n      } else {\r\n        console.log(`[FlirtEasy] Scroll had no effect, trying next candidate...`);\r\n      }\r\n    }\r\n  }\r\n\r\n  console.warn('[FlirtEasy] All scroll attempts failed');\r\n  return false;\r\n}\r\n\r\nasync function sendMessageToCurrentChat(message) {\r\n  console.log(`[FlirtEasy] Sending message to current chat: ${message}`);\r\n\r\n  if (!isOnChatPage()) {\r\n    return { success: false, error: 'Not on a chat page' };\r\n  }\r\n\r\n  const result = await sendMessage(message);\r\n  return result;\r\n}\r\n\r\n\r\nasync function randomHeartMessage() {\r\n  try {\r\n    console.log('[FlirtEasy] randomHeartMessage: Starting...');\r\n\r\n    await waitRandom(1000, 1500);\r\n\r\n    const selectors = [\r\n      'button[role=\"checkbox\"][aria-checked=\"false\"].End\\\\(0\\\\)',\r\n      'button[role=\"checkbox\"][aria-checked=\"false\"]',\r\n      'button[role=\"checkbox\"]'\r\n    ];\r\n\r\n    let heartButtons = [];\r\n    for (const selector of selectors) {\r\n      heartButtons = document.querySelectorAll(selector);\r\n      if (heartButtons.length > 0) {\r\n        console.log(`[FlirtEasy] randomHeartMessage: Found ${heartButtons.length} buttons with selector: ${selector}`);\r\n        break;\r\n      }\r\n    }\r\n\r\n    if (heartButtons.length === 0) {\r\n      console.log('[FlirtEasy] No heart buttons found');\r\n      return;\r\n    }\r\n\r\n    const randomIndex = Math.floor(Math.random() * Math.min(heartButtons.length, 3));\r\n    const heartBtn = heartButtons[randomIndex];\r\n\r\n    console.log(`[FlirtEasy] randomHeartMessage: Clicking heart at index ${randomIndex}`);\r\n    heartBtn.click();\r\n    console.log('[FlirtEasy] randomHeartMessage: Heart clicked successfully!');\r\n    await waitRandom(500, 1000);\r\n  } catch (error) {\r\n    console.error('[FlirtEasy] randomHeartMessage: Error clicking heart:', error);\r\n  }\r\n}\r\n\r\n\r\nasync function startVisualTrainingMode() {\r\n  console.log('[FlirtEasy] Starting visual training mode');\r\n\r\n  const existing = document.getElementById('flirteasy-training-overlay');\r\n  if (existing) {\r\n    console.log('[FlirtEasy] Training overlay already active, skipping duplicate');\r\n    return { success: true };\r\n  }\r\n\r\n  // Get current settings to preserve liked photos\r\n  const settings = await new Promise((resolve) => {\r\n    chrome.runtime.sendMessage({ action: 'getSettings' }, (response) => {\r\n      resolve(response || {});\r\n    });\r\n  });\r\n\r\n  let trainingCount = settings.visualPreferences?.likedPhotos?.length || 0;\r\n  const targetCount = 50;\r\n  const circumference = 477.5;\r\n  const initPct = Math.round((trainingCount / targetCount) * 100);\r\n  const initOffset = Math.round(circumference * (1 - trainingCount / targetCount) * 10) / 10;\r\n  const initScaleY = Math.max(0.03, trainingCount / targetCount);\r\n\r\n  const overlay = document.createElement('div');\r\n  overlay.id = 'flirteasy-training-overlay';\r\n  overlay.style.cssText = 'position: fixed; top: 20px; left: 500px; z-index: 999999;';\r\n\r\n  overlay.innerHTML = `\r\n    <style>\r\n      #flirteasy-training-overlay { font-family: -apple-system, BlinkMacSystemFont, \"Segoe UI\", Roboto, sans-serif; }\r\n      .fle-card {\r\n        background: #f0f3f8;\r\n        border-radius: 32px;\r\n        box-shadow: 14px 14px 28px rgba(209,217,230,0.75), -14px -14px 28px rgba(255,255,255,0.55);\r\n        padding: 22px 20px 20px 16px;\r\n        display: flex;\r\n        gap: 16px;\r\n        width: 282px;\r\n        cursor: move;\r\n        position: relative;\r\n        overflow: hidden;\r\n      }\r\n      .fle-left {\r\n        display: flex;\r\n        flex-direction: column;\r\n        align-items: center;\r\n        gap: 5px;\r\n        flex-shrink: 0;\r\n      }\r\n      .fle-vbar {\r\n        width: 26px;\r\n        flex: 1;\r\n        min-height: 60px;\r\n        background: #f0f3f8;\r\n        border-radius: 999px;\r\n        box-shadow: inset 6px 6px 12px #d1d9e6, inset -6px -6px 12px #ffffff;\r\n        position: relative;\r\n        overflow: hidden;\r\n      }\r\n      .fle-vbar-fill {\r\n        position: absolute;\r\n        bottom: 0;\r\n        left: 50%;\r\n        width: 16px;\r\n        height: 100%;\r\n        background: linear-gradient(to top, #f22565, #fc427b);\r\n        border-radius: 999px;\r\n        box-shadow: 0 3px 8px rgba(242,37,101,0.35);\r\n        transform-origin: bottom center;\r\n        transform: translateX(-50%) scaleY(0.03);\r\n        transition: transform 0.4s ease;\r\n        animation: fle-vpulse 2.2s ease-in-out infinite;\r\n      }\r\n      @keyframes fle-vpulse {\r\n        0%, 100% { box-shadow: 0 3px 8px rgba(242,37,101,0.35); }\r\n        50% { box-shadow: 0 3px 16px rgba(242,37,101,0.6); }\r\n      }\r\n      .fle-pct {\r\n        font-size: 11px;\r\n        font-weight: 800;\r\n        color: #f22565;\r\n        line-height: 1;\r\n        text-align: center;\r\n      }\r\n      .fle-hint {\r\n        background: #f0f3f8;\r\n        border-radius: 12px;\r\n        box-shadow: inset 3px 3px 7px #d1d9e6, inset -3px -3px 7px #ffffff;\r\n        padding: 8px 10px;\r\n        display: flex;\r\n        align-items: flex-start;\r\n        gap: 7px;\r\n      }\r\n      .fle-hint-icon { flex-shrink: 0; margin-top: 1px; display: flex; align-items: center; }\r\n      .fle-hint-text { font-size: 10px; font-weight: 600; color: #475569; line-height: 1.45; }\r\n      .fle-right {\r\n        flex: 1;\r\n        display: flex;\r\n        flex-direction: column;\r\n        gap: 8px;\r\n        min-width: 0;\r\n      }\r\n      .fle-title {\r\n        font-size: 12px;\r\n        font-weight: 800;\r\n        color: #1a2b4c;\r\n        letter-spacing: 0.02em;\r\n        line-height: 1.2;\r\n        margin: 0 0 3px 0;\r\n        text-transform: uppercase;\r\n      }\r\n      .fle-desc {\r\n        font-size: 11px;\r\n        font-weight: 500;\r\n        color: #8c9bb1;\r\n        margin: 0;\r\n        line-height: 1.4;\r\n      }\r\n      .fle-badge {\r\n        background: #f0f3f8;\r\n        border-radius: 999px;\r\n        box-shadow: 4px 4px 10px #d1d9e6, -4px -4px 10px #ffffff;\r\n        padding: 5px 11px;\r\n        display: inline-flex;\r\n        flex-direction: column;\r\n        align-items: center;\r\n        line-height: 1.35;\r\n      }\r\n      .fle-badge-row {\r\n        display: flex;\r\n        gap: 8px;\r\n        justify-content: center;\r\n        margin-top: 2px;\r\n      }\r\n      .fle-badge-lbl {\r\n        font-size: 9px;\r\n        font-weight: 700;\r\n        color: #475569;\r\n        letter-spacing: 0.03em;\r\n      }\r\n      .fle-badge-pink {\r\n        font-size: 9px;\r\n        font-weight: 700;\r\n        color: #f22565;\r\n      }\r\n      .fle-circle-wrap { display: flex; justify-content: center; position: relative; }\r\n      .fle-circle {\r\n        width: 148px;\r\n        height: 148px;\r\n        background: #f0f3f8;\r\n        border-radius: 50%;\r\n        box-shadow: inset 6px 6px 12px #d1d9e6, inset -6px -6px 12px #ffffff;\r\n        position: relative;\r\n        display: flex;\r\n        align-items: center;\r\n        justify-content: center;\r\n      }\r\n      .fle-ring-svg {\r\n        position: absolute;\r\n        inset: 0;\r\n        width: 100%;\r\n        height: 100%;\r\n        overflow: visible;\r\n      }\r\n      .fle-ring-track {\r\n        fill: none;\r\n        stroke: #fce2eb;\r\n        stroke-width: 6;\r\n        stroke-linecap: round;\r\n      }\r\n      .fle-ring-progress {\r\n        fill: none;\r\n        stroke: url(#fle-pg);\r\n        stroke-width: 6;\r\n        stroke-linecap: round;\r\n        stroke-dasharray: ${circumference};\r\n        transform: rotate(-90deg);\r\n        transform-origin: 88px 88px;\r\n        transition: stroke-dashoffset 0.4s ease;\r\n      }\r\n      .fle-circle-inner {\r\n        display: flex;\r\n        flex-direction: column;\r\n        align-items: center;\r\n        z-index: 1;\r\n        pointer-events: none;\r\n      }\r\n      .fle-count {\r\n        font-size: 48px;\r\n        font-weight: 900;\r\n        color: #1a2b4c;\r\n        line-height: 1;\r\n        letter-spacing: -0.02em;\r\n      }\r\n      .fle-liked-lbl {\r\n        font-size: 10px;\r\n        font-weight: 800;\r\n        color: #f22565;\r\n        letter-spacing: 0.18em;\r\n        margin-top: 3px;\r\n      }\r\n      .fle-goal-lbl {\r\n        font-size: 9px;\r\n        font-weight: 700;\r\n        color: #8c9bb1;\r\n        letter-spacing: 0.1em;\r\n        text-transform: uppercase;\r\n        margin-top: 2px;\r\n      }\r\n      @keyframes fle-milestone-pop {\r\n        0%   { transform: scale(1); }\r\n        30%  { transform: scale(1.13); box-shadow: inset 6px 6px 12px #d1d9e6, inset -6px -6px 12px #ffffff, 0 0 0 6px rgba(242,37,101,0.15); }\r\n        55%  { transform: scale(0.96); }\r\n        75%  { transform: scale(1.06); }\r\n        100% { transform: scale(1); box-shadow: inset 6px 6px 12px #d1d9e6, inset -6px -6px 12px #ffffff; }\r\n      }\r\n      .fle-circle--celebrate {\r\n        animation: fle-milestone-pop 0.65s cubic-bezier(0.34, 1.56, 0.64, 1) forwards;\r\n      }\r\n      @keyframes fle-celebrate-in {\r\n        0%   { opacity: 0; transform: scale(0.45); }\r\n        60%  { opacity: 1; transform: scale(1.1); }\r\n        80%  { transform: scale(0.96); }\r\n        100% { opacity: 1; transform: scale(1); }\r\n      }\r\n      @keyframes fle-celebrate-out {\r\n        0%   { opacity: 1; transform: scale(1); }\r\n        100% { opacity: 0; transform: scale(0.8); }\r\n      }\r\n      @keyframes fle-ripple-out {\r\n        0%   { transform: translate(-50%,-50%) scale(1); opacity: 0.55; }\r\n        100% { transform: translate(-50%,-50%) scale(2.1); opacity: 0; }\r\n      }\r\n      @keyframes fle-check-draw {\r\n        to { stroke-dashoffset: 0; }\r\n      }\r\n      @keyframes fle-cel-label-in {\r\n        0%   { opacity: 0; transform: translateY(7px) scale(0.9); }\r\n        100% { opacity: 1; transform: translateY(0) scale(1); }\r\n      }\r\n      @keyframes fle-shimmer-sweep {\r\n        0%   { left: -70%; }\r\n        100% { left: 140%; }\r\n      }\r\n      .fle-ripple {\r\n        position: absolute;\r\n        width: 148px;\r\n        height: 148px;\r\n        border-radius: 50%;\r\n        border: 2px solid rgba(242,37,101,0.55);\r\n        top: 50%;\r\n        left: 50%;\r\n        transform: translate(-50%,-50%) scale(1);\r\n        animation: fle-ripple-out 1.4s ease-out forwards;\r\n        pointer-events: none;\r\n        z-index: 19;\r\n      }\r\n      .fle-circle-celebration {\r\n        position: absolute;\r\n        inset: 0;\r\n        border-radius: 50%;\r\n        background: linear-gradient(145deg, #ff5a8a 0%, #f22565 60%, #c9144e 100%);\r\n        display: flex;\r\n        flex-direction: column;\r\n        align-items: center;\r\n        justify-content: center;\r\n        gap: 4px;\r\n        z-index: 20;\r\n        pointer-events: none;\r\n        overflow: hidden;\r\n        animation: fle-celebrate-in 0.55s cubic-bezier(0.34, 1.56, 0.64, 1) forwards;\r\n        box-shadow: 0 0 0 0 rgba(242,37,101,0.5);\r\n      }\r\n      .fle-circle-celebration.fle-celebrate-out {\r\n        animation: fle-celebrate-out 0.45s ease forwards;\r\n      }\r\n      .fle-cel-shimmer {\r\n        position: absolute;\r\n        top: -10%;\r\n        left: -70%;\r\n        width: 45%;\r\n        height: 120%;\r\n        background: linear-gradient(90deg, transparent, rgba(255,255,255,0.28), transparent);\r\n        transform: skewX(-18deg);\r\n        animation: fle-shimmer-sweep 1s ease 0.35s forwards;\r\n        pointer-events: none;\r\n      }\r\n      .fle-check-path {\r\n        stroke-dasharray: 32;\r\n        stroke-dashoffset: 32;\r\n        animation: fle-check-draw 0.42s ease 0.28s forwards;\r\n      }\r\n      .fle-cel-label {\r\n        color: #fff;\r\n        font-size: 8.5px;\r\n        font-weight: 900;\r\n        letter-spacing: 0.16em;\r\n        text-transform: uppercase;\r\n        text-align: center;\r\n        line-height: 1.35;\r\n        opacity: 0;\r\n        animation: fle-cel-label-in 0.35s ease 0.55s forwards;\r\n        text-shadow: 0 1px 4px rgba(0,0,0,0.15);\r\n      }\r\n      .fle-stop-btn {\r\n        background: #f0f3f8;\r\n        border: none;\r\n        border-radius: 16px;\r\n        box-shadow: 7px 7px 14px #d1d9e6, -7px -7px 14px #ffffff;\r\n        color: #f22565;\r\n        font-size: 12px;\r\n        font-weight: 800;\r\n        letter-spacing: 0.15em;\r\n        text-transform: uppercase;\r\n        height: 48px;\r\n        width: 100%;\r\n        cursor: pointer;\r\n        outline: none;\r\n        transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);\r\n        font-family: inherit;\r\n        margin-top: 2px;\r\n      }\r\n      .fle-stop-btn:active {\r\n        box-shadow: inset 5px 5px 10px #d1d9e6, inset -5px -5px 10px #ffffff;\r\n        transform: translateY(1px);\r\n      }\r\n      .fle-confirm {\r\n        position: absolute;\r\n        inset: 0;\r\n        background: #f0f3f8;\r\n        border-radius: 32px;\r\n        display: flex;\r\n        flex-direction: column;\r\n        align-items: center;\r\n        justify-content: center;\r\n        padding: 24px 22px;\r\n        gap: 9px;\r\n        opacity: 0;\r\n        pointer-events: none;\r\n        transition: opacity 0.25s ease;\r\n        z-index: 10;\r\n        text-align: center;\r\n      }\r\n      .fle-confirm--show { opacity: 1; pointer-events: all; }\r\n      .fle-confirm-title {\r\n        font-size: 13px;\r\n        font-weight: 800;\r\n        color: #1a2b4c;\r\n        line-height: 1.3;\r\n        margin: 4px 0 0 0;\r\n      }\r\n      .fle-confirm-msg {\r\n        font-size: 10.5px;\r\n        font-weight: 500;\r\n        color: #8c9bb1;\r\n        line-height: 1.55;\r\n        margin: 0;\r\n      }\r\n      .fle-keep-btn {\r\n        width: 100%;\r\n        height: 46px;\r\n        background: linear-gradient(135deg, #fc427b, #f22565);\r\n        border: none;\r\n        border-radius: 14px;\r\n        color: #fff;\r\n        font-size: 12px;\r\n        font-weight: 800;\r\n        letter-spacing: 0.1em;\r\n        text-transform: uppercase;\r\n        cursor: pointer;\r\n        font-family: inherit;\r\n        box-shadow: 0 4px 14px rgba(242,37,101,0.35);\r\n        transition: opacity 0.2s;\r\n        margin-top: 6px;\r\n      }\r\n      .fle-keep-btn:hover { opacity: 0.9; }\r\n      .fle-stop-anyway-btn {\r\n        background: transparent;\r\n        border: none;\r\n        color: #94a3b8;\r\n        font-size: 11px;\r\n        font-weight: 600;\r\n        cursor: pointer;\r\n        font-family: inherit;\r\n        text-decoration: underline;\r\n        text-underline-offset: 2px;\r\n        padding: 2px 0;\r\n      }\r\n    </style>\r\n    <div class=\"fle-card\" id=\"draggable\">\r\n      <div class=\"fle-confirm\" id=\"fle-confirm\">\r\n        <svg width=\"40\" height=\"40\" viewBox=\"0 0 24 24\" fill=\"none\">\r\n          <circle cx=\"12\" cy=\"12\" r=\"11\" fill=\"#fce4ec\"/>\r\n          <path d=\"M12 7v5.5\" stroke=\"#f22565\" stroke-width=\"2\" stroke-linecap=\"round\"/>\r\n          <circle cx=\"12\" cy=\"16.5\" r=\"1.1\" fill=\"#f22565\"/>\r\n        </svg>\r\n        <p class=\"fle-confirm-title\">AI hasn't learned your type yet</p>\r\n        <p class=\"fle-confirm-msg\" id=\"fle-confirm-msg\"></p>\r\n        <button class=\"fle-keep-btn\" id=\"fle-keep-btn\">Keep Going</button>\r\n        <button class=\"fle-stop-anyway-btn\" id=\"fle-stop-anyway-btn\">Stop anyway</button>\r\n      </div>\r\n      <div class=\"fle-left\">\r\n        <div class=\"fle-vbar\">\r\n          <div class=\"fle-vbar-fill\" id=\"fle-vbar-fill\" style=\"transform:translateX(-50%) scaleY(${initScaleY})\"></div>\r\n        </div>\r\n        <span class=\"fle-pct\" id=\"fle-pct\">${initPct}%</span>\r\n      </div>\r\n      <div class=\"fle-right\">\r\n        <div>\r\n          <h1 class=\"fle-title\">Training Visual Preference</h1>\r\n          <p class=\"fle-desc\">Like profiles you enjoy to help AI learn your taste</p>\r\n        </div>\r\n        <div class=\"fle-hint\">\r\n          <span class=\"fle-hint-icon\">\r\n            <svg width=\"16\" height=\"16\" viewBox=\"0 0 24 24\" fill=\"none\" xmlns=\"http://www.w3.org/2000/svg\">\r\n              <circle cx=\"12\" cy=\"12\" r=\"11\" fill=\"#fce4ec\"/>\r\n              <path d=\"M10 8l4 4-4 4\" stroke=\"#f22565\" stroke-width=\"2.2\" stroke-linecap=\"round\" stroke-linejoin=\"round\"/>\r\n            </svg>\r\n          </span>\r\n          <span class=\"fle-hint-text\">Just tap <b style=\"color:#f22565\">Like</b> on profiles you find attractive. Every like teaches AI your type.</span>\r\n        </div>\r\n        <div class=\"fle-circle-wrap\">\r\n          <div class=\"fle-circle\">\r\n            <svg class=\"fle-ring-svg\" viewBox=\"0 0 176 176\">\r\n              <defs>\r\n                <linearGradient id=\"fle-pg\" x1=\"0%\" y1=\"0%\" x2=\"100%\" y2=\"100%\">\r\n                  <stop offset=\"0%\" stop-color=\"#fc427b\"/>\r\n                  <stop offset=\"100%\" stop-color=\"#f22565\"/>\r\n                </linearGradient>\r\n              </defs>\r\n              <circle class=\"fle-ring-track\" cx=\"88\" cy=\"88\" r=\"76\"/>\r\n              <circle class=\"fle-ring-progress\" id=\"fle-ring\" cx=\"88\" cy=\"88\" r=\"76\"\r\n                stroke-dashoffset=\"${initOffset}\"/>\r\n            </svg>\r\n            <div class=\"fle-circle-inner\">\r\n              <span class=\"fle-count\" id=\"fle-count\">${trainingCount}</span>\r\n              <span class=\"fle-liked-lbl\">LIKED</span>\r\n              <span class=\"fle-goal-lbl\">OF 50 GOAL</span>\r\n            </div>\r\n          </div>\r\n        </div>\r\n        <div class=\"fle-badge-row\">\r\n          <div class=\"fle-badge\">\r\n            <span class=\"fle-badge-lbl\" id=\"fle-m1-lbl\">${trainingCount >= 20 ? '✓ AI active!' : trainingCount === 0 ? 'Like 20 profiles' : `${20 - trainingCount} more to go`}</span>\r\n            <span class=\"fle-badge-pink\" id=\"fle-m1-sub\">${trainingCount >= 20 ? 'Keep going' : 'AI starts working'}</span>\r\n          </div>\r\n          <div class=\"fle-badge\">\r\n            <span class=\"fle-badge-lbl\" id=\"fle-m2-lbl\">${trainingCount >= 50 ? '50 likes done' : trainingCount >= 20 ? `${50 - trainingCount} more likes` : 'Then 50 total'}</span>\r\n            <span class=\"fle-badge-pink\" id=\"fle-m2-sub\">${trainingCount >= 50 ? 'AI is ready!' : 'Full precision'}</span>\r\n          </div>\r\n        </div>\r\n        <button class=\"fle-stop-btn\" id=\"exitTrainingBtn\">Stop Training</button>\r\n      </div>\r\n    </div>\r\n  `;\r\n\r\n  // Make draggable\r\n  let isDragging = false;\r\n  let currentX, currentY, initialX, initialY;\r\n\r\n  overlay.addEventListener('mousedown', (e) => {\r\n    if (e.target.id === 'exitTrainingBtn') return;\r\n    isDragging = true;\r\n    initialX = e.clientX - (overlay.offsetLeft || (window.innerWidth - overlay.offsetWidth - 20));\r\n    initialY = e.clientY - overlay.offsetTop;\r\n  });\r\n\r\n  document.addEventListener('mousemove', (e) => {\r\n    if (!isDragging) return;\r\n    e.preventDefault();\r\n    currentX = e.clientX - initialX;\r\n    currentY = e.clientY - initialY;\r\n    overlay.style.left = currentX + 'px';\r\n    overlay.style.top = currentY + 'px';\r\n    overlay.style.right = 'auto';\r\n  });\r\n\r\n  document.addEventListener('mouseup', () => {\r\n    isDragging = false;\r\n  });\r\n\r\n  document.body.appendChild(overlay);\r\n\r\n  const counterEl = document.getElementById('fle-count');\r\n\r\n  let stopTraining = false;\r\n\r\n  const isContextValid = () => !!chrome.runtime?.id;\r\n\r\n  const updateTrainingUI = (count) => {\r\n    if (counterEl) counterEl.textContent = String(count);\r\n    const ringEl = document.getElementById('fle-ring');\r\n    if (ringEl) ringEl.style.strokeDashoffset = String(Math.round(circumference * (1 - count / targetCount) * 10) / 10);\r\n    const vbarFill = document.getElementById('fle-vbar-fill');\r\n    if (vbarFill) vbarFill.style.transform = `translateX(-50%) scaleY(${Math.max(0.03, count / targetCount)})`;\r\n    const pctEl = document.getElementById('fle-pct');\r\n    if (pctEl) pctEl.textContent = `${Math.round((count / targetCount) * 100)}%`;\r\n    const m1Lbl = document.getElementById('fle-m1-lbl');\r\n    const m1Sub = document.getElementById('fle-m1-sub');\r\n    const m2Lbl = document.getElementById('fle-m2-lbl');\r\n    const m2Sub = document.getElementById('fle-m2-sub');\r\n    if (count < 20) {\r\n      if (m1Lbl) m1Lbl.textContent = `${20 - count} more to go`;\r\n      if (m1Sub) m1Sub.textContent = 'AI starts working';\r\n      if (m2Lbl) m2Lbl.textContent = 'Then 50 total';\r\n      if (m2Sub) m2Sub.textContent = 'Full precision';\r\n    } else if (count < 50) {\r\n      if (m1Lbl) m1Lbl.textContent = '✓ AI active!';\r\n      if (m1Sub) m1Sub.textContent = 'Keep going';\r\n      if (m2Lbl) m2Lbl.textContent = `${50 - count} more likes`;\r\n      if (m2Sub) m2Sub.textContent = 'Full precision';\r\n      if (count === 20) {\r\n        const circle = document.querySelector('#flirteasy-training-overlay .fle-circle');\r\n        if (circle) {\r\n          [0, 280, 560].forEach((delay) => {\r\n            const ripple = document.createElement('div');\r\n            ripple.className = 'fle-ripple';\r\n            ripple.style.animationDelay = `${delay}ms`;\r\n            circle.appendChild(ripple);\r\n            setTimeout(() => ripple.remove(), 1800 + delay);\r\n          });\r\n          const celebDiv = document.createElement('div');\r\n          celebDiv.className = 'fle-circle-celebration';\r\n          celebDiv.innerHTML = `\r\n            <div class=\"fle-cel-shimmer\"></div>\r\n            <svg width=\"48\" height=\"48\" viewBox=\"0 0 48 48\" fill=\"none\">\r\n              <circle cx=\"24\" cy=\"24\" r=\"21\" fill=\"rgba(255,255,255,0.14)\" stroke=\"rgba(255,255,255,0.4)\" stroke-width=\"1.5\"/>\r\n              <path class=\"fle-check-path\" d=\"M14 24l7.5 8 12.5-14\" stroke=\"#fff\" stroke-width=\"3.2\" stroke-linecap=\"round\" stroke-linejoin=\"round\"/>\r\n            </svg>\r\n            <div class=\"fle-cel-label\">AI Active!<br>Keep going</div>\r\n          `;\r\n          circle.appendChild(celebDiv);\r\n          setTimeout(() => {\r\n            celebDiv.classList.add('fle-celebrate-out');\r\n            setTimeout(() => celebDiv.remove(), 450);\r\n          }, 2500);\r\n        }\r\n      }\r\n    } else {\r\n      if (m1Lbl) m1Lbl.textContent = '✓ AI active!';\r\n      if (m1Sub) m1Sub.textContent = 'Keep going';\r\n      if (m2Lbl) m2Lbl.textContent = '50 likes done';\r\n      if (m2Sub) m2Sub.textContent = 'AI is ready!';\r\n    }\r\n  };\r\n\r\n  // Listen for all like actions (button click, swipe right, keyboard) via API intercept\r\n  const likeButtonListener = async () => {\r\n    console.log('[FlirtEasy] Training: Like action detected');\r\n\r\n    const photoUrl = extractProfilePhotoUrl();\r\n    console.log('[FlirtEasy] Training: Photo URL:', photoUrl);\r\n\r\n    if (photoUrl) {\r\n      if (!isContextValid()) {\r\n        trainingCount++;\r\n        console.log(`[FlirtEasy] Training: Context invalid, counting locally ${trainingCount}/50`);\r\n        updateTrainingUI(trainingCount);\r\n        return;\r\n      }\r\n\r\n      console.log('[FlirtEasy] Training: Converting to permanent Base64...');\r\n      let permanentPhoto = typeof tinderImageToBase64 === 'function' ? await tinderImageToBase64(photoUrl) : null;\r\n\r\n      if (!permanentPhoto) {\r\n        console.log('[FlirtEasy] Training: Direct fetch failed, trying background fetch...');\r\n        if (isContextValid()) {\r\n          try {\r\n            const bgResult = await new Promise((resolve, reject) => {\r\n              try {\r\n                chrome.runtime.sendMessage({ action: 'fetchPhotoBase64', url: photoUrl }, res => {\r\n                  if (chrome.runtime.lastError) reject(chrome.runtime.lastError);\r\n                  else resolve(res);\r\n                });\r\n              } catch (e) { reject(e); }\r\n            });\r\n            permanentPhoto = (bgResult && bgResult.success) ? bgResult.base64 : photoUrl;\r\n          } catch (e) {\r\n            console.log('[FlirtEasy] Training: Background fetch failed, using raw URL');\r\n            permanentPhoto = photoUrl;\r\n          }\r\n        } else {\r\n          permanentPhoto = photoUrl;\r\n        }\r\n      }\r\n      console.log('[FlirtEasy] Training: Full-size Base64 ready.');\r\n\r\n      if (!isContextValid()) {\r\n        trainingCount++;\r\n        console.log(`[FlirtEasy] Training: Context invalid after fetch, counting locally ${trainingCount}/50`);\r\n        updateTrainingUI(trainingCount);\r\n        return;\r\n      }\r\n\r\n      let response;\r\n      try {\r\n        response = await new Promise((resolve, reject) => {\r\n          try {\r\n            chrome.runtime.sendMessage({ action: 'addLikedPhoto', photoUrl: permanentPhoto }, res => {\r\n              if (chrome.runtime.lastError) reject(chrome.runtime.lastError);\r\n              else resolve(res);\r\n            });\r\n          } catch (e) { reject(e); }\r\n        });\r\n      } catch (e) {\r\n        console.log('[FlirtEasy] Training: sendMessage failed, counting locally');\r\n        trainingCount++;\r\n        updateTrainingUI(trainingCount);\r\n        return;\r\n      }\r\n      console.log('[FlirtEasy] Training: Photo saved, response:', response);\r\n\r\n      if (response && response.success) {\r\n        trainingCount = response.count;\r\n        updateTrainingUI(trainingCount);\r\n        console.log(`[FlirtEasy] Training: Progress ${trainingCount}/50`);\r\n\r\n        if (trainingCount >= 50) {\r\n          if (isContextValid()) {\r\n            try { chrome.runtime.sendMessage({ action: 'trainingCompleted' }); } catch (e) {}\r\n          }\r\n\r\n          const circle = document.querySelector('#flirteasy-training-overlay .fle-circle');\r\n          if (circle) {\r\n            [0, 220, 440, 660].forEach((delay) => {\r\n              const ripple = document.createElement('div');\r\n              ripple.className = 'fle-ripple';\r\n              ripple.style.animationDelay = `${delay}ms`;\r\n              ripple.style.borderColor = `rgba(16,185,129,${0.6 - delay * 0.0003})`;\r\n              circle.appendChild(ripple);\r\n              setTimeout(() => ripple.remove(), 2000 + delay);\r\n            });\r\n            const doneDiv = document.createElement('div');\r\n            doneDiv.className = 'fle-circle-celebration';\r\n            doneDiv.style.background = 'linear-gradient(145deg, #34d399 0%, #10b981 55%, #059669 100%)';\r\n            doneDiv.innerHTML = `\r\n              <div class=\"fle-cel-shimmer\"></div>\r\n              <svg width=\"52\" height=\"52\" viewBox=\"0 0 52 52\" fill=\"none\">\r\n                <circle cx=\"26\" cy=\"26\" r=\"23\" fill=\"rgba(255,255,255,0.15)\" stroke=\"rgba(255,255,255,0.45)\" stroke-width=\"1.5\"/>\r\n                <path class=\"fle-check-path\" d=\"M15 26l8 9 14-16\" stroke=\"#fff\" stroke-width=\"3.4\" stroke-linecap=\"round\" stroke-linejoin=\"round\"/>\r\n              </svg>\r\n              <div class=\"fle-cel-label\" style=\"font-size:9px;\">AI Fully<br>Trained!</div>\r\n            `;\r\n            circle.appendChild(doneDiv);\r\n\r\n            setTimeout(() => {\r\n              doneDiv.classList.add('fle-celebrate-out');\r\n              setTimeout(() => {\r\n                doneDiv.remove();\r\n                const btn = document.getElementById('exitTrainingBtn');\r\n                if (btn) {\r\n                  btn.textContent = '✓ Training Complete!';\r\n                  btn.style.background = '#10b981';\r\n                  btn.style.boxShadow = 'none';\r\n                  btn.style.color = '#ffffff';\r\n                  btn.style.letterSpacing = '0.1em';\r\n                }\r\n                setTimeout(() => {\r\n                  const overlayCard = overlay.firstElementChild;\r\n                  if (overlayCard) {\r\n                    overlayCard.style.transition = 'all 0.5s cubic-bezier(0.4, 0, 0.2, 1)';\r\n                    overlayCard.style.transform = 'scale(0.9)';\r\n                    overlayCard.style.opacity = '0';\r\n                  }\r\n                  setTimeout(() => {\r\n                    overlay.remove();\r\n                    document.removeEventListener('flirteasy:tinderLike', likeButtonListener);\r\n                  }, 500);\r\n                }, 1200);\r\n              }, 450);\r\n            }, 2800);\r\n          }\r\n        }\r\n      }\r\n    }\r\n  };\r\n\r\n  document.addEventListener('flirteasy:tinderLike', likeButtonListener);\r\n\r\n  const doStopTraining = () => {\r\n    const btn = document.getElementById('exitTrainingBtn');\r\n    const overlayCard = overlay.firstElementChild;\r\n    if (btn) { btn.textContent = 'Stopping...'; btn.style.pointerEvents = 'none'; }\r\n    if (overlayCard) { overlayCard.style.transition = 'opacity 0.3s ease'; overlayCard.style.opacity = '0'; }\r\n    if (isContextValid()) { try { chrome.runtime.sendMessage({ action: 'trainingExited' }); } catch (e) {} }\r\n    setTimeout(() => {\r\n      overlay.remove();\r\n      document.removeEventListener('flirteasy:tinderLike', likeButtonListener);\r\n    }, 300);\r\n  };\r\n\r\n  document.getElementById('exitTrainingBtn').addEventListener('click', () => {\r\n    if (trainingCount < 20) {\r\n      const confirmPanel = document.getElementById('fle-confirm');\r\n      const msg = document.getElementById('fle-confirm-msg');\r\n      const remaining = 20 - trainingCount;\r\n      if (msg) msg.textContent = `Like just ${remaining} more profile${remaining !== 1 ? 's' : ''} to reach Basic Calibration — that's when AI starts recognizing your type.`;\r\n      if (confirmPanel) confirmPanel.classList.add('fle-confirm--show');\r\n      document.getElementById('fle-keep-btn').addEventListener('click', () => {\r\n        confirmPanel.classList.remove('fle-confirm--show');\r\n      }, { once: true });\r\n      document.getElementById('fle-stop-anyway-btn').addEventListener('click', doStopTraining, { once: true });\r\n      return;\r\n    }\r\n    doStopTraining();\r\n  });\r\n\r\n  console.log('[FlirtEasy] Training: Loop finished');\r\n  return { success: true };\r\n}\r\n\r\nfunction extractProfilePhotoUrl() {\r\n  console.log('[FlirtEasy] Searching for profile photo...');\r\n\r\n  // Find all visible image divs with Tinder URLs\r\n  const photoDivs = document.querySelectorAll('div[role=\"img\"]');\r\n  const tinderUrls = [];\r\n\r\n  for (const div of photoDivs) {\r\n    if (div.style.backgroundImage?.includes('images-ssl.gotinder.com')) {\r\n      const match = div.style.backgroundImage.match(/url\\([\"']?([^\"')]+)[\"']?\\)/);\r\n      if (match?.[1]) {\r\n        tinderUrls.push(match[1]);\r\n      }\r\n    }\r\n  }\r\n\r\n  console.log('[FlirtEasy] Found', tinderUrls.length, 'Tinder image URLs');\r\n\r\n  // Return the LAST one (most likely the profile being viewed, not your own)\r\n  if (tinderUrls.length > 0) {\r\n    const url = tinderUrls[tinderUrls.length - 1];\r\n    console.log('[FlirtEasy] Using last URL:', url);\r\n    return url;\r\n  }\r\n\r\n  console.log('[FlirtEasy] No photo found');\r\n  return null;\r\n}\r\n\r\n\r\n// ===== ACHIEVEMENT SYSTEM =====\r\n// Achievement tracker loading with queue for early requests\r\nlet achievementTrackerReady = false;\r\nconst achievementQueue = [];\r\n\r\n// Load achievement scripts when DOM is ready\r\nfunction loadAchievementScripts() {\r\n  console.log('[FlirtEasy] Loading achievement scripts...');\r\n\r\n  // Load core first\r\n  const coreScript = document.createElement('script');\r\n  coreScript.src = chrome.runtime.getURL('features/achievements/achievements-core.js');\r\n  coreScript.onload = () => {\r\n    console.log('[FlirtEasy] Achievement core loaded successfully');\r\n\r\n    // Then load tracker\r\n    const trackerScript = document.createElement('script');\r\n    trackerScript.src = chrome.runtime.getURL('features/achievements/achievements-tracker.js');\r\n    trackerScript.onload = () => {\r\n      console.log('[FlirtEasy] Achievement tracker loaded successfully');\r\n\r\n      // Listen for ready event from page context\r\n      window.addEventListener('achievement:trackerReady', () => {\r\n        console.log('[FlirtEasy] AchievementTracker is ready (via event)');\r\n        achievementTrackerReady = true;\r\n\r\n        console.log(`[FlirtEasy] Processing ${achievementQueue.length} queued achievement checks`);\r\n\r\n        // Process all queued checks\r\n        achievementQueue.forEach(({ stats }) => {\r\n          // Send via custom event to page context\r\n          window.dispatchEvent(new CustomEvent('achievement:check', {\r\n            detail: { stats }\r\n          }));\r\n        });\r\n\r\n        // Clear queue\r\n        achievementQueue.length = 0;\r\n      }, { once: true });\r\n    };\r\n    trackerScript.onerror = (e) => {\r\n      console.error('[FlirtEasy] Failed to load achievement tracker script:', e);\r\n    };\r\n    console.log('[FlirtEasy] Appending tracker script...');\r\n    document.head.appendChild(trackerScript);\r\n  };\r\n  coreScript.onerror = (e) => {\r\n    console.error('[FlirtEasy] Failed to load achievement core script:', e);\r\n  };\r\n  console.log('[FlirtEasy] Appending core script...');\r\n  document.head.appendChild(coreScript);\r\n\r\n  // Load UI overlay\r\n  const overlayScript = document.createElement('script');\r\n  overlayScript.src = chrome.runtime.getURL('features/achievements/achievements-tinder.js');\r\n  overlayScript.onload = () => {\r\n    console.log('[FlirtEasy] Achievement overlay loaded successfully');\r\n  };\r\n  overlayScript.onerror = (e) => {\r\n    console.error('[FlirtEasy] Failed to load achievement overlay script:', e);\r\n  };\r\n  console.log('[FlirtEasy] Appending overlay script...');\r\n  document.head.appendChild(overlayScript);\r\n}\r\n\r\n// Start loading when DOM is ready (Chrome Extension context only)\r\nif (window.chrome?.runtime?.id && window.chrome.runtime.id !== 'flirteasy-on-device') {\r\n  if (document.readyState === 'loading') {\r\n    document.addEventListener('DOMContentLoaded', loadAchievementScripts);\r\n  } else {\r\n    loadAchievementScripts();\r\n  }\r\n}\r\n\r\n// Listen for achievement unlocks from page context\r\nwindow.addEventListener('achievement:unlocked', (event) => {\r\n  console.log('[FlirtEasy] Achievement unlocked:', event.detail.badge);\r\n\r\n  // Notify background\r\n  chrome.runtime.sendMessage({\r\n    action: 'badgeUnlocked',\r\n    badge: event.detail.badge\r\n  });\r\n});\r\n\r\n// Listen for open panel requests from page context\r\nwindow.addEventListener('achievement:openPanel', () => {\r\n  chrome.runtime.sendMessage({ action: 'openAchievements' });\r\n});\r\n\r\nconsole.log('[FlirtEasy] Achievement system integrated with Tinder UI');\r\n\r\n// ── LOGIN HELPER: LANDING PAGE → \"CREATE ACCOUNT\" ──\r\n// Deliberately only two steps, and nothing after them:\r\n//   Step 1 — the WebView lands on tinder.com\r\n//   Step 2 — click \"Create account\" on the landing page\r\n// That opens Tinder's own login / signup sheet and the user finishes manually.\r\n// No hamburger menu, no nav drawer, no per-provider automation, no page-state\r\n// polling. Everything the helper does is bounded and it stops for good after it\r\n// hands over.\r\nif (typeof isLoggedIn === 'function') {\r\n  if (isLoggedIn()) {\r\n    if (typeof window.ORCHESTRATOR_USER_ID !== 'undefined') {\r\n      fetch(`http://host.docker.internal:3000/login-success?userId=${window.ORCHESTRATOR_USER_ID}&platform=tinder`)\r\n        .catch(err => console.warn('[Content] Failed to notify orchestrator of login success:', err));\r\n    }\r\n  } else if (typeof window.__feCreateAccountHelperStarted === 'undefined') {\r\n    window.__feCreateAccountHelperStarted = true;\r\n\r\n    const isVisible = (el) => {\r\n      if (!el) return false;\r\n      try {\r\n        const rect = el.getBoundingClientRect();\r\n        if (rect.width <= 0 || rect.height <= 0) return false;\r\n        const style = window.getComputedStyle(el);\r\n        return style.display !== 'none' && style.visibility !== 'hidden' && style.opacity !== '0';\r\n      } catch (_) {\r\n        return el.offsetWidth > 0 || el.offsetHeight > 0;\r\n      }\r\n    };\r\n\r\n    // Full pointer + mouse sequence: React ignores a bare .click() on some of\r\n    // Tinder's CTAs. Synthetic events are unaffected by pointer-events CSS.\r\n    const clickElement = (el) => {\r\n      if (!el) return;\r\n      try {\r\n        const rect = el.getBoundingClientRect();\r\n        const x = rect.left + rect.width / 2;\r\n        const y = rect.top + rect.height / 2;\r\n        const opts = {\r\n          bubbles: true,\r\n          cancelable: true,\r\n          view: window,\r\n          clientX: x,\r\n          clientY: y,\r\n          screenX: x,\r\n          screenY: y,\r\n        };\r\n        try { el.dispatchEvent(new PointerEvent('pointerdown', opts)); } catch (_) {}\r\n        try { el.dispatchEvent(new MouseEvent('mousedown', opts)); } catch (_) {}\r\n        try { el.dispatchEvent(new PointerEvent('pointerup', opts)); } catch (_) {}\r\n        try { el.dispatchEvent(new MouseEvent('mouseup', opts)); } catch (_) {}\r\n        try { el.dispatchEvent(new MouseEvent('click', opts)); } catch (_) {}\r\n      } catch (_) {}\r\n      try { el.click(); } catch (_) {}\r\n    };\r\n\r\n    // Tinder's login / signup sheet is up, so step 2 landed and we are done.\r\n    // #rebrand-mobile-menu is the mobile nav drawer, which is also role=\"dialog\"\r\n    // and must not be mistaken for the sheet.\r\n    const isLoginSheetOpen = () => {\r\n      try {\r\n        if (document.querySelector('input[type=\"tel\"], input[type=\"email\"], input[autocomplete=\"one-time-code\"]')) {\r\n          return true;\r\n        }\r\n        const dialogs = Array.from(document.querySelectorAll(\r\n          '[aria-labelledby=\"MODAL_LOGIN\"], [role=\"dialog\"], div[aria-modal=\"true\"]'\r\n        ));\r\n        return dialogs.some(d => d.id !== 'rebrand-mobile-menu' && isVisible(d));\r\n      } catch (_) {\r\n        return false;\r\n      }\r\n    };\r\n\r\n    const findCreateAccount = () => {\r\n      const candidates = Array.from(document.querySelectorAll('a, button, [role=\"button\"]'));\r\n      return candidates.find(el => {\r\n        if (!isVisible(el)) return false;\r\n        const text = (el.innerText || el.textContent || '').trim().toLowerCase();\r\n        const aria = (el.getAttribute('aria-label') || '').trim().toLowerCase();\r\n        const label = text || aria;\r\n        return label === 'create account' ||\r\n          label === 'create an account' ||\r\n          label === 'sign up' ||\r\n          aria === 'create account' ||\r\n          aria === 'create an account';\r\n      });\r\n    };\r\n\r\n    console.log('[Tinder Login] Waiting for landing page \"Create account\" button...');\r\n\r\n    let attempts = 0;\r\n    let clicks = 0;\r\n    const createAccountTimer = setInterval(() => {\r\n      attempts++;\r\n\r\n      // Hard stop after ~20s so nothing keeps running behind the user.\r\n      if (attempts > 40) {\r\n        console.log('[Tinder Login] \"Create account\" not found in time — leaving the page to the user.');\r\n        clearInterval(createAccountTimer);\r\n        return;\r\n      }\r\n\r\n      try {\r\n        if (isLoggedIn() || isLoginSheetOpen()) {\r\n          console.log('[Tinder Login] Login sheet is open — helper done, handing over to the user.');\r\n          clearInterval(createAccountTimer);\r\n          return;\r\n        }\r\n\r\n        // A couple of retries in case React had not hydrated on the first click.\r\n        if (clicks >= 3) {\r\n          clearInterval(createAccountTimer);\r\n          return;\r\n        }\r\n\r\n        const btn = findCreateAccount();\r\n        if (btn) {\r\n          clicks++;\r\n          console.log(`[Tinder Login] Clicking \"Create account\" (attempt ${clicks})...`);\r\n          clickElement(btn);\r\n        }\r\n      } catch (_) {\r\n        clearInterval(createAccountTimer);\r\n      }\r\n    }, 500);\r\n  }\r\n}\r\n\r\n// ── PAGE↔CONTENT SCRIPT BRIDGE ──\r\nwindow.addEventListener('message', (event) => {\r\n  if (event.source !== window || !event.data || event.data.type !== 'FLIRTEASY_CMD') return;\r\n  const { action, reqId } = event.data;\r\n  const respond = (payload) => window.postMessage({ type: 'FLIRTEASY_RESP', reqId, ...payload }, '*');\r\n\r\n  if (action === 'startAgent') {\r\n    chrome.runtime.sendMessage({ action: 'startAgent', platform: 'tinder' }, (res) => {\r\n      respond({ success: res?.success ?? true });\r\n    });\r\n  } else if (action === 'stopAgent') {\r\n    chrome.runtime.sendMessage({ action: 'stopAgent' }, (res) => {\r\n      respond({ success: res?.success ?? true });\r\n    });\r\n  } else if (action === 'getAuthStatus') {\r\n    // ← NEW: React Native app can probe current auth status at any time\r\n    const loggedIn = typeof isLoggedIn === 'function' ? isLoggedIn() : false;\r\n    respond({ success: true, isLoggedIn: loggedIn, url: window.location.href });\r\n  } else {\r\n    respond({ success: false, error: `Unknown action: ${action}` });\r\n  }\r\n});\r\n\r\n// ── TWO-WAY LOGOUT WATCHDOG (On-Device Mode) ──\r\n// Continuously monitors isLoggedIn() and fires FE_AUTH_STEP:'logged_out'\r\n// when the user manually logs out from within the Tinder browser.\r\n// This enables real-time two-way sync: login AND logout events both propagate\r\n// to the React Native home screen via the ReactNativeWebView bridge.\r\nif (typeof window._feLogoutWatchdogStarted === 'undefined') {\r\n  window._feLogoutWatchdogStarted = true;\r\n\r\n  var _extractTinderAuthToken = function() {\r\n    try {\r\n      var t = localStorage.getItem('TinderWeb/APIToken');\r\n      if (t) return String(t).replace(/^[\"'](.*)[\"']$/, '$1').trim();\r\n      var s = localStorage.getItem('TinderWeb/APIStore');\r\n      if (s) {\r\n        var p = JSON.parse(s);\r\n        var tok = p && (p.token || p.auth_token || (p.user && p.user.api_token));\r\n        if (tok) return String(tok).replace(/^[\"'](.*)[\"']$/, '$1').trim();\r\n      }\r\n    } catch (_) {}\r\n    return null;\r\n  };\r\n\r\n  // Seed initial state — if already logged in at inject time, mark as such\r\n  let _wasLoggedIn = typeof isLoggedIn === 'function' ? isLoggedIn() : false;\r\n\r\n  // Send initial status immediately so app gets the true state on WebView load\r\n  try {\r\n    if (window.ReactNativeWebView && window.ReactNativeWebView.postMessage) {\r\n      if (!window.__feLogoutInProgress) {\r\n        window.ReactNativeWebView.postMessage(JSON.stringify({\r\n          type: 'FE_PAGE_STATUS',\r\n          isLoggedIn: _wasLoggedIn,\r\n          token: _wasLoggedIn ? _extractTinderAuthToken() : null,\r\n          url: window.location.href,\r\n        }));\r\n      }\r\n    }\r\n  } catch (_) {}\r\n\r\n  setInterval(() => {\r\n    if (window.__feLogoutInProgress) return;\r\n    if (typeof isLoggedIn !== 'function') return;\r\n    const nowLoggedIn = isLoggedIn();\r\n\r\n    if (_wasLoggedIn && !nowLoggedIn) {\r\n      // ── Transition: LOGGED IN → LOGGED OUT ──\r\n      console.log('[FlirtEasy] Logout detected! Notifying React Native app...');\r\n      try {\r\n        if (window.ReactNativeWebView && window.ReactNativeWebView.postMessage) {\r\n          window.ReactNativeWebView.postMessage(JSON.stringify({\r\n            type: 'FE_AUTH_STEP',\r\n            step: 'logged_out',\r\n            url: window.location.href,\r\n          }));\r\n        }\r\n      } catch (_) {}\r\n    } else if (!_wasLoggedIn && nowLoggedIn) {\r\n      // ── Transition: LOGGED OUT → LOGGED IN ──\r\n      console.log('[FlirtEasy] Login detected by watchdog! Notifying React Native app...');\r\n      // Orchestrator notify for Neko / VPS mode (this used to live in the old\r\n      // multi-step login interval, which no longer exists).\r\n      if (typeof window.ORCHESTRATOR_USER_ID !== 'undefined') {\r\n        fetch(`http://host.docker.internal:3000/login-success?userId=${window.ORCHESTRATOR_USER_ID}&platform=tinder`)\r\n          .catch(err => console.warn('[Content] Failed to notify orchestrator of login success:', err));\r\n      }\r\n      try {\r\n        if (window.ReactNativeWebView && window.ReactNativeWebView.postMessage) {\r\n          window.ReactNativeWebView.postMessage(JSON.stringify({\r\n            type: 'FE_AUTH_STEP',\r\n            step: 'logged_in',\r\n            token: _extractTinderAuthToken(),\r\n            url: window.location.href,\r\n          }));\r\n        }\r\n      } catch (_) {}\r\n    }\r\n\r\n    _wasLoggedIn = nowLoggedIn;\r\n  }, 2000); // Poll every 2 seconds\r\n}\r\n\n\n})();\ntrue;\n";
export default CONTENT_SCRIPT_BUNDLE;
