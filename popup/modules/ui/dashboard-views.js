/**
 * Dashboard UI Views Module
 * Encapsulates the HTML templates for the Dashboard / Activity tab
 * NOTE: This template is kept in sync with popup.html activityTab content
 *       but is not dynamically injected — the HTML is the source of truth.
 */

const DashboardViews = {
  /**
   * Returns the main Activity Tab content (reference only)
   */
  getActivityTemplate() {
    return `<!-- Template maintained in popup.html activityTab section -->`;
  }
};

window.DashboardViews = DashboardViews;

